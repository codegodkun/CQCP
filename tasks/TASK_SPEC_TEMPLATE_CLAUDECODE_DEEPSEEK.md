# TASK SPEC — [TASK-XXX] [任务名称]
# 适用环境：Claude Code（DeepSeek 模型）— 统一执行环境
# 同一本地项目文件夹下与 CODEX 协作

> **版本**：v0.4
> **状态**：Draft / Ready for Execution / In Progress / Done
> **创建日期**：YYYY-MM-DD
> **起草**：CODEX
> **执行环境**：Claude Code（DeepSeek 模型）
> **TASK_SPEC 类型**：execution / readonly-review
> **父任务**：[关联 TASK 编号，如无则 N/A]
> **关联 ADR**：[如 ADR-005, ADR-010]
> **所在分支**：[git branch 名称]

---

<!--
════════════════════════════════════════════════════════
  给使用者的说明（填写完成后可删除此注释块）

  本模板分两个视角：

  A. CODEX 视角（第 0~9、11~12 节）
     CODEX 负责填写，你审核后定稿。
     填写时注意：所有示例内容必须替换为当前任务实际内容，
     不得保留与当前任务无关的示例占位文字。

  B. Claude Code（DeepSeek 模型）执行视角
     将"DeepSeek 执行指令块"整块粘贴为 Claude Code 首条 prompt。
     DeepSeek 执行完成后将实现报告粘贴至第 10 节。

  关键约束：
  - Claude Code 有本地文件系统和终端权限，必须通过本模板明确边界。
  - DeepSeek 在 Claude Code 场景下的主动停止、主动提问和边界遵守行为
    不应被默认信任，因此必须通过显式 STOP 指令约束。
  - Git 工作区规则是协作安全的基础，不得省略。
  - 本模板新增 `TASK_SPEC 类型` 字段，用于区分执行型 `TASK_SPEC` 与只读复查型 `TASK_SPEC`。
  - 类型为 `execution`：使用本模板。
  - 类型为 `readonly-review`：使用 `tasks/TASK_SPEC_REVIEW_TEMPLATE_READONLY.md`。
  - readonly-review 的工作区规则、证据规则、STOP 规则和输出格式，以 readonly-review 专用模板为准。
  - Claude Code / DeepSeek 只能执行冻结的局部 TASK_SPEC，不得直接承接父 TASK，不得 commit / push。
  - Codex 在角色分离试点中只写规格并审查，不直接编写业务代码。
════════════════════════════════════════════════════════
-->

---

## 0. 任务摘要

**执行模式**：Claude Code 软件壳 + DeepSeek API 推理，运行于本地项目文件夹

**一句话任务**：
> [由 CODEX 填写：实现什么 / 输入是什么 / 输出是什么 / 不涉及什么]
> 示例格式："实现 [模块名]，输入 [输入描述]，输出 [输出描述]，不涉及 [明确排除的范围]。"

### 0.1 角色与执行门禁

- 本 `TASK_SPEC` 必须关联一个已存在的父 `TASK`；Claude Code / DeepSeek 不得直接执行父任务。
- Codex 负责冻结规格、审查编码前规格映射计划、审查实现报告和 `git diff`；在角色分离试点中不得直接编写本任务业务代码。
- Claude Code / DeepSeek 只能修改授权文件，不得 commit，不得 push。
- 独立 agent 只做只读核查，不得承担本 `TASK_SPEC` 的实现。
- 不得以 `CURRENT_CONTEXT.md` 的完成自述替代真实代码、测试、原始 console 输出或 commit 证据。

### 0.2 编码前规格映射计划（实现前硬门禁）

Claude Code / DeepSeek 在修改任何代码前，必须先输出以下计划并等待 Codex 明确放行：

```text
验收断言映射：
- 断言 1：如何通过真实输入、代码路径和测试证明或证伪
- 断言 2：如何通过真实输入、代码路径和测试证明或证伪

关键字段 / 信号计算：
- [字段或信号]：真实输入来源、计算条件、失败条件

明确不修改路径：
- [路径 / 方法 / 模块]

范围外风险：
- [风险及不在本 TASK_SPEC 处理的理由]

预计测试变更：
- [测试文件、场景和断言]
```

Codex 未明确放行前，不得进入实现。事后测试通过不能替代该门禁。

**文件访问范围**（Claude Code 有文件系统权限，必须明确三类边界）：

```
✅ 允许修改：
  [本任务可创建或修改的目录/文件，每行一条，精确到模块级]
  示例：
  src/[module]/
  src/[module]/__tests__/

👀 允许只读参考：
  [可以读取以理解接口或上下文，但不得修改的模块]
  示例：
  src/[upstream-module]/       # 理解上游输出类型
  src/[shared-types]/          # 读取共享类型定义

⛔ 禁止访问：
  [不应读取、不应修改的高风险、敏感或与本任务无关且可能造成误操作的区域]
  示例：
  .env
  .env.*
  secrets/
  credentials/
  config/production/
  db/migrations/              # 除非本任务明确涉及数据库迁移
  deploy/                     # 除非本任务明确涉及部署配置
  infra/                      # 除非本任务明确涉及基础设施
  [其他包含密钥、生产配置、迁移脚本或高风险操作的路径]
```

---

## 1. 上下文注入（DeepSeek 必读）

### 1.1 项目定位

> 由 CODEX 填写当前项目的一句话定位，不得使用通用描述。
> 示例："本项目是合同质量控制中台 V2，核心链路为 [链路摘要]。"

### 1.2 本任务在链路中的位置

```
[由 CODEX 用箭头标出当前任务模块在整体链路中的位置]
[用【】标记本任务模块，其余模块标注是否在本任务范围内]

示例：
[上游模块]（不在本任务范围）
      ↓
【本任务模块】← 本任务
      ↓
[下游模块]（不在本任务范围）
```

### 1.3 架构红线（违反时必须 STOP，不得自行绕过）

> 由 CODEX 从 PRD / ARCHITECTURE / ADR 中提取与本任务直接相关的红线。
> 不得照抄所有红线，只保留本任务实现中可能触碰的部分。
> DeepSeek 若发现实现方向与红线冲突，立即输出：
> `[STOP: 红线冲突 — <红线名称> — <冲突描述> — 需要 CODEX 决策]`

| 红线标识 | 内容 | 本任务相关性 |
|----------|------|-------------|
| [RED-001] | [由 CODEX 填写] | [为什么与本任务相关] |
| [RED-002] | [由 CODEX 填写] | [为什么与本任务相关] |

### 1.4 冻结契约（只能消费，不能修改，不能新增）

> 由 CODEX 从 PRD / ADR 中提取与本任务直接相关的冻结内容。
> 不相关的契约不必全量复制，避免干扰 DeepSeek 理解范围。
> 发现契约之外的需求时，输出：
> `[STOP: 契约缺失 — 需要 <内容描述>，当前冻结契约未定义]`

```
[由 CODEX 填写，例如：]
- 字段 Key：[相关字段列表]
- 错误码：[相关错误码列表]
- 枚举值：[相关枚举列表]
- 数值存储口径：[相关口径说明]
- [其他冻结内容]
```

### 1.5 本任务领域术语

> 由 CODEX 根据当前任务填写，只列与本任务实现直接相关的术语。
> 不得保留与当前任务无关的示例术语。

| 术语 | 定义 |
|------|------|
| [术语1] | [由 CODEX 填写定义] |
| [术语2] | [由 CODEX 填写定义] |

---

## 2. 接口契约（Interface Contract）

> CODEX 定义，DeepSeek 只能实现，不能修改签名。
> 发现签名需要调整时，输出：
> `[STOP: 接口歧义 — <描述> — 建议修改方向]`，不得自行改动。

### 2.1 输入

```typescript
// 由 CODEX 填写实际接口定义
// 注明每个字段是否必填、来源和语义
interface [TaskInput] {
  // [字段名]: [类型]  // [是否必填]，[语义说明]
}
```

### 2.2 输出

```typescript
// 由 CODEX 填写实际接口定义
// 注明每个字段的语义和约束
interface [TaskOutput] {
  // [字段名]: [类型]  // [语义说明]
}
```

### 2.3 错误处理约定

| 情形 | 处理方式 |
|------|----------|
| [由 CODEX 填写情形] | [由 CODEX 填写处理方式] |

### 2.4 禁止触碰的接口和模块

> 发现需要调用以下接口时，立即 STOP，不得自行实现替代版本。

```
❌ [由 CODEX 填写，精确到方法或模块名]
❌ [每行一条]
```

---

## 3. 行为规格（Behavior Spec）

### 3.1 Happy Path（按步骤实现）

```
[由 CODEX 填写执行步骤，编号列出]
1. [步骤1]
2. [步骤2]
...
```

### 3.2 Edge Cases（必须处理，不得跳过）

| 场景 | 期望行为 |
|------|----------|
| [由 CODEX 填写场景] | [由 CODEX 填写期望行为] |

### 3.3 禁止操作

> 以下是给 Claude Code Agent 的硬性执行约束。
> 遇到任何需要执行以下操作的情况，立即输出 STOP，不得自行决定继续。

```
❌ 不得修改 §0"允许修改"范围之外的任何文件
❌ 不得写入 §0"允许只读参考"或"禁止访问"中的文件
❌ 不得运行数据库 migration 命令（除非 §9 测试命令明确允许）
❌ 不得调用任何外部 API 或模型 endpoint
❌ 不得引入冻结契约之外的字段 key、错误码或枚举值
❌ 不得在没有 STOP 输出的情况下自行解决架构级歧义
❌ [由 CODEX 补充本任务特定禁止操作]
```

---

## 4. 架构空白处理规则

> Claude Code（DeepSeek 模型）不得因为实现方便而自行补全以下内容。
> 如果实现必须依赖下列内容，立即输出：
> `[STOP: 架构空白 — <缺少什么> — 需要 CODEX 决策]`

```
❌ 不得新增领域模型
❌ 不得新增数据库字段或表
❌ 不得新增 API 字段或端点
❌ 不得新增状态枚举值
❌ 不得新增未在 ADR / PRD / ARCHITECTURE 中定义的抽象层
❌ 不得新增未批准的依赖库
❌ [由 CODEX 补充项目特定禁止新增内容]
```

---

## 5. 技术栈约束（来自 [ADR-XXX]，不得引入新依赖）

> 需要新依赖时，输出：
> `[STOP: 依赖缺失 — 需要 <库名>，原因 <描述>]`，不得自行安装。

| 能力 | 指定库/工具 | 说明 |
|------|------------|------|
| [由 CODEX 填写] | [库名] | [说明] |

**语言**：[由 CODEX 填写]
**数据库**：[本任务是否涉及 DB 写入，涉及则说明]
**测试框架**：[由 CODEX 填写]

---

## 6. Git 工作区规则

> 以下规则适用于 Claude Code 在本地项目文件夹中的所有操作。
> 违反任何一条，立即输出 STOP。

```
执行前：
  ✅ 必须确认当前分支为：[由 CODEX 填写分支名]
  ✅ 必须确认 git status --short 输出为空（工作区干净）
  ✅ 如工作区不干净，立即 STOP，不得自行处理未提交内容

执行中：
  ❌ 不得执行 git commit
  ❌ 不得执行 git push
  ❌ 不得自行切换分支（除非本 Task Spec 明确要求）
  ❌ 不得执行 git reset --hard
  ❌ 不得执行 git clean -fd
  ❌ 不得执行 git checkout .（覆盖工作区内容）
  ❌ 不得删除未在 §0"允许修改"中明确列出的文件
  ❌ 不得在 Codex 尚未审查并放行编码前规格映射计划时修改代码

完成后必须输出：
  - git status --short 结果
  - git diff --stat 结果
  - 实际修改、新增、删除的文件完整列表
```

---

## 7. 版本化要求

> 以下产物必须携带版本引用，确保历史快照不因后续迭代改变。

- [ ] [由 CODEX 填写需要版本化的字段或产物]
- [ ] [每行一条]

---

## 8. 验收标准（Acceptance Criteria）

> CODEX 审查时逐条核查；DeepSeek 完成后必须对照自检，结果写入第 10.4 节。
> 每条 Must Pass 必须可证伪：应写成给定输入或状态下可以判定真假的具体断言，不得只写描述性目标。

### Must Pass（必须全部通过）

- [ ] [由 CODEX 填写，每条可验证]
- [ ] [每行一条]

### 同根因分批修复一致性

- [ ] 本任务是否与已完成批次属于同一语义根因：[是 / 否]
- [ ] 如为“是”，列出前一批 `TASK_SPEC`、修复原则和本批对应关系。
- [ ] 逐项说明关键字段 / 信号的语义是一致还是存在差异；存在差异时给出可验证理由。
- [ ] Codex 必须在审查记录中明确接受或拒绝差异理由。

### 评测 / fixture / expected 正确答案来源

- [ ] 本任务是否涉及评测、fixture 或 expected JSON：[是 / 否]
- [ ] 如为“是”，说明 expected 值如何确定、由谁确定、是否依赖被测系统自身输出。
- [ ] 明确是否存在 parser / 被测系统输出倒填 expected 的循环验证。
- [ ] 如存在依赖，只能声明“一致性 / 回归稳定性”，不得声明“独立正确性”。

### Must Not（出现任一项即触发 B 或 C 结论）

- [ ] ❌ 修改了 §0"允许修改"范围之外的文件
- [ ] ❌ 写入了 §0"允许只读参考"或"禁止访问"中的文件
- [ ] ❌ 引入了 §5 之外的新依赖
- [ ] ❌ 补全了 §4 禁止补全的架构内容
- [ ] ❌ 执行了 §6 禁止的 Git 操作
- [ ] ❌ [由 CODEX 补充项目特定 Must Not]

### 测试场景覆盖

- [ ] [由 CODEX 填写，精确到具体场景]
- [ ] [每行一条]

---

## 9. 测试与验证命令

> 由 CODEX 明确指定，DeepSeek 必须按此执行，不得自行选择命令。
> 命令执行结果必须写入实现报告第 10.5 节。

```
必须运行：
  [由 CODEX 填写，例如：]
  - gradle test
  - npm run test
  - pytest src/[module]/tests/

可选运行（环境允许时执行）：
  [由 CODEX 填写，例如：]
  - gradle build

禁止运行（未经 CODEX 明确授权）：
  - 任何数据库 migration 命令
  - docker compose up / down
  - 任何需要外部网络的命令
  - 任何修改环境变量或配置文件的命令
  - [由 CODEX 补充项目特定禁止命令]
```

**测试失败时的处理规则**：
- 若测试失败，必须在实现报告中区分：**代码失败**（逻辑错误）vs **环境失败**（依赖缺失、权限、网络）
- 代码失败：修复后重新运行，不得跳过
- 环境失败：记录失败原因和命令输出，输出 `[STOP: 环境失败 — <命令> — <错误摘要> — 需要人工确认]`，不得自行处理环境问题

---

## ══════════ DeepSeek 执行指令块 ══════════

<!--
  使用说明：
  将以下整块内容（包含代码块标记）粘贴为 Claude Code 的首条 prompt。
  粘贴前确认 [由 CODEX 填写] 的占位符已全部替换为实际内容。
-->

```
你正在使用 Claude Code 在本地项目文件夹中执行子任务，推理模型为 DeepSeek。
本次任务由 CODEX 规划，执行完成后由 CODEX 审查。

你在 Claude Code 场景下的主动停止、主动提问和边界遵守行为不应被默认信任，
因此本任务通过显式 STOP 指令约束所有边界行为。遇到触发条件时必须停止并输出，
不得自行判断"这个情况应该可以继续"。

【第一步：执行前必读文件】
在做任何实现之前，必须先读取以下文件。
如果任一文件不存在、无法读取，或内容与本 Task Spec 冲突，立即 STOP：

  - AGENTS.md
  - CURRENT_CONTEXT.md
  - PRD.md
  - ARCHITECTURE.md
  - 父 TASK 文件（路径：[由 CODEX 填写]）
  - 当前 TASK_SPEC 文件（路径：[由 CODEX 填写]）

不得只依赖本 prompt 的摘要内容进行实现。

【第二步：确认 Git 工作区】
读取文件后，执行：
  git branch --show-current
  git status --short

确认：
  - 当前分支为 [由 CODEX 填写分支名]
  - git status --short 输出为空

任一条件不满足，立即 STOP：
  [STOP: Git 工作区异常 — <具体描述> — 需要人工处理后再继续]

【第三步：执行任务】
按照 Task Spec 第 0~4 节执行。
文件访问严格按 §0 三类边界：允许修改 / 允许只读参考 / 禁止访问。

遇到以下任何情况，立即停止，输出 STOP，不得自行决策继续：
  - 发现需要修改禁止范围内的文件
  - 发现需要写入"允许只读参考"中的文件
  - 发现接口契约有歧义或缺失
  - 发现需要引入新的依赖库
  - 发现实现方向与架构红线冲突
  - 发现需要新增领域模型、字段、枚举或抽象层
  - 发现需要调用任何模型 endpoint
  - 工作区出现预期之外的状态变化

STOP 输出格式（严格遵守）：
  [STOP: <类型> — <具体描述> — <建议或问题>]
  类型：红线冲突 / 接口歧义 / 契约缺失 / 架构空白 / 依赖缺失 /
        文件越界 / Git异常 / 环境失败 / 其他

【第四步：运行测试】
按照 Task Spec §9 指定的命令运行测试。
- 必须运行的命令全部执行，不得跳过
- 禁止运行的命令不得执行
- 测试失败时区分代码失败和环境失败，并记录完整命令输出

【第五步：完成后输出实现报告】
完成所有实现和测试后，必须依次执行并输出：
  git status --short
  git diff --stat

然后输出以下报告（格式严格，不得省略任何节）：

IMPLEMENTATION_REPORT_START
---
Git 状态：
  当前分支：[输出]
  git status --short：[输出，干净则写 "clean"]
  git diff --stat：[输出]
  实际修改/新增/删除文件：
    - [文件路径]：[新增 / 修改 / 删除]
    （严格区分"允许修改"范围内外）

测试执行结果：
  必须运行的命令：
    - [命令]：[✅ 通过 / ❌ 代码失败 / ⚠️ 环境失败]
    - 失败时附输出摘要：[摘要]
  可选命令（若执行）：
    - [命令]：[结果]
  环境失败记录：
    - [命令]：[错误摘要] | 判断依据：[为什么判断为环境失败而不是代码失败]
    （无环境失败则写：无）

假设列表：
  - [假设]：[内容] | 原因：[原因]
  （无假设则写：无）

歧义列表：
  - [歧义]：[描述] | 我的处理：[描述] | 建议确认：[问题]
  （无歧义则写：无）

未实现 / 降级实现：
  - [项目]：[原因] | 建议后续：[建议]
  （全部实现则写：无）

STOP 记录：
  - [STOP 原文] | 处理结果：[等待人工 / 已按指示继续]
  （无 STOP 则写：无）

自检结果（对照 Task Spec §8 逐条）：
  Must Pass：
    - [验收项]：✅ 通过 / ❌ 未通过 / ⚠️ 部分通过
  Must Not：
    - 文件"允许修改"范围约束：✅ 未越界 / ❌ 有越界
    - 文件"允许只读"未被写入：✅ 未写入 / ❌ 有写入
    - 新依赖引入：✅ 无 / ❌ 有
    - 架构空白补全：✅ 无 / ❌ 有
    - Git 操作合规：✅ 合规 / ❌ 违规
    - 禁止命令未执行：✅ 未执行 / ❌ 有执行

需要 CODEX 审查的决策点：
  - [决策点]：[描述]
  （无则写：无）
---
IMPLEMENTATION_REPORT_END
```

---

## 10. 实现报告（DeepSeek 完成后填写）

> 将 DeepSeek 输出的 `IMPLEMENTATION_REPORT` 内容完整粘贴至此。
> 你不需要手动整理，直接复制粘贴。
> 如有 STOP 记录，在 10.3 节补充你的处理决策。

### 10.1 Git 状态与修改文件列表

（粘贴自 DeepSeek 输出）

### 10.2 假设 / 歧义 / 未实现

（粘贴自 DeepSeek 输出）

### 10.3 STOP 记录及你的处理决策

（粘贴自 DeepSeek 输出，并在每条 STOP 下补充：你如何响应 / 是否已解决）

### 10.4 自检结果

（粘贴自 DeepSeek 输出）

### 10.5 测试执行结果

（粘贴自 DeepSeek 输出）

### 10.6 需要 CODEX 审查的决策点

（粘贴自 DeepSeek 输出）

---

## 11. CODEX 审查记录

> CODEX 对照 §8 验收标准和实现报告（§10）逐条审查后填写。
> 最终结论只能选择以下三项之一。

```
A. 可以合并
   — 所有 Must Pass 通过，所有 Must Not 未触发，Git 状态合规，
     测试通过（或环境失败已由人工确认为非代码问题）

B. 需要 Claude Code 修改后再审
   — 存在可修复问题，但不需要推翻设计
   — 必须列出修改项，DeepSeek 修改完成后重新提交第 10 节

C. 禁止合并，必须回滚或重做
   — 触发了 Must Not，或存在架构级问题
   — 必须说明原因，并明确是回滚到任务开始前状态，还是完全重做
```

| 审查项 | 结论 | 备注 |
|--------|------|------|
| Must Pass 全部通过 | ✅ / ❌ / ⚠️ | |
| Must Not 全部未触发 | ✅ / ❌ | |
| 文件访问三类边界合规 | ✅ / ❌ | |
| Git 工作区规则合规 | ✅ / ❌ | |
| 架构空白未被补全 | ✅ / ❌ | |
| 接口契约符合性 | ✅ / ❌ / ⚠️ | |
| 版本化字段完整性 | ✅ / ❌ / ⚠️ | |
| 测试结果（代码层面） | ✅ / ❌ / ⚠️环境失败已确认 | |

**结论**：[A / B / C]
**原因**：
**必须修改项**（B 时填写）：
**回滚或重做说明**（C 时填写）：
**可选优化项**（下一任务可处理）：
**是否允许进入下一任务**：是 / 否

---

## 12. 后续任务联动

> 结论为 A 后，CODEX 解锁以下任务。

| 后续任务 | 依赖本任务的产物 | 状态 |
|----------|-----------------|------|
| TASK-XXX [任务名] | [具体产物] | 🔒 待本任务 A 结论 |
| [其他] | [产物] | 🔒 |

---

## 附录：本任务引用的文档

| 文档 | 关键章节 |
|------|----------|
| AGENTS.md | [相关章节] |
| PRD.md | [相关章节] |
| ARCHITECTURE.md | [相关章节] |
| CURRENT_CONTEXT.md | 当前阶段已确认事项 |
| [其他文档] | [章节] |

---

*本 Task Spec 由 CODEX 起草，你审核定稿后，将执行指令块粘贴进 Claude Code 启动任务。*
*DeepSeek 完成后将实现报告粘贴至第 10 节，CODEX 审查后填写第 11 节。*
*结论 A 后，由 CODEX 按父 TASK 要求更新 CURRENT_CONTEXT.md、父 TASK 完成记录、changelog/当前月份.md；如项目使用 context_history.md，也同步更新。*

---

## 13. TASK_SPEC 类型字段说明

### 13.1 类型值

| 类型值 | 含义 | 使用模板 |
|---|---|---|
| `execution` | 执行型 `TASK_SPEC` | `tasks/TASK_SPEC_TEMPLATE_CLAUDECODE_DEEPSEEK.md` |
| `readonly-review` | 只读复查型 `TASK_SPEC` | `tasks/TASK_SPEC_REVIEW_TEMPLATE_READONLY.md` |

### 13.2 类型选择规则

* `execution`：父 `TASK` 边界已冻结，需要局部实现、测试、bugfix 或 refactor。
* `readonly-review`：目标执行型 `TASK_SPEC` 交付物已就绪，且 Codex 判断需要独立只读复查。

### 13.3 路由规则

* 当类型为 `execution` 时，使用本模板。
* 当类型为 `readonly-review` 时，使用 `tasks/TASK_SPEC_REVIEW_TEMPLATE_READONLY.md`。
* readonly-review 的执行规则、工作区规则、证据规则和输出格式，以 readonly-review 专用模板为准。

### 13.4 设定约束

* `TASK_SPEC 类型` 字段只能由 Codex 设定。
* 外部顾问不得替代 Codex 设定该字段。
* readonly-review 输出只作为 Codex 的 Review Intake Decision 输入，不自动代表接纳。
