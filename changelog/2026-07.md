# 2026-07 changelog

## 2026-07-13 TASK_SPEC-DATA-001-A 实现提交与 push

- 用户明确授权 Codex 精确 stage 并 commit 8 个已接纳实现路径，在无问题时 push；staged diff 恰好为 8 个路径，`git diff --cached --check` 通过。
- 实现提交为 `32b4c414349118b9225a65fa08eb4e3466f82a2e`（`test: add accepted human anchor fixtures`），共 8 files / 1899 insertions。
- 已 push 到 `origin/codex/task-data-001-a-human-anchor-conversion` 并建立 upstream；提交后隔离 worktree 干净。未创建 PR，未运行完整 MVP E2E，不归档 `TASK-EVAL-001`，不进入 `TASK-028` / `TASK-031` / `TASK-032`。

## 2026-07-13 TASK_SPEC-DATA-001-A test-only revision 接纳

- Claude Code / DeepSeek 已只修改新建的 `HumanAnchorGroundTruthFixtureTest.java`，补齐 16 个直接字段 JSON string 类型、派生 boolean 类型、XLSX 行顺序、19 个 required columns、精确 18-key schema 和完整 positive-case baseline 断言；其他 7 个实现路径未扩大范围。
- Codex 重跑 `HumanAnchorGroundTruthFixtureTest` 为 10/10、`ParserBackedEvidenceOverlapBaselineTest` 为 4/4，均 0 failures / 0 errors；`git diff --check` 通过。
- 独立 agent 对 test-only delta、其他 7 个实现路径、人工来源独立性和 canonical baseline 给出 `GO`，无 blocking findings。
- Codex Decision：`GO / IMPLEMENTATION ACCEPTED / INDEPENDENT READ-ONLY AUDIT GO / AWAITING CODEX COMMIT AUTHORIZATION / NO PUSH`。未 commit 8 个实现路径，未运行完整 MVP E2E，不归档 `TASK-EVAL-001`，不进入 `TASK-028` / `TASK-031` / `TASK-032`。

## 2026-07-13 TASK_SPEC-DATA-001-A 首轮实现 Review NO-GO

- Claude Code / DeepSeek 已在隔离 worktree 完成 8 个允许路径的首轮实现；当前三份 fixture 为 22/19/22 共 63 条，57 条纳入、6 条排除，16 个直接字段为 JSON string，18 个 key 齐全且顺序与 XLSX 一致；三份 expected 仅新增 `goldenExpected.humanAnchorGroundTruth`。
- Codex 重跑 `HumanAnchorGroundTruthFixtureTest` 为 10/10、`ParserBackedEvidenceOverlapBaselineTest` 为 4/4，均 `BUILD SUCCESSFUL`；独立 agent 对路径范围、人工来源独立性和当前 canonical baseline 给出 `GO`。
- Codex Decision：`NO-GO / TEST CONTRACT REVISION REQUIRED / DATA ARTIFACTS RETAINED / NO COMMIT OR PUSH`。阻塞项为新测试未有效锁定 JSON 类型、occurrence 顺序、全部 required columns、精确 18-key schema 和完整 positive-case baseline。
- 下一步只允许修改 `HumanAnchorGroundTruthFixtureTest.java`；不得修改当前 fixture、expected JSON、README 或其他文件，不运行完整 MVP E2E，不归档 `TASK-EVAL-001`，不进入 `TASK-028` / `TASK-031` / `TASK-032`。

## 2026-07-13 TASK_SPEC-DATA-001-A 第二修订版计划 GO

- Claude Code / DeepSeek 第二修订版计划已覆盖前两轮八项修订要求：16 个 XLSX 直接字段与 2 个 `notes` 派生字段分开验证，直接字段空字符串保留为 JSON `""`，其余来源、排除语义、canonical baseline 和测试边界均保持冻结。
- Codex 只读检查人工 XLSX，确认 `occurrenceNo` 是 `001-PA-01` 等字符串追溯 ID；绑定澄清为 JSON string，不接受计划中“额外写入 JSON number”的孤立表述，也不新增数字序号字段。
- Codex Decision：`GO / CODING-PLAN ACCEPTED WITH BINDING CLARIFICATION / IMPLEMENTATION AUTHORIZED WITHIN §0.3 / NO COMMIT OR PUSH`。
- 本轮仅同步门禁与项目记忆；未修改 fixture、expected JSON、测试、实现、DOCX、人工 XLSX 或 sample matrix，未运行完整 MVP E2E，不归档 `TASK-EVAL-001`，不进入 `TASK-028` / `TASK-031` / `TASK-032`。

## 2026-07-13 TASK_SPEC-DATA-001-A 修订版计划复审 NO-GO

- Claude Code / DeepSeek 修订版计划已响应首轮六项要求；计划前后主工作区和隔离 worktree 仍 clean，未修改文件。
- Codex Decision：`NO-GO / CODING-PLAN REVISION ROUND 2 REQUIRED / NO IMPLEMENTATION AUTHORIZATION`。
- 剩余两项：18 字段测试必须拆为 16 个 XLSX 直接字段和 2 个 `notes` 派生字段；XLSX 直接字段的空字符串必须保留为 JSON `""`，只有纳入项 `exclusionReason` 为 JSON `null`。
- 未运行测试或完整 MVP E2E，未修改 fixture、expected JSON、测试或实现文件。

## 2026-07-13 TASK_SPEC-DATA-001-A 首版编码前计划审查 NO-GO

- 用户回传 Claude Code / DeepSeek §0.2 编码前规格映射计划；计划前后主工作区和隔离 worktree 均 clean，未修改 fixture、expected JSON、测试或实现文件。
- Codex Decision：`NO-GO / CODING-PLAN REVISION REQUIRED / NO IMPLEMENTATION AUTHORIZATION`。
- 必须修订 6 项：occurrence schema 应为 18 字段；排除原因缺失必须 STOP 且不得补值；`humanAnchorGroundTruth` 必须是 `goldenExpected` sibling；全部人工可见值按字符串读取；既有 expected 不变不得同源自证；补足 XLSX `docxPath` 到 fixture `sourceDocx` 的唯一映射和 STOP 条件。
- 未运行测试或完整 MVP E2E，不归档 `TASK-EVAL-001`，不进入 `TASK-028` / `TASK-031` / `TASK-032`。

## 2026-07-13 TASK-DATA-001 可重建基线与隔离 worktree

- 提交前独立只读审计最终 `GO` 后，7 个允许路径已精确提交为 `a2e9c085cd4a073d7f9dcba55cf891ace3d556da`（`data: freeze accepted MVP human anchors`）；未 push。
- 已创建隔离分支 `codex/task-data-001-a-human-anchor-conversion` 和 worktree `C:\Users\1\Documents\CQCP-worktrees\task-data-001-a`；已接受 XLSX 可由 Git 重建，隔离 worktree 初始状态干净。
- `TASK_SPEC-DATA-001-A` Decision 更新为 `GO TO CODING-PLAN MAPPING / CLEAN ISOLATED BASELINE VERIFIED / NO IMPLEMENTATION AUTHORIZATION`。
- 当前只允许 Claude Code / DeepSeek 输出 §0.2 编码前规格映射计划并停止；未修改 fixture、expected JSON、测试或实现文件，未运行完整 MVP E2E。

## 2026-07-13 TASK-DATA-001 提交前审计阻塞修正

- 独立只读审计首次结论为 `NO-GO`：项目记忆混淆“人工 anchor 已接受”与“fixture/E2E 尚未验证”，且 DOCX 003 用户侧修正缺少正式任务边界例外。
- 已统一状态口径：`TASK-DATA-001` 的 63 条 `BLOCK` / `TABLE_CELL` 逐出处明细已接受为独立人工 ground truth；尚未完成 fixture / expected JSON / 测试转换，也未运行完整 MVP E2E；`TASK-EVAL-001` 继续 active 且不归档。
- 已记录 DOCX 003 为用户侧修正例外，Codex 未编辑该文件。相对提交前 `HEAD` 的三处可见文本变化为：金额大写改为 `壹万贰仟捌佰元整`、签约通知表改为 `二标段`、《阳光合作协议》前言改为 `二标段`；Word 保存同时改变 9 个 OOXML 部件。
- DOCX 003 SHA-256 为 `48BB0FC63A0182BAFF5D7BD3E38F3A4409F68E7C371E5D24C02970C6362BF7B3`；人工 XLSX SHA-256 为 `3DC0DABE37DF2556B5F48B68600A0E24F092373FBD96B1E65B696FF0B9AEBA0B`。
- 阻塞修正后的最终独立只读审计结论为 `GO`，允许提交范围精确为人工 XLSX、用户侧 DOCX 003 修正、父 TASK、转换 TASK_SPEC、`CURRENT_CONTEXT.md`、`tasks/MVP_TASK_MAP.md` 和本 changelog 共 7 个路径。未修改 fixture、expected JSON、测试、sample matrix、parser 或 Review Engine，未运行完整 MVP E2E。

## 2026-07-12 TASK_SPEC-DATA-001-A 编码前基线门禁 NO-GO

- 按冻结规格只读核对 `AGENTS.md`、`CURRENT_CONTEXT.md`、父任务和 `TASK_SPEC-DATA-001-A`。
- 当前分支为 `master`，`git status --short` 非空；已接受 XLSX 在文件系统中存在，但尚未被 Git 跟踪，无法从当前 Git 基线重建。
- Codex Decision：`NO-GO / EXECUTION BASELINE NOT READY / PLAN REQUEST NOT DISPATCHED / NO IMPLEMENTATION AUTHORIZATION`。
- 本轮未要求 Claude Code / DeepSeek 输出编码前规格映射计划，未修改 fixture、expected JSON、测试、DOCX、sample matrix 或业务代码。
- 下一步必须先完成当前成果的提交前审查和可重建基线准备，再由 Codex 创建干净隔离执行环境；不得为获得 clean status 回退或隐藏用户侧 DOCX 003 修改。

## 2026-07-12 TASK_SPEC-DATA-001-A 转换规格冻结

- 新建 `tasks/active/TASK_SPEC-DATA-001-A-human-anchor-fixture-expected-test-conversion.md`，关联父任务 `TASK-DATA-001`。
- 冻结转换产物：001/002/003 三份规范化 human-anchor fixture JSON、三份 expected JSON 的 `humanAnchorGroundTruth` 引用对象、一个 XLSX → fixture → expected 追溯完整性定向测试和 fixture README 更新。
- 冻结数量：22/19/22 共 63 条，57 条纳入一致性评价、6 条标题/前言名称排除；禁止把排除项删除或把多个 occurrence 合并。
- 明确现有 `expectedCanonicalAnchors[]` 继续作为 parser-backed 历史回归基线，不得由 parser actual 倒填或冒充人工 ground truth；本规格禁止修改 parser、CandidateResolver、EvidenceSlot、SourceAnchor 或 Review Engine。
- 当前 Decision 为 `SPEC FROZEN / CODING-PLAN MAPPING ONLY / NO IMPLEMENTATION AUTHORIZATION`；主工作区不干净，执行前必须由 Codex 准备包含已接受 XLSX 的干净隔离基线。
- 本轮只创建和冻结 `TASK_SPEC` 并同步项目记忆；不修改 fixture、expected JSON、测试、DOCX、sample matrix 或业务代码，不运行完整 MVP E2E，不归档 `TASK-EVAL-001`，不进入 `TASK-028` / `TASK-031` / `TASK-032`。

## 2026-07-12 TASK-DATA-001 人工 ground truth 接受

- 根据 `ZK` 的最终确认，将三份合同 63 条逐出处明细全部提升为 `ACCEPTED_HUMAN_GROUND_TRUTH`；`groundTruthSource` 保持 `MANUAL_DOCX_REVIEW`。
- 提升前验证 63 条均为 `MATCH`，63 条均为人工来源，6 条合同标题/前言名称的“排除”判断继续保留并标记为已最终接受。
- 27 条审核点汇总仅作为总览，不作为单条 anchor 转换输入；本轮不修改其语义为逐出处 ground truth。
- 下一步 fixture / expected JSON / 测试转换必须另行创建并冻结关联 `TASK-DATA-001` 的 `TASK_SPEC`；本轮不执行转换。
- 不修改业务代码、测试代码、DOCX、expected JSON、`cqcp-mvp-sample-matrix.xlsx`、parser、`CandidateResolver`、`EvidenceSlot`、`SourceAnchor` 或 Review Engine；不运行完整 MVP E2E，不归档 `TASK-EVAL-001`，不补足 DoD #12，不进入 `TASK-028` / `TASK-031` / `TASK-032`。

## 2026-07-12 TASK-DATA-001 逐出处人工确认

- `ZK` 确认三份合同 63 条明细的 H 列观察值和 J-O 列粒度、原文、页码及结构上下文；I 列全部更新为 `MATCH`。
- 6 条合同标题/前言中的合同名称在 T 列标记“排除”，仅保留为可追溯出处，不参与甲乙方名称一致性结论。
- 63 条明细的来源和状态更新为 `MANUAL_DOCX_REVIEW / ANNOTATED_PENDING_REVIEW`；未提升为 `ACCEPTED_HUMAN_GROUND_TRUTH`。
- 确认 `CQCP-MVP-DOCX-003` 样本可用、脱敏合规，data owner 为 `ZK`。
- 不修改业务代码、测试代码、DOCX、expected JSON、`cqcp-mvp-sample-matrix.xlsx`、parser、`CandidateResolver`、`EvidenceSlot`、`SourceAnchor` 或 Review Engine；不运行完整 MVP E2E，不归档 `TASK-EVAL-001`，不补足 DoD #12，不进入 `TASK-028` / `TASK-031` / `TASK-032`。

## 2026-07-12 TASK-DATA-001 逐出处 anchor 拆分待确认

- 保留 `CQCP-MVP-DOCX-001` / `002` / `003` 的 27 条审核点人工汇总；直接读取当前三份 DOCX，将同一审核点的多个可见出处拆为 63 条独立明细，写入 XLSX 的“anchor明细待确认”工作表。
- 每条明细分别记录 `occurrenceNo`、观察值、比较结果、`BLOCK` / `TABLE_CELL` 实际粒度、原文、页码和结构上下文；名称全文多次出现时不预设主次，逐出处核对，任一纳入范围的出处不一致即形成审核点不一致。
- 63 条明细全部标记为 `AI_ASSISTED_SPLIT_FROM_MANUAL_SUMMARY_NOT_GROUND_TRUTH / NEEDS_REVIEW`，等待 `zk` 直接对照 DOCX 逐条确认；未把 Codex 拆分结果声明为人工 ground truth。
- 删除过期“AI预提取草稿”工作表；修正 `003` 合同金额汇总为正文 `12800 / 壹万贰仟捌佰元整`，并按正文主 anchor 记为 `BLOCK`。
- 不修改业务代码、测试代码、DOCX、expected JSON、`cqcp-mvp-sample-matrix.xlsx`、parser、`CandidateResolver`、`EvidenceSlot`、`SourceAnchor` 或 Review Engine；不运行完整 MVP E2E，不归档 `TASK-EVAL-001`，不补足 DoD #12，不进入 `TASK-028` / `TASK-031` / `TASK-032`。

## 2026-07-10 TASK-DATA-001 第 3 份候选与 AI 预提取草稿

- 复核用户在 `TASK-DATA-001-human-anchor-template.xlsx` 中完成的 `CQCP-MVP-DOCX-001` / `002` 共 18 条人工标注；当前均为 `ANNOTATED_PENDING_REVIEW`，未提升为 `ACCEPTED_HUMAN_GROUND_TRUTH`。
- 只读比较 `CQCP-MVP-DOCX-003` / `004`：`003` 含封面信息表和签约通知书金额单元格，可补充 `TABLE_CELL` 人工定位，故补选为第 3 份候选；`004` 未发现表格，不占用本任务候选名额。
- 在 `outputs/task-data-001-anchor-template/TASK-DATA-001-human-anchor-template.xlsx` 新增“AI预提取草稿”工作表，填入 `003` 的 9 个审核点；前三项为 `TABLE_CELL`，其余为 `BLOCK`，全部标为 `NEEDS_REVIEW` 和 `AI_ASSISTED_PREANNOTATION_NOT_GROUND_TRUTH`，等待 `zk` 直接阅读 DOCX 后确认。
- 记录 `003` 的高风险待确认项：文件名为“二标段”但签约通知表为“三标段”；合同小写总价为 `12800`，大写为“捌仟捌佰肆拾捌元整”。记录 `004` 的后续线索：预付款 15%、结算款 100% / 97% 并存、质量保函 3%。
- 不修改业务代码、测试代码、DOCX、expected JSON、`cqcp-mvp-sample-matrix.xlsx`、parser、`CandidateResolver`、`EvidenceSlot`、`SourceAnchor` 或 Review Engine；不运行完整 MVP E2E，不归档 `TASK-EVAL-001`，不补足 DoD #12，不进入 `TASK-028` / `TASK-031` / `TASK-032`。

## 2026-07-09 TASK-DATA-001 样本可用性人工确认

- 用户确认 `CQCP-MVP-DOCX-001` 与 `CQCP-MVP-DOCX-002` 均可用于项目验证，data owner / 确认人为 `zk`。
- 用户确认两份样本脱敏合规已确认。
- 本次确认只覆盖样本可用性和脱敏合规，不等于人工 anchor 标准答案已完成；真实 DOCX `TABLE_CELL` 人工 anchor 仍需按 `TASK-DATA-001` 模板人工标注。
- 本轮仅同步任务文件、`CURRENT_CONTEXT.md`、`tasks/MVP_TASK_MAP.md` 和本 changelog；不修改代码、测试、DOCX 样本、expected JSON、`cqcp-mvp-sample-matrix.xlsx`、parser、`CandidateResolver`、`EvidenceSlot`、`SourceAnchor`、Review Engine、workflow、ADR 或 PRD。

## 2026-07-09 TASK-DATA-001 MVP E2E 人工 anchor 准备执行

- 冻结真实 DOCX `TABLE_CELL` 人工 anchor 标注规则：人工 ground truth 必须来自人工直接阅读 DOCX、业务方确认材料或 data owner 明确确认，不得由 parser / AI / 被测系统 actual 输出倒填。
- 选定 `CQCP-MVP-DOCX-001` 与 `CQCP-MVP-DOCX-002` 作为本轮候选样本，覆盖工程采购合同和产品采购合同；data owner、人工确认者、样本可用性和是否补选第 3 份样本均标记为“待确认”。
- 定义人工标注最小字段模板，覆盖 `sampleId`、`docxPath`、`reviewPointCode`、`expectedCandidateValue`、`anchorGranularity`、`humanAnchorText`、`tableContext`、`rowContext`、`cellContext`、`groundTruthSource`、`dataOwner`、`independenceStatement`、`status` 等字段。
- 明确后续 fixture / expected JSON / 测试转换必须另行冻结关联 `TASK-DATA-001` 的 `TASK_SPEC`；只有 `status = ACCEPTED_HUMAN_GROUND_TRUTH` 且 data owner / 人工复核证据已记录时，才可作为真实 DOCX `TABLE_CELL` expected anchor 来源。
- 本轮不修改业务代码、测试代码、DOCX 样本、expected JSON、`cqcp-mvp-sample-matrix.xlsx`、parser、`CandidateResolver`、`EvidenceSlot`、`SourceAnchor`、Review Engine、workflow、ADR、PRD、OpenAPI、Docker 或数据库迁移；不运行完整 MVP E2E，不归档 `TASK-EVAL-001`，不补足 DoD #12，不进入 `TASK-028` / `TASK-031` / `TASK-032`。

## 2026-07-09 TASK-DATA-001 Review Intake / 任务建档

- 新建 `tasks/active/TASK-DATA-001-mvp-e2e-human-anchor-preparation.md`，承接 MVP E2E 人工 anchor 准备和真实 DOCX `TABLE_CELL` 标准答案前置工作。
- Review Intake Decision：`GO TO TASK-DATA-001 TASK BUILD / NO IMPLEMENTATION AUTHORIZATION`。
- 明确本任务只冻结人工 anchor 标注规则、候选样本选择原则和人工标注字段模板；不修改代码、测试、fixture、expected JSON、workflow、ADR、PRD、OpenAPI、Docker 或数据库迁移。
- 明确人工 anchor ground truth 必须独立于 parser / AI / 被测系统 actual 输出；后续转换为 fixture / expected JSON / 测试必须另行冻结 `TASK_SPEC`。
- 本轮不归档 `TASK-EVAL-001`，不补足 DoD #12，不进入 `TASK-028` / `TASK-031` / `TASK-032`。

## 2026-07-09 治理规则收敛与低风险文档批处理

- 将 `TASK-EVAL-001` 独立审计后状态收口与治理规则收敛合并为一次低风险文档批处理；不新建 TASK，不派独立 agent，不归档 `TASK-EVAL-001`。
- 明确低风险文档同步、状态摘要、changelog 补录、路径修正和 post-merge 状态写回可由 Codex 自查并合并式 Memory Writeback；不要求每个小状态变化单独收尾。
- 明确 Next Task Handoff Prompt 仅在存在明确下一任务文件、编号、目标和边界时输出；v3 强门禁不自动泛化到普通文档状态同步。
- 强门禁继续保留：父任务归档、`push` / `merge`、进入 `TASK-028` / `TASK-031` / `TASK-032`、`EvidenceSlot` / `CandidateResolver` / `SourceAnchor`、expected fixture / 评测指标、workflow / CI / required checks / branch protection、Codex 自写代码后自审、工作区或远程状态不清、人工 anchor ground truth。
- 本轮不修改代码、测试、fixture、expected JSON、workflow、ADR、PRD、OpenAPI、Docker 或数据库迁移，不 push。

## 2026-07-09 TASK-EVAL-001 独立审计后状态收口

- 用户确认 `TASK-EVAL-001` 独立只读审计已完成；Codex Review Intake Decision 为 `NO-GO TO ARCHIVE / KEEP ACTIVE`。
- 明确 DoD #12 固定未通过、不可补足，`TASK-GOV-005` 不补足 DoD #12；真实 DOCX `TABLE_CELL` 仍未验证；expected / anchor 当前只证明 parser-backed 一致性，不证明独立人工 ground truth。
- `TASK-028` / `TASK-031` / `TASK-032` 继续 `NO-GO`；后续实际推进方向合并为 `TASK-DATA-001` / MVP E2E 人工 anchor 准备，不再围绕 `TASK-EVAL-001` 反复写文档。
- 该状态收口已与治理规则收敛合并为同一次低风险文档批处理；不创建新任务，不归档 `TASK-EVAL-001`，不修改代码、测试、fixture、expected JSON、workflow、ADR 或 PRD，不 push。

## 2026-07-09 TASK-033 提交前收口授权

- 用户确认独立只读审计已对 `TASK-033` 归档迁移与 Memory Writeback 给出 `GO`。
- 用户授权执行提交前收口、精确 stage、commit 与 push；不允许进入下一任务。
- 本轮仅补记提交前审计与授权证据，不改变 `TASK-033` 归档边界，不归档 `TASK-EVAL-001`，不进入 `TASK-028` / `TASK-031` / `TASK-032`。

## 2026-07-08 TASK-033 父任务归档迁移

- 执行 `TASK-033` 归档迁移，将任务文件从 `tasks/active/TASK-033-mvp-e2e-sample-acceptance-spec-freeze.md` 移动到 `tasks/done/TASK-033-mvp-e2e-sample-acceptance-spec-freeze.md`。
- PR #24 已合并规格冻结建档，merge commit 为 `880893639ada9fa5e2d42b3d2bccb1662e37a5c9`；PR #25 已合并 post-merge 状态写回，merge commit 为 `a60fc9f`，head commit 为 `79c0b3b`。
- Codex 归档 Review Intake Decision 为 `GO_TO_ARCHIVE_WITH_POST_MERGE_SYNC_SATISFIED`；归档前独立只读审计结论 `NEEDS_POST_MERGE_SYNC` 指出的状态残留已通过 PR #25 与本次归档迁移清理。
- 本轮仅同步 `TASK-033` 任务文件、`docs/VERIFY.md`、`CURRENT_CONTEXT.md`、`tasks/MVP_TASK_MAP.md` 和本 changelog；不归档 `TASK-EVAL-001`，不补足 `TASK-EVAL-001` DoD #12，不进入 `TASK-028` / `TASK-031` / `TASK-032`。
- 本轮不修改代码、测试、fixture、expected JSON、workflow、ADR 或 PRD，不授权 Gemma Provider、parser provenance、`SourceAnchor` 或 `TABLE_CELL` coverage 实现，不把 AI/parser 输出当作人工 anchor 标准答案。

## 2026-07-08 TASK-033 post-merge 状态写回

- PR #24 已合并，merge commit 为 `880893639ada9fa5e2d42b3d2bccb1662e37a5c9`。
- 根据 `TASK-033` 父任务归档前独立只读审计结论 `NEEDS_POST_MERGE_SYNC`，清理 post-merge 过期状态残留。
- 本轮仅同步 `tasks/active/TASK-033-mvp-e2e-sample-acceptance-spec-freeze.md`、`CURRENT_CONTEXT.md`、`tasks/MVP_TASK_MAP.md` 和本 changelog 的 post-merge 状态；不归档 `TASK-033`，不归档 `TASK-EVAL-001`，不补足 `TASK-EVAL-001` DoD #12，不进入 `TASK-028` / `TASK-031` / `TASK-032`。
- 本轮不修改代码、测试、fixture、expected JSON、workflow、ADR 或 PRD，不授权 Gemma Provider、parser provenance、`SourceAnchor` 或 `TABLE_CELL` coverage 实现，不把 AI/parser 输出当作人工 anchor 标准答案。

## 2026-07-07 TASK-033 MVP 端到端样本验收规格冻结建档

- 新建 `tasks/active/TASK-033-mvp-e2e-sample-acceptance-spec-freeze.md`，将 `TASK-033` 定界为 Codex 主控的 MVP 端到端样本验收规格冻结任务，不是 CC-DS 执行环境实现 `TASK_SPEC`。
- 在 `docs/VERIFY.md` 增加 MVP 端到端样本验收规格入口，明确样本选择、输入字段、验收命令、证据口径和 expected 来源独立性要求。
- 明确本任务不运行完整验收，不修改代码、测试、fixture、expected JSON、parser、`CandidateResolver`、`EvidenceSlot`、`SourceAnchor`、workflow、ADR 或 PRD。
- 明确不归档 `TASK-EVAL-001`，不补足 DoD #12，不进入 `TASK-028` / `TASK-031` / `TASK-032`，不迁移 historical active，不授权 Gemma Provider、parser provenance、`SourceAnchor` 或 `TABLE_CELL` coverage 实现。
- 明确 `TASK-DATA-001` 承接真实 DOCX `TABLE_CELL` 人工 anchor 标准答案准备；不得把 AI/parser 输出当作人工 anchor 标准答案。
- 本任务正式提交前独立 agent 只读复核已完成，Decision 为 `GO`，无 blocking findings；PR #24 已合并，merge commit 为 `880893639ada9fa5e2d42b3d2bccb1662e37a5c9`。

## 2026-07-07 TASK-GOV-006 远端状态矫正

- 修正 `TASK-GOV-006` 过期状态：PR #18 已合并，head commit 为 `432a63a25b0352e5ba9768f68f32c95a266474e4`，merge commit 为 `d3d5d1b507d233b5ff9a20350fad7b0c05a36cf9`。
- PR #18 触发的 CI 已通过，`Authorization evidence check`、`Backend Gradle tests`、`Admin web lint, tests, and build` 均成功完成。
- 明确 `Authorization evidence check` 已在 PR #18 中成功运行，但它不是 required status check，也不证明用户授权、测试、独立审计或 Memory Writeback 已真实发生。
- 本轮仅修改 `CURRENT_CONTEXT.md`、`tasks/MVP_TASK_MAP.md`、`tasks/active/TASK-GOV-006-submit-authorization-evidence-gate.md` 和本 changelog；不修改代码、测试、workflow，不迁移 `tasks/active` 文件，不归档 `TASK-EVAL-001`，不进入 `TASK-028` / `TASK-031` / `TASK-032`。

## 2026-07-06 TASK-030 父任务归档

- 已按 `GO_TO_ARCHIVE_WITH_CONDITIONS_SATISFIED` 执行 `TASK-030` 父任务归档流程。
- 已将 `TASK-030` 父任务文件和 `TASK_SPEC-030-A/B/C` 从 `tasks/active/` 移动到 `tasks/done/`。
- 已同步 `CURRENT_CONTEXT.md`、`tasks/MVP_TASK_MAP.md` 和本 changelog 的归档状态。
- 本次归档不修改业务代码、测试、fixture、expected JSON、OpenAPI、数据库、Docker、workflow、ADR 或 PRD；不授权 runtime loader、数据库资产表、发布审批、model / budget profile 源定义、`TASK-028`、`TASK-031`、`TASK-032` 或 `TASK-033`。

## 2026-07-06 TASK-030 归档 Review Intake Decision

- PR #21 已合并 `TASK-030` post-merge 状态写回，head commit 为 `40f28e7159f0cfc7d63546db07c5b8819edbc0c3`，merge commit 为 `ef4643c7d11b1209c935229b4e39b76d659db00f`。
- PR #21 三项 PR workflow checks 均为 `SUCCESS`：`Authorization evidence check`、`Backend Gradle tests`、`Admin web lint, tests, and build`；其中 `Authorization evidence check` 不是 required status check。
- 独立只读审计结论为 `GO_WITH_CONDITIONS`；B1-B5 条件已通过 PR #21 修正并合并。
- Codex 归档 Review Intake Decision：`GO_TO_ARCHIVE_WITH_CONDITIONS_SATISFIED`。
- 本 Decision 只授权 `TASK-030` 父任务归档流程；不授权 runtime loader、数据库资产表、发布审批、model / budget profile 源定义、`TASK-028`、`TASK-031`、`TASK-032` 或 `TASK-033`。

## 2026-07-06 TASK-030 PR #20 合并与父任务收口判断

- PR #20 已合并 `TASK-030` Review assets 版本化治理 A/B/C 当前批次，head commit 为 `29d4a94eaf07b37831f07dd93f78d3027b770f01`，merge commit 为 `eadd017808fc8c2471fb0ac081f1e68e22dce5f5`。
- post-merge 只读核查结论为 `POST_MERGE_GO`：本地 `master` 已与 `origin/master` 对齐，工作区 clean，变更文件均属于 `TASK-030` A/B/C 授权范围，7 个 Review assets JSON 均通过 `node scripts/validate-review-assets.mjs` 校验。
- 父任务收口判断为 `READY_FOR_INDEPENDENT_PRE_ARCHIVE_AUDIT / NOT_ARCHIVED`：Codex post-merge 只读核查不能替代父任务归档前独立 agent 只读审计，因此当前不得归档 `TASK-030`。
- 修正 `TASK_SPEC-030-A/B/C` 顶部状态残留：由“待提交”改为“PR #20 已合并 / 待父任务归档前独立只读审计”，不改变执行记录中的历史阶段事实。
- 本轮仅同步 `TASK-030` 父任务、`CURRENT_CONTEXT.md`、`tasks/MVP_TASK_MAP.md` 和本 changelog 的状态记录；不修改业务代码、测试、fixture、expected JSON、OpenAPI、数据库、Docker、workflow、ADR 或 PRD。
- 本轮不进入 `TASK-028` / `TASK-031` / `TASK-032`，不创建 `TASK-033`，不解除 `TASK-EVAL-001` blocked，不启用 runtime loader，也不声明 `RuleSetVersion` 已在生产 runtime 绑定。

## 2026-07-05 TASK_SPEC-030-C Review assets 版本治理与变更流程最小建档

- 按 Codex 审查放行的编码前规格映射计划，由 Claude Code 执行 `TASK_SPEC-030-C`。
- 更新 `packages/review-assets/README.md`：新增版本命名规则（`vYYYYMMDD.N`）、新增/升级资产流程、RuleSetVersion manifest `moduleVersions` 引用规则（`path`/`assetId`/`version` 一致性）、校验命令说明和 runtime loader 未启用声明。
- 新建 `tasks/active/TASK_SPEC-030-C-review-assets-version-governance-docs.md`。
- 更新父任务 `TASK-030` 执行记录、`CURRENT_CONTEXT.md` 和本 changelog（仅摘要和引用路径）。
- 本轮不修改 7 个 JSON 资产文件、`scripts/validate-review-assets.mjs`、业务代码、测试、fixture、expected JSON、OpenAPI、数据库、Docker、workflow、ADR 或 PRD；不进入 `TASK-028` / `TASK-031` / `TASK-032`，不创建 `TASK-033`，不接入 Gemma / A30。
- `node scripts/validate-review-assets.mjs` 仍通过（7 文件全通过）。

## 2026-07-05 TASK_SPEC-030-B Review assets schema 与 manifest 引用校验

- 按 Codex 放行的编码前规格映射计划，由 Claude Code 执行 `TASK_SPEC-030-B`。
- 新增 `scripts/validate-review-assets.mjs`，无第三方依赖，仅使用 `node:fs` 和 `node:path`。
- 脚本校验 `packages/review-assets/` 下所有 JSON 的必需字段（`assetId`、`assetType`、`version`、`status`、`source`、`changeReason`）、`assetId` 唯一性、`source.type=code-current-mapping`、`source.runtimeBinding=NOT_BOUND`、`runtimePolicy.loaderEnabled` 不为 `true`、RuleSet manifest `moduleVersions` 引用文件存在且 `assetId`/`version` 一致。
- 校验通过时退出码为 0，失败时收集全部错误后统一输出再退出码 1。
- 当前 7 个资产文件全部通过校验。
- 本轮不修改业务代码、测试、fixture、expected JSON、OpenAPI、数据库、Docker、workflow、ADR 或 PRD；不进入 `TASK-028` / `TASK-031` / `TASK-032`，不创建 `TASK-033`，不接入 Gemma / A30。

## 2026-07-05 TASK_SPEC-030-A Review assets 静态源文件与 manifest 最小建档

- 按用户授权推进并完成 `TASK_SPEC-030-A`，新增 `tasks/active/TASK_SPEC-030-A-review-assets-static-source-manifest.md`。
- 在 `packages/review-assets/` 下补齐首批静态资产源文件，覆盖 `rule-sets`、`review-point-definitions`、`pattern-libraries`、`field-lexicons`、`prompts`、`contract-type-profiles`、`evidence-selectors`。
- 新增 `ruleset-v20260705.1.json` 作为 RuleSetVersion manifest，引用 review point、pattern library、field lexicon、prompt、contract type profile 和 evidence selector 的模块版本。
- 所有资产均声明当前只是“代码现状映射”，不声明生产 runtime 已绑定；本轮不启用 runtime loader，不接入 Gemma / A30。
- 本轮不修改业务代码、测试、fixture、expected JSON、OpenAPI、数据库、Docker、workflow、ADR 或 PRD；不进入 `TASK-028` / `TASK-031` / `TASK-032`，不创建 `TASK-033`。

## 2026-07-05 TASK-030 Review assets 版本化治理只读盘点

- 按用户要求直接执行 `TASK-030`，完成 Review assets 相关规则、正则、Prompt、合同类型画像、EvidenceSelector、模型 profile / prompt 配置的只读盘点。
- 结论：`packages/review-assets/` 当前只有目录骨架和 `.gitkeep`；真实规则 / 正则 / EvidenceSlot runtime / CandidateResolver / deterministic rule 仍主要位于后端 Java 代码；execution、snapshot、TuningPacket 和 ModelCallIntent 已预留版本引用字段，但尚无资产源文件、manifest、schema 校验、runtime loader、发布审批或数据库资产表。
- 已在 `TASK-030` 中记录一期最小版本字段、建议目录命名、后续 `TASK_SPEC` 切分建议和 ADR 判断：静态资产建档与校验默认可走普通任务；runtime loading、数据库持久化、发布治理、模型职责或 EvidenceSlot / CandidateResolver 行为变化必须重新判断 ADR。
- 本轮仅修改任务文件、`CURRENT_CONTEXT.md`、`tasks/MVP_TASK_MAP.md` 和本 changelog；不修改业务代码、测试、fixture、expected JSON、OpenAPI、数据库、Docker、workflow、ADR 或 PRD；不进入 `TASK-028` / `TASK-031` / `TASK-032`，不解除 `TASK-EVAL-001` blocked，不创建 `TASK-033`，不派发 CC-DS 执行环境。

## 2026-07-05 完成态复核优先治理规则

- 按用户确认，后续 CQCP 任务推进采用“完成态复核优先”规则，避免审计过载。
- 普通任务执行过程中不反复请求外部复核，默认完成后一次性提交 `diff`、测试输出、`git status`、风险说明和 Memory Writeback 状态供复核。
- 明确父任务归档、`push` / `merge`、进入 `TASK-028` / `TASK-031` / `TASK-032`、`EvidenceSlot` / `CandidateResolver` / `SourceAnchor`、expected fixture / 评测指标、workflow / CI / required checks / branch protection、Codex 自写代码后自审、工作区或远程状态不清等节点必须追加独立 agent 只读复核。
- 同步 `docs/DEVELOPMENT.md`、`docs/VERIFY.md`、`CURRENT_CONTEXT.md`、`tasks/MVP_TASK_MAP.md` 与 `TASK-GOV-006` 完成态复核记录；独立只读复核结论为 `GO`，无阻塞问题；本轮仅修改治理文档和项目记忆，不修改业务代码、workflow、测试、fixture、expected JSON、OpenAPI、数据库、Docker、PRD、架构文档或 ADR。

## 2026-07-04 TASK-EVAL-001 归档前审计启动前收口判断

- 按用户要求先处理 `TASK-EVAL-001` 的归档前审计 / 收口判断。
- Codex 预审结论为 `NO-GO TO ARCHIVE / INDEPENDENT AUDIT REQUIRED / BASELINE NOT CLEAN`。
- 判断依据：rebaseline 后归档门禁要求独立 agent 只读审计；本轮 Codex 只能做预审与收口判断，不能替代独立审计；当前主工作区仍有未提交治理改动，不能作为干净归档审计基线。
- 明确当前不得归档 `TASK-EVAL-001`，不得恢复 `GO TO ARCHIVE WITH CONDITIONS`，不得把 `TASK-GOV-005`、事后独立复核或 `30/30 PASS` 写成本父任务 DoD #12 已通过，也不得进入 `TASK-028` / `TASK-031` / `TASK-032`。
- 已在 `TASK-EVAL-001` 中记录建议独立只读审计输入；后续需等待独立 agent 审计报告返回后，Codex 再单独给出 Review Intake Decision。

## 2026-07-04 TASK-GOV-006 提交前授权证据模板与 PR 文本门禁

- 按用户确认启动 `TASK-GOV-006`，本地准备提交前授权证据模板、PR 模板和 `Authorization evidence check` PR body 文本门禁。
- 新增 `docs/governance/pre-submit-authorization-evidence-template.md`，统一记录任务编号、Governance Mode、允许文件、禁止文件、commit / push / merge 授权、测试证据、独立审计状态、Memory Writeback 和范围外确认。
- 新增 `.github/pull_request_template.md` 与 `scripts/check-pr-authorization-evidence.mjs`，并提供正反 fixture；新增 workflow job 仅在 `pull_request` 事件检查 PR body 字段是否存在且非占位；本轮本地正向 fixture 已通过、反向 fixture 已按预期失败。后续 PR #18 已完成真实 PR 触发验证，`Authorization evidence check` 成功运行；该 check 不是 required status check，也不证明真实授权事实。
- 同步 `docs/DEVELOPMENT.md`、`docs/VERIFY.md`、`CURRENT_CONTEXT.md` 与 `tasks/MVP_TASK_MAP.md`，明确该检查只验证文本字段，不证明授权、测试、独立审计或 Memory Writeback 已真实发生。
- 本轮不修改业务代码、测试、fixture、expected JSON、OpenAPI、数据库、Docker、PRD、架构文档或 ADR；不配置 branch protection、repository ruleset 或 required status checks；不进入 `TASK-028` / `TASK-031` / `TASK-032`。

## 2026-07-04 TASK-GOV-005 治理债务定界

- 继续处理 `TASK-GOV-005`，将历史 commit / push 授权证据债务定界为 `BOUNDARY RECORDED / NO RECOVERY PATH / NO IMPLEMENTATION AUTHORIZATION`。
- 明确不再尝试恢复 `TASK-EVAL-001-A/B` 历史授权链；该缺口作为长期治理边界保留，不追溯否定已 merge / push 内容、事后独立审计或定向测试 `30/30 PASS`。
- 明确 `TASK-GOV-005` 不补足 `TASK-EVAL-001` DoD #12，不支撑父任务归档，不授权 `TASK-028` / `TASK-031` / `TASK-032`，也不覆盖 `DEBT-001-03` 或 `DEBT-001-05`。
- 记录独立只读审计结论：`GO`，无阻塞问题；非阻塞问题为完成记录措辞可能混淆“债务定界完成”和“任务归档完成”，已补充说明任务仍长期保留 active。
- 同步 `CURRENT_CONTEXT.md` 与 `tasks/MVP_TASK_MAP.md`；本轮不修改业务代码、测试、fixture、expected JSON、OpenAPI、数据库、Docker、workflow、PRD、架构文档或 ADR。
## 2026-07-04 TASK-EVAL-001 rebaseline

- 按用户要求处理 `TASK-EVAL-001` rebaseline，更新父任务文件、`CURRENT_CONTEXT.md` 与 `tasks/MVP_TASK_MAP.md`。
- 明确 rebaseline 后不再按原父任务 DoD 伪装为 12/12 全通过：DoD #1 至 #11 仅保留为既有独立确认摘要，DoD #12 固定为未通过、未补足。
- 明确 `TASK-EVAL-001-A/B` 历史实现结果只能作为技术质量信号和后续参考，不作为父任务归档通过证据；`TASK-GOV-005` 只治理历史授权证据债务，不补足父任务 DoD。
- 本轮不归档 `TASK-EVAL-001`，不恢复 `GO TO ARCHIVE WITH CONDITIONS`，不解除 `TASK-028` / `TASK-031` / `TASK-032` 门禁，不创建或派发 `TASK_SPEC`。
- 本轮仅修改治理文档；不修改业务代码、测试、fixture、expected JSON、OpenAPI、数据库、Docker、workflow、PRD、架构文档或 ADR。

## 2026-07-04 MVP 上线路线调整

- 按用户确认，取消单独创建 MVP 上线 / readiness 任务的路线，后续按正常开发顺序和任务地图推进。
- 同步 `CURRENT_CONTEXT.md` 与 `tasks/MVP_TASK_MAP.md`，明确当前不因此进入 `TASK-029`，也不解除 `TASK-028` / `TASK-031` / `TASK-032` 门禁。
- 本轮仅做项目记忆修正；不修改代码、测试、fixture、expected JSON、OpenAPI、数据库、Docker、workflow、PRD、架构文档或 ADR。

## 2026-07-03 TASK-DEBT-001 归档

- 经用户明确授权，执行 `TASK-DEBT-001` 父任务归档流程。
- 将 `tasks/active/TASK-DEBT-001-review-engine-verified-defects-and-coverage-gap.md` 移动到 `tasks/done/TASK-DEBT-001-review-engine-verified-defects-and-coverage-gap.md`。
- 更新 `CURRENT_CONTEXT.md` 与 `tasks/MVP_TASK_MAP.md`，将 `TASK-DEBT-001` 从 active 状态改为已完成并归档。
- 归档依据：2026-07-02 父任务归档前独立只读审计 `GO`、Codex Review Intake Decision `GO TO ARCHIVE WITH CONDITIONS`、PR #14 归档前审计写回、PR #15 归档前文档状态同步。
- 归档不授权 parser provenance / `SourceAnchor`、real DOCX `TABLE_CELL`、`TASK-028`、`TASK-031` 或 `TASK-032`。

## 2026-07-03 CURRENT_CONTEXT 瘦身与 TASK-DEBT-001 状态同步

- 已同步本地 `master` 与 `origin/master`；最新合并为 PR #14，merge commit `13794fb2f09e02dad3b1d556cb6a5b9af7731c66`，head commit `d232df3fa320ae1ef85578942a366e125b961a0b`。
- 已将 `CURRENT_CONTEXT.md` 瘦身为当前阶段、活跃任务、最近已合并 PR、阻塞项、禁止进入项、待确认事项、下一步和参考路径。
- 已更新 `tasks/MVP_TASK_MAP.md` 和 `tasks/active/TASK-DEBT-001-review-engine-verified-defects-and-coverage-gap.md`，仅将 PR #14 记录为父任务归档前审计写回。
- 父任务 `TASK-DEBT-001` 仍为 active，未归档。
- `DEBT-001-03` parser provenance / `SourceAnchor`、`DEBT-001-05` real DOCX `TABLE_CELL`、`TASK-028`、`TASK-031` 和 `TASK-032` 仍未获授权，未进入。

## 2026-07-02 TASK-DEBT-001 parent pre-archive audit writeback

- Recorded independent read-only audit conclusion for `TASK-DEBT-001`: `GO`.
- Recorded Codex parent Review Intake Decision: `GO TO ARCHIVE WITH CONDITIONS`.
- Confirmed `TASK_SPEC-DEBT-001-A/B/C` all have landing evidence:
  - A: commit `3223d6760a977fe9deaf722e63b50bcbb6ce3611` and post-push independent read-only review `GO`.
  - B: PR #4 merge commit `da724ba49c6a33347641950101a84a84dfa8c000`, audit basis `CQCP_AUDIT GO`.
  - C: PR #13 merge commit `15888df4f0e89882814940c5eca0fc948fd1fef0`, Codex Review Intake `GO` and independent read-only audit `GO`.
- Aligned stale status text in `TASK_SPEC-DEBT-001-C`, `TASK-DEBT-001`, `CURRENT_CONTEXT.md` and `tasks/MVP_TASK_MAP.md`.
- Parent `TASK-DEBT-001` remains active and not archived.
- Parser provenance / `SourceAnchor`, real DOCX `TABLE_CELL`, `TASK-028`, `TASK-031` and `TASK-032` remain unauthorized.

## 2026-07-01 TASK_SPEC-DEBT-001-C pre-commit writeback

- Completed controlled implementation record for `TASK_SPEC-DEBT-001-C resolveRatioEvidence candidate collection topology` on branch `codex/task-debt-001-c-implementation`.
- `resolveRatioEvidence()` now removes the non-prepayment semantic-candidate early return and sends the ordered deduplicated candidate set through `resolveFromCandidates(...)` / `MinimalCandidateResolver`.
- Added regression coverage for `MONTHLY` progress payment conflict where semantic `70%` and whole-text fallback `85%` resolve to `AMBIGUOUS / CONFLICTED / SYS_ROLE_CONFLICT`.
- Required checks from `apps/api-server`:
  - `ParserBackedReviewInputPreparerEvidenceTest`: `BUILD SUCCESSFUL`.
  - `MinimalCandidateResolverTest`: `BUILD SUCCESSFUL`.
  - `MinimalReviewEngineTest`: `BUILD SUCCESSFUL`.
- Codex Review Intake Decision: `GO`.
- Independent read-only audit decision: `GO`, no blocking findings.
- Scope remains limited to `TASK_SPEC-DEBT-001-C`; parent `TASK-DEBT-001` is still active and not archived.
- This change does not authorize parser provenance, real DOCX `TABLE_CELL`, `TASK-028`, `TASK-031` or `TASK-032`.
