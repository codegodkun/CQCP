# 2026-07 changelog

## 2026-07-31 MVP-002 Core blocking causes 与历史证据路径收敛

- 首轮冻结 `45568f…` 的 blocking causes 已限定整改完成：Worker 只消费已校验
  DOCX byte snapshot；R7 绑定当前 HEAD/tree/source closure；浏览器验收保留真实
  filechooser、network、DOM、console/dialog 事实；Compose transport 计数替代脚本
  常量；Core architecture 与 Provider attempt contract 分离；父 TASK 叙事同步。
- 正式验证在 `core-node-test` 发现旧 Track B v2 dispatch 仍读取 current-HEAD R7
  的 live manifest。27 个 packet hash 未变，失败仅由历史 R7 identity 与当前 R7
  共用路径造成。旧 packet manifest 和 R7 manifest 已按 dispatch 原始 SHA-256
  固化为历史 fixture；verify-only override 必须成对提供并再次匹配 dispatch hash，
  不允许 create 模式使用，也不覆盖当前 R7。
- Track B v2 contract 定向回归 `7/7`、历史 dispatch 与 opinion chain direct verify
  均通过。当前源码和 Memory Writeback 形成最终 candidate 后，仍须从该 HEAD
  从零重建 R7、Compose/browser、完整验证、immutable freeze 与三方审计；此前
  `7fdf542…` 的浏览器/验证证据随源码变化失效，不得用于最终收口。

## 2026-07-31 MVP-002 Core 首轮最终冻结被独立审计否决

- Core 冻结
  `45568f205597d7532a1e1efa1d9cc496d160bb1a8d27f3a7dcd78f310e2ad1bc`
  的两名全新 Codex 审计员均返回 `NO_GO`；代码/架构侧为
  `P1=2 / P2=1`，测试/安全侧为 `P1=4`。冻结已只读封存到
  `outputs/task-mvp-002/core-audit/freeze-rejected-45568f20/`。
- blocking causes 限定为：Worker 安全读取后按路径重开 DOCX 的 TOCTOU、
  formal R7 未绑定当前 HEAD/tree/source closure、browser filechooser 与恶意正文
  安全断言缺少原始事件/DOM 证据、零模型外呼为脚本常量，以及 Core
  ARCHITECTURE 混入 Provider attempt contract。另修正三个父 TASK 的过期状态叙事。
- `fa869af3b79f0e417a9a3de5f5630dcbe4fd5885` 上的 Compose/browser、
  formal verification 与 freeze 全部降为失效历史证据。整改后必须重新提交候选、
  从零重建证据和 freeze，并由 CC AUDIT 与两个全新 Codex auditor 对同一新 hash
  完整重审；旧报告不得复用。

## 2026-07-31 MILESTONE-MVP-002 Core 中途收敛

- 项目负责人确认先收口 M1、M2、已完成 M3 证据、TASK-034 R7 与 TASK-036 seam；
  Provider A0、standing/CC 传输、A1/A2/A3 不进入 Core source diff。
- 补齐 Windows document read/download directory junction、Secret root junction 和
  单一 Secret sentinel 证据；定向组合 34/34 通过。
- 将盲评 source、Track B corpus、人工 anchor workbook 与 D1 oracle 从 backend/CI
  的隐式 `outputs/**` 依赖迁入明确 test resources；Node evidence validator 仍显式
  消费并 hash 绑定外部派生 evidence。
- 新增 Core-only path allowlist、Provider-free JS import closure、完整验证入口和
  immutable freeze 入口；formal package 只按 hash 引用 R7/M3/browser/Compose
  evidence，不执行公网模型调用。
- 旧 Track B run-v3 保留为
  `NO_GO_MODEL_MISMATCH / providerAdmission=NOT_ESTABLISHED`；Core merge 后才创建
  一组模型未见的 12 packet holdout，人工答案先封印且只正式运行一次。

## 2026-07-29 MVP-002 原子文件读取与模型安全定点加固

- DOCX 工作台读取改为单一 bounded byte snapshot：SHA、preview、响应头与下载
  body 不再二次按路径打开。Linux 使用 root/task 两级 `SecureDirectoryStream`，
  Windows 使用拒绝 reparse 且不共享删除的原生目录/文件句柄；同 SHA/size 的目录
  替换竞态测试确认 fail closed、parser 零调用。
- Secret 文件只允许受控根目录 direct-child、strict UTF-8 和 16 KiB；Linux
  相对安全打开，Windows `NOFOLLOW_LINKS + NOSHARE_DELETE` 并复核 identity。
  IPv4 allowlist 按 IANA special-purpose registry 精确修正特殊/文档网段，避免
  过宽拒绝相邻普通公网地址；Flyway V3 PUBLIC profile CHECK、管理页 auth
  fail-closed 与 OpenAPI `problem+json` 409 回归同步补齐。
- 两项定点独立复核均关闭原 finding：M1 为 `RESOLVED`，M2 为
  `GO / P0=P1=P2=blocking=0`。最新中间验证为全新隔离 PostgreSQL 后端
  `887/887`、`bootJar`，admin-web `70/70`、lint/build，以及 Linux Compose
  真实验收通过。最终浏览器证据、人工 Track B ground truth/admission、完整重复
  验证与 CC + 两个全新 Codex 的同 hash 三审仍未完成，不能声明 Milestone 完成。

## 2026-07-14 TASK_SPEC-036-A 未激活 carrier 规格冻结

- 用户单独授权进入 `TASK-036` 生产实现治理流程；Codex 创建分支 `codex/task-036-a-consistency-set-provenance`，只读核查 resolver、preparer、engine、composer、persistent query 拓扑并验证六类定向测试基线 40/40、第二组 25/25。
- 新建并冻结 `tasks/active/TASK_SPEC-036-A-same-value-occurrence-provenance.md`。A 仅建立 resolver occurrence 保留、显式 `PointEvidence` carrier 与 engine 多 anchor 投影能力；现有 preparer 不填充 occurrences，当前 RuleSetVersion 与普通任务仍走 legacy 单 anchor。
- 第一轮独立规格审计为 `NO_GO`：禁止在无 lineage 时把 BLOCK 当 cell fallback 删除；禁止未绑定版本便改变普通任务 anchor 基数；只读 persistent adapter 只能证明 JSON deserialization + query，不能声称写入 round-trip。Codex 全部接受并修订。
- 第二轮独立规格审计为 `GO`；允许范围固定为 2 个生产文件和 5 个测试文件，新增 7 个可证伪测试，第一组完成后须 `>=47`、第二组保持 `=25`，均零 failure/error/skipped。
- 规格冻结阶段 Decision 为 `SPEC_FROZEN / INDEPENDENT_SPEC_AUDIT_GO / PRE-CODING_PLAN_PENDING / NO CODE AUTHORIZATION`；该阶段只派发 Claude Code / DeepSeek 编码前规格映射计划，后续状态见下列实现与接纳记录。
- Claude Code / DeepSeek 已提交只读编码前规格映射计划；Codex 审查结论为 `GO / IMPLEMENTATION AUTHORIZED`。放行仅限冻结的 2 个生产文件与 5 个测试文件，并强制所有 resolver 退出分支保留 raw snapshot、显式 carrier preflight 先于业务比较、非空 carrier 不回退 legacy 空 anchor、现有 preparer 不改。
- TASK_SPEC-036-A 实现完成：resolver 保留 dedup 前 raw occurrence snapshot 与 HIGH exact selected occurrences；`PointEvidence` / engine 增加未激活显式 carrier、可靠性 preflight 与有序 BLOCK/TABLE_CELL anchor 投影。生产 preparer、composer、persistent store/query、API、数据库与 RuleSetVersion 均未修改。
- Codex 复验第一组 47/47、第二组 25/25。第二组补充向量后首次在沙箱中新 Gradle daemon 因插件解析失败而未进入测试；随后经批准只使用本机离线缓存重跑成功，环境失败事实保留但不计作测试失败。
- 独立实现审计先指出“两条不同 block 同值 → selected occurrences 精确 2”冻结向量未直接断言；执行方在原测试方法内补充，未新增第八个测试。最终独立审计 `GO`，无 blocking finding；Codex Review Intake 为 `ACCEPT_IMPLEMENTATION`。
- A 的七文件代码 diff 为 `621 insertions / 18 deletions`，`git diff --check` 通过。用户随后授权精确提交本轮 7 个实现文件和 5 个治理文档；最终 commit hash 以本次 Git 历史为准，不追加第二次自述提交。
- 本轮除冻结的 2 个生产文件、5 个测试文件和 5 个治理文档外，未修改 parser、preparer、fixture、expected JSON、DOCX、XLSX、matrix、OpenAPI、数据库、workflow、ADR 或 ARCHITECTURE；不 push、不 merge。

## 2026-07-14 TASK-034 失败根因拆分、后续契约冻结与独立审计

- 只读核查确认 18 个 candidate mismatch 是验收 raw 表示形态与 PRD 生产规范值的错位，不是生产 `CandidateResolver` 缺陷：15 个比例为 `70%` 对 `70` 等百分比展示/数值差异，3 个税额公式为人工复合说明对 production `taxAmount` 标量。
- 新建 `TASK-035`，冻结 test-only `mvp-e2e-candidate-comparison-v2` typed projection；raw expected / actual 必须保留，expected projection 不读取 actual，不修改 human fixture / expected JSON。首轮独立审计对数值词法和 tax grammar 给出 `NO_GO`，整改及复审 findings 收口后最终 delta 核对为 `GO`；Codex Review Intake 为 `CONTRACT_FROZEN / READY_FOR_USER_IMPLEMENTATION_AUTHORIZATION`，当前仍无实现或正式重跑授权。
- anchor 根因进一步确认为多出处 provenance 丢失：57 条纳入人工 occurrence 对 27 个 actual anchors；27 个 anchors 中仅 4 个有 row-level `previewElementRef`，23 个无 ref。当前 resolver 先执行 occurrence-insensitive dedup，再投影为首个 selected candidate，形成双重折叠；单补 cellIndex 不足以解决。
- 新建 `TASK-036` 与 `ADR-016` Draft，冻结 semantic value group / occurrence provenance 分离、`CONSISTENCY_SET_READY` admission gate、`maxCandidates` / `occurrenceBudget` 分离、可靠异值与归属歧义分流、点级 `pointResults[].sourceAnchors[]` 真源和版本化 scope / exclusion policy。现有 API/snapshot/JSONB list 兼容性已只读确认。
- `TASK-036` / `ADR-016` 首轮独立审计四项阻断经整改、复审与非阻断 findings 收口后，最终 delta 核对为 `GO`；Codex Review Intake 为 `READY_FOR_USER_ADR_DECISION / NO_IMPLEMENTATION_AUTHORIZATION`。`ADR-016` 未获用户接受、`docs/ARCHITECTURE.md` 未同步前，不冻结或派发生产实现 `TASK_SPEC`。
- 本轮未修改生产代码、测试、fixture、expected、DOCX、XLSX、matrix、workflow、公共 API、数据库、既有 accepted ADR 或 `docs/ARCHITECTURE.md`，不进入 `TASK-028` / `TASK-031` / `TASK-032`。
- 用户随后明确接受 `ADR-016`，并要求先同步 `docs/ARCHITECTURE.md`、不得直接进入生产实现。ADR 状态已更新为 `Accepted`，ARCHITECTURE 更新为 v0.10；接受后独立只读审计 `GO`，确认窄例外、双预算、GlobalConsistencyCheck 边界、点级 anchor 真源与 UI 语义均与 ADR 等价，未引入公共 API、migration 或实现授权。
- 用户授权创建并派发 `TASK_SPEC-035-A`。规格冻结为单一 test-only harness 文件、精确 comparison v2 grammar/serialization、expected 独立性和 formal E2E 三重门禁；首轮独立审计发现测试类名大小写与正式重跑门禁两项阻断，整改后复审 `GO`。
- 当前环境已验证正确类名 `Task034MvpE2eAcceptanceHarnessTest` 配合 `--rerun-tasks` 实际执行 13 tests、0 failures/errors/skipped；错误的 `Task034MvpE2EAcceptanceHarnessTest` filter 会 `No tests found`。规格要求实现后 XML 实际计数至少 15，既有四类回归严格 27。
- 命令验证期间未带 filter 的模块全量测试执行 103 tests / 1 failure；失败为 Spring context 连接 PostgreSQL hostname 时 `UnknownHostException`，属于环境依赖，不作为本规格定向测试结论，也未放宽任何验收门槛。
- `TASK_SPEC-035-A` 当前只放行 Claude Code / DeepSeek 编码前规格映射计划；Codex 未明确 `GO / IMPLEMENTATION AUTHORIZED` 前不得编码。`TASK-036` 当前仍无生产实现授权。
- 后续编码前规格映射计划经 Codex 修正并明确放行；Claude Code / DeepSeek 仅修改既有 test-only harness，实现四个冻结 profile、独立 expected/actual projection、精确 grammar、canonical comparable string/null 与六个输出字段，未运行正式模式。
- Codex 重新执行并读取 XML：`Task034MvpE2eAcceptanceHarnessTest` 为 `15/15`，四类既有回归合计 `27/27`，均 0 failures / 0 errors / 0 skipped；`git diff --check` 通过。
- 独立只读实现审计结论为 `GO`，无 blocking 或 non-blocking finding；Codex Review Intake 为 `ACCEPT_IMPLEMENTATION / READY_FOR_PRECISE_COMMIT`。
- 用户授权后，Codex 精确提交唯一 harness 文件；提交为 `52d73b3`（`test: implement task 035 candidate comparison v2`）。未 push、未 merge，未修改生产链路、fixture、expected、DOCX、XLSX、matrix、正式 v1 outputs、API、数据库、workflow、ADR 或 ARCHITECTURE。
- `TASK-035` candidate comparison 路线已接纳，但正式 MVP E2E 仍不得重跑：`TASK-036 / ADR-016` 的生产实现门禁尚未完成且未获授权，ADR 接受和 ARCHITECTURE 同步不构成生产实现授权。

## 2026-07-14 TASK-034 test-only harness 与正式 MVP E2E

- `TASK_SPEC-034-A` 编码前规格映射计划经 Codex 放行后，由 Claude Code / DeepSeek 完成 test-only harness；Codex Review Intake 与独立只读复核均接纳。实现提交为 `99bea3a6a3ce0cbecf337e76692aac3a6c428228`。
- 首次正式写 manifest 暴露 Jackson 未注册 Java Time module；仅在 harness 内执行最小修复并增加非正式 round-trip 自测，提交为 `46a625a5eb5aee8ff5a31f86bb7300fb2d8e703a`。最终 harness 13/13、既有四类定向回归 27/27。
- 正式 MVP E2E 按 001→002→003 执行，3 份真实 DOCX 均完成同 run parser、审核状态机、结果快照和同 task 查询；27 个 `PointStatus` 均为 `PASS`，三份结果均无 Finding、无 `SYS-*`。
- candidate comparison 为 9 `MATCH` / 18 `MISMATCH`；63 条 occurrence 保持 57 纳入 / 6 排除，57 条纳入全部 `NOT_OBSERVABLE`，6 条排除全部 `EXCLUDED`。未使用宽松匹配或 actual 倒填人工 expected。
- 正式最终判定为 `FAIL`，不是 `BLOCKED_ENVIRONMENT`。父任务保持 active；candidate 表示契约与 actual `SourceAnchor` 精度缺口需另行定界并判断 ADR，不授权直接修改生产链路，也不进入 `TASK-028` / `TASK-031` / `TASK-032`。
- 交付物写入 `outputs/task-034-mvp-e2e-acceptance/`：run manifest、3 份样本 JSON、63 行 comparison CSV 和 console summary。未修改生产 parser、`CandidateResolver`、`EvidenceSlot`、`SourceAnchor`、Review Engine、公共 API、数据库、workflow、ADR、DOCX、XLSX、matrix、fixture 或 expected JSON。
- 精确提交时保留 comparison CSV 中两处 parser 原文 evidence 的末尾空格；因此 `git diff --check` 对该正式数据工件报告 2 条 trailing whitespace。未手工清洗正式证据，排除该 CSV 后其余 staged diff check 通过。

## 2026-07-14 TASK-034 Phase 0 入口门禁 NO-GO

- 只读审计确认：当前仓库分别存在真实 DOCX parser-backed 状态机、结果快照和 `GET /api/v1/tasks/{taskId}/result` 查询组件，但不存在满足 TASK-034 六项条件的单一执行入口。
- 三项直接缺口：现有查询测试未与同一次真实 DOCX run 串联；actual `candidateValue` 只存在于内部 `PointEvidence`、未进入 snapshot/query 输出；人工位置到 actual anchor 的 bridge/63 条逐 occurrence 比较不存在。
- Phase 0 唯一结论为 `NO_GO_TEST_ONLY_HARNESS_REQUIRED`，父任务结果为 `STOPPED_FOR_TASK_SPEC_034_A`；未运行 001/002/003 正式 MVP E2E，未生成 Phase 1 样本结论。
- 后端四类定向测试合并复跑 27/27，0 failures / 0 errors；`npm.cmd run test:admin-web` 因缺 `vitest`、`npm.cmd run build:admin-web` 因缺 `tsc` 保留环境失败，未安装依赖规避。
- 新增 `outputs/task-034-mvp-e2e-acceptance/entrypoint-audit.md`，并冻结 `tasks/active/TASK_SPEC-034-A-test-only-e2e-harness.md`；规格当前仅 `Ready for Coding-Plan Mapping / NO IMPLEMENTATION AUTHORIZATION`，不得直接实现 harness。
- 本轮未修改生产 parser、CandidateResolver、EvidenceSlot、SourceAnchor、Review Engine、公共 API、数据库、workflow、ADR、DOCX、XLSX、matrix、fixture 或 expected JSON；未 commit、push 或 merge；不归档 `TASK-EVAL-001`，不进入 `TASK-028` / `TASK-031` / `TASK-032`。

## 2026-07-13 TASK-034 正式 MVP E2E 人工 anchor 验收建档

- PR #30 已使用 merge commit 合并 `TASK-DATA-001` 父任务归档，merge commit 为 `01e59f54284bbab5409f0d7fd392acfd96d7ff83`；三项 checks 均为 `SUCCESS`。
- 新建 `tasks/active/TASK-034-mvp-e2e-human-anchor-acceptance-execution.md`，类型为 Codex 主控正式验收任务。
- 冻结 `CQCP-MVP-DOCX-001/002/003` 和 63 条人工 occurrence：57 条纳入一致性判断，6 条合同标题或前言名称保持排除。
- 正式执行前必须先完成 Phase 0 只读入口门禁，证明真实 DOCX 可串联 parser、审核状态机、结果快照与查询，并能输出可与人工 occurrence 比较的证据；入口不足时停止并单独冻结 test-only `TASK_SPEC-034-A`。
- TASK-034 建档独立只读复核结论为 `GO`，无 blocking findings；复核确认编号、样本计数、ground truth 独立性、STOP 门禁、基础命令和 4 个逻辑文件范围一致。
- 本轮只创建任务和 Memory Writeback，不运行完整 MVP E2E，不修改代码、测试、fixture、expected JSON、DOCX、XLSX、matrix、公共 API、数据库、workflow、ADR 或生产数据结构，不归档 `TASK-EVAL-001`，不补足 DoD #12，不进入 `TASK-028` / `TASK-031` / `TASK-032`。

## 2026-07-13 TASK-DATA-001 父任务归档准备

- PR #29 post-merge 状态写回已合并，merge commit 为 `2b30bf303642d10156eec5844ee09718adb595b3`，三项 checks 均为 `SUCCESS`。
- 独立 agent 完整归档前审计首次因父任务一处过期执行中状态给出 `NO-GO`；单行修正后聚焦复核为 `GO`，无 blocking finding，允许 Codex 进入父任务归档 Review Intake。
- Codex 归档 Decision 为 `GO_TO_ARCHIVE / INDEPENDENT PRE_ARCHIVE AUDIT SATISFIED / FULL MVP E2E NOT VERIFIED`；父任务迁移到 `tasks/done/`，`TASK-EVAL-001` 保持 active，DoD #12 未补足。
- 下一步按单独任务建立 `TASK-034` 正式 MVP E2E 验收；本轮不创建该任务，不进入 `TASK-028` / `TASK-031` / `TASK-032`。

## 2026-07-13 TASK_SPEC-DATA-001-A PR #28 合并与 post-merge 准备

- PR #28 已使用 merge commit 合并，merge commit 为 `23c66aaed34326f242f9fb395d784518421f1575`，head commit 为 `782f0cbcb935b51b7d5d54537730f3abce4a0e22`。
- 最终三项 checks 均为 `SUCCESS`：`Authorization evidence check`、`Backend Gradle tests`、`Admin web lint, tests, and build`。
- `TASK_SPEC-DATA-001-A` 已迁移到 `tasks/done/`；`TASK-DATA-001` 标记为 `READY_FOR_INDEPENDENT_PRE_ARCHIVE_AUDIT`，尚未归档。
- 本轮只做 post-merge 状态写回准备；不声明完整 MVP E2E 已通过，不归档 `TASK-EVAL-001`，不补足 DoD #12，不进入 `TASK-028` / `TASK-031` / `TASK-032`。

## 2026-07-13 TASK_SPEC-DATA-001-A PR #28 创建

- 已从 `codex/task-data-001-a-human-anchor-conversion` 向 `master` 创建正式 PR #28：`https://github.com/codegodkun/CQCP/pull/28`。
- PR 创建时 head 为 `8c51355c34f09c7c5f7a2add370766d96219206b`，包含人工 ground truth 冻结、编码前规格门禁、8 个实现路径、Codex Review Intake、独立只读审计和 Memory Writeback 的完整任务链。
- GitHub Actions run `29252107007` 已通过：`Authorization evidence check`、`Backend Gradle tests`、`Admin web lint, tests, and build` 均为 success。
- 未获合并授权，不执行 merge；不运行完整 MVP E2E，不归档 `TASK-EVAL-001`，不进入 `TASK-028` / `TASK-031` / `TASK-032`。

## 2026-07-13 TASK_SPEC-DATA-001-A 实现提交与 push

- 用户明确授权 Codex 精确 stage 并 commit 8 个已接纳实现路径，在无问题时 push；staged diff 恰好为 8 个路径，`git diff --cached --check` 通过。
- 实现提交为 `32b4c414349118b9225a65fa08eb4e3466f82a2e`（`test: add accepted human anchor fixtures`），共 8 files / 1899 insertions。
- 已 push 到 `origin/codex/task-data-001-a-human-anchor-conversion` 并建立 upstream；提交后隔离 worktree 干净。未创建 PR，未运行完整 MVP E2E，不归档 `TASK-EVAL-001`，不进入 `TASK-028` / `TASK-031` / `TASK-032`。

## 2026-07-13 TASK_SPEC-DATA-001-A test-only revision 接纳

- Claude Code / DeepSeek 已只修改新建的 `HumanAnchorGroundTruthFixtureTest.java`，补齐 16 个直接字段 JSON string 类型、派生 boolean 类型、XLSX 行顺序、19 个 required columns、精确 18-key schema 和完整 positive-case baseline 断言；其他 7 个实现路径未扩大范围。
- Codex 重跑 `HumanAnchorGroundTruthFixtureTest` 为 10/10、`ParserBackedEvidenceOverlapBaselineTest` 为 4/4，均 0 failures / 0 errors；`git diff --check` 通过。
- 独立 agent 对 test-only delta、其他 7 个实现路径、人工来源独立性和 canonical baseline 给出 `GO`，无 blocking findings。
- Codex Decision：`GO / IMPLEMENTATION ACCEPTED / INDEPENDENT READ-ONLY AUDIT GO / AWAITING CODEX COMMIT AUTHORIZATION / NO PUSH`。未 commit 8 个实现路径，未运行完整 MVP E2E，不归档 `TASK-EVAL-001`，不进入 `TASK-028` / `TASK-031` / `TASK-032`。

## 2026-07-13 TASK_SPEC-DATA-001-A 首轮实现 Review NO-GO

- Claude Code / DeepSeek 已在隔离 worktree 完成 8 个允许路径的首轮实现；当前三份 fixture 为 22/19/22 共 63 条，57 条纳入、6 条排除，16 个直接字段为 JSON string，18 个 key 齐全且顺序与 XLSX 一致；三份 expected 仅新增 `goldenExpected.humanAnchorGroundTruth`。
- Codex 重跑 `HumanAnchorGroundTruthFixtureTest` 为 10/10、`ParserBackedEvidenceOverlapBaselineTest` 为 4/4，均 `BUILD SUCCESSFUL`；独立 agent 对路径范围、人工来源独立性和当前 canonical baseline 给出 `GO`。
- Codex Decision：`NO-GO / TEST CONTRACT REVISION REQUIRED / DATA ARTIFACTS RETAINED / NO COMMIT OR PUSH`。阻塞项为新测试未有效锁定 JSON 类型、occurrence 顺序、全部 required columns、精确 18-key schema 和完整 positive-case baseline。
- 下一步只允许修改 `HumanAnchorGroundTruthFixtureTest.java`；不得修改当前 fixture、expected JSON、README 或其他文件，不运行完整 MVP E2E，不归档 `TASK-EVAL-001`，不进入 `TASK-028` / `TASK-031` / `TASK-032`。

## 2026-07-13 TASK_SPEC-DATA-001-A 第二修订版计划 GO

- Claude Code / DeepSeek 第二修订版计划已覆盖前两轮八项修订要求：16 个 XLSX 直接字段与 2 个 `notes` 派生字段分开验证，直接字段空字符串保留为 JSON `""`，其余来源、排除语义、canonical baseline 和测试边界均保持冻结。
- Codex 只读检查人工 XLSX，确认 `occurrenceNo` 是 `001-PA-01` 等字符串追溯 ID；绑定澄清为 JSON string，不接受计划中“额外写入 JSON number”的孤立表述，也不新增数字序号字段。
- Codex Decision：`GO / CODING-PLAN ACCEPTED WITH BINDING CLARIFICATION / IMPLEMENTATION AUTHORIZED WITHIN §0.3 / NO COMMIT OR PUSH`。
- 本轮仅同步门禁与项目记忆；未修改 fixture、expected JSON、测试、实现、DOCX、人工 XLSX 或 sample matrix，未运行完整 MVP E2E，不归档 `TASK-EVAL-001`，不进入 `TASK-028` / `TASK-031` / `TASK-032`。

## 2026-07-13 TASK_SPEC-DATA-001-A 修订版计划复审 NO-GO

- Claude Code / DeepSeek 修订版计划已响应首轮六项要求；计划前后主工作区和隔离 worktree 仍 clean，未修改文件。
- Codex Decision：`NO-GO / CODING-PLAN REVISION ROUND 2 REQUIRED / NO IMPLEMENTATION AUTHORIZATION`。
- 剩余两项：18 字段测试必须拆为 16 个 XLSX 直接字段和 2 个 `notes` 派生字段；XLSX 直接字段的空字符串必须保留为 JSON `""`，只有纳入项 `exclusionReason` 为 JSON `null`。
- 未运行测试或完整 MVP E2E，未修改 fixture、expected JSON、测试或实现文件。

## 2026-07-13 TASK_SPEC-DATA-001-A 首版编码前计划审查 NO-GO

- 用户回传 Claude Code / DeepSeek §0.2 编码前规格映射计划；计划前后主工作区和隔离 worktree 均 clean，未修改 fixture、expected JSON、测试或实现文件。
- Codex Decision：`NO-GO / CODING-PLAN REVISION REQUIRED / NO IMPLEMENTATION AUTHORIZATION`。
- 必须修订 6 项：occurrence schema 应为 18 字段；排除原因缺失必须 STOP 且不得补值；`humanAnchorGroundTruth` 必须是 `goldenExpected` sibling；全部人工可见值按字符串读取；既有 expected 不变不得同源自证；补足 XLSX `docxPath` 到 fixture `sourceDocx` 的唯一映射和 STOP 条件。
- 未运行测试或完整 MVP E2E，不归档 `TASK-EVAL-001`，不进入 `TASK-028` / `TASK-031` / `TASK-032`。

## 2026-07-13 TASK-DATA-001 可重建基线与隔离 worktree

- 提交前独立只读审计最终 `GO` 后，7 个允许路径已精确提交为 `a2e9c085cd4a073d7f9dcba55cf891ace3d556da`（`data: freeze accepted MVP human anchors`）；未 push。
- 已创建隔离分支 `codex/task-data-001-a-human-anchor-conversion` 和 worktree `C:\Users\1\Documents\CQCP-worktrees\task-data-001-a`；已接受 XLSX 可由 Git 重建，隔离 worktree 初始状态干净。
- `TASK_SPEC-DATA-001-A` Decision 更新为 `GO TO CODING-PLAN MAPPING / CLEAN ISOLATED BASELINE VERIFIED / NO IMPLEMENTATION AUTHORIZATION`。
- 当前只允许 Claude Code / DeepSeek 输出 §0.2 编码前规格映射计划并停止；未修改 fixture、expected JSON、测试或实现文件，未运行完整 MVP E2E。

## 2026-07-13 TASK-DATA-001 提交前审计阻塞修正

- 独立只读审计首次结论为 `NO-GO`：项目记忆混淆“人工 anchor 已接受”与“fixture/E2E 尚未验证”，且 DOCX 003 用户侧修正缺少正式任务边界例外。
- 已统一状态口径：`TASK-DATA-001` 的 63 条 `BLOCK` / `TABLE_CELL` 逐出处明细已接受为独立人工 ground truth；尚未完成 fixture / expected JSON / 测试转换，也未运行完整 MVP E2E；`TASK-EVAL-001` 继续 active 且不归档。
- 已记录 DOCX 003 为用户侧修正例外，Codex 未编辑该文件。相对提交前 `HEAD` 的三处可见文本变化为：金额大写改为 `壹万贰仟捌佰元整`、签约通知表改为 `二标段`、《阳光合作协议》前言改为 `二标段`；Word 保存同时改变 9 个 OOXML 部件。
- DOCX 003 SHA-256 为 `48BB0FC63A0182BAFF5D7BD3E38F3A4409F68E7C371E5D24C02970C6362BF7B3`；人工 XLSX SHA-256 为 `3DC0DABE37DF2556B5F48B68600A0E24F092373FBD96B1E65B696FF0B9AEBA0B`。
- 阻塞修正后的最终独立只读审计结论为 `GO`，允许提交范围精确为人工 XLSX、用户侧 DOCX 003 修正、父 TASK、转换 TASK_SPEC、`CURRENT_CONTEXT.md`、`tasks/MVP_TASK_MAP.md` 和本 changelog 共 7 个路径。未修改 fixture、expected JSON、测试、sample matrix、parser 或 Review Engine，未运行完整 MVP E2E。

## 2026-07-12 TASK_SPEC-DATA-001-A 编码前基线门禁 NO-GO

- 按冻结规格只读核对 `AGENTS.md`、`CURRENT_CONTEXT.md`、父任务和 `TASK_SPEC-DATA-001-A`。
- 当前分支为 `master`，`git status --short` 非空；已接受 XLSX 在文件系统中存在，但尚未被 Git 跟踪，无法从当前 Git 基线重建。
- Codex Decision：`NO-GO / EXECUTION BASELINE NOT READY / PLAN REQUEST NOT DISPATCHED / NO IMPLEMENTATION AUTHORIZATION`。
- 本轮未要求 Claude Code / DeepSeek 输出编码前规格映射计划，未修改 fixture、expected JSON、测试、DOCX、sample matrix 或业务代码。
- 下一步必须先完成当前成果的提交前审查和可重建基线准备，再由 Codex 创建干净隔离执行环境；不得为获得 clean status 回退或隐藏用户侧 DOCX 003 修改。

## 2026-07-12 TASK_SPEC-DATA-001-A 转换规格冻结

- 新建 `tasks/active/TASK_SPEC-DATA-001-A-human-anchor-fixture-expected-test-conversion.md`，关联父任务 `TASK-DATA-001`。
- 冻结转换产物：001/002/003 三份规范化 human-anchor fixture JSON、三份 expected JSON 的 `humanAnchorGroundTruth` 引用对象、一个 XLSX → fixture → expected 追溯完整性定向测试和 fixture README 更新。
- 冻结数量：22/19/22 共 63 条，57 条纳入一致性评价、6 条标题/前言名称排除；禁止把排除项删除或把多个 occurrence 合并。
- 明确现有 `expectedCanonicalAnchors[]` 继续作为 parser-backed 历史回归基线，不得由 parser actual 倒填或冒充人工 ground truth；本规格禁止修改 parser、CandidateResolver、EvidenceSlot、SourceAnchor 或 Review Engine。
- 当前 Decision 为 `SPEC FROZEN / CODING-PLAN MAPPING ONLY / NO IMPLEMENTATION AUTHORIZATION`；主工作区不干净，执行前必须由 Codex 准备包含已接受 XLSX 的干净隔离基线。
- 本轮只创建和冻结 `TASK_SPEC` 并同步项目记忆；不修改 fixture、expected JSON、测试、DOCX、sample matrix 或业务代码，不运行完整 MVP E2E，不归档 `TASK-EVAL-001`，不进入 `TASK-028` / `TASK-031` / `TASK-032`。

## 2026-07-12 TASK-DATA-001 人工 ground truth 接受

- 根据 `ZK` 的最终确认，将三份合同 63 条逐出处明细全部提升为 `ACCEPTED_HUMAN_GROUND_TRUTH`；`groundTruthSource` 保持 `MANUAL_DOCX_REVIEW`。
- 提升前验证 63 条均为 `MATCH`，63 条均为人工来源，6 条合同标题/前言名称的“排除”判断继续保留并标记为已最终接受。
- 27 条审核点汇总仅作为总览，不作为单条 anchor 转换输入；本轮不修改其语义为逐出处 ground truth。
- 下一步 fixture / expected JSON / 测试转换必须另行创建并冻结关联 `TASK-DATA-001` 的 `TASK_SPEC`；本轮不执行转换。
- 不修改业务代码、测试代码、DOCX、expected JSON、`cqcp-mvp-sample-matrix.xlsx`、parser、`CandidateResolver`、`EvidenceSlot`、`SourceAnchor` 或 Review Engine；不运行完整 MVP E2E，不归档 `TASK-EVAL-001`，不补足 DoD #12，不进入 `TASK-028` / `TASK-031` / `TASK-032`。

## 2026-07-12 TASK-DATA-001 逐出处人工确认

- `ZK` 确认三份合同 63 条明细的 H 列观察值和 J-O 列粒度、原文、页码及结构上下文；I 列全部更新为 `MATCH`。
- 6 条合同标题/前言中的合同名称在 T 列标记“排除”，仅保留为可追溯出处，不参与甲乙方名称一致性结论。
- 63 条明细的来源和状态更新为 `MANUAL_DOCX_REVIEW / ANNOTATED_PENDING_REVIEW`；未提升为 `ACCEPTED_HUMAN_GROUND_TRUTH`。
- 确认 `CQCP-MVP-DOCX-003` 样本可用、脱敏合规，data owner 为 `ZK`。
- 不修改业务代码、测试代码、DOCX、expected JSON、`cqcp-mvp-sample-matrix.xlsx`、parser、`CandidateResolver`、`EvidenceSlot`、`SourceAnchor` 或 Review Engine；不运行完整 MVP E2E，不归档 `TASK-EVAL-001`，不补足 DoD #12，不进入 `TASK-028` / `TASK-031` / `TASK-032`。

## 2026-07-12 TASK-DATA-001 逐出处 anchor 拆分待确认

- 保留 `CQCP-MVP-DOCX-001` / `002` / `003` 的 27 条审核点人工汇总；直接读取当前三份 DOCX，将同一审核点的多个可见出处拆为 63 条独立明细，写入 XLSX 的“anchor明细待确认”工作表。
- 每条明细分别记录 `occurrenceNo`、观察值、比较结果、`BLOCK` / `TABLE_CELL` 实际粒度、原文、页码和结构上下文；名称全文多次出现时不预设主次，逐出处核对，任一纳入范围的出处不一致即形成审核点不一致。
- 63 条明细全部标记为 `AI_ASSISTED_SPLIT_FROM_MANUAL_SUMMARY_NOT_GROUND_TRUTH / NEEDS_REVIEW`，等待 `zk` 直接对照 DOCX 逐条确认；未把 Codex 拆分结果声明为人工 ground truth。
- 删除过期“AI预提取草稿”工作表；修正 `003` 合同金额汇总为正文 `12800 / 壹万贰仟捌佰元整`，并按正文主 anchor 记为 `BLOCK`。
- 不修改业务代码、测试代码、DOCX、expected JSON、`cqcp-mvp-sample-matrix.xlsx`、parser、`CandidateResolver`、`EvidenceSlot`、`SourceAnchor` 或 Review Engine；不运行完整 MVP E2E，不归档 `TASK-EVAL-001`，不补足 DoD #12，不进入 `TASK-028` / `TASK-031` / `TASK-032`。

## 2026-07-10 TASK-DATA-001 第 3 份候选与 AI 预提取草稿

- 复核用户在 `TASK-DATA-001-human-anchor-template.xlsx` 中完成的 `CQCP-MVP-DOCX-001` / `002` 共 18 条人工标注；当前均为 `ANNOTATED_PENDING_REVIEW`，未提升为 `ACCEPTED_HUMAN_GROUND_TRUTH`。
- 只读比较 `CQCP-MVP-DOCX-003` / `004`：`003` 含封面信息表和签约通知书金额单元格，可补充 `TABLE_CELL` 人工定位，故补选为第 3 份候选；`004` 未发现表格，不占用本任务候选名额。
- 在 `outputs/task-data-001-anchor-template/TASK-DATA-001-human-anchor-template.xlsx` 新增“AI预提取草稿”工作表，填入 `003` 的 9 个审核点；前三项为 `TABLE_CELL`，其余为 `BLOCK`，全部标为 `NEEDS_REVIEW` 和 `AI_ASSISTED_PREANNOTATION_NOT_GROUND_TRUTH`，等待 `zk` 直接阅读 DOCX 后确认。
- 记录 `003` 的高风险待确认项：文件名为“二标段”但签约通知表为“三标段”；合同小写总价为 `12800`，大写为“捌仟捌佰肆拾捌元整”。记录 `004` 的后续线索：预付款 15%、结算款 100% / 97% 并存、质量保函 3%。
- 不修改业务代码、测试代码、DOCX、expected JSON、`cqcp-mvp-sample-matrix.xlsx`、parser、`CandidateResolver`、`EvidenceSlot`、`SourceAnchor` 或 Review Engine；不运行完整 MVP E2E，不归档 `TASK-EVAL-001`，不补足 DoD #12，不进入 `TASK-028` / `TASK-031` / `TASK-032`。

## 2026-07-09 TASK-DATA-001 样本可用性人工确认

- 用户确认 `CQCP-MVP-DOCX-001` 与 `CQCP-MVP-DOCX-002` 均可用于项目验证，data owner / 确认人为 `zk`。
- 用户确认两份样本脱敏合规已确认。
- 本次确认只覆盖样本可用性和脱敏合规，不等于人工 anchor 标准答案已完成；真实 DOCX `TABLE_CELL` 人工 anchor 仍需按 `TASK-DATA-001` 模板人工标注。
- 本轮仅同步任务文件、`CURRENT_CONTEXT.md`、`tasks/MVP_TASK_MAP.md` 和本 changelog；不修改代码、测试、DOCX 样本、expected JSON、`cqcp-mvp-sample-matrix.xlsx`、parser、`CandidateResolver`、`EvidenceSlot`、`SourceAnchor`、Review Engine、workflow、ADR 或 PRD。

## 2026-07-09 TASK-DATA-001 MVP E2E 人工 anchor 准备执行

- 冻结真实 DOCX `TABLE_CELL` 人工 anchor 标注规则：人工 ground truth 必须来自人工直接阅读 DOCX、业务方确认材料或 data owner 明确确认，不得由 parser / AI / 被测系统 actual 输出倒填。
- 选定 `CQCP-MVP-DOCX-001` 与 `CQCP-MVP-DOCX-002` 作为本轮候选样本，覆盖工程采购合同和产品采购合同；data owner、人工确认者、样本可用性和是否补选第 3 份样本均标记为“待确认”。
- 定义人工标注最小字段模板，覆盖 `sampleId`、`docxPath`、`reviewPointCode`、`expectedCandidateValue`、`anchorGranularity`、`humanAnchorText`、`tableContext`、`rowContext`、`cellContext`、`groundTruthSource`、`dataOwner`、`independenceStatement`、`status` 等字段。
- 明确后续 fixture / expected JSON / 测试转换必须另行冻结关联 `TASK-DATA-001` 的 `TASK_SPEC`；只有 `status = ACCEPTED_HUMAN_GROUND_TRUTH` 且 data owner / 人工复核证据已记录时，才可作为真实 DOCX `TABLE_CELL` expected anchor 来源。
- 本轮不修改业务代码、测试代码、DOCX 样本、expected JSON、`cqcp-mvp-sample-matrix.xlsx`、parser、`CandidateResolver`、`EvidenceSlot`、`SourceAnchor`、Review Engine、workflow、ADR、PRD、OpenAPI、Docker 或数据库迁移；不运行完整 MVP E2E，不归档 `TASK-EVAL-001`，不补足 DoD #12，不进入 `TASK-028` / `TASK-031` / `TASK-032`。

## 2026-07-09 TASK-DATA-001 Review Intake / 任务建档

- 新建 `tasks/active/TASK-DATA-001-mvp-e2e-human-anchor-preparation.md`，承接 MVP E2E 人工 anchor 准备和真实 DOCX `TABLE_CELL` 标准答案前置工作。
- Review Intake Decision：`GO TO TASK-DATA-001 TASK BUILD / NO IMPLEMENTATION AUTHORIZATION`。
- 明确本任务只冻结人工 anchor 标注规则、候选样本选择原则和人工标注字段模板；不修改代码、测试、fixture、expected JSON、workflow、ADR、PRD、OpenAPI、Docker 或数据库迁移。
- 明确人工 anchor ground truth 必须独立于 parser / AI / 被测系统 actual 输出；后续转换为 fixture / expected JSON / 测试必须另行冻结 `TASK_SPEC`。
- 本轮不归档 `TASK-EVAL-001`，不补足 DoD #12，不进入 `TASK-028` / `TASK-031` / `TASK-032`。

## 2026-07-09 治理规则收敛与低风险文档批处理

- 将 `TASK-EVAL-001` 独立审计后状态收口与治理规则收敛合并为一次低风险文档批处理；不新建 TASK，不派独立 agent，不归档 `TASK-EVAL-001`。
- 明确低风险文档同步、状态摘要、changelog 补录、路径修正和 post-merge 状态写回可由 Codex 自查并合并式 Memory Writeback；不要求每个小状态变化单独收尾。
- 明确 Next Task Handoff Prompt 仅在存在明确下一任务文件、编号、目标和边界时输出；v3 强门禁不自动泛化到普通文档状态同步。
- 强门禁继续保留：父任务归档、`push` / `merge`、进入 `TASK-028` / `TASK-031` / `TASK-032`、`EvidenceSlot` / `CandidateResolver` / `SourceAnchor`、expected fixture / 评测指标、workflow / CI / required checks / branch protection、Codex 自写代码后自审、工作区或远程状态不清、人工 anchor ground truth。
- 本轮不修改代码、测试、fixture、expected JSON、workflow、ADR、PRD、OpenAPI、Docker 或数据库迁移，不 push。

## 2026-07-09 TASK-EVAL-001 独立审计后状态收口

- 用户确认 `TASK-EVAL-001` 独立只读审计已完成；Codex Review Intake Decision 为 `NO-GO TO ARCHIVE / KEEP ACTIVE`。
- 明确 DoD #12 固定未通过、不可补足，`TASK-GOV-005` 不补足 DoD #12；真实 DOCX `TABLE_CELL` 仍未验证；expected / anchor 当前只证明 parser-backed 一致性，不证明独立人工 ground truth。
- `TASK-028` / `TASK-031` / `TASK-032` 继续 `NO-GO`；后续实际推进方向合并为 `TASK-DATA-001` / MVP E2E 人工 anchor 准备，不再围绕 `TASK-EVAL-001` 反复写文档。
- 该状态收口已与治理规则收敛合并为同一次低风险文档批处理；不创建新任务，不归档 `TASK-EVAL-001`，不修改代码、测试、fixture、expected JSON、workflow、ADR 或 PRD，不 push。

## 2026-07-09 TASK-033 提交前收口授权

- 用户确认独立只读审计已对 `TASK-033` 归档迁移与 Memory Writeback 给出 `GO`。
- 用户授权执行提交前收口、精确 stage、commit 与 push；不允许进入下一任务。
- 本轮仅补记提交前审计与授权证据，不改变 `TASK-033` 归档边界，不归档 `TASK-EVAL-001`，不进入 `TASK-028` / `TASK-031` / `TASK-032`。

## 2026-07-08 TASK-033 父任务归档迁移

- 执行 `TASK-033` 归档迁移，将任务文件从 `tasks/active/TASK-033-mvp-e2e-sample-acceptance-spec-freeze.md` 移动到 `tasks/done/TASK-033-mvp-e2e-sample-acceptance-spec-freeze.md`。
- PR #24 已合并规格冻结建档，merge commit 为 `880893639ada9fa5e2d42b3d2bccb1662e37a5c9`；PR #25 已合并 post-merge 状态写回，merge commit 为 `a60fc9f`，head commit 为 `79c0b3b`。
- Codex 归档 Review Intake Decision 为 `GO_TO_ARCHIVE_WITH_POST_MERGE_SYNC_SATISFIED`；归档前独立只读审计结论 `NEEDS_POST_MERGE_SYNC` 指出的状态残留已通过 PR #25 与本次归档迁移清理。
- 本轮仅同步 `TASK-033` 任务文件、`docs/VERIFY.md`、`CURRENT_CONTEXT.md`、`tasks/MVP_TASK_MAP.md` 和本 changelog；不归档 `TASK-EVAL-001`，不补足 `TASK-EVAL-001` DoD #12，不进入 `TASK-028` / `TASK-031` / `TASK-032`。
- 本轮不修改代码、测试、fixture、expected JSON、workflow、ADR 或 PRD，不授权 Gemma Provider、parser provenance、`SourceAnchor` 或 `TABLE_CELL` coverage 实现，不把 AI/parser 输出当作人工 anchor 标准答案。

## 2026-07-08 TASK-033 post-merge 状态写回

- PR #24 已合并，merge commit 为 `880893639ada9fa5e2d42b3d2bccb1662e37a5c9`。
- 根据 `TASK-033` 父任务归档前独立只读审计结论 `NEEDS_POST_MERGE_SYNC`，清理 post-merge 过期状态残留。
- 本轮仅同步 `tasks/active/TASK-033-mvp-e2e-sample-acceptance-spec-freeze.md`、`CURRENT_CONTEXT.md`、`tasks/MVP_TASK_MAP.md` 和本 changelog 的 post-merge 状态；不归档 `TASK-033`，不归档 `TASK-EVAL-001`，不补足 `TASK-EVAL-001` DoD #12，不进入 `TASK-028` / `TASK-031` / `TASK-032`。
- 本轮不修改代码、测试、fixture、expected JSON、workflow、ADR 或 PRD，不授权 Gemma Provider、parser provenance、`SourceAnchor` 或 `TABLE_CELL` coverage 实现，不把 AI/parser 输出当作人工 anchor 标准答案。

## 2026-07-07 TASK-033 MVP 端到端样本验收规格冻结建档

- 新建 `tasks/active/TASK-033-mvp-e2e-sample-acceptance-spec-freeze.md`，将 `TASK-033` 定界为 Codex 主控的 MVP 端到端样本验收规格冻结任务，不是 CC-DS 执行环境实现 `TASK_SPEC`。
- 在 `docs/VERIFY.md` 增加 MVP 端到端样本验收规格入口，明确样本选择、输入字段、验收命令、证据口径和 expected 来源独立性要求。
- 明确本任务不运行完整验收，不修改代码、测试、fixture、expected JSON、parser、`CandidateResolver`、`EvidenceSlot`、`SourceAnchor`、workflow、ADR 或 PRD。
- 明确不归档 `TASK-EVAL-001`，不补足 DoD #12，不进入 `TASK-028` / `TASK-031` / `TASK-032`，不迁移 historical active，不授权 Gemma Provider、parser provenance、`SourceAnchor` 或 `TABLE_CELL` coverage 实现。
- 明确 `TASK-DATA-001` 承接真实 DOCX `TABLE_CELL` 人工 anchor 标准答案准备；不得把 AI/parser 输出当作人工 anchor 标准答案。
- 本任务正式提交前独立 agent 只读复核已完成，Decision 为 `GO`，无 blocking findings；PR #24 已合并，merge commit 为 `880893639ada9fa5e2d42b3d2bccb1662e37a5c9`。

## 2026-07-07 TASK-GOV-006 远端状态矫正

- 修正 `TASK-GOV-006` 过期状态：PR #18 已合并，head commit 为 `432a63a25b0352e5ba9768f68f32c95a266474e4`，merge commit 为 `d3d5d1b507d233b5ff9a20350fad7b0c05a36cf9`。
- PR #18 触发的 CI 已通过，`Authorization evidence check`、`Backend Gradle tests`、`Admin web lint, tests, and build` 均成功完成。
- 明确 `Authorization evidence check` 已在 PR #18 中成功运行，但它不是 required status check，也不证明用户授权、测试、独立审计或 Memory Writeback 已真实发生。
- 本轮仅修改 `CURRENT_CONTEXT.md`、`tasks/MVP_TASK_MAP.md`、`tasks/active/TASK-GOV-006-submit-authorization-evidence-gate.md` 和本 changelog；不修改代码、测试、workflow，不迁移 `tasks/active` 文件，不归档 `TASK-EVAL-001`，不进入 `TASK-028` / `TASK-031` / `TASK-032`。

## 2026-07-06 TASK-030 父任务归档

- 已按 `GO_TO_ARCHIVE_WITH_CONDITIONS_SATISFIED` 执行 `TASK-030` 父任务归档流程。
- 已将 `TASK-030` 父任务文件和 `TASK_SPEC-030-A/B/C` 从 `tasks/active/` 移动到 `tasks/done/`。
- 已同步 `CURRENT_CONTEXT.md`、`tasks/MVP_TASK_MAP.md` 和本 changelog 的归档状态。
- 本次归档不修改业务代码、测试、fixture、expected JSON、OpenAPI、数据库、Docker、workflow、ADR 或 PRD；不授权 runtime loader、数据库资产表、发布审批、model / budget profile 源定义、`TASK-028`、`TASK-031`、`TASK-032` 或 `TASK-033`。

## 2026-07-06 TASK-030 归档 Review Intake Decision

- PR #21 已合并 `TASK-030` post-merge 状态写回，head commit 为 `40f28e7159f0cfc7d63546db07c5b8819edbc0c3`，merge commit 为 `ef4643c7d11b1209c935229b4e39b76d659db00f`。
- PR #21 三项 PR workflow checks 均为 `SUCCESS`：`Authorization evidence check`、`Backend Gradle tests`、`Admin web lint, tests, and build`；其中 `Authorization evidence check` 不是 required status check。
- 独立只读审计结论为 `GO_WITH_CONDITIONS`；B1-B5 条件已通过 PR #21 修正并合并。
- Codex 归档 Review Intake Decision：`GO_TO_ARCHIVE_WITH_CONDITIONS_SATISFIED`。
- 本 Decision 只授权 `TASK-030` 父任务归档流程；不授权 runtime loader、数据库资产表、发布审批、model / budget profile 源定义、`TASK-028`、`TASK-031`、`TASK-032` 或 `TASK-033`。

## 2026-07-06 TASK-030 PR #20 合并与父任务收口判断

- PR #20 已合并 `TASK-030` Review assets 版本化治理 A/B/C 当前批次，head commit 为 `29d4a94eaf07b37831f07dd93f78d3027b770f01`，merge commit 为 `eadd017808fc8c2471fb0ac081f1e68e22dce5f5`。
- post-merge 只读核查结论为 `POST_MERGE_GO`：本地 `master` 已与 `origin/master` 对齐，工作区 clean，变更文件均属于 `TASK-030` A/B/C 授权范围，7 个 Review assets JSON 均通过 `node scripts/validate-review-assets.mjs` 校验。
- 父任务收口判断为 `READY_FOR_INDEPENDENT_PRE_ARCHIVE_AUDIT / NOT_ARCHIVED`：Codex post-merge 只读核查不能替代父任务归档前独立 agent 只读审计，因此当前不得归档 `TASK-030`。
- 修正 `TASK_SPEC-030-A/B/C` 顶部状态残留：由“待提交”改为“PR #20 已合并 / 待父任务归档前独立只读审计”，不改变执行记录中的历史阶段事实。
- 本轮仅同步 `TASK-030` 父任务、`CURRENT_CONTEXT.md`、`tasks/MVP_TASK_MAP.md` 和本 changelog 的状态记录；不修改业务代码、测试、fixture、expected JSON、OpenAPI、数据库、Docker、workflow、ADR 或 PRD。
- 本轮不进入 `TASK-028` / `TASK-031` / `TASK-032`，不创建 `TASK-033`，不解除 `TASK-EVAL-001` blocked，不启用 runtime loader，也不声明 `RuleSetVersion` 已在生产 runtime 绑定。

## 2026-07-05 TASK_SPEC-030-C Review assets 版本治理与变更流程最小建档

- 按 Codex 审查放行的编码前规格映射计划，由 Claude Code 执行 `TASK_SPEC-030-C`。
- 更新 `packages/review-assets/README.md`：新增版本命名规则（`vYYYYMMDD.N`）、新增/升级资产流程、RuleSetVersion manifest `moduleVersions` 引用规则（`path`/`assetId`/`version` 一致性）、校验命令说明和 runtime loader 未启用声明。
- 新建 `tasks/active/TASK_SPEC-030-C-review-assets-version-governance-docs.md`。
- 更新父任务 `TASK-030` 执行记录、`CURRENT_CONTEXT.md` 和本 changelog（仅摘要和引用路径）。
- 本轮不修改 7 个 JSON 资产文件、`scripts/validate-review-assets.mjs`、业务代码、测试、fixture、expected JSON、OpenAPI、数据库、Docker、workflow、ADR 或 PRD；不进入 `TASK-028` / `TASK-031` / `TASK-032`，不创建 `TASK-033`，不接入 Gemma / A30。
- `node scripts/validate-review-assets.mjs` 仍通过（7 文件全通过）。

## 2026-07-05 TASK_SPEC-030-B Review assets schema 与 manifest 引用校验

- 按 Codex 放行的编码前规格映射计划，由 Claude Code 执行 `TASK_SPEC-030-B`。
- 新增 `scripts/validate-review-assets.mjs`，无第三方依赖，仅使用 `node:fs` 和 `node:path`。
- 脚本校验 `packages/review-assets/` 下所有 JSON 的必需字段（`assetId`、`assetType`、`version`、`status`、`source`、`changeReason`）、`assetId` 唯一性、`source.type=code-current-mapping`、`source.runtimeBinding=NOT_BOUND`、`runtimePolicy.loaderEnabled` 不为 `true`、RuleSet manifest `moduleVersions` 引用文件存在且 `assetId`/`version` 一致。
- 校验通过时退出码为 0，失败时收集全部错误后统一输出再退出码 1。
- 当前 7 个资产文件全部通过校验。
- 本轮不修改业务代码、测试、fixture、expected JSON、OpenAPI、数据库、Docker、workflow、ADR 或 PRD；不进入 `TASK-028` / `TASK-031` / `TASK-032`，不创建 `TASK-033`，不接入 Gemma / A30。

## 2026-07-05 TASK_SPEC-030-A Review assets 静态源文件与 manifest 最小建档

- 按用户授权推进并完成 `TASK_SPEC-030-A`，新增 `tasks/active/TASK_SPEC-030-A-review-assets-static-source-manifest.md`。
- 在 `packages/review-assets/` 下补齐首批静态资产源文件，覆盖 `rule-sets`、`review-point-definitions`、`pattern-libraries`、`field-lexicons`、`prompts`、`contract-type-profiles`、`evidence-selectors`。
- 新增 `ruleset-v20260705.1.json` 作为 RuleSetVersion manifest，引用 review point、pattern library、field lexicon、prompt、contract type profile 和 evidence selector 的模块版本。
- 所有资产均声明当前只是“代码现状映射”，不声明生产 runtime 已绑定；本轮不启用 runtime loader，不接入 Gemma / A30。
- 本轮不修改业务代码、测试、fixture、expected JSON、OpenAPI、数据库、Docker、workflow、ADR 或 PRD；不进入 `TASK-028` / `TASK-031` / `TASK-032`，不创建 `TASK-033`。

## 2026-07-05 TASK-030 Review assets 版本化治理只读盘点

- 按用户要求直接执行 `TASK-030`，完成 Review assets 相关规则、正则、Prompt、合同类型画像、EvidenceSelector、模型 profile / prompt 配置的只读盘点。
- 结论：`packages/review-assets/` 当前只有目录骨架和 `.gitkeep`；真实规则 / 正则 / EvidenceSlot runtime / CandidateResolver / deterministic rule 仍主要位于后端 Java 代码；execution、snapshot、TuningPacket 和 ModelCallIntent 已预留版本引用字段，但尚无资产源文件、manifest、schema 校验、runtime loader、发布审批或数据库资产表。
- 已在 `TASK-030` 中记录一期最小版本字段、建议目录命名、后续 `TASK_SPEC` 切分建议和 ADR 判断：静态资产建档与校验默认可走普通任务；runtime loading、数据库持久化、发布治理、模型职责或 EvidenceSlot / CandidateResolver 行为变化必须重新判断 ADR。
- 本轮仅修改任务文件、`CURRENT_CONTEXT.md`、`tasks/MVP_TASK_MAP.md` 和本 changelog；不修改业务代码、测试、fixture、expected JSON、OpenAPI、数据库、Docker、workflow、ADR 或 PRD；不进入 `TASK-028` / `TASK-031` / `TASK-032`，不解除 `TASK-EVAL-001` blocked，不创建 `TASK-033`，不派发 CC-DS 执行环境。

## 2026-07-05 完成态复核优先治理规则

- 按用户确认，后续 CQCP 任务推进采用“完成态复核优先”规则，避免审计过载。
- 普通任务执行过程中不反复请求外部复核，默认完成后一次性提交 `diff`、测试输出、`git status`、风险说明和 Memory Writeback 状态供复核。
- 明确父任务归档、`push` / `merge`、进入 `TASK-028` / `TASK-031` / `TASK-032`、`EvidenceSlot` / `CandidateResolver` / `SourceAnchor`、expected fixture / 评测指标、workflow / CI / required checks / branch protection、Codex 自写代码后自审、工作区或远程状态不清等节点必须追加独立 agent 只读复核。
- 同步 `docs/DEVELOPMENT.md`、`docs/VERIFY.md`、`CURRENT_CONTEXT.md`、`tasks/MVP_TASK_MAP.md` 与 `TASK-GOV-006` 完成态复核记录；独立只读复核结论为 `GO`，无阻塞问题；本轮仅修改治理文档和项目记忆，不修改业务代码、workflow、测试、fixture、expected JSON、OpenAPI、数据库、Docker、PRD、架构文档或 ADR。

## 2026-07-04 TASK-EVAL-001 归档前审计启动前收口判断

- 按用户要求先处理 `TASK-EVAL-001` 的归档前审计 / 收口判断。
- Codex 预审结论为 `NO-GO TO ARCHIVE / INDEPENDENT AUDIT REQUIRED / BASELINE NOT CLEAN`。
- 判断依据：rebaseline 后归档门禁要求独立 agent 只读审计；本轮 Codex 只能做预审与收口判断，不能替代独立审计；当前主工作区仍有未提交治理改动，不能作为干净归档审计基线。
- 明确当前不得归档 `TASK-EVAL-001`，不得恢复 `GO TO ARCHIVE WITH CONDITIONS`，不得把 `TASK-GOV-005`、事后独立复核或 `30/30 PASS` 写成本父任务 DoD #12 已通过，也不得进入 `TASK-028` / `TASK-031` / `TASK-032`。
- 已在 `TASK-EVAL-001` 中记录建议独立只读审计输入；后续需等待独立 agent 审计报告返回后，Codex 再单独给出 Review Intake Decision。

## 2026-07-04 TASK-GOV-006 提交前授权证据模板与 PR 文本门禁

- 按用户确认启动 `TASK-GOV-006`，本地准备提交前授权证据模板、PR 模板和 `Authorization evidence check` PR body 文本门禁。
- 新增 `docs/governance/pre-submit-authorization-evidence-template.md`，统一记录任务编号、Governance Mode、允许文件、禁止文件、commit / push / merge 授权、测试证据、独立审计状态、Memory Writeback 和范围外确认。
- 新增 `.github/pull_request_template.md` 与 `scripts/check-pr-authorization-evidence.mjs`，并提供正反 fixture；新增 workflow job 仅在 `pull_request` 事件检查 PR body 字段是否存在且非占位；本轮本地正向 fixture 已通过、反向 fixture 已按预期失败。后续 PR #18 已完成真实 PR 触发验证，`Authorization evidence check` 成功运行；该 check 不是 required status check，也不证明真实授权事实。
- 同步 `docs/DEVELOPMENT.md`、`docs/VERIFY.md`、`CURRENT_CONTEXT.md` 与 `tasks/MVP_TASK_MAP.md`，明确该检查只验证文本字段，不证明授权、测试、独立审计或 Memory Writeback 已真实发生。
- 本轮不修改业务代码、测试、fixture、expected JSON、OpenAPI、数据库、Docker、PRD、架构文档或 ADR；不配置 branch protection、repository ruleset 或 required status checks；不进入 `TASK-028` / `TASK-031` / `TASK-032`。

## 2026-07-04 TASK-GOV-005 治理债务定界

- 继续处理 `TASK-GOV-005`，将历史 commit / push 授权证据债务定界为 `BOUNDARY RECORDED / NO RECOVERY PATH / NO IMPLEMENTATION AUTHORIZATION`。
- 明确不再尝试恢复 `TASK-EVAL-001-A/B` 历史授权链；该缺口作为长期治理边界保留，不追溯否定已 merge / push 内容、事后独立审计或定向测试 `30/30 PASS`。
- 明确 `TASK-GOV-005` 不补足 `TASK-EVAL-001` DoD #12，不支撑父任务归档，不授权 `TASK-028` / `TASK-031` / `TASK-032`，也不覆盖 `DEBT-001-03` 或 `DEBT-001-05`。
- 记录独立只读审计结论：`GO`，无阻塞问题；非阻塞问题为完成记录措辞可能混淆“债务定界完成”和“任务归档完成”，已补充说明任务仍长期保留 active。
- 同步 `CURRENT_CONTEXT.md` 与 `tasks/MVP_TASK_MAP.md`；本轮不修改业务代码、测试、fixture、expected JSON、OpenAPI、数据库、Docker、workflow、PRD、架构文档或 ADR。
## 2026-07-04 TASK-EVAL-001 rebaseline

- 按用户要求处理 `TASK-EVAL-001` rebaseline，更新父任务文件、`CURRENT_CONTEXT.md` 与 `tasks/MVP_TASK_MAP.md`。
- 明确 rebaseline 后不再按原父任务 DoD 伪装为 12/12 全通过：DoD #1 至 #11 仅保留为既有独立确认摘要，DoD #12 固定为未通过、未补足。
- 明确 `TASK-EVAL-001-A/B` 历史实现结果只能作为技术质量信号和后续参考，不作为父任务归档通过证据；`TASK-GOV-005` 只治理历史授权证据债务，不补足父任务 DoD。
- 本轮不归档 `TASK-EVAL-001`，不恢复 `GO TO ARCHIVE WITH CONDITIONS`，不解除 `TASK-028` / `TASK-031` / `TASK-032` 门禁，不创建或派发 `TASK_SPEC`。
- 本轮仅修改治理文档；不修改业务代码、测试、fixture、expected JSON、OpenAPI、数据库、Docker、workflow、PRD、架构文档或 ADR。

## 2026-07-04 MVP 上线路线调整

- 按用户确认，取消单独创建 MVP 上线 / readiness 任务的路线，后续按正常开发顺序和任务地图推进。
- 同步 `CURRENT_CONTEXT.md` 与 `tasks/MVP_TASK_MAP.md`，明确当前不因此进入 `TASK-029`，也不解除 `TASK-028` / `TASK-031` / `TASK-032` 门禁。
- 本轮仅做项目记忆修正；不修改代码、测试、fixture、expected JSON、OpenAPI、数据库、Docker、workflow、PRD、架构文档或 ADR。

## 2026-07-03 TASK-DEBT-001 归档

- 经用户明确授权，执行 `TASK-DEBT-001` 父任务归档流程。
- 将 `tasks/active/TASK-DEBT-001-review-engine-verified-defects-and-coverage-gap.md` 移动到 `tasks/done/TASK-DEBT-001-review-engine-verified-defects-and-coverage-gap.md`。
- 更新 `CURRENT_CONTEXT.md` 与 `tasks/MVP_TASK_MAP.md`，将 `TASK-DEBT-001` 从 active 状态改为已完成并归档。
- 归档依据：2026-07-02 父任务归档前独立只读审计 `GO`、Codex Review Intake Decision `GO TO ARCHIVE WITH CONDITIONS`、PR #14 归档前审计写回、PR #15 归档前文档状态同步。
- 归档不授权 parser provenance / `SourceAnchor`、real DOCX `TABLE_CELL`、`TASK-028`、`TASK-031` 或 `TASK-032`。

## 2026-07-03 CURRENT_CONTEXT 瘦身与 TASK-DEBT-001 状态同步

- 已同步本地 `master` 与 `origin/master`；最新合并为 PR #14，merge commit `13794fb2f09e02dad3b1d556cb6a5b9af7731c66`，head commit `d232df3fa320ae1ef85578942a366e125b961a0b`。
- 已将 `CURRENT_CONTEXT.md` 瘦身为当前阶段、活跃任务、最近已合并 PR、阻塞项、禁止进入项、待确认事项、下一步和参考路径。
- 已更新 `tasks/MVP_TASK_MAP.md` 和 `tasks/active/TASK-DEBT-001-review-engine-verified-defects-and-coverage-gap.md`，仅将 PR #14 记录为父任务归档前审计写回。
- 父任务 `TASK-DEBT-001` 仍为 active，未归档。
- `DEBT-001-03` parser provenance / `SourceAnchor`、`DEBT-001-05` real DOCX `TABLE_CELL`、`TASK-028`、`TASK-031` 和 `TASK-032` 仍未获授权，未进入。

## 2026-07-02 TASK-DEBT-001 parent pre-archive audit writeback

- Recorded independent read-only audit conclusion for `TASK-DEBT-001`: `GO`.
- Recorded Codex parent Review Intake Decision: `GO TO ARCHIVE WITH CONDITIONS`.
- Confirmed `TASK_SPEC-DEBT-001-A/B/C` all have landing evidence:
  - A: commit `3223d6760a977fe9deaf722e63b50bcbb6ce3611` and post-push independent read-only review `GO`.
  - B: PR #4 merge commit `da724ba49c6a33347641950101a84a84dfa8c000`, audit basis `CQCP_AUDIT GO`.
  - C: PR #13 merge commit `15888df4f0e89882814940c5eca0fc948fd1fef0`, Codex Review Intake `GO` and independent read-only audit `GO`.
- Aligned stale status text in `TASK_SPEC-DEBT-001-C`, `TASK-DEBT-001`, `CURRENT_CONTEXT.md` and `tasks/MVP_TASK_MAP.md`.
- Parent `TASK-DEBT-001` remains active and not archived.
- Parser provenance / `SourceAnchor`, real DOCX `TABLE_CELL`, `TASK-028`, `TASK-031` and `TASK-032` remain unauthorized.

## 2026-07-01 TASK_SPEC-DEBT-001-C pre-commit writeback

- Completed controlled implementation record for `TASK_SPEC-DEBT-001-C resolveRatioEvidence candidate collection topology` on branch `codex/task-debt-001-c-implementation`.
- `resolveRatioEvidence()` now removes the non-prepayment semantic-candidate early return and sends the ordered deduplicated candidate set through `resolveFromCandidates(...)` / `MinimalCandidateResolver`.
- Added regression coverage for `MONTHLY` progress payment conflict where semantic `70%` and whole-text fallback `85%` resolve to `AMBIGUOUS / CONFLICTED / SYS_ROLE_CONFLICT`.
- Required checks from `apps/api-server`:
  - `ParserBackedReviewInputPreparerEvidenceTest`: `BUILD SUCCESSFUL`.
  - `MinimalCandidateResolverTest`: `BUILD SUCCESSFUL`.
  - `MinimalReviewEngineTest`: `BUILD SUCCESSFUL`.
- Codex Review Intake Decision: `GO`.
- Independent read-only audit decision: `GO`, no blocking findings.
- Scope remains limited to `TASK_SPEC-DEBT-001-C`; parent `TASK-DEBT-001` is still active and not archived.
- This change does not authorize parser provenance, real DOCX `TABLE_CELL`, `TASK-028`, `TASK-031` or `TASK-032`.

## 2026-07-24 Task Level 与 Git 收口治理优化

- 创建 `TASK-GOV-007`，将项目工作分为 `L0 探索 / L1 小文档 / L2 Feature / L3 高风险治理`。
- 明确 TASK、TASK_SPEC、commit、PR、merge 和 Milestone 是不同单位：TASK_SPEC 保持细粒度执行和 Review Intake，L2 默认一个 Feature PR，Milestone 默认承担任务归档和长期项目记忆收口。
- 独立审计绑定冻结 diff/head SHA 和实际风险；普通 push 不因传输动作重复完整内容审计。人工 ground truth、expected/fixture、核心审核链路、生产激活、数据库/API/CI/安全等 L3 强门禁继续保留。
- 从 `origin/master@97ef08f` 重建后的 16 文件冻结差异已通过独立只读审计，12 项验收全部通过、无 blocking findings；Codex Review Intake 为 `ACCEPT_GOVERNANCE_DIFF / GO_TO_COMMIT_AND_PR`，审计状态写回仍需聚焦增量复核。
- 低价值计划/status/push/checks/post-merge 提交默认并入 Feature/Milestone；只有会改变下一执行门禁的 post-merge 事实才即时写回。
- 从 `origin/master@97ef08f` 在隔离分支重建治理 diff，不带入 TASK-036 B1/B2/C1、C2 或 MVP Feature 规划内容；原工作区审计结论不复用，重建后的冻结 diff须重新独立审计。
- 用户已授权必要的 commit、push、PR 和 merge；当前待审计状态增量复核、PR/CI 与 merge，尚不得写成 PR 或 merge 已发生。

## 2026-07-24 Execution Binding Release 与 Demo Profile 冻结

- `TASK-GOV-007` 已通过 PR #33 合并，merge commit 为 `1f62320f20ec29c52f49c0ed33c4244bb1be669e`，三项 CI 全部通过。
- 创建 L3 `TASK-037`、`ADR-017` 与 `TASK_SPEC-037-A`，冻结 V1 execution 全部 14 个 `NOT NULL` version/model 字段的部署级不可变 binding source。
- 方案采用 `MVP_DEMO_MOCK`，限定 `usageScope=DEMO / secretRequired=false / readinessStatus=READY`；不得伪造 secret 或声明 Production Ready。
- migration 必须 seed `STANDARD / DEEP_REVIEW / EVALUATION` 三类不可变 budget profile；Demo binding 只消费 active `STANDARD`。
- legacy `v20260705.1` 只表示 code-current 行为与静态 module traceability；review-assets 继续保持 `DRAFT / NOT_BOUND`，不启用 runtime loader，不激活 TASK-036-C2。
- 独立规格审计最终 `GO`，ADR-017 已接受；六项首轮 blocking findings 与两项 delta finding 均已闭合，Codex 规格 Review Intake 为 `ACCEPT_ADR_AND_SPEC / GO_TO_ARCHITECTURE_SYNC / PRE_CODE_PLAN_REQUIRED`。
- `docs/ARCHITECTURE.md` 升级至 v0.11，并同步 `docs/backend.md`、`docs/database.md`；架构同步增量审计结论为 `GO`。
- Claude Code 编码前 `AC1~AC18` 计划初版因未授权测试依赖、跨表约束暗示、failure reason 分类和 PostgreSQL lifecycle 示例等问题被 Codex 阻断；修订版关闭七项问题后，Codex Decision 为 `ACCEPT_PRE_CODE_PLAN / GO_TO_IMPLEMENT`。此状态不代表实现已完成。
- TASK_SPEC-037-A 已实现 V2 三类 budget seed、`MVP_DEMO_MOCK`、完整 14 字段 binding、19 字段 digest、fail-closed Catalog 与 PostgreSQL lifecycle/FK/readiness 验证；一期 selector 限定为 `MVP_DEMO / DEMO / ENGINEERING`。
- 独立实现审计首轮发现 placeholder digest、非 Demo selector、FK 假阳性与 lifecycle/seed 断言缺口；修复后第二轮结论为 `GO`，无 P1/P2 blocker。
- Codex 实现 Review Intake 为 `ACCEPT_IMPLEMENTATION / GO_TO_COMMIT_PR`。全新 PostgreSQL 16.14 空库定向 45 tests、全量 backend 157 tests、review-assets validator 与 `git diff --check` 全部通过；当前仍待 PR/CI/merge，不写成已合并。

## 2026-07-28 FEATURE-MVP-001 实现与真实 Demo 验收

- `TASK-037 / ADR-017` 已通过 PR #34 合并，merge commit
  `401fd05b7a6c23014adb4f5511533467016c37ba`；FEATURE-MVP-001 从该基线建立，
  保持 legacy `v20260705.1` 与 `MVP_DEMO_MOCK`，不激活 TASK-036-C2。
- `TASK_SPEC-MVP-001-A~E` 已完成并接纳，Feature 内 commits 为
  `f8d76e7 / 2120399 / 0eb9d9e / 9fcf857 / 539c9e6`。
- `TASK_SPEC-MVP-001-F` 已在标准 Docker Compose 环境通过真实浏览器完成
  `/review/new -> 202 创建 -> 双 identity 状态轮询 -> Single Worker -> 正式结果页`
  闭环；Task/Execution 最终 `SUCCESS`，9 个审核点全部 `PASS`，
  `FULL_REVIEWED / HIGH`。
- PostgreSQL 只读核对确认 Task、Execution、12 条阶段事件和 ReviewResultSnapshot
  identity 一致；snapshot 保存 17 个结构化字段、9 个点级结果和 8 个去重
  SourceAnchor。源 DOCX 与受控持久化文件 SHA-256 一致。
- F 回归暴露 `ReviewTaskCreationIntegrationTest` 未关闭 Worker 且清理遗漏 FK 子表的
  测试隔离缺陷；按 `TASK_SPEC-MVP-001-F1` 定点修复并提交为 `439a020`。
  定向 16/16、连续两轮后端全量强制回归均为 313/313。
- admin-web 62/62、lint、production build、review-assets validator 7/7 与
  `git diff --check` 通过。脱敏运行证据位于
  `outputs/task-mvp-001-demo-acceptance/`。
- 当前只完成实现、F 验收和 Memory Writeback；尚未完成 CC AUDIT、Codex subagents
  双重独立审计、Feature PR、CI 或 merge，不声明 Production Ready、正式 TASK-034
  质量 E2E 或 57/57 coverage。

## 2026-07-28 FEATURE-MVP-001 首轮审计 NO-GO 与 F2 定点修复

- 首轮 Codex 双代理只读审计中，代码/API/数据库/状态机审计为 `GO`，测试/前端安全/
  Compose/真实证据审计为 `NO-GO`；CC AUDIT 因总门禁已失败而未发送，旧 index tree
  `c3f2c7a12e66d83351f822869d3c379c1c3478c3` 失效。
- `TASK_SPEC-MVP-001-F2` 先以 `2 failed / 31 passed` 复现：创建响应接受
  whitespace-only identity，正式结果 query 会静默 trim 非规范 executionId。
  定点修复后定向 33/33、admin-web 全量 63/63、lint/build 与 validator 7/7 通过。
- 后端通过仓库 Gradle 8.10.2 连接 Compose PostgreSQL test DB 连续两轮强制全量
  313/313；Compose test profile 的一次外部依赖 `Tag mismatch` 失败也已原样保留，
  未用其替代测试执行结果。
- Compose 使用本次修复重建成功；定点 HTTP 创建返回 202，新 Task/Execution 最终
  `SUCCESS`，真实浏览器从精确状态路由自动进入正式结果路由，仍为 9/9 PASS。
  PostgreSQL 与上传文件持久化复核通过，保持 legacy `v20260705.1` 和
  `MVP_DEMO_MOCK`。
- 测试、Compose、HTTP、数据库、首次 QUEUED DOM 与重建后结果 DOM 的原始命令、
  退出码和 stdout/stderr 已固化到 `outputs/task-mvp-001-demo-acceptance/raw/`；
  当前等待新 manifest 冻结及 CC AUDIT + 两个 Codex subagent 全量重新审计。
- 第二次冻结复核发现数据库证据的 `wc -c` stdout 暴露容器绝对上传根，并指出
  修复前 red 只有任务自述、无冻结 stdout；因此 v2 基线再次失效，CC AUDIT 仍未发送。
- 数据库证据现改为受控 placeholder 命令与 size-only stdout。另在独立临时副本以
  `HEAD@439a020` 旧实现 + staged F2 tests 固化 `EXIT_CODE=1 / 2 failed /
  31 passed`，当前 staged 实现相同用例固化 `EXIT_CODE=0 / 33 passed`；
  同时纠正 CURRENT_CONTEXT 的 F2 归档清单与 TASK-037 已合并事实。

## 2026-07-28 FEATURE-MVP-001 双重审计与归档收口

- 最终 v5 冻结基线为 Feature base `401fd05b7a6c23014adb4f5511533467016c37ba`、
  HEAD `439a02012c54f11577c6e916591d69db2a7d5d58`、index tree
  `dc81a8c3d2636844db7e7e85748ca507720323fd`，覆盖 base → index 102 个
  changed paths；102/102 文件与 37/37 原始证据 SHA-256 均匹配。
- full diff SHA-256 为
  `D60D11D81D6FBEEBE29FF32553B2A6159853820C988D8A8278F605AD559BC37F`；
  freeze manifest SHA-256 为
  `E7F6A67D36F253FFFC3704DE5E7BFF128DE8E64798FCCCA45995B61022D92C4E`。
- CC AUDIT 第三方只读审计结论为 `GO`，`P0=0 / P1=0 / P2=0`、无 blocking
  findings；两个 Codex 独立只读 subagent 分别覆盖代码/API/数据库/状态机与
  测试/前端安全/Compose/真实 Demo，结论均为 `GO`。
- 最终门禁
  `CC_AUDIT=GO AND CODEX_SUBAGENT_AUDIT=GO AND blocking findings=0`
  已满足。Codex 最终 Review Intake 为
  `ACCEPT_FEATURE_IMPLEMENTATION / DUAL_AUDIT_GO / GO_TO_COMMIT_PR_CI_MERGE`。
- 父 `TASK-MVP-001` 与 A~F/F1/F2 已批量迁移到 `tasks/done/`。当前仅待同一
  Feature 的完成态 commit、一个 PR、required CI 与 merge；不运行或改写
  TASK-034，不声明 57/57 coverage 或 Production Ready，不激活 `v20260715.1`。

## 2026-07-28 FEATURE-MVP-001 PR #35 backend CI NO-GO

- 完成态 commit `1dbef9cdbcb0e98996c58b262d1b668f0d06941e` 已 push，PR #35
  已创建；`Authorization evidence check` 与 admin-web CI 通过。
- backend CI run `30326662660` / job `90173392371` 在 313 项中有 3 项失败：
  `ac13_finalSymlink_failsClosed`、`ac13_parserFailure_failsClosed`、
  `ac13_parentDirSymlink_failsClosed`。失败发生于 Linux fixture 构造阶段，
  分别为 `NoSuchFileException` / `AccessDeniedException`。
- 根因为 `SingleReviewWorkerIntegrationTest.uploadRoot()` 在未设置环境变量时
  硬编码 `/data/cqcp/uploads`，与应用配置真源默认 `./data/uploads` 不一致。
- 创建 `TASK_SPEC-MVP-001-F3`，只允许测试注入同一 Spring
  `cqcp.review.upload-root` property；不修改业务代码或 workflow。
- F3 验证通过：未设置 `CQCP_UPLOAD_ROOT` 时，host 定向 11/11、backend 全量
  313/313，Linux `gradle:8.10.2-jdk21` + 只读依赖缓存定向 11/11。一次在线
  Linux 依赖下载在进入测试前因 `Illegal packet size` 失败，未作为通过证据。
- 父 TASK 已重新 active，v5 与完成态增量审计基线失效；当时门禁要求 F3 验证后
  重新冻结并重跑 CC AUDIT 与两个 Codex subagent，双重 GO 前不得 push 修复或 merge。
- v6 测试/安全审计为 GO，但代码审计因只有 F3 人工摘要、缺少原始 green console
  （P1），以及父 TASK 残留“PR 尚未创建”等 v5 旧叙事（P2）判定 NO-GO；
  v6 基线永久失效。
- 已重新执行并冻结 host targeted 11/11、host full 313/313、Linux offline
  targeted 11/11 的原始 stdout/stderr，并固化 Linux online 依赖失败原始输出；
  父 TASK 的 v5 完成段已明确标为历史失效。下一基线必须重跑 CC AUDIT 与两个
  Codex subagent。
- v7 代码审计因 `CURRENT_CONTEXT.md` 首段仍保留 v5 放行口径判定 NO-GO；
  测试/安全审计另指出父 TASK Handoff 落后于 v7、F3 summary 捕获时间早于重新
  执行的原始 console。三项状态/元数据 finding 已补正，v7 基线永久失效。
- v8 两个 subagent 均因 `CURRENT_CONTEXT.md`“下一步”仍列出已完成的 F3
  实现和测试而判定 NO-GO；该过期步骤已删除。v8 其余代码、raw console、
  109/109 paths、43/43 evidence hashes、安全和范围核验均通过。

## 2026-07-28 FEATURE-MVP-001 F3 v9 双重审计 GO 与归档

- v9 冻结基线：base `401fd05b7a6c23014adb4f5511533467016c37ba`、HEAD
  `1dbef9cdbcb0e98996c58b262d1b668f0d06941e`、index tree
  `c37c328841be41a09b84c4a96056b0282c881f05`，覆盖 109 个 base → index
  changed paths，tracked unstaged 为 0，`git diff --cached --check` 通过。
- full diff SHA-256 为
  `D5E88C6070223F273771D2D1BB3BAE22E99B08D40AF02E3D8F3225D521AF597E`；
  freeze manifest SHA-256 为
  `27EE638BF399D03F1E0F49FB40B2C2C0216DA9F1971F04DBFFEC0B07AE67AECF`；
  run-manifest SHA-256 为
  `CE60C4764029B7D66F0F283465118AE382EB1F2AFCA2D0CD1F2B5085668864BE`。
  独立核验确认 109/109 文件与 43/43 evidence hashes 匹配。
- Codex 代码/API/数据库/状态机/范围 subagent：
  `CODE_AUDIT=GO / P0=0 / P1=0 / P2=0 / blocking=0`。
- Codex 测试/前端安全/Compose/真实 Demo subagent：
  `TEST_SECURITY_AUDIT=GO / P0=0 / P1=0 / P2=0 / blocking=0`。
- CC AUDIT 会话 `CQCP FEATURE-MVP-001 v9 freeze audit`：
  `CC_AUDIT=GO / P0=0 / P1=0 / P2=0 / BLOCKING_FINDINGS=0`，全程只读。
- 最终门禁
  `CC_AUDIT=GO AND CODEX_SUBAGENT_AUDIT=GO AND blocking findings=0`
  已满足。Codex Review Intake 为
  `ACCEPT_F3_IMPLEMENTATION / V9_DUAL_AUDIT_GO / GO_TO_COMMIT_PUSH_PR_CI_MERGE`。
- 父 `TASK-MVP-001` 与 `TASK_SPEC-MVP-001-F3` 已迁移到 `tasks/done/`。当前仅
  待同一 PR #35 的 F3 commit/push、三项 CI 全绿与 remote head 一致时的
  条件式 merge；不运行或改写 TASK-034，不声明 57/57 coverage 或 Production
  Ready，不激活 `v20260715.1`。

## 2026-07-28 FEATURE-MVP-001 PR #35 CI 与 merge 完成

- F3 与归档写回提交 `332d365e75b00102e8ce6a54df716024c7b9288c` 已 push，
  且与 PR #35 最终 head 一致。
- CI run `30333059261` 三项检查全部成功：
  `Authorization evidence check` job `90192109604`、`Backend Gradle tests`
  job `90192109599`、`Admin web lint, tests, and build` job `90192109597`。
- PR #35 于 `2026-07-28T05:56:19Z` 合并，merge commit 为
  `ca2798cd4db400f1fe512e2a13c0d40624929b7d`。
- `FEATURE-MVP-001` 已完成，无剩余 Feature 开发或集成门禁。本写回不运行或改写
  TASK-034，不声明 57/57 coverage 或 Production Ready，不激活
  `v20260715.1`。

## 2026-07-28 MILESTONE-MVP-002 可实施范围与条件阻塞写回

- 在隔离工作区 `C:\tmp\cqcp-mvp002`、分支 `codex/task-mvp-002` 上，以
  `origin/master@1035739b751386176e47c6871738a62bff86de02` 为唯一基线实施；
  未使用原 dirty TASK-036 工作区，未执行 commit、push、PR 或 merge。
- 新建 `TASK-MVP-002 / TASK-MODEL-001 / TASK-EVAL-002 / TASK-MODEL-002`，接受
  ADR-018、ADR-019 并同步架构。工作台只读消费既有 SourceAnchor；Model Profile
  使用 server-side Secret Reference；公网 DeepSeek 仅允许授权后的 EVALUATION。
- 完成 execution 级任务清单、精确历史结果查询、parser-backed preview、原始 DOCX
  下载与左右审核工作台；真实 Compose/浏览器验证上传、清单、SUCCESS、row-level
  定位与下载，source/header/download SHA-256 相同。
- 完成 Flyway V3、immutable Model Profile config version、endpoint alias allowlist、
  Secret readiness/connectivity 与管理台页面。PUBLIC profile 强制
  EVALUATION disabled/unbound；缺 Secret 连通测试在网络前返回 `SECRET_MISSING`；
  `MVP_DEMO_MOCK` 未改变。
- 完成三份 Codex Track A 盲评：模型意见与人工 ground truth 27/27 一致，CQCP
  deterministic 对照 9/27。现有证据不足以做唯一根因归因，保持 `UNRESOLVED`；
  Track A 不替代 Track B。
- DeepSeek 因没有明确外发授权
  `EXTERNAL_EGRESS_NOT_AUTHORIZED / networkAttempted=false`；Track B 因 TASK-034
  与 TASK-036 runtime seam 阻塞。`TASK-MODEL-002` gate 为 `NO_GO_BLOCKED`，没有
  Provider adapter、PUBLIC binding、shadow/guarded assist 或 `REVIEWING_MODEL`
  runtime wiring。
- 最终强制验证与统一 hash 三审尚待执行；最终事实源为
  `outputs/task-mvp-002/audit/freeze-manifest.json` 与
  `outputs/task-mvp-002/audit/final-decision.json`。本写回不声明 Milestone 完成、
  Production Ready、TASK-034 通过或 `TASK-028 / TASK-031 / TASK-032` 解锁。

## 2026-07-28 MILESTONE-MVP-002 首轮审计整改与重新验证

- 首轮两个 Codex 独立审计均为 `NO_GO`，旧冻结与全部旧审计结论永久失效；CC 的
  `opus/sonnet` 别名错误路由到 `fable-5` 并返回 503，不构成有效 CC AUDIT。最终
  CC AUDIT 只允许显式 `deepseek-v4-flash`。
- Model 安全整改增加所有 `/api/admin/**` 的后端 Bearer 鉴权，direct/proxied
  未认证为 `401`、已认证非 Admin 为 `403`；token 只在当前页面内存和
  Authorization header 中使用。
- `deepseek-official` 只允许
  `env:CQCP_MODEL_DEEPSEEK_API_KEY` 或
  `file:/run/secrets/cqcp-model-deepseek-api-key`，应用 allowlist 与 PostgreSQL
  CHECK 同时拒绝数据库密码、其他环境变量和无关文件。endpoint 固定为
  `https://api.deepseek.com`，全部 DNS answer 校验后通过 request-scoped resolver
  固定到实际连接，阻断 DNS rebinding。
- OpenAPI 已补齐 document `503`、Admin `401/403`、PUT path-only profileCode 与严格
  integer 语义；DeepSeek runner 对所有 fetch 后失败真实记录
  `networkAttempted=true`。
- Track A 第二轮在 agent 启动前冻结 dispatch；三名全新
  `fork_turns="none"` Codex agent 的意见绑定 agent/task、输入/prompt/dispatch hash、
  时间、严格 schema 与证据逐字来源，并在解盲前生成 opinion seal。解盲仍为 Codex
  27/27、CQCP 9/27；DeepSeek 未外发，Track B 仍阻塞。
- 真实浏览器通过 file chooser 上传 DOCX，验证 execution 列表/精确结果、
  block-level parser identity 定位、下载、Model Profile 状态和恶意合同名只作为
  React 文本节点渲染；该轮五张截图与结构化断言绑定同一 Compose
  task/execution/hash。
- 该轮门禁曾以 PowerShell 7.6.3、后端 372 tests、admin-web 66 tests、Node
  11 tests 等通过，但随后第二轮冻结审计判定 `NO_GO`，因此这些计数与证据已经失效，
  不作为最终结论。
- 当前仍待对新内容重新冻结，并由显式 `deepseek-v4-flash` CC AUDIT 与两个全新
  Codex subagent 同哈希从零审计。本写回不声明 Milestone 完成、Production Ready、
  TASK-034 通过或 `TASK-028 / TASK-031 / TASK-032` 解锁。

## 2026-07-28 MILESTONE-MVP-002 第二轮冻结审计 NO-GO 与定点整改

- 前一统一冻结包
  `099376344bd05ebfcc6c9cd4b02029c15865ed66ecd085801686cca4fd3bc98e`
  的代码/架构审计和测试/安全审计均为 `NO_GO`，旧冻结、旧测试计数和全部旧结论
  失效。代码/架构报告为 `P1=1 / P2=3 / blocking=4`，测试/安全报告为
  `P2=1 / blocking=1`。
- 新增并接受 ADR-020：`GET /api/review/tasks`、execution document preview 与原始
  DOCX 下载统一进入后端 Management Bearer 边界；read-only token 仅能读取工作台，
  访问 `/api/admin/**` 返回 `403`，匿名或未知 token 在进入 service 前返回稳定
  `401`。前端 token 只保留在 React 内存与 Authorization header，不进入 URL 或
  页面持久化。
- Connectivity Apache client 显式关闭 automatic retries；transport-level 测试对
  成功、401/403/404/429/5xx 等响应断言每次管理操作严格只有一个 `/models` 请求。
- Gradle test 任务全局关闭 scheduled worker，统一验证脚本要求在同一隔离
  PostgreSQL 数据库上连续执行两次全量 suite，专门关闭独立审计复现的 lease
  repository 竞争。
- 新增可重建恶意 DOCX 生成脚本，把
  `<img src=x onerror=alert('cqcp-xss')>恶意合同正文` 写入真实
  `word/document.xml`。Compose 已验证 parser 输出精确 block；真实浏览器截图证明
  该正文只作为文本显示，`img[src=x]=0`、inline handler `=0`、dialog `=false`。
- 真实浏览器还暴露并修复了 authenticated Blob 下载的时序：隐藏下载链接先挂载到
  DOM，再触发、移除并延迟撤销 object URL；页面显示从受控接口读取的真实 Blob
  字节数。浏览器证据不再虚构工具未暴露的 OS download event，文件完整性仍由绑定
  Compose 的 source/header/download SHA-256 三方一致证明。
- 浏览器 file chooser 上传已经绑定服务器 task/execution 与 source/header/download
  SHA-256；最终统一验证通过：PowerShell 7.6.3，后端连续两次均为 `376/376`，
  admin-web `68/68`，Node 盲评脚本 `11/11`，bootJar、lint/build、OpenAPI、
  freeze/unblind、Provider gate、浏览器证据校验和隔离 Compose PostgreSQL 均成功。
- 当前仍待生成新统一冻结并完成三份从零审计。CC AUDIT 只允许显式
  `deepseek-v4-flash`，并且发送冻结源码与证据前仍需用户明确外发授权。未执行
  commit、push、PR 或 merge。

## 2026-07-28 MILESTONE-MVP-002 第三轮冻结审计 NO-GO 与安全整改

- 冻结包 `a04237321753dc2449c58bbf236b89f8a17d93fb4d53ea30d222b90ceb35b9da`
  的代码/架构审计为 `P1=2 / P2=1 / blocking=3`，测试/安全审计为
  `P1=1 / P2=1 / blocking=2`，两份报告均为 `NO_GO`；该冻结及其全部结论失效。
- 两路审计共同确认 Spring MVC 会忽略 matrix parameter，而原 Filter 直接匹配
  `requestURI`，可让 `/api/admin;v=1/...`、`/api/review/tasks;v=1` 和
  `.../document;v=1` 绕过 Bearer 门禁。Filter 现按 canonical application path
  移除每段 matrix content；Spring Boot integration test 验证匿名变体在 service 前
  均返回 `401`。
- IPv6 endpoint classification 改为 fail closed：只接纳 `2000::/3` global
  unicast，并拒绝 IETF special、documentation、NAT64/IPv4 translation、
  `2002::/16` 6to4 等 conversion/transition range；公网 IPv6 正例与上述负例均有
  回归。
- 任务清单在 service 层拒绝 `page * size` int offset 溢出，极大分页组合返回稳定
  `400`，不再泄漏 `ArithmeticException` 为 `500`。
- 状态文档改为只绑定“当前 verified manifest”，不再把 freeze create 这种执行后会
  立即过期的步骤写成长期状态；任一 subject 变化仍使既有三审全部失效。
- 整改后最终统一验证通过：PowerShell 7.6.3，后端连续两次均为 44 suites、
  `379/379` 且 failures/errors/skipped 为 0，admin-web `68/68`，Node 盲评脚本
  `11/11`，bootJar、lint/build、OpenAPI、freeze/unblind、Provider gate、浏览器
  证据和隔离 Compose PostgreSQL 均成功，临时数据库与 Compose stack 已清理。
- CC AUDIT 仍只允许精确 `deepseek-v4-flash`；发送冻结源码与证据前仍需用户明确
  外发授权。未执行 commit、push、PR 或 merge。

## 2026-07-29 MILESTONE-MVP-002 第四轮冻结审计 NO-GO 与 HEAD 鉴权整改

- 冻结包 `843d32686c6d477f6e136f046233ea045a19d6880b27eb836825e91408e131e1`
  的测试/安全审计复现 P1：Spring MVC 自动用 GET handler 响应 HEAD，而工作台
  Filter 只识别 GET，匿名 HEAD 可在鉴权前调用合同读取 service 并暴露
  `Content-Disposition` 与文档 SHA-256 响应头。该包、未完成的同轮审计和全部旧
  结论立即失效。
- Filter 已把 GET/HEAD 统一为受保护安全读取；Spring Boot integration test 覆盖任务
  清单、preview、download 及 matrix-parameter 变体，匿名请求均在 service 前
  `401`。ADR-020、ARCHITECTURE v0.12、后端文档和父 TASK 已同步此边界；
  ARCHITECTURE 版本头同时明确 ADR-018 至 ADR-020 已纳入。
- 整改后统一验证再次通过：PowerShell 7.6.3，后端连续两次均为 44 suites、
  `380/380` 且 failures/errors/skipped 为 0，admin-web `68/68`，盲评脚本
  `11/11`；bootJar、lint/build、OpenAPI、freeze/unblind、Provider gate、浏览器
  证据和隔离 Compose PostgreSQL 全部成功，临时数据库与 Compose stack 已清理。
- 用户明确授权把新冻结审计包（源码 diff、测试、ADR、任务及脱敏评测证据）发送给
  CC 当前连接的外部 `deepseek-v4-flash`，且仅用于只读 CC AUDIT。该授权不覆盖
  DeepSeek runtime evaluation、Provider 激活或桌面 KEY 读取；本轮不使用
  `DEEP SEEK API代理 gemma.txt` 或桌面 Secret。
- CLI 对精确 `deepseek-v4-flash` 的两次尝试均由网关返回无可用 channel 的 `503`
  且 `input_tokens=0`，未形成审计。后续必须通过 Claude 桌面 CQCP_AUDIT 会话并
  可见选择精确 `deepseek-v4-flash`，不得回退 Opus、Fable 或其他模型。
- 未执行 commit、push、PR 或 merge；新统一冻结完成后，两名全新 Codex 审计员与
  CC AUDIT 必须绑定同一 package/manifest/full-diff hash 从零审计。

## 2026-07-29 MILESTONE-MVP-002 第五轮冻结审计 NO-GO 与证据链整改

- 冻结包 `a4a5c4bd6a71d0d1a81358d307e4c687a40706dc646d11a137c372385c477061`
  的 CC AUDIT 经 Claude 桌面 CQCP_AUDIT 会话、可见选择精确
  `deepseek-v4-flash` 后给出 `GO / P0=P1=P2=blocking=0`；测试/安全 Codex 审计
  随后给出 `NO_GO / P2=2 / blocking=2`。按全员重审规则，该包、CC GO 与未完成的
  代码/架构审计全部失效。
- P2-1：`model-provider-gate.json` 的 `evidence.currentContextSha256` 来自最终
  Memory Writeback 之前，未绑定冻结包内的 `CURRENT_CONTEXT.md`。整改要求固定为：
  所有受审 Memory 文档完成后重新运行 Provider gate，再冻结 subject。
- P2-2：`TASK-MODEL-001` 完成记录仍写 `379/379`，与冻结 XML 的
  `380/380` 不一致；现已更正为 `380/380`。
- 旧 CC 报告只保留为失效轮次证据，不得进入最终 Decision。第五轮整改后的统一
  验证重新执行两轮后端 `380/380`、admin-web `68/68`、Node `11/11`、bootJar、
  lint/build、OpenAPI、Provider gate、浏览器证据与隔离 Compose；最终结果只以本轮
  `verification-summary.json` 和后续新冻结 manifest 为事实源。CC AUDIT 与两个
  全新 Codex 审计员仍须对新哈希全部从零审计。
- 未执行 commit、push、PR 或 merge；未读取或使用桌面 DeepSeek/Gemma KEY。

## 2026-07-29 MILESTONE-MVP-002 第六轮冻结审计 NO-GO 与任务地图整改

- 冻结包 `4b1b117fdb5740e87bdf3553b0b7baeb457839da000160ffdc18ac8e8c9e08b0`
  的 CC AUDIT 在 Claude 桌面、精确 `deepseek-v4-flash` 下给出
  `GO / P0=P1=P2=blocking=0`；代码/架构 Codex 审计随后发现
  `tasks/MVP_TASK_MAP.md` 当前状态仍写旧 `379/379`，与冻结 XML、父 TASK、
  CURRENT_CONTEXT 和 changelog 的 `380/380` 冲突。按门禁，该包、CC GO 与未完成
  的测试/安全审计全部失效。
- 任务地图已更新为 2026-07-29，删除过期“第三轮整改”当前状态，列明全部失效冻结，
  统一测试计数改为 `380/380`，并明确用户的 DeepSeek 外发授权仅覆盖只读
  CC AUDIT，不覆盖 runtime evaluation 或 Provider 激活。
- 因任务地图属于最终 Memory Writeback，本次更正后再次重建
  `model-provider-gate.json.evidence.currentContextSha256`；统一验证重新执行两轮
  后端 `380/380`、admin-web `68/68`、Node `11/11`、bootJar、lint/build、
  OpenAPI、Provider gate、浏览器证据与隔离 Compose。新一轮 CC AUDIT 和两个全新
  Codex 审计必须绑定同一 package/manifest/full-diff hash 从零执行。
- 未执行 commit、push、PR 或 merge；未读取或使用桌面 DeepSeek/Gemma KEY。

## 2026-07-29 MILESTONE-MVP-002 第七轮冻结审计 NO-GO 与授权状态整改

- 冻结包 `39e9a511e56bd826d5f767afdb62ff300688a1aa074652cea64dbfe32b935ef2`
  的 CC AUDIT 在 Claude 桌面、精确 `deepseek-v4-flash` 下给出
  `GO / P0=P1=P2=blocking=0`；测试/安全 Codex 审计随后发现
  `CURRENT_CONTEXT.md` 同时记录只读 CC AUDIT 审计包外发“已授权”和“仍需用户明确
  授权”，结论为 `NO_GO / P2=1 / blocking=1`。按门禁，该包、CC GO 与未完成的
  代码/架构审计全部失效。
- 项目记忆现统一区分两类授权：用户已授权把冻结审计包及其中脱敏评测证据发送给
  外部 `deepseek-v4-flash`，且仅用于只读 CC AUDIT；三份样本的 DeepSeek runtime
  evaluation 外发授权仍未取得。该整改不放行 Track B、Provider 或桌面 Secret 读取。
- 因本次 Memory Writeback 改变 CURRENT_CONTEXT hash，Provider gate 与完整统一验证
  必须重新执行，再生成新冻结；CC AUDIT 与两个全新 Codex 审计员须对新哈希全部从零
  审计。
- 未执行 commit、push、PR 或 merge；未读取或使用桌面 DeepSeek/Gemma KEY。

## 2026-07-29 MILESTONE-MVP-002 第八轮前置增量：D1/D2、R7 与 Track B 重建

- 第七轮之后纳入 TASK-036 D1/D2：`v20260729.1` 版本化比例范围与非活跃付款分支
  分类器，以及 `FamilyModelCallPlan`、局部 `RuntimeEvidencePacket` 和
  model-assist eligibility seam。D1 定向验证为 `443/443`，D2 定向验证为
  `20/20`；此前形成的 TASK-034 R7、Track B 和冻结审计证据随 subject 变化全部
  失效，不得继续引用为当前结论。
- 统一预检发现并修复一项 review-assets 测试夹具问题：破坏性
  `occurrenceBudget == maxCandidates` 变体必须同时作用于 v15/v29，避免越过
  allowlisted delta；Node review-assets 测试现为 `100/100`。
- 后端全量预检发现三项兼容性回归并以最小边界修复：LEGACY 规则集的当事方表格候选
  恢复 block identity，consistency 版本仍使用 parser-issued cell identity；测试中的
  虚构 `ruleset-v1` 改为精确 legacy `v20260705.1`；未知规则集按 C2 激活门禁在
  `BUILDING_EVIDENCE` fail closed。隔离 PostgreSQL 下后端全量预检为 51 suites、
  `799/799`，failures/errors/skipped 均为 0。
- 修复后的正式 TASK-034 R7 重新运行并重新封存：27/27 candidate MATCH、27/27
  PointStatus PASS、57 个生产候选 MATCHED、6 个 human EXCLUDED，0 SYS/Finding；
  seal SHA-256 为
  `cda48fce5e0207590d781cf2e3dafd05f3de57dde43cd5ea5b1799e8dd6d81b3`，
  manifest SHA-256 为
  `cfb768d47ab8792254bbd1ab7b8ebc602b4f75a9fc755ac5d2c046bba3dfa27c`。
- Track B 使用新 R7 重新生成 27 个 runtime-isomorphic packet、9 个 family plan 和
  57 个 occurrence，并由三名全新、互相隔离的 Codex blind agents 评测；dispatch
  SHA-256 为
  `213256b521c7234ba974a87475d644b53a8bd826fc53421183696b8937f91144`，
  seal SHA-256 为
  `86e07663bfe734b5b99b01003ec5d032cd1872cd85bc6b1806b89a72615dbf7c`。
  27/27 均为 deterministic HIGH / zero-call，因此只证明“没有不必要模型调用”，
  不建立 Provider admission。
- `TASK-MODEL-002` 继续为 `NO_GO / BLOCKED`，当前机器可核验阻塞项固定为：
  三份样本的 DeepSeek runtime evaluation 外发未授权、Track B 没有 eligible
  ambiguity 样本、前序 integration audits 尚未全部 GO。
- 当前 subject 的最终统一验证已通过：PowerShell 7.6.3；后端连续两次均为
  51 suites、`799/799`，D1 `443/443`、D2 `20/20`、admin-web `68/68`、
  盲评 `17/17`、review-assets `100/100`；bootJar、lint/build、OpenAPI、
  Provider fail-closed、浏览器证据和隔离 Compose PostgreSQL 全部成功，临时
  数据库与 Compose stack 已清理。重新冻结和同哈希 CC AUDIT + 两名全新 Codex
  独立审计仍待执行。

## 2026-07-29 MILESTONE-MVP-002 第九轮冻结审计 NO-GO 与精确查询证据整改

- 冻结包
  `0c77803c50e7625931d75bfdd5880fdae1308d401539a61717efe87eef54781b`
  （manifest
  `d625ec6b7d94378b9c8c5f3f529e85aee4f3d18a5809094c74f5db9cca40f57b`，
  full diff
  `e81aba7a121848c4b7c44fce8b80c24917e1214707db81ab1fe3a979a58f4cc3`）
  的代码/架构审计为 `NO_GO / P2=2 / blocking=2`，测试/安全审计为
  `NO_GO / P2=1 / blocking=1`；CC AUDIT 未发送。该包与全部旧结论失效。
- 两份 Codex 报告共同确认 `model-provider-gate.json` 与生成器没有
  `evidence.currentContextSha256`，但项目记忆声称已经绑定；代码/架构审计还确认
  正式 R7 harness 使用 `getResult(taskId)` latest 语义、manifest query path
  不含 `executionId`，不能证明同 task 多 execution 隔离。
- Harness 现为每个正式 execution 额外保存更新的
  `<executionId>-latest-decoy` snapshot，先证明 latest 查询命中 decoy，再用
  `getResult(taskId, executionId)` 精确取回原 snapshot。run manifest 与 sample
  同时记录 `EXACT_EXECUTION_ID`、decoy identity 和隔离断言；R7 verifier 与
  Provider gate 均 fail closed 校验该证据。
- R7 R8 重封为 27/27 candidate `MATCH`、27/27 `PASS`、57 `MATCHED` +
  6 human `EXCLUDED`、70 production ledger、0 SYS/Finding，
  `executionScopedQuery=true`；seal SHA-256 为
  `05593a35090477a31bde3e43309342f560c87157e007fb60515e55d9c8471579`，
  run manifest SHA-256 为
  `55702828c1d1b2fbc9b4817e926b58438978c987d592cb225968941a1c2ada83`。
- R7 变化后 Track B 从零重建。v5 dispatch
  `12ce8fa8c3ce4e1a4d86b1c3dcc12c6842c5c45a6eb41d00b3240d274958f346`
  在三名全新 `fork_turns="none"` 盲评 agent 启动前冻结；27/27 意见均为
  `DETERMINISTIC_HIGH_ZERO_CALL`，seal 为
  `edcfa024dfe2cbf6c5b1fe6ca1427a91d61101dd8903f19f61c70294f1fe2970`。
  Provider admission 仍为 `NOT_ESTABLISHED_ZERO_ELIGIBLE_SAMPLE`。
- 最终统一验证 20/20 步退出码 0：PowerShell 7.6.3；后端连续两次均为
  51 suites、799/799/0/0/0；D1 443/443、D2 20/20、review-assets 100/100、
  admin-web 68/68、盲评 Node 17/17；bootJar、lint/build、OpenAPI、Provider
  fail-closed、浏览器证据和隔离 Compose PostgreSQL 均通过，临时数据库已清理。
- 未执行 commit、push、PR 或 merge。最终 Memory Writeback 后须重建 Provider
  gate，再生成新冻结；CC `deepseek-v4-flash` 与两个全新 Codex 审计员必须绑定
  同一新 hash 从零审计。
- 未读取、使用或复制桌面 DeepSeek/Gemma KEY；未向 DeepSeek 发送三份 runtime
  样本；未执行 commit、push、PR 或 merge。

## 2026-07-29 MILESTONE-MVP-002 前一范围三审 GO 与 DeepSeek Track A 执行

- 前一已实施范围最终冻结包
  `39ae5e17c64953509c010948e8fa291ec1b248799f074634bf255798675184b3`
  （manifest
  `245730985a724da32ea308d0871b7a352ea86f1f61dc69d32302f3167f779dfa`，
  full diff
  `ddfac44a9752b860e9941fd4eeacea894bf9a8df0085f56303907cadeabdad99`）
  已由 CC `deepseek-v4-flash`、Codex 代码/架构审计员与 Codex 测试/安全审计员
  对同一 hash 给出 `GO / P0=P1=P2=blocking=0`。该结论只覆盖当时 subject。
- 用户随后明确授权三份脱敏 runtime evaluation 样本 DeepSeek 公网外发，并授权
  Codex 从桌面文件读取 KEY。KEY 仅在调用进程内使用，未打印、持久化、写库、
  进入日志或审计包；调用固定为官方 `api.deepseek.com`。鉴权后的 `/models`
  只返回 `deepseek-v4-flash` 与 `deepseek-v4-pro`。
- 三份授权 artifact 逐 blind input SHA-256 绑定；无网络 preflight 全部返回
  `PREFLIGHT_AUTHORIZED_NO_NETWORK`。DeepSeek Track A 使用
  `deepseek-v4-pro`、non-streaming JSON mode；早期 empty/invalid 响应均按
  `SCHEMA_INVALID_OR_EMPTY` fail closed 并保留稳定类别，不保存 raw response 或
  reasoning。补强精确 JSON schema 指令后的三份最终输出均为
  `finish_reason=stop`、每份 9 项并通过 strict schema、opaque location 和逐字
  evidence quote 校验。
- DeepSeek 意见 seal SHA-256 为
  `7992da86307ebc5cc0ff5977792770d996c6e268f6374b3b3734b669926dfa85`；
  新解盲报告为 Codex 27/27、DeepSeek 27/27 与人工 ground truth 一致，CQCP
  deterministic 对照 9/27。Track A 结果仍不替代 Track B Provider admission。
- 用户接受建立 12–18 个独立脱敏 MEDIUM/CONFLICTED 最小 EvidencePacket 及
  全量准入阈值。Codex 已生成 18 packet 草案：8 MEDIUM、7 CONFLICTED 可调用，
  另有 deterministic HIGH/no-candidate/budget 三个零调用控制；corpus SHA-256
  为 `3c77d2b45cb7220b83d965ac363465d508c8b86697a479dd1ecd42de9bdb9bcb`。
  盲态 corpus 不含 proposed expected；人工确认表仍为
  `PENDING_HUMAN_CONFIRMATION_NOT_GROUND_TRUTH`，确认前不得派发模型或建立
  Provider admission。
- 独立只读语料审计发现 8 个 packet 的 diagnostic 使用了非生产名称；Codex 已将
  CONFLICTED 修正为 `SYS_ROLE_CONFLICT`、no-candidate 修正为
  `SYS_INDEX_INCOMPLETE`，并新增 Java 定向测试把 18 个 JSON 直接反序列化为
  `RuntimeEvidencePacket`、逐个以生产 eligibility 重算 admission。重建后的
  corpus SHA-256 为
  `3c77d2b45cb7220b83d965ac363465d508c8b86697a479dd1ecd42de9bdb9bcb`；
  独立审计从新 hash 重审后为 `GO / P0=P1=P2=blocking=0`。
- 已实现并离线测试人工确认 hash seal、确认前禁止 dispatch、只包含 15 个
  eligible packet 的盲态 model input、DeepSeek 独立外发授权/preflight/fail-closed
  runner，以及 Codex + DeepSeek 两份意见先封存后解盲的 admission evaluator。
  三个零调用控制不会进入 model input；只有 15+15 个模型意见和 3 个 policy control
  全部匹配人工答案，才会写出 `ADMITTED / ESTABLISHED`。当前没有实际人工 seal、
  Track B dispatch 或 Track B 公网调用。
- 新 DeepSeek 与 admission 草案改变了受审 subject，前一三审不能覆盖新增内容。
  `TASK-MODEL-002` 继续 `NO_GO / BLOCKED`；未实现 Provider、PUBLIC binding、
  shadow/guarded assist 或 `REVIEWING_MODEL`，未执行 commit、push、PR 或 merge。

## 2026-07-29 Track B admission 草案增量统一验证

- 修正 Provider gate 测试中的过期断言：三份 Track A 样本的逐 hash 外发授权现为
  `externalEgressAuthorized=true`；当前机器阻塞项精确为
  `trackBProviderAdmission` 与 `precedingIntegrationAuditsAllGo`。
- 对新增 DeepSeek Track A 证据链、18 packet admission 草案、人工确认 seal、
  eligible-only dispatch、DeepSeek fail-closed runner 和双意见 admission
  evaluator 所组成的 pre-human-confirmation subject 重新执行统一验证。20/20
  步退出码均为 0：PowerShell 7.6.3；后端连续两次均为 52 suites、
  800/800/0/0/0；D1 443/443、D2 20/20、review-assets 100/100、
  admin-web 68/68、盲评 Node 26/26；bootJar、lint/build、OpenAPI、
  Provider fail-closed、既有浏览器证据重验和全新隔离 Compose PostgreSQL 均通过。
  隔离数据库与 Compose stack 已清理。
- 修正 `docs/ai-review.md` 与 `TASK-MODEL-001` 中仍称三份样本未获外发授权的
  陈旧叙述。Track A 授权/执行已完成，但不替代 Track B admission。
- 旧 `39ae5e...` 审计决定逐字归档到
  `outputs/task-mvp-002/audit/historical-decisions/final-decision-39ae5e.json`；
  当前 `final-decision.json` 改为 `INVALIDATED_SUBJECT_CHANGED`，防止机器或人工
  直接读取旧 `GO` 后误认为新增 subject 已通过审计。
- 为 2026-07-28 `acceptance-report.md` 增加历史快照范围声明：其中 TASK-034
  v1 `FAIL` 与“未外发”只描述当次浏览器验收，不覆盖 2026-07-29 R7 或已授权
  Track A 评测。
- 当前仍没有人工 ground truth seal、实际 Track B dispatch 或 Track B 公网调用。
  项目负责人确认及外发授权后会改变受审 subject，届时必须再次完整验证、冻结，
  并由 CC AUDIT 与两个全新 Codex subagent 对完全相同的 hash 从零审计。
- 未实现 Provider runtime，未执行 commit、push、PR 或 merge。

## 2026-07-29 MVP-002 审计缺口补强

- Track A immutable freeze manifest 恢复为 authorization-neutral；外发权限改由
  与 sample/input hash、endpoint、model、purpose 和有效期绑定的独立授权 overlay
  提供。生成器测试现对 checked-in manifest 和 blind inputs 做 byte-for-byte
  重建校验；DeepSeek evidence chain seal 升级后 SHA-256 为
  `7992da86307ebc5cc0ff5977792770d996c6e268f6374b3b3734b669926dfa85`。
- Track B 新增独立 `eligibility-source-signals.json`，SHA-256 为
  `61e66c329c1a712d69f68bb827ca5245e33be2171b8ea22b1a444427850d7976`。
  Java runtime contract 只从该冻结信号重建 role/candidate/anchor/budget 输入，
  不再从 corpus admission reasonCodes 反推测试输入；corpus hash 保持
  `3c77d2b45cb7220b83d965ac363465d508c8b86697a479dd1ecd42de9bdb9bcb`。
- Provider gate 升级为 schema v3，新增机器检查
  `seamIntegratedIntoMainline` 与 `providerContractShadowPlanAudited`。当前精确
  阻塞项为这两项、`trackBProviderAdmission` 和
  `precedingIntegrationAuditsAllGo`，因此仍为 `NO_GO_BLOCKED`。
- 补强工作台/Admin 每个真实 endpoint method 与每个 URI segment 的 matrix
  parameter 鉴权用例；补齐 preview 全字段、download UTF-8 文件名/hash/length/
  bytes、稳定 404 与四类 409 ProblemDetail、CRLF header injection 拒绝。
- 在 Windows 用真实 directory junction 验证合同读取与 Secret file reference
  对 reparse point fail closed，并补充 service 将文件安全异常稳定映射为 404。
- 新增单一 fake Secret sentinel 集成验证：同一 sentinel 贯穿 resolver →
  connectivity boundary，扫描 HTTP body/header、应用输出、异常与 PostgreSQL
  Model Profile/Connectivity/Snapshot/stage/TuningPacket 表面；同时证明 PUBLIC
  disabled/unbound connectivity 操作不创建 execution、Snapshot、stage log 或
  TuningPacket。该验证不读取真实 KEY、不进行公网调用。
- 补强后的统一验证从头重跑并 20/20 PASS：后端连续两次均为 53 suites、
  `878/878/0/0/0`，D1 `443/443`、D2 `20/20`、review-assets `100/100`、
  admin-web `68/68`、盲评 Node `26/26`；bootJar、lint/build、OpenAPI、
  Provider fail-closed、既有浏览器证据重验和全新隔离 Compose PostgreSQL
  均通过，隔离数据库与 Compose stack 已清理。
- 人工 ground truth 确认表补充 18 个 case 的候选原文、candidate value、
  occurrenceId 与 SourceAnchor 逐项展开；生成器定向测试 `1/1` PASS，corpus
  与 eligibility signals 的 SHA-256 均保持不变。实际 admission 后仍会从头
  重跑统一验证，当前不据此提前冻结最终审计包。

## 2026-07-29 Track B admission 双门禁与执行凭据补强

- 将人工 ground truth 从“本地文件声明”升级为 challenge-bound 显式确认：当前
  nonce `TBH-85979b182999401db7802676d51901a1` 同时绑定 corpus、人工复核
  Markdown、draft 与 challenge hash；seal 必须包含项目负责人显式给出的 18 条
  decisions，不能从 Codex proposed expected 自动复制。
- 将公网外发授权拆为人工 seal 之后的第二阶段 challenge：精确绑定 model-input
  SHA、dispatch SHA、官方 endpoint、`deepseek-v4-pro`、15 个 eligible packet、
  3 个排除的 zero-call control 和一次性执行。此前 Track A 的宽泛授权不得复用。
- admission model input 采用严格字段 allowlist，并在 dispatch、runner 和
  evaluator 三处重算 18/15/3、packet identity、可靠 anchor、完整预算和 control
  集；可信 prompt SHA-256 固定为
  `e0d3e9951362f0550682701d1dc9ab1c34bb5fe117a7ef7e21145e82ca2f6e07`。
- DeepSeek 生产 runner 拒绝 `NODE_OPTIONS` 预加载、限制唯一不可变输出路径、
  以 streaming reader 在 1 MiB 处中止、要求 `application/json` 和返回 model
  identity，并只保存清洗后的 provider response receipt。Codex 模型意见新增
  collaboration execution receipt；最终审计必须对照真实 agent transcript。
- 所有 opinion/seal/report 关键工件使用 exclusive create；seal 时间禁止未来值。
  `unblind.mjs` 同时校验既有 v2 zero-call evidence 与后续 v3 admission chain；
  最终审计冻结清单已纳入完整人工/dispatch/authorization/opinion/report 链。
- 盲评工具测试 `49/49` PASS。当前仍未取得 18 条人工 decisions，未生成真实
  dispatch，未读取 KEY，未调用 DeepSeek，Provider gate 保持
  `NO_GO_BLOCKED`。

## 2026-07-30 Track B 首次精确外发在 HTTP 前关闭

- 项目负责人确认 Track B run-v3 challenge
  `TBE-ab0690237f6b4cbe94bfe903c05bcc5c`，精确绑定 challenge SHA-256
  `4ae1d9cb4b7f0646430db9fac47a93bac70d7474ea8e8097aee0761a2819e060`、
  dispatch `53866c34355aafa80fc38235a554a8a3ebdc768e8731d61edb103e8930a985d8`、
  model input `193fe851dccfd1fee06d152c7e82eccde42d869b14cc194107fa623225d059f6`
  与 provider call set
  `15548bcf946a0e60acdfe12b8de4ae26ab645c6134e8572a4bb10e9312adb0bb`，
  范围仅为官方 `api.deepseek.com` 的 `deepseek-v4-pro`、6 calls / 15 个脱敏
  eligible inputs，排除 3 controls、人工 ground truth 与 Finding/verdict。
- 授权固化后，无网络 preflight 按预期返回
  `PREFLIGHT_AUTHORIZED_NO_NETWORK`。正式 runner 只在进程内读取桌面文件首行的
  direct DeepSeek KEY，清除 proxy/TLS bypass 环境后启动；在 execution claim 与
  HTTP request 前因 `ENDPOINT_DNS_REJECTED` fail closed。blocked artifact
  SHA-256 为
  `7c0ec3089dc15ae7899177d4ae70176ec9fe14a1a8d33dcbb9ba496594e92dba`，
  明确 `networkAttempted=false`、0 provider request、无 Provider response，
  KEY 未打印、未持久化、未进入审计工件。
- 诊断回归复现双栈 `Promise.all` 的单侧超时缺陷：任一地址族超时会丢弃另一族
  已返回的合法公网地址。修复后仅在至少一个地址族成功且全部返回地址通过公网
  策略时继续；两族均失败、空结果或混入私网/保留地址仍 fail closed。transport
  与 runner 定向测试 `10/10` PASS。
- 首次 challenge、authorization 与 preflight 已归档，禁止重用。新恢复 challenge
  为 `TBE-37929406a2154d3c8277f815d019e31b`，SHA-256
  `4a35b4e858d8fedecd580fc9afceca7c8ff67d17ba5c6881f3594f9af5e83335`；
  三个 payload hash 与 6/15/3 范围不变。未取得绑定该新 challenge 的人工确认前，
  不重新读取 KEY、不执行 DNS 或网络调用，Track B admission 与 Provider 继续
  `NO_GO_BLOCKED`。

## 2026-07-30 Track B 第一次恢复仍在 HTTP 前关闭

- 项目负责人确认恢复 challenge
  `TBE-37929406a2154d3c8277f815d019e31b`（SHA-256
  `4a35b4e858d8fedecd580fc9afceca7c8ff67d17ba5c6881f3594f9af5e83335`）。
  授权固化与无网络 preflight 均通过；正式 runner 再次在 claim/HTTP 前以
  `ENDPOINT_DNS_REJECTED` 关闭。blocked artifact SHA-256 为
  `d4e60788fcf876e585173201a7b661b09763cb1c5b6697c8a50a428bf59dae86`，
  `networkAttempted=false`、0 provider request、无 Provider response。
- 单地址族容错已经生效仍失败，结合约 27 秒延迟与 Node nameserver
  `127.0.0.1`，把根因收窄为本机 c-ares 的 A/AAAA 两路均无可用地址。resolver
  升级为 `request-scoped-pinned-dns-v2`：只有 c-ares 零地址时才调用 Windows
  OS resolver；OS 返回的全部地址仍须通过公网/保留地址策略，随后按 request
  pin IP，并继续使用官方 hostname 的 TLS SNI、证书链与 hostname verification。
- 新增 c-ares 双失败/OS 成功、OS 混入私网仍拒绝等回归；定向 15/15、完整 Node
  121/121 PASS。第二份 challenge、authorization 与 preflight 已归档且禁止重用。
- 当前第三个恢复 challenge 为
  `TBE-26a03928386a4b258ec653dbe3cdb8c6`，SHA-256
  `119d87b1a805e048ddeedaf4f19903d76d8b88bdaae5932ce4bc6b384ba4ad07`，
  有效期至 `2026-07-31T00:18:09.167Z`；payload 的 dispatch/model-input/
  provider-call-set hash 和 6/15/3 范围不变。绑定该新 hash 的确认前不读取 KEY、
  不执行 DNS/网络，Track B admission 与 Provider 继续 `NO_GO_BLOCKED`。

## 2026-07-30 Track B 第三次执行非 stop 后严格关闭

- 项目负责人确认 challenge
  `TBE-26a03928386a4b258ec653dbe3cdb8c6`（SHA-256
  `119d87b1a805e048ddeedaf4f19903d76d8b88bdaae5932ce4bc6b384ba4ad07`）。
  resolver v2 成功获得并 pin 公网地址，runner 创建一次性 claim 后进入固定请求；
  前三个 call strict accepted，第 4 个因 `finish_reason != stop` 以
  `SCHEMA_INVALID_OR_EMPTY / FINISH_REASON_NOT_STOP` 关闭。blocked artifact
  SHA-256 为
  `a3f16f928054a1a9354ef136138b5a1a29c7e256a1be0dba0bcc89aa759d9f39`；
  未生成 DeepSeek opinion，未保存具体非 stop enum、raw response 或 reasoning。
  claim SHA-256
  `4fec7d72ffa91e9f7ae8902d76a9146ba85b31661a86cc48a23c7e0251224bc9`
  已消费，禁止自动重放。
- DeepSeek 官方 V4 thinking 默认开启；旧 builder v2 未显式指定 thinking，且
  `max_tokens=1500`。由于稳定工件只记录“非 stop”，thinking 耗尽预算只能作为
  高概率解释，不能记录为已证根因。整个旧 dispatch
  `53866c34355aafa80fc38235a554a8a3ebdc768e8731d61edb103e8930a985d8`
  generation 连同三次授权、claim、blocked 工件与旧 Codex opinion 已完整归档。
- builder v3 显式 `thinking={"type":"disabled"}`。一度将 max tokens 调到 4096
  的 generation 在授权、claim、DNS/网络前被架构复核否决并归档：Track B 必须
  runtime-isomorphic，不能绕开 `docs/ARCHITECTURE.md` 冻结的
  `maxOutputTokens=1500`。最终 builder v3 保持 1500；当前最大合法 JSON 约
  1096 bytes，官方四类非 stop enum 与 runner 实际发送体均补充 fail-closed 测试。
- 当前 dispatch SHA-256 为
  `c382102c9feb7f46caeedac4b70d80272a121375c3b3d91408550c855757ba61`，
  model input 保持
  `193fe851dccfd1fee06d152c7e82eccde42d869b14cc194107fa623225d059f6`，
  provider call set 为
  `0fdc0d794c412c6c190f165bf4a3e5df26589a382276a2df5646d42dbfdd374f`。
  全新无历史上下文 Codex blind opinion 已封印为
  `015d1eebab45d6e6b8d3009becba69e6987ffbe9e183e5e2e4ed880d3e3fc3a4`。
  新 egress challenge 为 `TBE-4c55952decc64252bc4877fe7a14003b`，
  SHA-256
  `9b6b3e4a412f2d7d0ef80263818f1e9d4c4e41d1d333f783db4284812a77a234`；
  绑定当前三个 hash 的新确认前不读取 KEY、不执行 DNS/网络，Provider 继续
  `NO_GO_BLOCKED`。
