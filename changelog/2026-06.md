# changelog/2026-06.md

## 2026-06-13 INFRA-001 Docker 唯一标准开发环境收口

执行 `INFRA-001：CQCP Docker 唯一标准开发环境收口`，本轮只收口 CQCP Docker / Compose / 端口 / 网络 / 数据卷 / 环境变量标准，不修改业务功能逻辑、不进入 `TASK-020`、不修改 PRD / ARCHITECTURE / 数据库迁移、不触碰另一个本地项目。

本轮完成：
- 固定 CQCP 标准 Compose project name 为 `cqcp`，在 `deploy/env/.env.example` 中新增 `COMPOSE_PROJECT_NAME=cqcp`。
- 更新 `deploy/compose/compose.yml`，保持三服务均由 Docker Compose 管理：
  - `postgres`
  - `api-server`
  - `admin-web`
- 固定宿主机端口：
  - 前端 `15173`
  - 后端 `18080`
  - PostgreSQL `54329`
- 不使用 `container_name`，避免容器名冲突。
- 为 PostgreSQL 增加 CQCP 专属 named volume：`cqcp_postgres_data`。
- 前端容器通过 Nginx 模板 `/api/` 反向代理到 `api-server:8080`，不在业务代码中写死 `localhost:18080`。
- 后端默认数据库连接口径调整为 `postgres:5432`，Compose 内通过 `CQCP_DB_URL` 注入。
- `docs/deployment.md` 明确 Docker Compose 是 CQCP 唯一标准开发、验证、测试方式；非 Docker 启动只允许临时故障定位，不作为验收依据。

本轮验证：
- `docker ps --filter "name=cqcp-postgres-test"` 确认旧测试容器 `cqcp-postgres-test` 仍在运行，并占用宿主机 `54329`。
- `docker inspect cqcp-postgres-test` 确认旧容器使用匿名 Docker volume，环境变量显示 `POSTGRES_DB=cqcp_test`，未设置 `POSTGRES_USER`。
- `docker exec cqcp-postgres-test psql -U cqcp -d cqcp -c "\dt"` 失败，原因为角色 `cqcp` 不存在；该结果说明旧容器与当前标准 Compose 用户/库口径不一致。
- 基于容器实际环境，使用默认用户 `postgres` 只读检查 `cqcp_test`，确认仅存在 V1 基线 6 张表，且 6 张表行数均为 0。
- 已停止并删除旧容器 `cqcp-postgres-test`，未删除任何 Docker volume。
- `docker compose --env-file deploy/env/.env.example -f deploy/compose/compose.yml config` 通过，展开结果包含：
  - project name：`cqcp`
  - network：`cqcp_default`
  - volume：`cqcp_postgres_data`
  - ports：`15173:80`、`18080:8080`、`54329:5432`
- 已尝试执行 `docker compose --env-file deploy/env/.env.example -f deploy/compose/compose.yml up -d --build`；标准启动未成功，失败点为 Docker Hub 基础镜像拉取。
- `docker compose --env-file deploy/env/.env.example -f deploy/compose/compose.yml ps` 显示当前 CQCP Compose 无运行服务。

本轮阻塞：
- Docker Hub 基础镜像拉取阻塞仍未解除，失败镜像为 `gradle:8.10.2-jdk21` 与 `node:20-alpine`。
- 失败原因：Docker 构建阶段访问 `https://auth.docker.io/token` 获取匿名 token 超时。
- 前端访问、后端健康检查、PostgreSQL 标准容器状态均因 Compose 构建失败未能验证。

## 2026-06-13 本地开发端口文档收口

完成 CQCP 当前推荐本地开发端口的文档收口。本次只修改项目文档，不修改业务代码、不修改 Docker Compose、不新增启动脚本，也不进入 TASK-019/TASK-020 实现。

本次记录的本地开发/TASK 验证模式：
- PostgreSQL 使用 Docker 容器 `cqcp-postgres-test`，本机端口 `54329`。
- 后端使用本机 Spring Boot 进程，推荐端口 `18080`。
- 前端使用本机 Vite 开发服务，推荐端口 `15173`。
- 该混合启动模式仅作为本地开发和 TASK 验证方式，不代表最终 Docker Compose 交付方案。
- 完整 `docker compose up --build` 的基础镜像拉取问题后续单独收口，本次不处理。

## 2026-06-13 完成 TASK-019 Result Composer + ReviewResultSnapshot 最小合成

继续执行 `tasks/active/TASK-019-result-composer-review-result-snapshot.md`，本轮只完成正式最小 `Result Composer + ReviewResultSnapshot` 合成，不进入 `TASK-020` 状态机、不进入 `TASK-021` 接口、不扩展真实 parser/candidate/evidence 主链路。

本轮完成：
- 新增 `apps/api-server/src/main/java/com/cqcp/apiserver/reviewengine/ResultComposer.java`，落地正式最小 `ResultComposer` 与以下对象边界：
  - `ResultComposerInput`
  - `ReviewResultSnapshot`
  - `ReviewFinding`
  - `ReviewPointSnapshot`
  - `VersionReferences`
  - `SnapshotStatus`
- 明确并实现点级正式结果到快照的最小映射规则：
  - `ERROR / WARNING` -> 进入业务 `findings`
  - `PASS / NOT_CONCLUDED / SKIPPED` -> 不进入业务风险统计
  - `SYS-*` -> 保留在 `diagnostics`，不抬升为业务 finding
- 明确并实现最小 `sourceAnchors` 汇总逻辑，对点级锚点按 `blockId` 去重后进入正式快照。
- 在 `apps/api-server/src/main/java/com/cqcp/apiserver/reviewengine/MinimalReviewEngine.java` 为 `StructuredFieldSet` 补充最小 `asMap()` 导出能力，供 `ResultComposer` 使用现有结构化字段快照。
- 新增 `apps/api-server/src/test/java/com/cqcp/apiserver/reviewengine/ResultComposerTest.java`，验证：
  - 全结论场景下的 `SnapshotStatus.SUCCESS`
  - 含 `NOT_CONCLUDED` 场景下的 `SnapshotStatus.PARTIAL_SUCCESS`
  - `findings` 只由 `ERROR / WARNING` 生成
  - `diagnostics` 与 `findings` 分层保留
  - `sourceAnchors` 去重
  - 结构化字段快照与版本引用透传

本轮验证：
- 定向验证：`gradle test --tests com.cqcp.apiserver.reviewengine.ResultComposerTest --tests com.cqcp.apiserver.reviewengine.MinimalReviewEngineTest` -> 通过
- 全量后端构建：`gradle build` 未通过；失败点为 `CqcpApiServerApplicationTests` 启动时无法连接 PostgreSQL
- 现场复核：执行时 `docker ps` 为空，说明本地测试库容器未运行；当前失败属于环境依赖状态，不是 `TASK-019` 新增 composer 代码编译失败

本轮结论：
- `TASK-019` 已把 `TASK-018` 的点级审核结果草案推进为正式最小 `ReviewResultSnapshot` 内存对象。
- 当前后端已经具备可供后续 `TASK-020` 状态机和 `TASK-021` 结果查询接口消费的稳定快照合成边界。
- 本轮未触发 ADR；未修改数据库迁移、对外 API、普通结果页或管理台页面。
- 下一优先工作应切换到 `TASK-020 Task Execution 最小状态机`。

## 2026-06-12 完成 TASK-018 Review Engine 最小验证闭环

继续执行 `tasks/active/TASK-018-review-engine-minimal-validation.md`，本轮只完成最小确定性 `Review Engine` 范围，不进入 `TASK-019+`、不扩展真实 parser/candidate/model 主链路。

本轮完成：
- 新增 `apps/api-server/src/main/java/com/cqcp/apiserver/reviewengine/MinimalReviewEngine.java`，在独立 `reviewengine` 包内落地最小输入/输出对象与确定性裁判逻辑。
- 固化最小输出对象：
  - `PointReviewResult[]`
  - `ReviewSummary`
  - `ReviewCompleteness`
  - `ReviewResultSnapshotDraft`
  - `PointDiagnostic[]`
- 基于首批 9 个 frozen review point 落地最小规则：
  - 甲方名称一致性
  - 乙方名称一致性
  - 合同含税总金额一致性
  - 税额强校验
  - 税额弱校验
  - 五个月度付款比例一致性
  - `paymentMethod=MILESTONE` 下四个月度专属点 `SKIPPED`
- 明确 `SYS-*`、证据缺失、证据歧义和系统失败均进入诊断层，并映射为业务化 `NOT_CONCLUDED`。
- 新增 `apps/api-server/src/test/java/com/cqcp/apiserver/reviewengine/MinimalReviewEngineTest.java`，基于 4 份现有 expected fixtures 与 fixture 内 negative candidates，覆盖：
  - golden fixture 的 9 点 `PASS`
  - 主体不一致 -> `ERROR`
  - 税额弱校验偏差 -> `WARNING`
  - `MILESTONE` 下四个月度专属点 -> `SKIPPED`
  - `SYS-* -> NOT_CONCLUDED`
  - 证据歧义 -> `NOT_CONCLUDED`

本轮验证：
- `gradle test --tests com.cqcp.apiserver.reviewengine.MinimalReviewEngineTest` -> `BUILD SUCCESSFUL`
- `gradle test` -> `BUILD SUCCESSFUL in 6s`
- `gradle build` -> `BUILD SUCCESSFUL in 1s`

本轮结论：
- `TASK-018` 已把链路从“开发前验证基线”推进到“可生成真实点级审核结果的最小内存态实现”。
- 当前 `Review Engine` 仍基于 fixture `goldenExpected` 作为合同侧最小证据基线，不等同真实 parser/candidate/evidence 主链路。
- 下一优先工作应转为新建并执行 `TASK-019 Result Composer + Snapshot`，而不是继续扩展 parser、真实模型调用或完整管理台页面。
- 本轮未触发 ADR；未改变主审核链路、快照边界或状态契约。

## 2026-06-12 新建 TASK-018 Review Engine 最小验证闭环

基于 `TASK-016` 已完成 4 个开发前验证最小闭环、`TASK-017` 已具备首批 expected fixtures 基线的事实，明确下一优先实现任务不再是完整管理台页面，而是先落地最小确定性 `Review Engine`。

本次完成：
- 新建 `tasks/active/TASK-018-review-engine-minimal-validation.md`。
- 明确 `TASK-018` 目标是基于现有 4 份 `expected/*.json` 和首批 9 个 frozen review point，生成最小真实点级审核结果。
- 冻结本任务最小输入/输出边界，要求覆盖：
  - `PointReviewResult[]`
  - `ReviewSummary`
  - `ReviewCompleteness`
  - `ReviewResultSnapshot` 草案对象
  - `PointDiagnostic[]`
- 冻结本任务最小状态矩阵与规则范围，要求覆盖：
  - `PASS / WARNING / ERROR / NOT_CONCLUDED / SKIPPED`
  - `SYS-* -> NOT_CONCLUDED`
  - 甲方名称、乙方名称、合同总金额、税额强/弱校验、五个月度付款比例、`MILESTONE` 下四个比例点 `SKIPPED`
- 明确本任务不做真实 Word 正式抽取、不做复杂 CandidateResolver、不做真实模型调用、不做完整 EvidenceBundle、不做真实任务 API、不做完整管理台页面。
- 更新 `CURRENT_CONTEXT.md`，将当前下一优先 active 任务切换为 `TASK-018-review-engine-minimal-validation`，并把后续顺序明确为 `TASK-018 -> TASK-019 -> TASK-020`。

本次结论：
- 项目阶段已从“开发前验证收口”转入“最小确定性审核闭环实现准备”。
- 下一阶段主线应先让后端真实产出点级审核结果，再推进 Snapshot、执行状态机、API 和真实管理台页面。
- 本次未新增 ADR，未实现业务代码，只新增任务包与项目记忆写回。

## 2026-06-12 推进 TASK-016 管理台点级诊断与执行摘要验证最小闭环

继续执行 `tasks/active/TASK-016-mvp-development-validation.md`，本轮优先落地契约与最小展示验证；如需要前端页面，只做最小静态/测试可验证组件，不做完整交互、不做复杂路由、不做真实业务联调。

本轮完成：
- 新增 `apps/api-server/src/main/java/com/cqcp/apiserver/admindiagnostics/AdminTaskDiagnosticsSource.java`、`AdminTaskDiagnosticsView.java`、`AdminTaskDiagnosticsViewFactory.java`，落地管理台诊断摘要最小契约与映射。
- 新增 `apps/api-server/src/test/java/com/cqcp/apiserver/admindiagnostics/AdminTaskDiagnosticsViewFactoryTest.java`，验证点级状态计数、阶段日志、摘要级 `SYS-*` 诊断码与敏感字段排除边界。
- 新增 `apps/admin-web/src/AdminTaskDiagnosticsPreview.tsx`，作为最小静态诊断摘要预览组件。
- 更新 `apps/admin-web/src/App.tsx`、`apps/admin-web/src/App.test.tsx`、`apps/admin-web/src/styles.css`，将最小预览挂载到现有 scaffold 页面并补齐测试覆盖。
- 管理台最小展示明确只展示概览、点级状态、阶段日志、诊断码与业务化未结论提示，不展示 `FULL_PROMPT`、原始模型输出、密钥或栈追踪。

本轮验证：
- 在 `apps/api-server` 下显式设置 `JAVA_HOME`、前置 `Gradle` 到 `PATH`，并设置 `CQCP_DB_URL=jdbc:postgresql://localhost:54329/cqcp_app_test`、`CQCP_DB_USERNAME=postgres`、`CQCP_DB_PASSWORD=postgres`。
- 以 `require_escalated` 执行：
  - `gradle test` -> `BUILD SUCCESSFUL`
  - `gradle build` -> `BUILD SUCCESSFUL`
- 在 `apps/admin-web` 下以 `require_escalated` 执行：
  - `npm.cmd run build` -> 通过
  - `npm.cmd run lint` -> 通过
  - `npm.cmd run test` -> 通过

本轮结论：
- `TASK-016` 的第四个最小闭环“管理台点级诊断与执行摘要验证”已跑通。
- `TASK-016` 定义的 4 个 MVP 开发前验证主题现已全部具备可执行验证结果。
- 本轮未进入真实管理台任务详情 API、复杂路由、Review Engine、真实模型调用或业务联调开发。

## 2026-06-12 推进 TASK-016 Word parser Spike 最小闭环

继续执行 `tasks/active/TASK-016-mvp-development-validation.md`，本轮目标限定为“开发前验证与最小闭环推进”，不进入完整合同审核平台、复杂规则引擎或生产级模型治理。

本轮完成：
- 在 `apps/api-server/build.gradle.kts` 增加 `org.apache.poi:poi-ooxml`，仅用于 `.docx` parser Spike 验证。
- 新增 `apps/api-server/src/main/java/com/cqcp/apiserver/wordparser/WordParserSpikeDocument.java`，定义最小解析产物：`DocumentBlock / TableBlock / FormControlBlock / ParseQualityReport`。
- 新增 `apps/api-server/src/main/java/com/cqcp/apiserver/wordparser/DocxWordParserSpike.java`，基于 Apache POI 读取 `.docx`，输出结构块、表格行、符号勾选和解析质量摘要，并保留 `contextType / sourceOrigin / sourceExtractionMode / blockConfidence / previewAnchorLevel`。
- 新增 `apps/api-server/src/test/java/com/cqcp/apiserver/wordparser/DocxWordParserSpikeTest.java`，读取 `packages/test-fixtures/expected/*.json`，对现有 4 份 `.docx` 样例执行聚合验证。

本轮验证：
- 在 `apps/api-server` 下显式设置 `JAVA_HOME`、前置 `Gradle` 到 `PATH`，并设置 `CQCP_DB_URL=jdbc:postgresql://localhost:54329/cqcp_app_test`、`CQCP_DB_USERNAME=postgres`、`CQCP_DB_PASSWORD=postgres`。
- 以 `require_escalated` 执行 `gradle test`，结果为 `BUILD SUCCESSFUL in 7s`。
- 以 `require_escalated` 执行 `gradle build`，结果为 `BUILD SUCCESSFUL in 1s`。

本轮结论：
- `TASK-016` 的第一个最小闭环“Word parser Spike”已跑通，当前可确认 `.docx -> 结构块/表格/符号勾选/解析质量摘要` 的最小技术闭环成立。
- 本轮未实现结构化字段正式抽取、Review Engine、模型调用、AI 调优包或管理台业务页面。
- 后续仍应按 `TASK-016` 既定顺序推进：模型客户端契约测试 -> AI 调优包导出验证 -> 管理台点级诊断与执行摘要验证。

## 2026-06-12 推进 TASK-016 模型客户端契约测试最小闭环

继续执行 `tasks/active/TASK-016-mvp-development-validation.md`，本轮只推进第二个最小闭环“模型客户端契约测试”，不进入完整合同审核平台、复杂规则引擎或生产级模型治理。

本轮完成：
- 新增 `apps/api-server/src/main/java/com/cqcp/apiserver/modelgateway/*` 最小模型契约层。
- 新增 `ModelCallIntent`、`ModelProfile`、`ModelGatewayArtifact`、`ModelGatewayResult`、`ModelGatewayService` 与 `MockModelProvider`。
- 仅实现 `MOCK` provider，不接真实 `Gemma`、`DeepSeek` 或公网兼容 provider。
- 在 `ModelGatewayService` 中落地最小失败映射：
  - schema invalid -> `SYS_MODEL_OUTPUT_INCOMPLETE`
  - timeout -> `SYS_MODEL_TIMEOUT`
  - unavailable -> `SYS_MODEL_UNAVAILABLE`
  - conflict -> `SYS_MODEL_CONFLICT`

本轮验证：
- 新增 `apps/api-server/src/test/java/com/cqcp/apiserver/modelgateway/ModelGatewayServiceTest.java`。
- 覆盖 mock success、schema invalid、timeout、unavailable、conflict、disabled profile 六类场景。
- 在 `apps/api-server` 下显式设置 `JAVA_HOME`、前置 `Gradle` 到 `PATH`，并设置 `CQCP_DB_URL=jdbc:postgresql://localhost:54329/cqcp_app_test`、`CQCP_DB_USERNAME=postgres`、`CQCP_DB_PASSWORD=postgres`。
- 以 `require_escalated` 执行：
  - `gradle test` -> `BUILD SUCCESSFUL in 7s`
  - `gradle build` -> `BUILD SUCCESSFUL in 1s`

本轮结论：
- `TASK-016` 的第二个最小闭环“模型客户端契约测试”已跑通。
- 当前只证明模型接入层最小契约、mock success 与 `SYS-MODEL-*` 失败映射边界已具备可执行验证结果，不代表真实模型 endpoint 已接入。
- `TASK-016` 后续应继续推进：AI 调优包导出验证 -> 管理台点级诊断与执行摘要验证。

## 2026-06-12 推进 TASK-016 AI 调优包导出验证最小闭环

继续执行 `tasks/active/TASK-016-mvp-development-validation.md`，本轮只推进第三个最小闭环“AI 调优包导出验证”，不进入完整合同审核平台、复杂规则引擎或生产级模型治理。

本轮完成：
- 新增 `apps/api-server/src/main/java/com/cqcp/apiserver/tuning/*` 最小导出层。
- 新增 `ExportMode`、`ExecutionSummary`、`PointDiagnostic`、`VersionRefs`、`TuningPacketSource`、`ExportConfig`、`TuningPacketExport` 与 `TuningPacketExportService`。
- 仅实现 `SINGLE_POINT` 与 `FOCUSED` 两种导出模式。
- 在导出服务中固化治理边界：
  - 导出物必须包含诊断摘要与版本引用
  - 导出物必须排除 `FULL_CONTRACT_TEXT / FULL_PROMPT / FULL_RAW_OUTPUT / ENDPOINT_SECRET / STACK_TRACE`
  - 含敏感 payload 的诊断对象直接拒绝导出

本轮验证：
- 新增 `apps/api-server/src/test/java/com/cqcp/apiserver/tuning/TuningPacketExportServiceTest.java`。
- 覆盖 `SINGLE_POINT`、`FOCUSED`、敏感 payload 拒绝导出与空选择拒绝导出。
- 在 `apps/api-server` 下显式设置 `JAVA_HOME`、前置 `Gradle` 到 `PATH`，并设置 `CQCP_DB_URL=jdbc:postgresql://localhost:54329/cqcp_app_test`、`CQCP_DB_USERNAME=postgres`、`CQCP_DB_PASSWORD=postgres`。
- 以 `require_escalated` 执行：
  - `gradle test` -> `BUILD SUCCESSFUL in 7s`
  - `gradle build` -> `BUILD SUCCESSFUL in 1s`

本轮结论：
- `TASK-016` 的第三个最小闭环“AI 调优包导出验证”已跑通。
- 当前只证明导出模式、诊断摘要边界与敏感内容拒绝导出的最小契约已具备可执行验证结果，不代表完整调优治理链路已接通。
- `TASK-016` 后续只剩：管理台点级诊断与执行摘要验证。

## 2026-06-12 推进 TASK-006 前端依赖与本机环境补齐

继续执行 `tasks/active/TASK-006-scaffold-only-after-adr.md`，目标是补齐前端依赖并继续推进本机环境级验证，不进入 MVP 业务开发。
本次完成：
- 确认本机 `Node.js v24.16.0` 与 `npm 11.13.0` 已可用。
- 执行 `npm.cmd install`，在 workspace 落地前端依赖并生成 `package-lock.json`。
- 为 `apps/admin-web` 补齐最小测试类型声明和更稳的 JS 配置入口，使 `build/lint` 不再依赖缺失的测试类型或 TS 配置加载链。
- 确认 `apps/admin-web` 在本机可通过 `npm run build` 与 `npm run lint`。

本次未完成：
- `apps/admin-web` 的 `npm run test` 仍未通过。当前错误不是缺依赖，而是 Vitest 启动阶段被当前受限执行环境的目录读取权限拦截，报错 `Cannot read directory "../../../.."`，因此不能记为测试通过。
- 本机 `java -version` 与 `gradle -v` 仍失败，说明 `Java/Gradle` 尚未进入 `PATH`。
- 尝试通过 `Invoke-WebRequest` 下载 `JDK 21` 与 `Gradle 8.10.2` 未成功，当前执行环境无法连接外网下载源。
- 尝试通过 `docker run` 容器化方式补跑后端 `gradle test/build` 未完成，原因是审批链路超时，未返回可执行结果。
- 进一步通过 `where.exe /r "C:\Program Files"` 与 `where.exe /r "C:\Program Files (x86)"` 核对，当前本机常见安装目录下未发现可复用的 `java.exe` 或 `gradle.bat`。
- 进一步尝试绕开 workspace 脚本和最小化 Vitest 配置后，前端 `test` 仍在同一目录读取权限阶段失败，说明阻塞点仍是当前受限执行环境，而不是前端依赖缺失。

本次更新 `apps/admin-web/package.json`、`apps/admin-web/src/App.test.tsx`、`apps/admin-web/tsconfig.app.json`、`apps/admin-web/tsconfig.node.json`、`apps/admin-web/vite.config.js`、`package-lock.json`、`tasks/active/TASK-006-scaffold-only-after-adr.md`、`CURRENT_CONTEXT.md` 和 `changelog/2026-06.md`。
本次未新增 ADR，未创建业务代码；结论是“前端依赖与 build/lint 已补齐，但前端 test 与本机 Java/Gradle 仍未完成环境级验证”。

## 2026-06-12 继续补跑 TASK-006 应用层验证

继续执行 `tasks/active/TASK-006-scaffold-only-after-adr.md`，仍只做 scaffold 环境级验证，不进入 MVP 业务开发。
本次确认：
- 用户给出的 JDK 21 与 Gradle 8.10.2 安装路径实际存在：
  - `C:\Program Files\Microsoft\jdk-21.0.11.10-hotspot\bin\java.exe`
  - `C:\Program Files\Microsoft\jdk-21.0.11.10-hotspot\bin\javac.exe`
  - `C:\Gradle\gradle-8.10.2\bin\gradle.bat`
- 通过绝对路径执行 `java -version` 与 `javac -version` 均通过，说明 JDK 21 安装本体可用；但当前会话未自动继承 `JAVA_HOME/PATH`。
- `gradle.bat -v` 直接执行失败，原因是未设置 `JAVA_HOME`；在命令会话中显式补齐 `JAVA_HOME` 与 `PATH` 后，Gradle 可以启动。
- `docker ps -a --filter "name=cqcp-postgres-test"` 与 `Test-NetConnection localhost -Port 54329` 通过，说明 PostgreSQL 测试容器与端口映射可用。
- 已通过 `docker exec` 创建干净应用层测试库 `cqcp_app_test`。
- 在 `apps/api-server` 目录下显式设置 `CQCP_DB_URL=jdbc:postgresql://localhost:54329/cqcp_app_test`、`CQCP_DB_USERNAME=postgres`、`CQCP_DB_PASSWORD=postgres` 后执行 `gradle test`，失败点已变化：不再是数据库连接，而是 Gradle 无法从远程插件仓库解析 `org.springframework.boot` 插件。
- 失败后查询 `cqcp_app_test`，`public` schema 表数量仍为 `0`，说明应用层尚未进入 Flyway migration，`flyway_schema_history` 未生成。
- 前端 `apps/admin-web` 的 `npm.cmd run test` 再次复核，错误未变：Vitest/esbuild 启动阶段报 `Cannot read directory "../../../.."`，并在加载 `vite.config.js` 时失败，应继续记为环境/权限模型阻塞。

本次更新 `tasks/active/TASK-006-scaffold-only-after-adr.md`、`CURRENT_CONTEXT.md` 和 `changelog/2026-06.md`。
本次未新增 ADR，未修改业务代码；结论是“后端安装本体已存在，但应用层验证现阶段被 Gradle 远程插件解析阻塞；前端 build/lint 通过，test 仍环境阻塞”。

## 2026-06-12 补充 TASK-006 Flyway PostgreSQL 运行时依赖

继续执行 `tasks/active/TASK-006-scaffold-only-after-adr.md`，仍只做 scaffold 环境级验证修复。
本次完成：
- 检查 `apps/api-server/build.gradle.kts` 当前依赖配置。
- 按最小修复原则新增 `runtimeOnly("org.flywaydb:flyway-database-postgresql")`，用于补齐 Flyway PostgreSQL database module。

本次验证：
- `docker ps -a --filter "name=cqcp-postgres-test"` 通过。
- `Test-NetConnection localhost -Port 54329` 通过。
- 通过 `docker exec` 重建干净测试库 `cqcp_app_test`。
- 在 `apps/api-server` 目录下显式设置 `JAVA_HOME`、`PATH`、`CQCP_DB_URL=jdbc:postgresql://localhost:54329/cqcp_app_test`、`CQCP_DB_USERNAME=postgres`、`CQCP_DB_PASSWORD=postgres` 后执行 `gradle test`，仍未通过。
- 当前 `gradle test` 的真实失败点仍是 Spring Boot Gradle 插件解析：`org.springframework.boot` 插件无法从 `Gradle Central Plugin Repository` 拉取。
- 失败后查询 `cqcp_app_test`，`public` schema 表数量仍为 `0`，说明应用层尚未进入 Flyway，也未触达 PostgreSQL。

本次更新 `apps/api-server/build.gradle.kts`、`tasks/active/TASK-006-scaffold-only-after-adr.md`、`CURRENT_CONTEXT.md` 和 `changelog/2026-06.md`。
本次未新增 ADR，未修改 schema、migration SQL 或任何业务代码；结论是“Flyway PostgreSQL 运行时依赖已补齐，但后端应用层验证仍被 Gradle 远程插件解析阻塞”。

## 2026-06-12 Codex execution environment diagnostic

继续执行 `tasks/active/TASK-006-scaffold-only-after-adr.md`，本次只做 Codex 执行环境差异诊断，不修改业务代码、不修改 schema、不进入 `TASK-016`。

本次确认：
- Codex 当前工作目录正常：`C:\Users\1\Documents\CQCP`。
- Codex 会话中的 `JAVA_HOME` 为空，`PATH` 不包含 JDK 21 与 Gradle 8.10.2 路径，因此裸命令 `java`、`javac`、`gradle` 不可见。
- 绝对路径核对通过，说明 JDK 21 与 Gradle 8.10.2 的安装本体实际存在，不是安装目录错误。
- `plugins.gradle.org:443` 与 `repo.maven.apache.org:443` 在 Codex 会话中均表现为 `PingSucceeded=True` 但 `TcpTestSucceeded=False`。
- `Invoke-WebRequest https://plugins.gradle.org` 与 `Invoke-WebRequest https://repo.maven.apache.org/maven2/` 均失败，无法连接远程服务器。
- `netsh winhttp show proxy` 为直连；`npm.cmd config get proxy` / `https-proxy` 均为 `null`；`git config --global --get http.proxy` / `https.proxy` 均无返回。
- `$env:GRADLE_USER_HOME` 为空，但 `gradle test --stacktrace --info` 显示 Codex 内 Gradle 实际使用 `C:\Users\CodexSandboxOffline\.gradle`，而不是 `$env:USERPROFILE` 对应的 `C:\Users\1\.gradle`。
- `gradle test --stacktrace --info` 的真实失败点为：插件 `org.springframework.boot`，版本 `3.3.12`，仓库 `https://plugins.gradle.org:443`，错误类型 `java.net.SocketException: Permission denied: getsockopt`。

本次更新 `tasks/active/TASK-006-scaffold-only-after-adr.md`、`CURRENT_CONTEXT.md` 和 `changelog/2026-06.md`。
本次未新增 ADR，未修改业务代码；本项应记录为 `Codex execution environment diagnostic`，而不是 CQCP 后端工程失败。

## 2026-06-11 补跑 TASK-015 PostgreSQL 15 真实容器回放验证

基于用户同意，继续执行 `tasks/active/TASK-015-flyway-v1-bootstrap.md`，只补跑 `V1__cqcp_mvp_core_schema.sql` 的真实 PostgreSQL 15 容器回放验证，不扩展 schema、Repository、MyBatis Mapper 或业务代码。
本次确认：
- 复用本机现有 `cqcp-postgres-test` 容器，镜像为 `postgres:15`，数据库版本核验结果为 `PostgreSQL 15.18`。
- 容器数据库 `cqcp_test` 在执行前未包含本任务目标核心表。
- 通过 `Get-Content -Raw apps/api-server/src/main/resources/db/migration/V1__cqcp_mvp_core_schema.sql | docker exec -i cqcp-postgres-test psql -U postgres -d cqcp_test -v ON_ERROR_STOP=1` 成功执行迁移。
- 回放后确认 `task / execution / task_stage_log / review_result_snapshot / tuning_packet / point_diagnostic` 6 张核心表全部建成。
- 回放后确认 `uq_execution_single_non_terminal`、`uq_execution_task_execution` 以及 6 组最小查询索引全部存在。

本次更新 `tasks/active/TASK-015-flyway-v1-bootstrap.md`、`CURRENT_CONTEXT.md` 和 `changelog/2026-06.md`。
本次未新增 ADR，未创建业务代码；`TASK-015` 的数据库基线本身已完成真实容器验证，当前剩余环境问题只在 `Java/Gradle` 应用层验证与 Docker 本地配置可持续使用性。

## 2026-06-11 补跑 TASK-006 环境级验证

基于“当前阶段仍不能进入 MVP 业务开发”的约束，补跑 `TASK-006-scaffold-only-after-adr` 的环境级验证，只确认脚手架、工具链入口、Docker Compose 和 OpenAPI 静态基线是否可继续作为后续验证前提。
本次确认：
- `docker compose -f deploy/compose/compose.yml config` 通过，Compose 编排结构仍有效；同时暴露本机 `C:\Users\1\.docker\config.json` 访问警告，说明后续 Docker 联调前还需确认本地 Docker 配置可用性。
- `node --check apps/admin-web/eslint.config.js` 通过，前端 ESLint 配置文件语法有效。
- `package.json`、`apps/admin-web/package.json` 与 `packages/api-contracts/openapi.json` 已完成本地 JSON 解析校验。
- 已静态确认 `apps/api-server/build.gradle.kts`、`apps/api-server/settings.gradle.kts`、前后端 `Dockerfile`、Flyway migration 目录与 `packages/api-contracts/openapi.yaml/json` 均已落地。
- `npm.cmd --workspace apps/admin-web run build/lint/test` 仍全部失败，原因未变：前端依赖未安装，分别缺少 `tsc`、`eslint`、`vitest`。
- `java -version` 与 `gradle -v` 仍失败，原因未变：本机 `PATH` 中缺少 `Java` 与 `Gradle`。

本次更新 `tasks/active/TASK-006-scaffold-only-after-adr.md`、`CURRENT_CONTEXT.md` 和 `changelog/2026-06.md`。
本次未创建业务代码，未安装依赖，未新增 ADR；结论仅为“静态基线仍成立，但运行环境仍未达标”，不得记为 MVP 开发通过。

## 2026-06-11 完成 TASK-017 Expected 夹具首批落地

完成 `tasks/active/TASK-017-expected-fixtures-bootstrap.md`，基于 `packages/test-fixtures/` 现有 4 份 `.docx` 样例和 `cqcp-mvp-sample-matrix.xlsx` 生成第一批 `expected/*.json` 可执行夹具。
本次完成：
- 新增 `packages/test-fixtures/expected/CQCP-MVP-DOCX-001.json`
- 新增 `packages/test-fixtures/expected/CQCP-MVP-DOCX-002.json`
- 新增 `packages/test-fixtures/expected/CQCP-MVP-DOCX-003.json`
- 新增 `packages/test-fixtures/expected/CQCP-MVP-DOCX-004.json`
- 每份夹具均包含 `sampleId`、源文件映射、`goldenExpected`、`negativeCandidates`、`allDiffs`、`singlePointDiffs` 和 `formulaLinkedEffects`
- 结构化字段已按 MVP 冻结 code 规范化，包括 `pricingMode / paymentMethod / invoiceType`
- 更新 `packages/test-fixtures/README.md`，明确 `.docx`、matrix 与 `expected/*.json` 的映射关系和维护边界

本次验证：
- 静态核对通过：4 份 `.docx` 样例已一一对应到 4 份 `expected/*.json`
- JSON 解析通过：4 份 `expected/*.json` 均已完成 UTF-8 读取和 JSON 解析验证
- 人工核对通过：README、matrix 职责说明与夹具字段语义一致

本次更新 `packages/test-fixtures/expected/*.json`、`packages/test-fixtures/README.md`、`CURRENT_CONTEXT.md`、`changelog/2026-06.md` 和 `tasks/active/TASK-017-expected-fixtures-bootstrap.md`。
本次未新增 ADR，未实现 Word parser、Review Engine、模型调用或管理台能力，也未扩展样例数量。

## 2026-06-11 完成 TASK-016 MVP 开发前验证基线

完成 `TASK-016-mvp-development-validation`，在 `TASK-004 / TASK-005 / TASK-006 / TASK-014 / TASK-015` 已形成边界与骨架的前提下，整理首轮 MVP 开发前验证计划。
本次确认：
- 冻结首轮验证顺序为“样例准备与前置门槛 -> Word parser Spike -> 模型客户端契约测试 -> AI 调优包导出验证 -> 管理台点级诊断与执行摘要验证”。
- 明确 Word parser、模型客户端、AI 调优包、管理台诊断摘要四类验证主题的最小通过标准。
- 明确首轮样例必须覆盖标题/正文/表格/控件/附件区域、冲突证据或候选归属歧义、解析降级与调优包导出场景。
- 确认 `packages/test-fixtures/` 已包含 4 份 `.docx` 样例和 `cqcp-mvp-sample-matrix.xlsx`；该矩阵当前记录 `GOLDEN_EXPECTED`、`NEGATIVE_CANDIDATE`、单点错误/公式联动说明，并可作为后续 `expected.json` 的来源基础。
- 确认 `apps/admin-web` 目前暴露 `build/lint/test` 命令，`apps/api-server` 目前暴露 `gradle test/build` 建议入口，但运行时环境尚未满足补验证条件。
- 收敛样例口径为“平台不区分脱敏样例，由业务方或 data owner 判断样例是否可用于验证”，不再把“脱敏样例”作为单独验证分类。

本次验证：
- 人工审查通过：TASK-016 的验证主题、依赖关系、边界约束与上游任务结论一致。
- 静态核对通过：已确认 `packages/test-fixtures/README.md`、4 份 `.docx` 样例、`cqcp-mvp-sample-matrix.xlsx`、`apps/admin-web/package.json`、`apps/api-server/README.md` 与验证计划中的样例与命令入口一致。
- 未执行运行时测试：前端依赖、`Java/Gradle` 和 Docker PostgreSQL 回放条件仍未具备。

本次更新 `tasks/active/TASK-016-mvp-development-validation.md`、`CURRENT_CONTEXT.md` 和 `changelog/2026-06.md`。
本次未创建业务代码，未安装依赖，未新增 ADR；原因是本任务只整理 MVP 开发前验证基线，不触发架构、模型职责、数据库或治理边界变更。

## 2026-06-11 完成 TASK-015 Flyway V1 初始化迁移基线

完成 `TASK-015-flyway-v1-bootstrap`，在 `ADR-010 / ADR-011 / ADR-012` 已冻结的前提下，为 `apps/api-server` 落地第一版核心数据库迁移。
本次完成：
- 新增 `apps/api-server/src/main/resources/db/migration/V1__cqcp_mvp_core_schema.sql`。
- V1 只建立 `task`、`execution`、`task_stage_log`、`review_result_snapshot`、`tuning_packet`、`point_diagnostic` 六类核心表。
- 为 execution 单任务单 non-terminal 约束、阶段日志读取、结果快照读取和 tuning packet 从属读取建立最小索引。
- 明确 JSONB 仅用于结构化输入快照、结果快照、诊断和调优 payload，不扩展到治理表矩阵。
- 更新 `apps/api-server/src/main/resources/db/migration/README.md`，冻结 Flyway 目录与命名规则。
- 更新 `docs/database.md`，写回 V1 核心迁移基线。
- 新增 `decisions/ADR-013-v1-core-schema-bootstrap.md`，正式记录 V1 核心 schema 范围、JSONB 落地策略和最小索引策略。

本次验证：
- `node -e` 静态检查通过，确认迁移文件包含 6 个核心表定义。
- `rg -n` 静态检查通过，确认迁移 README、`docs/database.md` 和 `ADR-013` 已同步引用。
- 尝试使用 `postgres:15` 临时容器执行迁移，但 Docker API 访问需要提权；自动审批在时限内未返回结果，因此本轮未完成真实 PostgreSQL 回放验证。

本次更新 `CURRENT_CONTEXT.md`、`changelog/2026-06.md`、`tasks/active/TASK-015-flyway-v1-bootstrap.md`、`docs/database.md`、`apps/api-server/src/main/resources/db/migration/README.md` 和 `decisions/ADR-013-v1-core-schema-bootstrap.md`。
本次未创建业务审核逻辑、Repository、MyBatis Mapper 或治理/样本/权限扩展表。

## 2026-06-10 完成 TASK-006 纯技术脚手架

完成 `TASK-006-scaffold-only-after-adr`，在 `ADR-002 / ADR-003 / ADR-010 / ADR-011 / ADR-012` 均已 `Accepted` 的前提下落地纯技术脚手架。
本次完成：
- 新增根目录 `.gitignore` 和 workspace `package.json`，建立前端命令入口。
- 新增 `apps/admin-web/`：React 18 + TypeScript + Vite + Ant Design 5 + TanStack Query + React Router v6 + Zustand 的最小前端骨架，仅包含 scaffold 占位页、测试骨架和 `build/lint/test` 脚本。
- 新增 `apps/api-server/`：Java 21 + Spring Boot + Gradle Kotlin DSL + MyBatis + Flyway 的最小后端骨架，仅包含启动类、空测试、`application.yml` 和 migration 占位目录。
- 新增 `deploy/compose/compose.yml`、`deploy/nginx/default.conf`、`deploy/env/.env.example`，建立 Docker Compose / Nginx / 环境变量模板入口。
- 新增 `packages/review-assets/` 和 `packages/test-fixtures/` 占位目录，明确版本化审核资产与测试夹具归属边界。
- 未编写业务审核逻辑、数据库表结构、Word parser、模型客户端、管理台业务页面或外部 API 业务实现。

本次验证：
- `docker compose -f deploy/compose/compose.yml config` 通过，Compose 结构有效。
- `node --check apps/admin-web/eslint.config.js` 通过。
- 根目录与前端 `package.json` 经 `node -e "JSON.parse(...)"` 解析通过。
- `npm.cmd --workspace apps/admin-web run build/lint/test` 均失败，原因是当前未安装前端依赖，分别缺少 `tsc`、`eslint`、`vitest`。
- `gradle -v` 失败，原因是当前环境 `PATH` 缺少 `Gradle`；本机同时未检测到 `Java`。

本次更新 `CURRENT_CONTEXT.md`、`changelog/2026-06.md` 和 `tasks/active/TASK-006-scaffold-only-after-adr.md`。
本次未新增 ADR；原因是本轮只按已接受 ADR 落地脚手架，未触发新的架构、模型职责、数据库或治理决策。

## 2026-06-09 冻结领域模型并新�?ADR-012

完成 `TASK-012-domain-model-freeze`，正式冻�?V1 MVP 领域模型关系和职责边界，并新�?`decisions/ADR-012-domain-model-freeze.md` 作为长期决策文档�?
本次确认�?
- `Task` 是业务审核请求聚合，保存 `taskId`、创建来源、caller、合同基础元数据、结构化字段输入快照�?result URL 基础入口；不保存点级结果，不覆盖历史 execution�?- `Execution` 是一次具体审核运行，保存 `executionId`、状态机、stage、lease、attempt、timeout、阶段日志和当次版本引用；同一 `taskId` 下同时只能有一�?non-terminal execution�?- MVP 普通执行上限采�?`maxExecutionsPerTask=3`�?- 合同类型修正、预算升级、人工重跑、规则集重跑、模型升级或 parser 升级必须创建新的 `executionId`�?- `ReviewResultSnapshot` 是按 `taskId + executionId` 唯一绑定的不可变正式结果快照；后续规则、模型、parser、prompt、字段配置或审核点配置变化不得覆盖历史快照�?- `PointDiagnostic` 是点级诊断对象，服务管理台、评测报告和 `TuningPacket`；不是业�?finding，不改变正式点级状态�?- `ExecutionSummary` �?execution 级诊断摘要，服务管理台任务详情、评测报告和 AI 调优包；不替�?`ReviewResultSnapshot.summary`�?- `TuningPacket` 是从正式 execution / snapshot 派生的调优治理包；不改变当前正式 `ReviewResultSnapshot`，不自动调用公网 AI，不自动生成正式 `AITuningAdvice`，不自动修改生产审核配置�?- 普通结�?URL �?`executionId` 时展示指定历�?execution；不�?`executionId` 时选择最新未�?superseded �?terminal execution�?- `supersededReason` MVP 枚举冻结�?`TYPE_CORRECTION`、`BUDGET_UPGRADE`、`MANUAL_RERUN`、`RULESET_RERUN`、`MODEL_UPGRADE`、`PARSER_UPGRADE`、`ADMIN_RECOVERY`�?
本次同步更新 `decisions/ADR-003-task-execution-snapshot-model.md`，将其从 `Proposed` 推进�?`Accepted`，作�?`Task / Execution / ReviewResultSnapshot` 子模型决策；同步更新 `TASK-006-scaffold-only-after-adr`，把 `ADR-012` 加入 scaffold 必读前置�?
本次更新 `decisions/ADR-012-domain-model-freeze.md`、`decisions/ADR-003-task-execution-snapshot-model.md`、`tasks/active/TASK-012-domain-model-freeze.md`、`decisions/README.md`、`tasks/active/TASK-006-scaffold-only-after-adr.md`、`CURRENT_CONTEXT.md` �?`changelog/2026-06.md`�?
本次未创建业务代码，未搭建脚手架，未安装依赖，未创建数据�?migration �?OpenAPI 文件�?
## 2026-06-09 冻结项目目录与仓库结构并新增 ADR-011

完成 `TASK-011-repository-structure-freeze`，在 `TASK-010` 技术栈冻结后，正式冻结 V1 MVP 后续 scaffold 的目标仓库结构，并新�?`decisions/ADR-011-repository-structure-freeze.md` 作为长期决策文档�?
本次确认�?
- `apps/` 只放可运行应用�?- `apps/admin-web/` 承载 React 18 + TypeScript + Vite + Ant Design 5 管理台与普通结果页，后续由 Nginx 承载静态资源�?- `apps/api-server/` 承载 Spring Boot 后端 API、异步单 worker、Word parser、Review Engine、ModelGateway、结果快照写入和管理�?API�?- MVP 不拆独立 `apps/worker/`、`apps/result-portal/`、`apps/quality-workbench/` �?`apps/model-service/`�?- `packages/api-contracts/` 作为后续最�?OpenAPI 契约、错误码说明和生成配置归属目录；本次不提前定�?OpenAPI 细节�?- `packages/review-assets/` 作为版本化审核资产归属目录，包括 `ReviewPointDefinition`、规则、正则、prompt、字段词库、合同类型画像和 EvidenceSelector 的配置源，但不得替代后端最终裁判职责�?- `packages/test-fixtures/` 作为脱敏或合成样�?metadata、预期结果和评测夹具归属目录；真实样本或样本文件落地仍需 data owner 确认�?- `docs/` 继续承载模块级长期知识，不放运行时代码�?- `deploy/compose/`、`deploy/nginx/`、`deploy/env/` 分别承载 Docker Compose 编排、Nginx 配置和环境变量模板，不提交密钥�?- MVP 不引�?SDK、IAM、BI、RAG、Kubernetes、Helm、Terraform 等延后目录�?
本次同步更新 `decisions/ADR-001-technical-stack-and-repo-layout.md`，明确技术栈主引用为 `ADR-010`，仓库结构主引用�?`ADR-011`；同步更�?`TASK-006-scaffold-only-after-adr` 的前�?ADR 列表，后�?scaffold 必须读取 `ADR-011`�?
本次更新 `decisions/ADR-011-repository-structure-freeze.md`、`tasks/active/TASK-011-repository-structure-freeze.md`、`decisions/ADR-001-technical-stack-and-repo-layout.md`、`decisions/README.md`、`tasks/active/TASK-006-scaffold-only-after-adr.md`、`CURRENT_CONTEXT.md` �?`changelog/2026-06.md`�?
本次未创建业务代码，未搭建脚手架，未安装依赖，未创建实际 `apps/` �?`packages/` 目录，未提前进入 OpenAPI 细节�?
## 2026-06-09 冻结技术栈并新�?ADR-010

完成 `TASK-010-tech-stack-freeze`，正式冻�?V1 MVP 技术栈，并新增 `decisions/ADR-010-technology-stack-freeze.md` 作为长期决策文档�?
本次确认�?- Frontend 冻结�?React 18 + TypeScript 5.x + Vite + Ant Design 5 + TanStack Query + React Router v6 + Zustand
- Backend 冻结�?Java 21 LTS + Spring Boot 3.2+ + Gradle Kotlin DSL
- Database / Persistence 冻结�?PostgreSQL 15+ + Flyway + MyBatis only，MVP 不引�?JPA
- Document Processing 冻结�?Apache POI + docx4j + LibreOffice headless containerized conversion service，PDFBox 不进�?MVP
- Runtime / deployment 冻结�?Docker Compose + Nginx 静态托管前�?+ Spring Boot API
- Observability 冻结�?Spring Boot Actuator health / metrics，MVP 仅保留最小指�?- PostgreSQL JSONB 作为 `ReviewResultSnapshot / PointDiagnostic / TuningPacket` 等结构化字段与诊�?payload 的关键优势被正式记录�?ADR 口径
- 模型与硬件不作为核心技术栈冻结项；平台通过 `ModelGateway + ModelProfile + ReviewBudgetProfile` 隔离具体模型和显卡差�?
本次同步清理了文档中的旧口径�?- `docs/backend.md` �?`docs/database.md` 不再使用 `MySQL` 作为默认数据库假�?- `docs/frontend.md` 不再写死 `Gemma 4 26B via vLLM` 作为展示示例
- `CURRENT_CONTEXT.md`、`PRD.md`、`ROADMAP.md` 不再把“正式技术栈待确认”作为当前阻塞项
- 当前下一步调整为 `TASK-011 -> TASK-012 -> TASK-013`

本次更新 `decisions/ADR-010-technology-stack-freeze.md`、`decisions/README.md`、`tasks/active/TASK-010-tech-stack-freeze.md`、`CURRENT_CONTEXT.md`、`PRD.md`、`ROADMAP.md`、`docs/backend.md`、`docs/database.md`、`docs/frontend.md`、`docs/deployment.md` �?`changelog/2026-06.md`�?
本次未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库实例�?
## 2026-06-09 冻结第一�?MVP 范围

完成 `TASK-009-mvp-scope-freeze`，将此前已确认的 MVP 入口、输入格式、合同类型、结构化字段、首批审核点、证据定位、模型边界、管理台边界、结果页和调优治理边界收敛为第一�?MVP 冻结基线�?
本次确认�?
- 第一�?MVP 入口�?API-first + 简易管理台，所有入口进入同一审核链路�?- 第一�?MVP 仅支持单份中�?`.docx` 工程采购合同；`.doc`、PDF/OCR、外币合同延后�?- 首批结构化字段、字�?key、枚举编码、数值精度、币种、创建任务最小错误码已按 PRD 冻结契约执行�?- 首批审核点固定为 9 �?core review point，均默认适用于工程采购合同�?- MVP 仍不包含 Quality Copilot、完整质量治理工作台、复杂发布审批、SAP/OA 深度 correction 联调、生产保护能力、完整审计体系、复杂权限治理、自动候选优化和自动发布规则/prompt/pattern�?- scaffold 前置阻塞项调整为正式技术栈、项目目录、关�?ADR、数据库迁移策略、本�?Gemma/A30 接入方式、最�?OpenAPI 契约和相关技术基线；`V1 MVP 范围`、`第一批任务包`、`首批审核点基线` 不再作为范围层面的阻塞项�?
本次更新 `tasks/active/TASK-009-mvp-scope-freeze.md`、`CURRENT_CONTEXT.md`、`ROADMAP.md`、`PRD.md`、`tasks/active/TASK-001-v1-mvp-scope-gate.md` �?`changelog/2026-06.md`�?
本次未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库�?
## 2026-06-09 补充 DefinitionTermIndex 与诊断契�?
按最终文档修改意见完�?`TASK-008-architecture-prd-definition-index-alignment`�?
本次更新�?
- `docs/ARCHITECTURE.md` Section 3：补�?`.doc` 一期技术能力已设计，但 MVP 不对外开放，管理台上传界面仍标注“当前仅支持 DOCX”�?- `docs/ARCHITECTURE.md` Section 6：补�?`DefinitionTermIndex` 条目字段、Parser 触发模式、注入规则、未命中处理和冲突处理�?- `docs/ARCHITECTURE.md` Section 9.3：明�?`maxModelCallsPerFamily = 1` 仅限�?family primary call，second pass 不计入该限制，并�?`ReviewBudgetProfile` 独立控制�?- `docs/ARCHITECTURE.md` Section 10.1 / 10.6：补�?`TAX_RATE`、`COMPLETION_PAYMENT_RATIO`、`WARRANTY_RETENTION_RATIO` CandidateRole，明�?GemmaAssistedRoleResolution 禁止行为�?artifact 复用边界�?- `docs/ARCHITECTURE.md` Section 16：补�?`PointDiagnostic`、`ExecutionSummary`、`suspectedFailureClass` �?`AITuningAdvice.targetType` 枚举扩展�?- `PRD.md` Section 5.2：补�?DefinitionTermIndex 作为 MVP 基线能力之一�?- `PRD.md` Section 15.1：补充月度付款比例“累计口径”的解释，明确该说明不改�?MVP 不重复校验跨字段关系、不据此生成业务 finding 的范围约束�?
本次未写入用户标注为“降级处理（不写入主文档）”的 OptimizationEffect、CrossModelDiagnosticMode、maxModelCallsPerTask 详细行为、TuningPacketExportConfig 完整策略�?AITuningAdvice 完整字段定义�?
本次更新 `docs/ARCHITECTURE.md`、`PRD.md`、`CURRENT_CONTEXT.md`、`changelog/2026-06.md` �?`tasks/active/TASK-008-architecture-prd-definition-index-alignment.md`�?
本次未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库�?
## 2026-06-09 生成首批 ReviewPointDefinition 草案

完成 `TASK-007-first-review-point-definitions-draft`，基于已 Accepted �?`ADR-005-first-review-points-selection.md` 生成首批 9 �?`ReviewPointDefinition` 草案�?
本次形成�?
- 新增 `docs/review-point-definitions.md`，覆�?`PARTY_FIELDS`、`AMOUNT_TAX`、`PAYMENT_TERMS` 三个 ReviewPointFamily�?- �?9 �?core review point 定义 required structured fields、EvidenceSlot、acceptedRoles、executionStrategy、deterministicRule、modelAssistPolicy、sourceAnchorPolicy、状态降级规则和业务文案方向�?- 明确 `PASS / ERROR / WARNING` 必须有可靠证据和 SourceAnchor；证据不足、候选冲突、解析低置信、预算不足或模型异常输出 `NOT_CONCLUDED`，不得生成业�?finding�?- 明确付款比例四个点在 `paymentMethod=MILESTONE` 时输�?`SKIPPED`，不输出 `SYS-* / NOT_CONCLUDED` 或业�?finding�?- 保留待确认项：`defaultSeverity` 初始值、合同类型正式编码、上下文�?CandidateRole 是否正式化、`ReviewPointDefinition` 物理落地格式和业务文案模板落地方式�?
本次更新 `docs/review-point-definitions.md`、`tasks/active/TASK-007-first-review-point-definitions-draft.md`、`CURRENT_CONTEXT.md` �?`changelog/2026-06.md`�?
本次未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库�?
## 2026-06-09 确认 PRD 待确认项收口

本次继续进行 PRD 设计确认，收口以�?MVP 边界�?
- 税率新增固定 `CandidateRole=taxRate`，作�?`TAX_AMOUNT_FORMULA_CONSISTENCY` �?optional EvidenceSlot 候选角色；税率缺失或不可靠时不执行税额弱校验，不得生成无可靠证据的业务 finding�?- `requiresHigherBudget / recommendedBudgetProfile` 不进入第一�?MVP 普通结果页；普通结果页只展示业务化 `NOT_CONCLUDED` 原因和人工核对提示。管理台任务详情、评测报告和 AI 调优包可展示预算诊断�?- 首批结构化字段采�?MVP 冻结最小契约：字段 key 沿用 PRD 建议 key；`pricingMode` 使用 `FIXED_TOTAL_PRICE / PROVISIONAL_TOTAL_PRICE`，`paymentMethod` 使用 `MONTHLY / MILESTONE`，`invoiceType` 使用 `VAT_GENERAL / VAT_SPECIAL`；创建任务最小错误码�?`REQUIRED_FIELD_MISSING`、`CONDITIONAL_FIELD_MISSING`、`INVALID_ENUM_VALUE`、`INVALID_DECIMAL_SCALE`、`INVALID_PERCENT_RANGE`、`UNSUPPORTED_CURRENCY`、`UNSUPPORTED_FILE_TYPE`�?- MVP 验收样本由业务方提供脱敏或合成中�?`.docx` 工程采购合同。平台不负责自动识别样本是否已脱敏，也不�?MVP 中提供脱敏检测或自动脱敏能力；样本进入仓库或评测环境的前提，是业务方/data owner 已确认其可用于项目验证�?
本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`docs/ARCHITECTURE.md`、`docs/frontend.md`、`docs/backend.md`、`docs/database.md`、`docs/sap.md`、`docs/ai-review.md`、`decisions/ADR-005-first-review-points-selection.md`、`tasks/active/TASK-003-first-review-points-selection.md` �?`changelog/2026-06.md`�?
本次未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库�?
## 2026-06-06

初始�?Contract Quality Control Platform / 合同质量控制中台 V2 �?AI Coding 项目记忆系统�?
创建文件�?
- `AGENTS.md`
- `PROJECT_BRIEF.md`
- `CURRENT_CONTEXT.md`
- `ROADMAP.md`
- `tasks/TASK-000-template.md`
- `tasks/active/`
- `tasks/done/`
- `docs/frontend.md`
- `docs/backend.md`
- `docs/database.md`
- `docs/ai-review.md`
- `docs/sap.md`
- `docs/deployment.md`
- `decisions/ADR-000-template.md`
- `changelog/2026-06.md`

本次初始化只基于架构文档 `2026-06-06-contract-quality-control-platform-v2-design.md` 中明确写到的内容�?
本次未创建业务代码，未搭建前端或后端脚手架，未安装依赖�?
## 2026-06-07 确认 MVP 入口顺序

确认并沉淀 `TASK-002-entry-order-decision` 的入口顺序决策�?
本次完成�?
- 确认 MVP 入口顺序采用 API-first + 简易管理台�?- 完成 `TASK-002-entry-order-decision`，补充未入选入口的延后策略和完成记录�?- 新增 `decisions/ADR-004-mvp-entry-order.md`，状态为 `Accepted`�?- 更新 `CURRENT_CONTEXT.md`：记录入口顺序已确认，并调整下一步任务�?- 更新 `ROADMAP.md`：将入口顺序从待确认改为引用 `ADR-004`�?
本次未创建业务代码，未搭建脚手架，未安装依赖。普通结�?URL 访问控制、最小任务创�?API 字段、外部系统正式联调阶段仍待确认�?
待确认事项已写入 `CURRENT_CONTEXT.md` 和各模块文档�?
## 2026-06-07 完成首批审核点选择

完成 `TASK-003-first-review-points-selection`，从 `PARTY_FIELDS`、`AMOUNT_TAX`、`PAYMENT_TERMS` 中收�?MVP 首批审核点基线�?
本次形成�?
- `PARTY_FIELDS`: `PARTY_A_NAME_CONSISTENCY`、`PARTY_B_NAME_CONSISTENCY`，均�?core review point�?- `AMOUNT_TAX`: `CONTRACT_TOTAL_AMOUNT_CONSISTENCY` �?core review point；`TAX_AMOUNT_FORMULA_CONSISTENCY` 为非 core，是否保留在 MVP 仍待人工确认�?- `PAYMENT_TERMS`: `PREPAYMENT_RATIO_CONSISTENCY` �?core review point�?- 明确每个审核点的结构化字段、EvidenceSlot、规�?正则/Gemma/后端裁判策略�?`SYS-*` 降级边界�?- 明确延后�?Pilot 的审核点：签署页主体冲突、统一社会信用代码一致性、多主体关系、中文大写金额一致性、税率合法性、付款比例合计、付款节点顺序和付款条件完整性�?- 新增 `decisions/ADR-005-first-review-points-selection.md`，状态为 `Proposed`�?- 更新 `CURRENT_CONTEXT.md`、`ROADMAP.md`、`docs/ai-review.md` 和当前任务包�?
本次未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库。首批审核点、core 标记、税额公式点是否进入 MVP、首批合同类型、结构化字段来源、样本集 owner 和税�?CandidateRole 仍待人工确认�?
## 2026-06-07 创建 PRD 草案

基于原始架构文档、`PROJECT_BRIEF.md`、`CURRENT_CONTEXT.md`、`ROADMAP.md`、模块文档和 ADR，新�?`PRD.md` v0.1 草案�?
本次完成�?
- 汇总产品问题、解决方案、产品原则、用户角色、V1/MVP/Pilot/Production Readiness 范围�?- 补充用户故事、功能需求、非功能需求、实施决策、测试决策、验收标准、风险和待确认事项�?- 保留当前关键待确认项，不�?Proposed ADR 写成已确认事实�?- 明确 PRD 不替�?ADR、任务包或模块文档�?- 更新 `CURRENT_CONTEXT.md`，记录已�?PRD 草案�?
本次无当�?TASK 文件；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库�?
## 2026-06-07 确认 MVP 第一批验收口�?
确认 MVP 第一批验收口径：

- 使用 2-3 份脱敏或合成中文 Word 合同作为第一批验证样本�?- 系统必须完成解析、首批审核点执行、证据定位、结果快照和结果页展示�?- 核心审核点至少大部分形成 `PASS / WARNING / ERROR`�?- 所�?`NOT_CONCLUDED` 点必须有业务可读原因�?source/coverage explanation�?
本次更新 `PRD.md` �?`CURRENT_CONTEXT.md`。本次无当前 TASK 文件；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库�?
## 2026-06-07 确认 MVP 输入格式边界

确认 MVP 第一轮输入格式边界：

- MVP 仅支持单份中�?`.docx` 用户输入�?- `.doc` 不进入第一�?MVP，延后到后续开发�?- 管理台上传界面必须明显标注“当前仅支持 DOCX，DOC 待后续开发”�?- 前端需阻止 `.doc` 或其他非 `.docx` 文件提交；后端任务创�?API 仍需校验文件类型，避免绕过前端限制�?
本次更新 `PRD.md`、`ROADMAP.md`、`docs/frontend.md` �?`CURRENT_CONTEXT.md`。本次无当前 TASK 文件；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库�?
## 2026-06-07 确认首批结构化字段与字段配置能力

确认 MVP 首批结构化字段：

- 合同名称、甲方名称、乙方名称、项目名称�?- 合同总金额、合同不含税金额、合同税额、税率�?- 计价模式：固定总价、暂定总价�?- 付款方式：按月度付款、按节点付款�?- 按月度付款：预付款比例、进度款比例、竣工款比例、结算款比例、质保款比例�?- 按节点付款：预付款比例阶段固定，其他节点信息目前不确定�?- 发票类型：增值税普通发票、增值税专用发票�?
确认字段来源与配置能力：

- 管理台可以直接录入首批结构化字段�?- SAP/API 传入字段与管理台字段定义保持一致�?- 后台支持自定义新增字段�?- 自定义字段只进入结构化字段池和后续规则配置候选，不自动生成审核点、EvidenceSlot 或业务裁判逻辑�?- 首批结构化字段全部为必填字段；管理台�?SAP/API 任务创建必须执行一致的必填校验�?
本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`ROADMAP.md`、`docs/frontend.md`、`docs/backend.md` �?`docs/database.md`。本次无当前 TASK 文件；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库�?
## 2026-06-08 确认付款方式条件必填与节点文本边�?
- 首批结构化字段从“无条件全部必填”修正为“通用字段必填 + 按付款方式条件必填”�?- 按月度付款：预付款、进度款、竣工款、结算款、质保款比例必填，节点付款其他节点信息不适用�?- 按节点付款：预付款比例和节点付款其他节点信息必填，四个月度专属比例不适用�?- 不适用字段不提交空值、零值或虚假占位值�?- `节点付款其他节点信息` 在按节点付款场景中使用必填多行文本�?- 字段只用于任务录入、数据保存、结果快照和页面展示�?- MVP 暂不为该字段创建审核点或 EvidenceSlot，不据此生成业务 finding�?
本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`ROADMAP.md`、`docs/frontend.md`、`docs/backend.md` �?`docs/database.md`。本次无当前 TASK 文件；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库�?
## 2026-06-08 确认合同总金额含税语�?
- `合同总金额` 固定表示合同含税总金额�?- 管理台标签固定显示“合同总金额（含税）”�?- SAP/API �?`contractTotalAmount` 使用相同语义，不通过额外含税标志改变解释�?- 税额公式直接使用 `contractTotalAmount`、`taxExcludedAmount` �?`taxAmount`，不设置重复的含税金额输入字段�?
本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`ROADMAP.md`、`docs/frontend.md`、`docs/backend.md`、`docs/database.md` �?`ADR-005` 草案。本次无当前 TASK 文件；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库�?
## 2026-06-08 确认 MVP 币种边界

- MVP 固定只支持人民币 `CNY`�?- 管理台不提供币种选择，金额输入统一显示单位“元”�?- SAP/API 可省�?`currency`；若显式传入，只允许 `CNY`�?- �?`CNY` 输入在任务创建阶段拒绝，不进入审核链路�?- 外币合同延后到后续阶段�?
本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`ROADMAP.md`、`docs/frontend.md`、`docs/backend.md`、`docs/database.md`、`docs/sap.md` �?`ADR-005` 草案。本次无当前 TASK 文件；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库�?
## 2026-06-08 确认金额与税额裁判口�?
- CNY 金额字段使用十进制定点数并保留两位小数，不使用二进制浮点数参与确定性裁判�?- `合同总金额（含税�?= 合同不含税金�?+ 合同税额` 作为强校验，允许 `0.01` 元绝对误差�?- `合同税额 = 合同不含税金�?× 税率` �?MVP 仅作�?WARNING 校验，不直接�?ERROR，以兼容分项计税、多税率和逐行舍入差异�?- 后端负责确定性最终裁判；Gemma/A30 只可辅助抽取金额和税率�?- 证据不足、角色冲突或解析异常继续输出 `SYS-* / NOT_CONCLUDED`，不得生成无可靠证据的业�?finding�?
本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`docs/backend.md` �?`ADR-005` 草案。本次无当前 TASK 文件；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库�?
## 2026-06-08 确认比例字段表示口径

- 税率和付款比例统一使用百分比数值，`13` 表示 `13%`，不使用 `0.13`�?- 管理台、外围系统、SAP/API、数据库和结果快照采用相同表示方式；外围系统现有存储口径也是 `13`�?- 后端参与金额计算时统一将百分比数值除�?`100`�?- 比例合法范围�?`0` �?`100`；允许的小数精度仍待确认�?
本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`docs/backend.md`、`docs/database.md`、`docs/sap.md` �?`ADR-005` 草案。本次无当前 TASK 文件；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库�?
## 2026-06-08 确认税率与付款比例精�?
- 税率最多保�?4 位小数�?- 付款比例最多保�?2 位小数�?- 输入末尾零可省略，不改变业务语义�?- 超出精度的输入直接拒绝，不得静默四舍五入或截断�?
本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`docs/backend.md`、`docs/database.md`、`docs/sap.md` �?`ADR-005` 草案。本次无当前 TASK 文件；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库�?
## 2026-06-08 确认月度付款比例业务口径

- 月度付款的预付款、进度款、竣工款和结算款采用累计付款比例�?- 进度款包含预付款，竣工款包含进度款和预付款，结算款包含竣工款、进度款和预付款�?- 比例关系�?`预付款比�?�?进度款比�?�?竣工款比�?�?结算款比例`�?- 质保款为单独比例，并满足 `结算款比�?+ 质保款比�?= 100%`�?- MVP 不重复校验上述跨字段关系，也不据此生成业�?finding�?- 外围系统负责跨字段输入校验，管理台测试数据由测试人员控制；平台仍执行单字段范围、精度和条件必填校验�?
本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`docs/backend.md`、`docs/database.md`、`docs/sap.md` �?`ADR-005` 草案。本次无当前 TASK 文件；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库�?
## 2026-06-08 扩展 MVP 月度付款比例审核�?
- 月度付款的预付款、进度款、竣工款、结算款和质保款比例全部纳入 MVP 独立一致性审核�?- 新增 `PROGRESS_PAYMENT_RATIO_CONSISTENCY`、`COMPLETION_PAYMENT_RATIO_CONSISTENCY`、`SETTLEMENT_PAYMENT_RATIO_CONSISTENCY`、`WARRANTY_RETENTION_RATIO_CONSISTENCY`�?- 五项审核分别比较结构化比例与合同付款条款候选，分别输出状态、证据和原文定位�?- 五点可共享付款条款局�?EvidencePacket，但必须使用各自独立�?required EvidenceSlot�?- 比例之间的累计关系仍不由 MVP 重复校验，也不据此生�?finding�?- 五个月度付款比例一致性审核点全部标记�?core review point�?
本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`ROADMAP.md`、`docs/ai-review.md`、`TASK-003` �?`ADR-005` 草案；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库�?
## 2026-06-08 确认节点付款审核点适用�?
- 节点付款继续执行 `PREPAYMENT_RATIO_CONSISTENCY`�?- 进度款、竣工款、结算款和质保款四个比例审核点在节点付款下输�?`SKIPPED`�?- 不适用不属于证据不足或系统异常，不得输�?`SYS-* / NOT_CONCLUDED`，也不得生成业务 finding�?
本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`docs/ai-review.md`、`docs/backend.md`、`TASK-003` �?`ADR-005` 草案；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库�?
## 2026-06-08 确认审核点严重级别配置机�?
- 管理后台应可视化维护审核点配置，包括分类、启用状态、排序、依赖字段和默认严重级别�?- 明确业务不一致时输出 `ERROR` �?`WARNING` �?hardcode，由当次 execution 绑定的有�?`severityPolicy` 决定�?- `PASS`、`NOT_CONCLUDED`、`SKIPPED` 由后端裁判、证据状态和适用性决定，不作为管理员可选严重级别�?- `SYS-*` 系统诊断不得配置成业�?finding 级别�?- 审核点配置最初曾按“保存草�?-> 发布生效”记录；后续已调整为 MVP 保存后即对下一次新建审核任务生效，不设置单独发布流程�?- 历史 ReviewResultSnapshot 保存当次严重级别和版本，不随当前配置变化�?
本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`docs/frontend.md`、`docs/backend.md`、`docs/database.md` �?`ADR-005` 草案；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库�?
## 2026-06-08 调整审核点配置生效机�?
- 由于审核点修改和审核任务均为低频操作，MVP 不设置草�?发布流程�?- 管理员保存审核点配置后，下一次新建审核任务使用最新配置�?- 已创建、正在执行和历史审核结果不受后续配置变更影响�?- 每次保存应形成可追溯配置版本或变更记录，并由新建 execution 绑定当时有效配置�?
本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`docs/frontend.md`、`docs/backend.md`、`docs/database.md` �?`ADR-005` 草案；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库�?
## 2026-06-08 确认审核规则配置�?MVP 范围

- MVP 审核规则配置页保持与既有项目页面风格一致�?- MVP 只支持预置审核点列表展示、详情展开和有限编辑，不开放新增审核点能力�?- 列表支持按审核分类和状态排序，并支持按审核点名称或编码搜索�?- 详情/编辑字段包括审核点编码、名称、审核分类、默认级别、启用状态、排序、依赖字段和合同类型�?- 依赖字段来自全部合同字段下拉多选�?- 审核点编码可作为配置字段编辑，但不得借此新增审核点；EvidenceSlot、执行策略、后端裁判和模型辅助策略不允许在 MVP 管理台任意新增或改写�?- 新增审核点延后到后续阶段，通过独立任务配套规则、证据槽、裁判、结果展示和样本验证�?
本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`docs/frontend.md`、`docs/backend.md`、`docs/database.md`；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库�?
## 2026-06-08 确认审核点停用语�?
- 审核点停用后不进入新建任务的 ReviewExecutionPlan�?- 停用审核点不输出 `PASS / WARNING / ERROR / NOT_CONCLUDED / SKIPPED`，也不生成业�?finding�?- 普通结果页只展示本次实际启用并进入计划的审核点�?- ReviewResultSnapshot 记录当次配置版本、启用审核点清单和配置停用审核点清单�?- MVP 首批 9 个审核点默认全部启用；手动停�?core 审核点的测试结果不能用于证明 core 覆盖率达标�?
本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`docs/frontend.md`、`docs/backend.md`、`docs/database.md` �?`ADR-005` 草案；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库�?
## 2026-06-08 确认审核点依赖字段配置校�?
- 审核点依赖字段可编辑，但必须来自已启用合同字段�?- 每个审核点必须保留其运行所需�?required structured fields�?- 若依赖字段配置导致审核点不可执行，管理台和后端必须拒绝保存，并提示缺失的必需依赖字段�?- 配置错误不得延迟到任务执行阶段表现为 `SYS-MISSING-INPUT`�?
本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`docs/frontend.md`、`docs/backend.md` �?`docs/database.md`；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库�?
## 2026-06-08 确认合同字段停用反向依赖保护

- 合同字段被启用审核点依赖时，不允许直接停用或删除�?- 用户必须先调整审核点依赖字段，或停用相关审核点�?- 字段配置页和后端错误应提示依赖该字段的审核点清单�?- 该保护避免字段配置变化导致审核点配置不可执行�?
本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`docs/frontend.md`、`docs/backend.md` �?`docs/database.md`；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库�?
## 2026-06-08 确认审核点展示编码与内部 code 分离

- 管理台可编辑的审核点编码仅用于前台展示和人工定位�?- 展示编码不能为空，并且在当前审核点配置中唯一�?- 系统内部 `reviewPointCode` 是唯一、不可编辑主 key�?- 后端执行、EvidenceSlot、结果快照、API 和历史解释必须使用内�?`reviewPointCode`�?- ReviewResultSnapshot 同时保存当次展示编码和内�?`reviewPointCode`�?
本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`docs/frontend.md`、`docs/backend.md` �?`docs/database.md`；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库�?
## 2026-06-08 确认审核执行逻辑来源与迭代机�?
- MVP 第一版审核执行逻辑由项目团队预置生成，不由管理台用户生成�?- 执行逻辑通过 `ReviewPointDefinition`、EvidenceSlot、候选抽取规则、后端裁判规则和 Gemma 辅助策略固化�?- 管理台可只读展示执行逻辑摘要，但不开放执行逻辑编辑�?- 审核分类仅用于展示、筛选和排序，不参与执行路由�?- 审核结果不理想时，通过版本化规则、词库、ValueGrammar、EvidenceSlot policy、Gemma 局�?prompt、后端裁判或结果页解释迭代�?- 执行逻辑迭代必须通过脱敏/合成样本回归验证，新任务使用新版本，历史快照保持旧版本解释�?
本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`docs/frontend.md`、`docs/backend.md`、`docs/database.md` �?`docs/ai-review.md`；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库�?
## 2026-06-08 确认管理台执行逻辑摘要粒度

- MVP 审核点详情只展示业务可读的执行逻辑摘要�?- 摘要包括内部 code、执行策略名称、依赖字段、需要的证据类型、是否使�?Gemma 辅助、裁判说明和最近一次版本更新时间�?- MVP 普通配置页不展示完整正则、prompt、EvidenceSlot 细节或后端代码�?- 完整技术治理视图延后到后续阶段�?
本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`docs/frontend.md`、`docs/backend.md` �?`docs/ai-review.md`；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库�?
## 2026-06-08 确认审核点展示字段历史快照保�?
- 审核点名称、展示编码、审核分类、排序和合同类型变更只影响后续新任务�?- 已有任务结果页必须读�?ReviewResultSnapshot 中的当次展示字段�?- 历史结果不得用当前配置覆盖审核点展示字段或严重级别�?
本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`docs/frontend.md`、`docs/backend.md` �?`docs/database.md`；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库�?
## 2026-06-08 确认审核点合同类型字段语�?
- 审核点配置中的合同类型字�?MVP 仅用于展示和后续扩展预留�?- MVP 合同类型仅有“工程采购合同”�?- 该字段不参与复杂适用范围路由�?- 首批 9 个审核点默认适用于工程采购合同�?
本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`docs/frontend.md`、`docs/backend.md` �?`docs/database.md`；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库�?
## 2026-06-08 确认审核点排序字段语�?
- 排序字段只影响管理台列表展示顺序�?- 排序字段只影响普通结果页展示顺序�?- 排序字段不影响审核执行顺序�?- ReviewExecutionPlan 执行顺序由后端计划器根据依赖、证据复用和执行策略决定�?
本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`docs/frontend.md`、`docs/backend.md` �?`docs/database.md`；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库�?
## 2026-06-08 确认业务 finding 统计口径

- `ERROR` �?`WARNING` 都属于业�?finding�?- 结果页和 API 摘要必须统计 `plannedPointCount`、`passCount`、`errorCount` �?`warningCount`�?- `plannedPointCount` 表示本次启用并进�?ReviewExecutionPlan 的审核点总数；配置停用点不进入分母，适用�?`SKIPPED` 点进入分母�?- `NOT_CONCLUDED` 必须单独统计�?`notConcludedCount`，用于表示审核覆盖不足�?- `SKIPPED` 必须单独统计�?`skippedCount`，用于表示审核点不适用�?- 外部系统不得只看“是否存�?finding”，必须看级别�?- `PASS`、`NOT_CONCLUDED`、`SKIPPED` �?`SYS-*` 不计入业务风险数量�?- 默认级别只影响业�?finding 的严重程度，不影响是否生�?finding�?
本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`docs/frontend.md`、`docs/backend.md`、`docs/database.md` �?`docs/sap.md`；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库�?
## 2026-06-08 确认配置停用审核点展示口�?
- 普通结果页主摘要不展示配置停用审核点数量�?- 管理台和评测报告展示 `disabledByConfigCount` 和配置停用审核点清单�?- 配置停用点不进入 `plannedPointCount`，也不混入点级状态计数�?
本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`docs/frontend.md`、`docs/backend.md` �?`docs/database.md`；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库�?
## 2026-06-08 确认普通结果页审核点展示范�?
- 普通结果页默认展示进入 ReviewExecutionPlan 的全部审核点�?- 展示状态包�?`PASS / ERROR / WARNING / NOT_CONCLUDED / SKIPPED`�?- 页面可提供状态筛选，但默认不得隐�?`PASS` �?`SKIPPED`�?- 配置停用点不进入 ReviewExecutionPlan，因此不在普通结果页展示�?
本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`docs/frontend.md` �?`docs/backend.md`；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库�?
## 2026-06-08 确认 SKIPPED 展示和证据要�?
- `SKIPPED` 点不要求证据定位�?- `SKIPPED` 点不要求 EvidenceSlot�?- `SKIPPED` 点必须展示适用性原因�?- 适用性原因示例：“当前付款方式为按节点付款，进度款比例审核点不适用”�?
本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`docs/frontend.md`、`docs/backend.md` �?`docs/database.md`；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库�?
## 2026-06-08 确认 PASS 证据要求

- `PASS` 点必须有证据定位和可解释依据�?- 证据不足不得�?`PASS`�?- 证据不足、证据歧义或覆盖不足时，应输�?`NOT_CONCLUDED` 和业务可读原因�?
本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`docs/frontend.md`、`docs/backend.md` �?`docs/database.md`；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库�?
## 2026-06-08 确认 ERROR/WARNING 证据要求

- `ERROR` �?`WARNING` 点必须有可靠证据定位和可解释依据�?- 没有可靠证据不得生成业务 finding�?- 证据不足时应输出 `NOT_CONCLUDED` 和业务可读原因�?
本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`docs/frontend.md`、`docs/backend.md` �?`docs/database.md`；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库�?
## 2026-06-08 确认证据定位精度 MVP 要求

- MVP SourceAnchor 最低要求为 block/section/table row/cell 级定位�?- 精确字符范围不是 MVP 硬要求�?- 金额、税率和比例候选如有精确文本范围，应展示字符级高亮�?- 若缺少字符级范围但证据块和候选值可靠，仍可形成 `PASS / ERROR / WARNING`�?- 结果卡片必须展示合同原文候选值，辅助用户在证据块内定位�?- 缺少字符级范围本身不得导�?`NOT_CONCLUDED`�?
本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`docs/frontend.md`、`docs/backend.md` �?`docs/database.md`；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库�?
## 2026-06-08 确认证据块内多候选归属规�?
- block/section/table row/cell 级定位可以进�?MVP，但候选值必须可靠归属�?- 同一证据块内存在多个金额、税率或比例候选时，必须能确定当前审核点所需候选值�?- 候选值归属不清时，不得形�?`PASS / ERROR / WARNING`�?- 候选值归属不清应输出 `NOT_CONCLUDED`，业务原因示例：“证据块内存在多个候选，无法可靠归属”�?
本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`docs/frontend.md`、`docs/backend.md` �?`docs/database.md`；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库�?
## 2026-06-08 确认 Gemma 局部辅助候选归属边�?
- 候选归属不清时允许 Gemma 辅助判断�?- Gemma 输入仅限相关证据块和必要上下文，不得提交整份合同�?- Gemma 只辅�?CandidateResolver 选择候选�?- Gemma 不得直接输出最�?`PASS / ERROR / WARNING`�?- Gemma 辅助后仍无法可靠归属时，必须输出 `NOT_CONCLUDED`，不得猜测候选继续裁判�?- Gemma 不可用或超时时，不阻断所有审核点；规�?候选本身可靠的点继续由后端确定性裁判�?- 只有依赖 Gemma 消歧且无法完成消歧的点输�?`NOT_CONCLUDED`�?- 后端仍负责最终确定性裁判�?
本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`docs/ai-review.md` �?`docs/backend.md`；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库�?
## 2026-06-08 确认 Gemma 诊断可见�?
- 普通结果页不展示内�?`SYS-MODEL-*` 技术码�?- 普通结果页只展示业务化 `NOT_CONCLUDED` 原因�?- 管理台任务详情展示内�?`SYS-MODEL-*`、模型调用状态、超�?不可用原因和诊断摘要�?- 评测报告也可展示上述内部诊断，用于质量分析�?
本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`docs/frontend.md`、`docs/backend.md` �?`docs/ai-review.md`；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库�?
## 2026-06-08 确认模型原始输出可见�?
- MVP 不保存完整模型原始输出，也不在管理台展示完整模型原始输出�?- 只保存模型调用状态、`SYS-MODEL-*` 诊断码、schema 校验结果、redacted artifact 摘要、token 用量、耗时、模型版本、调用时间和必要的候选归属结论摘要�?- 受控保存完整 redacted raw output、TTL 和审计展开延后�?Pilot / Production Readiness�?- 普通结果页仍只展示业务�?`NOT_CONCLUDED` 原因�?
本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`docs/frontend.md`、`docs/backend.md` �?`docs/ai-review.md`；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库�?
## 2026-06-08 确认配置审核规则页面标签页范�?
- `审核点` 进入 MVP，支持列表、详情展开和有限编辑�?- `模型配置` �?MVP 只读展示当前模型版本�?endpoint 状态，不支持编辑�?- `提示词模板` �?MVP 不支持编辑，仅可展示业务摘要或延后�?- `最终提示词预览` �?MVP 不展示完�?prompt，延后到治理/技术视图�?- `任务日志` 不属于配置审核规则页面，应放在任务列�?任务详情层级�?
本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`docs/frontend.md`、`docs/backend.md` �?`docs/ai-review.md`；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库�?
## 2026-06-08 确认任务日志 MVP 记录粒度

- MVP 任务日志只记录阶段级事件，不记录每个内部函数调用�?- 阶段级事件用于判断任务卡在解析、索引、计划、证据构建、规则审核、模型辅助或结果合成等阶段�?- 任务日志可记�?`taskId`、`executionId`、`stageName`、`attempt`、事件类型、发生时间、耗时、摘要状态、业务化原因和摘要级 `SYS-*` 诊断�?- 模型阶段只展示调用状态、token 用量、耗时、模型版本、schema 校验和诊断摘要�?- 任务日志不得展示完整 prompt、完整模�?raw output、endpoint secret、stack trace 或逐函数调试日志�?
本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`docs/frontend.md`、`docs/backend.md` �?`changelog/2026-06.md`。本次无当前 TASK 文件；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库�?
## 2026-06-08 确认 Model Profile 切换与公网模型使用边�?
- 管理台支持维护多�?`Model Profile`，包括本�?Gemma4、公网兼�?OpenAI API 模型�?mock fallback�?- 任务详情页顶部概览区展示本次 execution 实际使用的审核模型，包括模型展示名、provider 类型、模型名称和配置版本摘要�?- 管理员可通过启用状态、密钥配置状态、默认模型和 `usageScope` 控制新任务可选模型�?- 公网 DeepSeek 等模�?profile 可在业务允许的场景下用于链路验证、演示、评测或内部审核任务；能否用于真实合同由业务方、管理员和部署环境承担配置责任�?- 每个 execution 固定绑定当次模型 profile 和配置版本，历史快照不随后续模型切换变化�?- 模型不论来自本地还是公网，都不得直接决定最终业�?finding；最终点级状态仍由后端裁判合成�?- 新增 `decisions/ADR-006-model-profile-switching-and-public-provider-scope.md`，状态为 `Accepted`�?
本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`docs/frontend.md`、`docs/backend.md`、`docs/ai-review.md`、`decisions/ADR-006-model-profile-switching-and-public-provider-scope.md` �?`changelog/2026-06.md`。本次无当前 TASK 文件；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库�?
## 2026-06-08 确认 AI 调优治理双链路架�?
- 项目定位继续保持�?Contract Quality Control Platform / 合同质量控制中台，不改为 AI 合同审核平台�?- 确认生产审核链路和调优治理链路必须彻底隔离�?- 生产审核链路�?`Contract -> Parser -> CandidateResolver -> EvidenceSlot -> Review Engine -> Finding -> ReviewResultSnapshot`，原则是稳定、可解释、可审计�?- 调优治理链路�?`ReviewResultSnapshot -> TuningPacket -> CrossModelDiagnostic -> AITuningAdvice -> Candidate Change -> Regression Validation -> Human Approval -> Release`，原则是实验、诊断、优化、治理�?- 确认核心红线：`AI Advice != Production Change`。任�?AI 建议不得直接修改 prompt、rule、CandidateResolver、EvidenceSlot、ModelProfile 或后端确定性裁判，不得影响当前正式 ReviewResultSnapshot�?- MVP 只建�?TuningPacket、PointDiagnostic、ExecutionSummary �?ExportConfig，支持复制诊断包�?AI 辅助分析；不自动调用公网 AI，不自动调优，不自动改规则�?- Pilot 才建�?CrossModelDiagnostic、CrossModelComparison 和结构化 AITuningAdvice�?- Production Readiness 才建�?Regression Center、Release Governance、Rollback、Audit �?OptimizationEffect�?- DefinitionTermIndex 确认为索引层能力，不属于 Parser；Parser 产出结构�?blocks 后，DefinitionTermIndex �?CandidateIndex 并行构建，并按需注入 compact definition context�?- 新增 `ADR-007-tuning-packet-architecture.md`、`ADR-008-definition-term-index.md` �?`ADR-009-ai-tuning-governance.md`，状态均�?`Accepted`�?
本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`docs/ARCHITECTURE.md`、`docs/ai-review.md`、`decisions/ADR-007-tuning-packet-architecture.md`、`decisions/ADR-008-definition-term-index.md`、`decisions/ADR-009-ai-tuning-governance.md` �?`changelog/2026-06.md`。本次无当前 TASK 文件；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库�?
## 2026-06-08 确认 AI 调优�?Tab MVP 边界

- 任务详情�?MVP 新增 `AI 调优包` tab�?- �?tab 只用于生成、导出和复制 TuningPacket，帮助人和外�?AI 分析问题，不是自动调优系统�?- MVP 允许生成 TuningPacket、导�?TuningPacket、复制到外部 AI，并保存外部 AI 建议文本�?- MVP 不允许自动调用公�?AI、自动生�?AITuningAdvice、自动执�?CrossModelDiagnostic�?- MVP 不允许自动修�?prompt、rule、pattern、field lexicon、CandidateResolver、EvidenceSlot、ModelProfile �?ReviewPointDefinition�?- MVP 不允许自动影响当�?ReviewResultSnapshot�?
本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`docs/frontend.md`、`docs/ai-review.md` �?`changelog/2026-06.md`。本次无当前 TASK 文件；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库�?
## 2026-06-08 确认普通结�?URL 访问控制口径

- MVP 普通结�?URL 不做平台侧登录、公开令牌或独立访问控制�?- 外围系统负责�?URL 进行编码、加密、分发和访问控制�?- 平台暂不在结�?URL 权限方向投入额外能力�?- 平台仍必须保证普通结�?URL 不暴�?prompt、raw output、endpoint、stack trace、admin logs、secret 或管理台诊断详情�?
本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`docs/frontend.md`、`docs/backend.md`、`docs/sap.md` �?`changelog/2026-06.md`。本次无当前 TASK 文件；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库�?
## 2026-06-08 确认任务创建模型选择边界

- 管理台创建任务时可以选择已启用且密钥已配置的模型 profile�?- 管理台不选择模型时，系统使用当前默认启用�?`Model Profile`�?- 外部 API MVP 不允许调用方直接指定 `modelProfileCode`�?- SAP/OA/采购系统创建任务时使用平台当前默认启用的 `Model Profile`，避免外部集成契约过早绑定模型配置细节�?- 外部系统可查看本�?execution 实际使用的审核模型摘要，但不能控制模型选择�?- 后续如需开放外�?caller 指定模型，必须通过 caller policy 白名单和独立 API 契约扩展�?
本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`docs/frontend.md`、`docs/backend.md`、`docs/sap.md` �?`changelog/2026-06.md`。本次无当前 TASK 文件；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库�?
## 2026-06-08 确认首批审核点基�?
- `ADR-005-first-review-points-selection.md` 状态从 `Proposed` 改为 `Accepted`�?- MVP 首批审核点固定为 9 个�?- 9 个审核点全部�?core review point�?- `TAX_AMOUNT_FORMULA_CONSISTENCY` 保留�?MVP，并作为 core review point�?- 后续如果样本验证发现税额公式误报高，应通过规则版本、容差策略和回归验证调整，不�?PRD 中继续悬置�?
本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`ROADMAP.md`、`docs/ai-review.md`、`tasks/active/TASK-003-first-review-points-selection.md`、`decisions/ADR-005-first-review-points-selection.md` �?`changelog/2026-06.md`。本次未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库�?
## 2026-06-08 确认 MVP 首批合同类型

- MVP 首批合同类型只做“工程采购合同”�?- 管理台合同类型字�?MVP 仅展示“工程采购合同”，作为后续多合同分类预留字段�?- 首批 9 个审核点默认适用于工程采购合同�?- 材料供货合同、费用合同、杂项合同延后到 Pilot 或后续阶段�?- 系统可保留合同类型扩展结构，�?MVP 不做复杂合同类型路由�?
本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`docs/ai-review.md` �?`changelog/2026-06.md`。本次无当前 TASK 文件；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库�?
## 2026-06-06 补齐项目记忆模块

基于已确认的 `AGENTS.md`、`CURRENT_CONTEXT.md`、`PROJECT_BRIEF.md` 和原始架构文档，补齐以下文件内容�?
- `ROADMAP.md`：补�?V1 建议里程碑、验证方向和待确认事项�?- `tasks/TASK-000-template.md`：补充任务元信息、执行约束、必�?按需文档、文档更新要求和完成记录字段�?- `docs/frontend.md`：补�?NOT_CONCLUDED 体验、发布页、诊断展示和权限边界�?- `docs/backend.md`：补充解析质量门槛、合同类型画像、候选归属、缺证据归因、任务幂等和点级结果摘要�?- `docs/database.md`：补充不可变快照、版本包白名单、caller policy 审批、审计摘要、reviewCompleteness 和临时运行审批�?- `docs/ai-review.md`：补充模型输入、requestedRoles 资格、second pass、合成优先级、脱敏测试和模型运行记录�?- `docs/sap.md`：补�?diagnostic-only 边界、correction 错误码、类型建议可见性和外部契约测试�?- `docs/deployment.md`：补充负载演练、stage 幂等、紧急变更、恢复流程�?0 天过�?API 行为和上线前样本验证�?- `decisions/ADR-000-template.md`：补充适用范围、回滚迁移和待确认区�?
本次补齐未创建业务代码，未搭建前端或后端脚手架，未安装依赖�?
## 2026-06-07 Superpowers 审查结论写回

�?Superpowers planning / design review / task decomposition 审查结论沉淀回项目记忆�?
本次结论�?
- 当前不建议直接开始编码�?- 当前可以开始“技术基线与 V1 MVP 收敛任务”�?- 不建议直接进�?project scaffold�?- scaffold 推迟到关�?ADR 完成之后�?- V1 愿景不等�?MVP，第一�?MVP 应聚焦单份中�?Word 合同的最小审核闭环�?- Quality Copilot、完整治理、复杂发布审批、SAP/OA 深度 correction、生产保护能力不应进入第一�?MVP�?
更新文件�?
- `CURRENT_CONTEXT.md`：写入当前不建议编码、不建议直接 scaffold、下一步优先创建并执行 MVP 收敛任务和关�?ADR�?- `ROADMAP.md`：新�?V1 分层，将 V1 拆为 MVP、Pilot、Production Readiness�?
新增任务包：

- `tasks/active/TASK-001-v1-mvp-scope-gate.md`
- `tasks/active/TASK-002-entry-order-decision.md`
- `tasks/active/TASK-003-first-review-points-selection.md`
- `tasks/active/TASK-004-word-parser-baseline-plan.md`
- `tasks/active/TASK-005-model-gateway-and-budget-baseline.md`
- `tasks/active/TASK-006-scaffold-only-after-adr.md`

新增 ADR 草案�?
- `decisions/ADR-001-technical-stack-and-repo-layout.md`
- `decisions/ADR-002-v1-result-and-diagnostic-contract.md`
- `decisions/ADR-003-task-execution-snapshot-model.md`

所有新�?ADR 状态均�?`Proposed`，等待人工确认；本次未创建业务代码，未搭建脚手架，未安装依赖�?
## 2026-06-07 启动技术基线与 V1 MVP 收敛任务

开始执行技术基线与 V1 MVP 收敛任务�?
本次完成�?
- 完成 `TASK-001-v1-mvp-scope-gate`：形�?V1 MVP / Pilot / Production Readiness 分层规划基线�?- 启动 `TASK-002-entry-order-decision`：比较管理台上传优先、外�?API 优先、API-first + 简易管理台三种入口顺序�?- �?`TASK-002` 写入初步推荐：API-first + 简易管理台，仍待人工确认�?- 更新 `CURRENT_CONTEXT.md` 的当前完成情况和下一步�?- 更新 `ROADMAP.md`，标�?MVP 分层来自 `TASK-001`，入口顺序推荐来�?`TASK-002` 且待确认�?
本次未创建业务代码，未搭建脚手架，未安装依赖。`ADR-001`、`ADR-002`、`ADR-003` 仍为 `Proposed`�?
## 2026-06-07 项目记忆写入规则治理

优化项目记忆写入规则的放置方式：

- `AGENTS.md`：保留项目记忆写入的短规则，只放强制原则�?`docs/context-management.md` 索引�?- `docs/context-management.md`：新增详细规则，说明必须写入的内容、写入哪个文件、不写入的内容、需要人工确认的内容、必须创�?ADR 的内容，以及每次任务结束�?Memory Writeback 输出格式�?- `CURRENT_CONTEXT.md`：记录本次文档治理已完成�?
本次调整未改变项目目标、V1 范围、业务边界或核心红线�?
本次未创建业务代码，未搭建前端或后端脚手架，未安装依赖�?
## 2026-06-07 Next Task Handoff Prompt 治理

为项目补�?Next Task Handoff Prompt 输出规则，并明确区分“可执行任务”和“征求意�?建议”�?
本次完成�?
- 更新 `AGENTS.md`：要求每次任务结束后必须输出 `Next Task Handoff`，并限制只有明确下一任务时才能输出独立代码块形式�?`Next Task Handoff Prompt`�?- 更新 `docs/context-management.md`：新�?`## Next Task Handoff` 章节，细化可复制代码块的适用条件、禁止条件、必含内容、无明确下一任务时的处理方式，以及“执行任务”和“建�?征求意见”的区分标准�?- 更新 `tasks/TASK-000-template.md`：新�?`## Next Task Handoff` 模板小节，要求任务完成时显式判断是否存在明确下一任务，并禁止把无任务编号或仅为建议的内容放入代码块�?- 更新 `CURRENT_CONTEXT.md`：记录本�?Next Task Handoff Prompt 文档治理已完成�?
本次调整仅涉及项目记忆文档治理，未修改业务代码，未创建脚手架，未安装依赖，未改变已确认的项目目标、业务边界、V1 范围或核心红线�?
## 2026-06-07 任务命名规范治理

统一任务文档的命名与标题规范，保持工程路径稳定，同时提升中文可读性�?
本次完成�?
- 更新 `tasks/TASK-000-template.md`：新增任务命名规范，明确文件名使用英�?slug，标题使用中文，正文以中文为主，英文 slug 仅作稳定路径和工程标识�?- 更新 `docs/context-management.md`：补充任务文档命名与语言规范，明确文件名可保留英�?slug 以兼容工程，面向人阅读的标题、目标、验收标准和交付摘要统一使用中文�?- 更新 `tasks/active/TASK-001-v1-mvp-scope-gate.md`、`tasks/active/TASK-002-entry-order-decision.md`、`tasks/active/TASK-003-first-review-points-selection.md`、`tasks/active/TASK-004-word-parser-baseline-plan.md`、`tasks/active/TASK-005-model-gateway-and-budget-baseline.md`、`tasks/active/TASK-006-scaffold-only-after-adr.md`：为现有仅英文标题的任务补齐中文标题，不改文件名�?- 更新 `CURRENT_CONTEXT.md`：记录本次任务命名规范治理已完成�?
本次调整仅涉及项目记忆文档治理，未修改业务代码，未创建脚手架，未安装依赖，未改变既有项目目标�?V1 范围�?
## 2026-06-08 确认 MVP 管理台登录与敏感诊断展示边界

确认 MVP 管理台暂不做平台内登录、账号体系和权限矩阵�?
本次确认�?
- 管理台访问控制由部署环境、内网、VPN、反向代理或外围系统承担�?- 管理台常规页面和常规 API 默认只展示摘要级诊断信息�?- 管理台不得展示或返回完整 prompt、完整模�?raw output、endpoint secret、stack trace、完整合同敏感调试包或密钥�?- 该限制不影响 AI 调优治理链路；MVP 治理链路依赖结构化、最小必要的 TuningPacket，而不是暴露全部原始调试内容�?- 如后续需要完整敏感诊断导出，应作�?Pilot / Production Readiness 的受控能力单独设计，纳入权限、脱敏、审批和审计�?- MVP 配置变更仍记录配置版本、变更时间和操作者占位信息；操作者可暂记�?`SYSTEM`、`ADMIN_PLACEHOLDER` 或环境账号�?
更新文件�?
- `PRD.md`
- `CURRENT_CONTEXT.md`
- `docs/frontend.md`
- `docs/backend.md`
- `docs/database.md`
- `tasks/active/TASK-001-v1-mvp-scope-gate.md`

本次未创建业务代码，未搭建脚手架，未安装依赖。本次不新增 ADR；该变更�?MVP 范围边界确认，未改变核心审核链路或调优治理红线�?
## 2026-06-09 ִ�� TASK-013 ��ʷ��������

��� `TASK-013-legacy-task-cleanup`���� `tasks/active/` �� `TASK-001` ~ `TASK-012` ִ��һ����ʷ����������鵵��
����ȷ�ϣ�
- `TASK-001 / TASK-002 / TASK-003 / TASK-007 / TASK-008 / TASK-009 / TASK-010 / TASK-011 / TASK-012` ״̬Ϊ������ɡ�������ɼ�¼����������ڡ�����ļ��Ͳ��Խ������Ǩ�Ƶ� `tasks/done/`
- `TASK-004-word-parser-baseline-plan`��`TASK-005-model-gateway-and-budget-baseline`��`TASK-006-scaffold-only-after-adr` ״̬δ��ɣ����������� `tasks/active/`
- ����û�����񱻱��Ϊ `Superseded` �� `Cancelled`�����������������Ч��ʷ���䣬δ����������ǵ�ǰ backlog
- ԭ��ռλΪ����С OpenAPI ��Լ���� `TASK-013` ����ѱ���������ռ�ã����� OpenAPI ���������±��

���θ��� `tasks/active/TASK-013-legacy-task-cleanup.md`��`CURRENT_CONTEXT.md`��`changelog/2026-06.md`������� active/done Ŀ¼Ǩ�ơ�
����δ����ҵ����룬δ����ּܣ�δ��װ������δ�޸ļܹ� ADR ���ۡ�

## 2026-06-10 TASK-004/TASK-005 ????

?? `TASK-004-word-parser-baseline-plan`??? `docs/word-parser-mvp-boundary.md`??? Word Parser MVP ????????/??/??/??/preview ??????????????
?? `TASK-005-model-gateway-and-budget-baseline`??? `docs/model-gateway-budget-baseline.md`?????????????????/??/Circuit Breaker ??????

???????
- ?? `tasks/active/TASK-014-minimal-openapi-contract.md`
- ?? `tasks/active/TASK-015-flyway-v1-bootstrap.md`
- ?? `tasks/active/TASK-016-mvp-development-validation.md`
- ?? `docs/backend.md`?`docs/ai-review.md`?`docs/deployment.md`
- ?? `tasks/done/TASK-004-word-parser-baseline-plan.md`
- ?? `tasks/done/TASK-005-model-gateway-and-budget-baseline.md`
- ?? `CURRENT_CONTEXT.md`

??????????`ADR-002` ?? Accepted?`STANDARD / DEEP_REVIEW / EVALUATION` ?? token ????????????preview ?????????? metadata ????????
????????????? Scaffold?????????? OpenAPI YAML/JSON ???????? Flyway migration ?????? ADR?

## 2026-06-10 完成 TASK-014 最小 OpenAPI 契约

完成 `TASK-014-minimal-openapi-contract`，在不扩展 MVP 范围的前提下落地最小公开 API 契约。
本次确认：
- 生成 `packages/api-contracts/openapi.yaml`、`packages/api-contracts/openapi.json` 和 `packages/api-contracts/README.md`。
- 冻结公开路径为 `POST /api/review/tasks`、`GET /api/review/tasks/{taskId}/executions/{executionId}`、`GET /api/review/results/{taskId}`。
- 公开任务创建采用 `multipart/form-data`：`file` 为 `.docx`，`metadata` 为 JSON；外部 API MVP 不允许调用方指定 `modelProfileCode`。
- 冻结 `Evidence`、`Candidate`、`ReviewResultSnapshot` 和 `DefinitionTermIndexEntry` 的最小接口表示；`DefinitionTermIndexEntry` 本版仅保留为组件 schema，不直接暴露到公开 path。
- 公开结果只暴露业务状态、证据、原文定位和业务化 `NOT_CONCLUDED` 原因；不暴露完整 `SYS-*` 技术码、预算诊断、`PointDiagnostic`、`TuningPacket`、prompt、raw output、endpoint secret 或 stack trace。
- 契约边界已与 `TASK-004`、`TASK-005`、`ADR-008`、`ADR-012`、`docs/backend.md` 和 `docs/sap.md` 人工核对一致。
本次更新 `packages/api-contracts/README.md`、`packages/api-contracts/openapi.yaml`、`packages/api-contracts/openapi.json`、`CURRENT_CONTEXT.md`、`changelog/2026-06.md` 和 `tasks/done/TASK-014-minimal-openapi-contract.md`。
本次未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库迁移。`openapi.json` 已完成本地 JSON 解析校验；当前运行环境缺少 YAML 解析工具链，因此 `openapi.yaml` 记录为人工镜像一致性复核通过。
本次未新增 ADR，也未修改 `ADR-002` 状态；原因是本任务只冻结最小公开契约，不在当前任务内扩大为结果诊断 ADR 状态治理。

## 2026-06-10 推进 ADR-002 为 Accepted

完成 `ADR-002-v1-result-and-diagnostic-contract` 收口，将其从 `Proposed` 推进为 `Accepted`。
本次确认：
- 正式冻结点级状态枚举为 `PASS / WARNING / ERROR / NOT_CONCLUDED / SKIPPED`。
- 正式冻结内部 `SYS-*` 只作为技术诊断，公开层统一映射为业务化 `NOT_CONCLUDED`。
- 正式冻结普通结果页与外部 API 不暴露完整 `SYS-*` 技术码、`PointDiagnostic`、`requiresHigherBudget`、`recommendedBudgetProfile`、`notConcludedDetail`、完整 prompt、raw output、endpoint secret 和 stack trace。
- 正式冻结 `PROVEN_REQUIRED_CLAUSE_ABSENT` 与普通 `EVIDENCE_NOT_FOUND` 的区分：前者可作为业务 finding，后者只进入 `NOT_CONCLUDED`。
- 正式冻结 `businessMessage` 由后端固定模板生成，不由模型或规则自由文本生成。
- `TASK-006-scaffold-only-after-adr` 的 `ADR-002` 门禁已解除，可作为后续脚手架直接前置引用。
本次更新 `decisions/ADR-002-v1-result-and-diagnostic-contract.md`、`tasks/active/TASK-006-scaffold-only-after-adr.md`、`CURRENT_CONTEXT.md` 和 `changelog/2026-06.md`。
本次未创建业务代码，未搭建脚手架，未安装依赖，未新增任务包。`ADR-002` 的收口依据来自 `PRD.md`、`docs/backend.md`、`docs/frontend.md`、`docs/ai-review.md`、`docs/sap.md`、`ADR-012` 和 `TASK-014` 已冻结边界。
## 2026-06-12 Codex execution environment escalated verification

继续执行 `TASK-006-scaffold-only-after-adr`，本轮只做 Codex 执行环境提权验证，不进入业务开发，不修改 schema，不修改 migration SQL，不处理前端。

本轮完成：
- 顺序重建 PostgreSQL 应用层测试库 `cqcp_app_test`。
- 在 `apps/api-server` 下按本机可用配置准备提权命令，显式设置 `JAVA_HOME`、`PATH` 与 `CQCP_DB_*`。
- 对 `gradle test` 连续两次发起 `require_escalated` 审批申请。

本轮真实结果：
- 两次提权审批均返回 `The automatic permission approval review did not finish before its deadline`。
- 因 `gradle test` 未实际执行，`gradle build` 本轮未启动。
- 本轮不能新增“后端应用层验证通过”结论，也不能将该状态写成 CQCP 后端工程失败。
- 当前仍只能确认：Codex 默认失败根因是沙箱网络限制；提权后是否可恢复与用户本机 PowerShell 一致的结果，仍待下一轮在审批链路正常时补验。
## 2026-06-12 Codex execution environment escalated verification retry

继续执行 `TASK-006-scaffold-only-after-adr`，本轮只补做 Codex 提权验证闭环，不进入业务开发，不修改 schema，不修改 migration SQL，不处理前端。

本轮完成：
- 在 `apps/api-server` 目录下，显式设置 `JAVA_HOME`、`PATH`、`CQCP_DB_URL`、`CQCP_DB_USERNAME`、`CQCP_DB_PASSWORD` 后，以 `require_escalated` 执行 `gradle test`。
- `gradle test` 结果为 `BUILD SUCCESSFUL in 6s`。
- 在同一组环境变量下，以 `require_escalated` 执行 `gradle build`。
- `gradle build` 结果为 `BUILD SUCCESSFUL in 1s`。

本轮结论：
- 可确认 Codex 默认失败根因是沙箱网络限制与默认会话未继承 `JAVA_HOME/PATH`，不是 CQCP 后端工程失败。
- 在提权执行条件下，`apps/api-server` 已恢复与用户本机 PowerShell 一致的通过结果。
- `TASK-006` 当前环境级结论应更新为：后端本机 Java/Gradle/Flyway 应用层验证通过；前端 `build/lint` 通过，`test` 仍为环境阻塞。
## 2026-06-12 Frontend test scaffold verification

继续执行 `TASK-006-scaffold-only-after-adr`，本轮只处理前端 `apps/admin-web` 的测试环境阻塞与最小 scaffold 修复，不进入 `TASK-016`，不新增业务功能。

本轮完成：
- 在 `apps/admin-web/vite.config.js` 中新增 Vitest `setupFiles` 配置。
- 新增 `apps/admin-web/src/test/setup.ts`，统一注入 `@testing-library/jest-dom/vitest` 与 `window.matchMedia` mock。
- 调整 `apps/admin-web/src/App.test.tsx`，移除重复的测试初始化导入。

本轮验证结果：
- 默认沙箱下执行 `npm.cmd run test` 仍失败，真实错误仍为目录读取权限阻塞：
  - `Cannot read directory "../../../.."`: Access is denied
  - `Could not resolve "...\\apps\\admin-web\\vite.config.js"`
- 以 `require_escalated` 执行 `npm.cmd run test` 后，目录权限阻塞消失，测试进入真实运行阶段。
- 提权后暴露出真实 scaffold 缺口：`window.matchMedia is not a function`。
- 完成最小修复后，再次以 `require_escalated` 执行 `npm.cmd run test`，结果为 `1 file passed / 1 test passed`。

本轮结论：
- 前端测试最初失败既有沙箱目录权限问题，也有 jsdom `matchMedia` 配置缺口。
- 当前 `apps/admin-web` 的测试在提权执行条件下已通过。
- 默认沙箱下的 `npm test` 仍不能记为通过，应继续记录为 Codex 执行环境权限阻塞。
## 2026-06-12 TASK-006 closeout review

继续执行 `TASK-006-scaffold-only-after-adr`，本轮只做环境级结论收口复核，不进入 `TASK-016`，不新增业务功能。

本轮确认：
- 后端 `apps/api-server` 在提权条件下已通过 `gradle test` 与 `gradle build`。
- 前端 `apps/admin-web` 已通过 `build/lint`，并在提权条件下通过 `npm test`。
- 默认沙箱下前端 `npm test` 仍受目录读取权限模型阻塞，但该问题属于 Codex 执行环境限制，不再视为 CQCP scaffold 工程阻塞。

本轮结论：
- `TASK-006` 可视为 scaffold 环境级验证收口完成。
- 后续继续推进时，应保留一条执行环境注意事项：前端测试在 Codex 默认沙箱下仍需提权或等效执行环境。
