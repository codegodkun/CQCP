# changelog/2026-06.md

## 2026-06-06

初始化 Contract Quality Control Platform / 合同质量控制中台 V2 的 AI Coding 项目记忆系统。

创建文件：

- `AGENTS.md`
- `PROJECT_BRIEF.md`
- `CURRENT_CONTEXT.md`
- `ROADMAP.md`
- `tasks/TASK-000-template.md`
- `tasks/active/`
- `tasks/done/`
- `docs/frontend.md`
- `docs/backend.md`
- `docs/database.md`
- `docs/ai-review.md`
- `docs/sap.md`
- `docs/deployment.md`
- `decisions/ADR-000-template.md`
- `changelog/2026-06.md`

本次初始化只基于架构文档 `2026-06-06-contract-quality-control-platform-v2-design.md` 中明确写到的内容。

本次未创建业务代码，未搭建前端或后端脚手架，未安装依赖。

## 2026-06-07 确认 MVP 入口顺序

确认并沉淀 `TASK-002-entry-order-decision` 的入口顺序决策。

本次完成：

- 确认 MVP 入口顺序采用 API-first + 简易管理台。
- 完成 `TASK-002-entry-order-decision`，补充未入选入口的延后策略和完成记录。
- 新增 `decisions/ADR-004-mvp-entry-order.md`，状态为 `Accepted`。
- 更新 `CURRENT_CONTEXT.md`：记录入口顺序已确认，并调整下一步任务。
- 更新 `ROADMAP.md`：将入口顺序从待确认改为引用 `ADR-004`。

本次未创建业务代码，未搭建脚手架，未安装依赖。普通结果 URL 访问控制、最小任务创建 API 字段、外部系统正式联调阶段仍待确认。

待确认事项已写入 `CURRENT_CONTEXT.md` 和各模块文档。

## 2026-06-07 完成首批审核点选择

完成 `TASK-003-first-review-points-selection`，从 `PARTY_FIELDS`、`AMOUNT_TAX`、`PAYMENT_TERMS` 中收敛 MVP 首批审核点基线。

本次形成：

- `PARTY_FIELDS`: `PARTY_A_NAME_CONSISTENCY`、`PARTY_B_NAME_CONSISTENCY`，均为 core review point。
- `AMOUNT_TAX`: `CONTRACT_TOTAL_AMOUNT_CONSISTENCY` 为 core review point；`TAX_AMOUNT_FORMULA_CONSISTENCY` 为非 core，是否保留在 MVP 仍待人工确认。
- `PAYMENT_TERMS`: `PREPAYMENT_RATIO_CONSISTENCY` 为 core review point。
- 明确每个审核点的结构化字段、EvidenceSlot、规则/正则/Gemma/后端裁判策略和 `SYS-*` 降级边界。
- 明确延后到 Pilot 的审核点：签署页主体冲突、统一社会信用代码一致性、多主体关系、中文大写金额一致性、税率合法性、付款比例合计、付款节点顺序和付款条件完整性。
- 新增 `decisions/ADR-005-first-review-points-selection.md`，状态为 `Proposed`。
- 更新 `CURRENT_CONTEXT.md`、`ROADMAP.md`、`docs/ai-review.md` 和当前任务包。

本次未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库。首批审核点、core 标记、税额公式点是否进入 MVP、首批合同类型、结构化字段来源、样本集 owner 和税率 CandidateRole 仍待人工确认。

## 2026-06-07 创建 PRD 草案

基于原始架构文档、`PROJECT_BRIEF.md`、`CURRENT_CONTEXT.md`、`ROADMAP.md`、模块文档和 ADR，新增 `PRD.md` v0.1 草案。

本次完成：

- 汇总产品问题、解决方案、产品原则、用户角色、V1/MVP/Pilot/Production Readiness 范围。
- 补充用户故事、功能需求、非功能需求、实施决策、测试决策、验收标准、风险和待确认事项。
- 保留当前关键待确认项，不把 Proposed ADR 写成已确认事实。
- 明确 PRD 不替代 ADR、任务包或模块文档。
- 更新 `CURRENT_CONTEXT.md`，记录已有 PRD 草案。

本次无当前 TASK 文件；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库。

## 2026-06-07 确认 MVP 第一批验收口径

确认 MVP 第一批验收口径：

- 使用 2-3 份脱敏或合成中文 Word 合同作为第一批验证样本。
- 系统必须完成解析、首批审核点执行、证据定位、结果快照和结果页展示。
- 核心审核点至少大部分形成 `PASS / WARNING / ERROR`。
- 所有 `NOT_CONCLUDED` 点必须有业务可读原因和 source/coverage explanation。

本次更新 `PRD.md` 和 `CURRENT_CONTEXT.md`。本次无当前 TASK 文件；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库。

## 2026-06-07 确认 MVP 输入格式边界

确认 MVP 第一轮输入格式边界：

- MVP 仅支持单份中文 `.docx` 用户输入。
- `.doc` 不进入第一轮 MVP，延后到后续开发。
- 管理台上传界面必须明显标注“当前仅支持 DOCX，DOC 待后续开发”。
- 前端需阻止 `.doc` 或其他非 `.docx` 文件提交；后端任务创建 API 仍需校验文件类型，避免绕过前端限制。

本次更新 `PRD.md`、`ROADMAP.md`、`docs/frontend.md` 和 `CURRENT_CONTEXT.md`。本次无当前 TASK 文件；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库。

## 2026-06-07 确认首批结构化字段与字段配置能力

确认 MVP 首批结构化字段：

- 合同名称、甲方名称、乙方名称、项目名称。
- 合同总金额、合同不含税金额、合同税额、税率。
- 计价模式：固定总价、暂定总价。
- 付款方式：按月度付款、按节点付款。
- 按月度付款：预付款比例、进度款比例、竣工款比例、结算款比例、质保款比例。
- 按节点付款：预付款比例阶段固定，其他节点信息目前不确定。
- 发票类型：增值税普通发票、增值税专用发票。

确认字段来源与配置能力：

- 管理台可以直接录入首批结构化字段。
- SAP/API 传入字段与管理台字段定义保持一致。
- 后台支持自定义新增字段。
- 自定义字段只进入结构化字段池和后续规则配置候选，不自动生成审核点、EvidenceSlot 或业务裁判逻辑。
- 首批结构化字段全部为必填字段；管理台和 SAP/API 任务创建必须执行一致的必填校验。

本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`ROADMAP.md`、`docs/frontend.md`、`docs/backend.md` 和 `docs/database.md`。本次无当前 TASK 文件；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库。

## 2026-06-08 确认付款方式条件必填与节点文本边界

- 首批结构化字段从“无条件全部必填”修正为“通用字段必填 + 按付款方式条件必填”。
- 按月度付款：预付款、进度款、竣工款、结算款、质保款比例必填，节点付款其他节点信息不适用。
- 按节点付款：预付款比例和节点付款其他节点信息必填，四个月度专属比例不适用。
- 不适用字段不提交空值、零值或虚假占位值。
- `节点付款其他节点信息` 在按节点付款场景中使用必填多行文本。
- 字段只用于任务录入、数据保存、结果快照和页面展示。
- MVP 暂不为该字段创建审核点或 EvidenceSlot，不据此生成业务 finding。

本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`ROADMAP.md`、`docs/frontend.md`、`docs/backend.md` 和 `docs/database.md`。本次无当前 TASK 文件；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库。

## 2026-06-08 确认合同总金额含税语义

- `合同总金额` 固定表示合同含税总金额。
- 管理台标签固定显示“合同总金额（含税）”。
- SAP/API 的 `contractTotalAmount` 使用相同语义，不通过额外含税标志改变解释。
- 税额公式直接使用 `contractTotalAmount`、`taxExcludedAmount` 和 `taxAmount`，不设置重复的含税金额输入字段。

本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`ROADMAP.md`、`docs/frontend.md`、`docs/backend.md`、`docs/database.md` 和 `ADR-005` 草案。本次无当前 TASK 文件；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库。

## 2026-06-08 确认 MVP 币种边界

- MVP 固定只支持人民币 `CNY`。
- 管理台不提供币种选择，金额输入统一显示单位“元”。
- SAP/API 可省略 `currency`；若显式传入，只允许 `CNY`。
- 非 `CNY` 输入在任务创建阶段拒绝，不进入审核链路。
- 外币合同延后到后续阶段。

本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`ROADMAP.md`、`docs/frontend.md`、`docs/backend.md`、`docs/database.md`、`docs/sap.md` 和 `ADR-005` 草案。本次无当前 TASK 文件；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库。

## 2026-06-08 确认金额与税额裁判口径

- CNY 金额字段使用十进制定点数并保留两位小数，不使用二进制浮点数参与确定性裁判。
- `合同总金额（含税） = 合同不含税金额 + 合同税额` 作为强校验，允许 `0.01` 元绝对误差。
- `合同税额 = 合同不含税金额 × 税率` 在 MVP 仅作为 WARNING 校验，不直接判 ERROR，以兼容分项计税、多税率和逐行舍入差异。
- 后端负责确定性最终裁判；Gemma/A30 只可辅助抽取金额和税率。
- 证据不足、角色冲突或解析异常继续输出 `SYS-* / NOT_CONCLUDED`，不得生成无可靠证据的业务 finding。

本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`docs/backend.md` 和 `ADR-005` 草案。本次无当前 TASK 文件；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库。

## 2026-06-08 确认比例字段表示口径

- 税率和付款比例统一使用百分比数值，`13` 表示 `13%`，不使用 `0.13`。
- 管理台、外围系统、SAP/API、数据库和结果快照采用相同表示方式；外围系统现有存储口径也是 `13`。
- 后端参与金额计算时统一将百分比数值除以 `100`。
- 比例合法范围为 `0` 至 `100`；允许的小数精度仍待确认。

本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`docs/backend.md`、`docs/database.md`、`docs/sap.md` 和 `ADR-005` 草案。本次无当前 TASK 文件；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库。

## 2026-06-08 确认税率与付款比例精度

- 税率最多保留 4 位小数。
- 付款比例最多保留 2 位小数。
- 输入末尾零可省略，不改变业务语义。
- 超出精度的输入直接拒绝，不得静默四舍五入或截断。

本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`docs/backend.md`、`docs/database.md`、`docs/sap.md` 和 `ADR-005` 草案。本次无当前 TASK 文件；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库。

## 2026-06-08 确认月度付款比例业务口径

- 月度付款的预付款、进度款、竣工款和结算款采用累计付款比例。
- 进度款包含预付款，竣工款包含进度款和预付款，结算款包含竣工款、进度款和预付款。
- 比例关系为 `预付款比例 ≤ 进度款比例 ≤ 竣工款比例 ≤ 结算款比例`。
- 质保款为单独比例，并满足 `结算款比例 + 质保款比例 = 100%`。
- MVP 不重复校验上述跨字段关系，也不据此生成业务 finding。
- 外围系统负责跨字段输入校验，管理台测试数据由测试人员控制；平台仍执行单字段范围、精度和条件必填校验。

本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`docs/backend.md`、`docs/database.md`、`docs/sap.md` 和 `ADR-005` 草案。本次无当前 TASK 文件；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库。

## 2026-06-08 扩展 MVP 月度付款比例审核点

- 月度付款的预付款、进度款、竣工款、结算款和质保款比例全部纳入 MVP 独立一致性审核。
- 新增 `PROGRESS_PAYMENT_RATIO_CONSISTENCY`、`COMPLETION_PAYMENT_RATIO_CONSISTENCY`、`SETTLEMENT_PAYMENT_RATIO_CONSISTENCY`、`WARRANTY_RETENTION_RATIO_CONSISTENCY`。
- 五项审核分别比较结构化比例与合同付款条款候选，分别输出状态、证据和原文定位。
- 五点可共享付款条款局部 EvidencePacket，但必须使用各自独立的 required EvidenceSlot。
- 比例之间的累计关系仍不由 MVP 重复校验，也不据此生成 finding。
- 五个月度付款比例一致性审核点全部标记为 core review point。

本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`ROADMAP.md`、`docs/ai-review.md`、`TASK-003` 和 `ADR-005` 草案；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库。

## 2026-06-08 确认节点付款审核点适用性

- 节点付款继续执行 `PREPAYMENT_RATIO_CONSISTENCY`。
- 进度款、竣工款、结算款和质保款四个比例审核点在节点付款下输出 `SKIPPED`。
- 不适用不属于证据不足或系统异常，不得输出 `SYS-* / NOT_CONCLUDED`，也不得生成业务 finding。

本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`docs/ai-review.md`、`docs/backend.md`、`TASK-003` 和 `ADR-005` 草案；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库。

## 2026-06-08 确认审核点严重级别配置机制

- 管理后台应可视化维护审核点配置，包括分类、启用状态、排序、依赖字段和默认严重级别。
- 明确业务不一致时输出 `ERROR` 或 `WARNING` 不 hardcode，由当次 execution 绑定的有效 `severityPolicy` 决定。
- `PASS`、`NOT_CONCLUDED`、`SKIPPED` 由后端裁判、证据状态和适用性决定，不作为管理员可选严重级别。
- `SYS-*` 系统诊断不得配置成业务 finding 级别。
- 审核点配置最初曾按“保存草稿 -> 发布生效”记录；后续已调整为 MVP 保存后即对下一次新建审核任务生效，不设置单独发布流程。
- 历史 ReviewResultSnapshot 保存当次严重级别和版本，不随当前配置变化。

本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`docs/frontend.md`、`docs/backend.md`、`docs/database.md` 和 `ADR-005` 草案；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库。

## 2026-06-08 调整审核点配置生效机制

- 由于审核点修改和审核任务均为低频操作，MVP 不设置草稿/发布流程。
- 管理员保存审核点配置后，下一次新建审核任务使用最新配置。
- 已创建、正在执行和历史审核结果不受后续配置变更影响。
- 每次保存应形成可追溯配置版本或变更记录，并由新建 execution 绑定当时有效配置。

本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`docs/frontend.md`、`docs/backend.md`、`docs/database.md` 和 `ADR-005` 草案；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库。

## 2026-06-08 确认审核规则配置页 MVP 范围

- MVP 审核规则配置页保持与既有项目页面风格一致。
- MVP 只支持预置审核点列表展示、详情展开和有限编辑，不开放新增审核点能力。
- 列表支持按审核分类和状态排序，并支持按审核点名称或编码搜索。
- 详情/编辑字段包括审核点编码、名称、审核分类、默认级别、启用状态、排序、依赖字段和合同类型。
- 依赖字段来自全部合同字段下拉多选。
- 审核点编码可作为配置字段编辑，但不得借此新增审核点；EvidenceSlot、执行策略、后端裁判和模型辅助策略不允许在 MVP 管理台任意新增或改写。
- 新增审核点延后到后续阶段，通过独立任务配套规则、证据槽、裁判、结果展示和样本验证。

本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`docs/frontend.md`、`docs/backend.md`、`docs/database.md`；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库。

## 2026-06-08 确认审核点停用语义

- 审核点停用后不进入新建任务的 ReviewExecutionPlan。
- 停用审核点不输出 `PASS / WARNING / ERROR / NOT_CONCLUDED / SKIPPED`，也不生成业务 finding。
- 普通结果页只展示本次实际启用并进入计划的审核点。
- ReviewResultSnapshot 记录当次配置版本、启用审核点清单和配置停用审核点清单。
- MVP 首批 9 个审核点默认全部启用；手动停用 core 审核点的测试结果不能用于证明 core 覆盖率达标。

本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`docs/frontend.md`、`docs/backend.md`、`docs/database.md` 和 `ADR-005` 草案；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库。

## 2026-06-08 确认审核点依赖字段配置校验

- 审核点依赖字段可编辑，但必须来自已启用合同字段。
- 每个审核点必须保留其运行所需的 required structured fields。
- 若依赖字段配置导致审核点不可执行，管理台和后端必须拒绝保存，并提示缺失的必需依赖字段。
- 配置错误不得延迟到任务执行阶段表现为 `SYS-MISSING-INPUT`。

本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`docs/frontend.md`、`docs/backend.md` 和 `docs/database.md`；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库。

## 2026-06-08 确认合同字段停用反向依赖保护

- 合同字段被启用审核点依赖时，不允许直接停用或删除。
- 用户必须先调整审核点依赖字段，或停用相关审核点。
- 字段配置页和后端错误应提示依赖该字段的审核点清单。
- 该保护避免字段配置变化导致审核点配置不可执行。

本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`docs/frontend.md`、`docs/backend.md` 和 `docs/database.md`；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库。

## 2026-06-08 确认审核点展示编码与内部 code 分离

- 管理台可编辑的审核点编码仅用于前台展示和人工定位。
- 展示编码不能为空，并且在当前审核点配置中唯一。
- 系统内部 `reviewPointCode` 是唯一、不可编辑主 key。
- 后端执行、EvidenceSlot、结果快照、API 和历史解释必须使用内部 `reviewPointCode`。
- ReviewResultSnapshot 同时保存当次展示编码和内部 `reviewPointCode`。

本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`docs/frontend.md`、`docs/backend.md` 和 `docs/database.md`；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库。

## 2026-06-08 确认审核执行逻辑来源与迭代机制

- MVP 第一版审核执行逻辑由项目团队预置生成，不由管理台用户生成。
- 执行逻辑通过 `ReviewPointDefinition`、EvidenceSlot、候选抽取规则、后端裁判规则和 Gemma 辅助策略固化。
- 管理台可只读展示执行逻辑摘要，但不开放执行逻辑编辑。
- 审核分类仅用于展示、筛选和排序，不参与执行路由。
- 审核结果不理想时，通过版本化规则、词库、ValueGrammar、EvidenceSlot policy、Gemma 局部 prompt、后端裁判或结果页解释迭代。
- 执行逻辑迭代必须通过脱敏/合成样本回归验证，新任务使用新版本，历史快照保持旧版本解释。

本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`docs/frontend.md`、`docs/backend.md`、`docs/database.md` 和 `docs/ai-review.md`；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库。

## 2026-06-08 确认管理台执行逻辑摘要粒度

- MVP 审核点详情只展示业务可读的执行逻辑摘要。
- 摘要包括内部 code、执行策略名称、依赖字段、需要的证据类型、是否使用 Gemma 辅助、裁判说明和最近一次版本更新时间。
- MVP 普通配置页不展示完整正则、prompt、EvidenceSlot 细节或后端代码。
- 完整技术治理视图延后到后续阶段。

本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`docs/frontend.md`、`docs/backend.md` 和 `docs/ai-review.md`；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库。

## 2026-06-08 确认审核点展示字段历史快照保护

- 审核点名称、展示编码、审核分类、排序和合同类型变更只影响后续新任务。
- 已有任务结果页必须读取 ReviewResultSnapshot 中的当次展示字段。
- 历史结果不得用当前配置覆盖审核点展示字段或严重级别。

本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`docs/frontend.md`、`docs/backend.md` 和 `docs/database.md`；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库。

## 2026-06-08 确认审核点合同类型字段语义

- 审核点配置中的合同类型字段 MVP 仅用于展示和后续扩展预留。
- MVP 合同类型仅有“工程采购合同”。
- 该字段不参与复杂适用范围路由。
- 首批 9 个审核点默认适用于工程采购合同。

本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`docs/frontend.md`、`docs/backend.md` 和 `docs/database.md`；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库。

## 2026-06-08 确认审核点排序字段语义

- 排序字段只影响管理台列表展示顺序。
- 排序字段只影响普通结果页展示顺序。
- 排序字段不影响审核执行顺序。
- ReviewExecutionPlan 执行顺序由后端计划器根据依赖、证据复用和执行策略决定。

本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`docs/frontend.md`、`docs/backend.md` 和 `docs/database.md`；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库。

## 2026-06-08 确认业务 finding 统计口径

- `ERROR` 和 `WARNING` 都属于业务 finding。
- 结果页和 API 摘要必须统计 `plannedPointCount`、`passCount`、`errorCount` 和 `warningCount`。
- `plannedPointCount` 表示本次启用并进入 ReviewExecutionPlan 的审核点总数；配置停用点不进入分母，适用性 `SKIPPED` 点进入分母。
- `NOT_CONCLUDED` 必须单独统计为 `notConcludedCount`，用于表示审核覆盖不足。
- `SKIPPED` 必须单独统计为 `skippedCount`，用于表示审核点不适用。
- 外部系统不得只看“是否存在 finding”，必须看级别。
- `PASS`、`NOT_CONCLUDED`、`SKIPPED` 和 `SYS-*` 不计入业务风险数量。
- 默认级别只影响业务 finding 的严重程度，不影响是否生成 finding。

本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`docs/frontend.md`、`docs/backend.md`、`docs/database.md` 和 `docs/sap.md`；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库。

## 2026-06-08 确认配置停用审核点展示口径

- 普通结果页主摘要不展示配置停用审核点数量。
- 管理台和评测报告展示 `disabledByConfigCount` 和配置停用审核点清单。
- 配置停用点不进入 `plannedPointCount`，也不混入点级状态计数。

本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`docs/frontend.md`、`docs/backend.md` 和 `docs/database.md`；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库。

## 2026-06-08 确认普通结果页审核点展示范围

- 普通结果页默认展示进入 ReviewExecutionPlan 的全部审核点。
- 展示状态包括 `PASS / ERROR / WARNING / NOT_CONCLUDED / SKIPPED`。
- 页面可提供状态筛选，但默认不得隐藏 `PASS` 或 `SKIPPED`。
- 配置停用点不进入 ReviewExecutionPlan，因此不在普通结果页展示。

本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`docs/frontend.md` 和 `docs/backend.md`；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库。

## 2026-06-08 确认 SKIPPED 展示和证据要求

- `SKIPPED` 点不要求证据定位。
- `SKIPPED` 点不要求 EvidenceSlot。
- `SKIPPED` 点必须展示适用性原因。
- 适用性原因示例：“当前付款方式为按节点付款，进度款比例审核点不适用”。

本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`docs/frontend.md`、`docs/backend.md` 和 `docs/database.md`；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库。

## 2026-06-08 确认 PASS 证据要求

- `PASS` 点必须有证据定位和可解释依据。
- 证据不足不得判 `PASS`。
- 证据不足、证据歧义或覆盖不足时，应输出 `NOT_CONCLUDED` 和业务可读原因。

本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`docs/frontend.md`、`docs/backend.md` 和 `docs/database.md`；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库。

## 2026-06-08 确认 ERROR/WARNING 证据要求

- `ERROR` 和 `WARNING` 点必须有可靠证据定位和可解释依据。
- 没有可靠证据不得生成业务 finding。
- 证据不足时应输出 `NOT_CONCLUDED` 和业务可读原因。

本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`docs/frontend.md`、`docs/backend.md` 和 `docs/database.md`；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库。

## 2026-06-08 确认证据定位精度 MVP 要求

- MVP SourceAnchor 最低要求为 block/section/table row/cell 级定位。
- 精确字符范围不是 MVP 硬要求。
- 金额、税率和比例候选如有精确文本范围，应展示字符级高亮。
- 若缺少字符级范围但证据块和候选值可靠，仍可形成 `PASS / ERROR / WARNING`。
- 结果卡片必须展示合同原文候选值，辅助用户在证据块内定位。
- 缺少字符级范围本身不得导致 `NOT_CONCLUDED`。

本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`docs/frontend.md`、`docs/backend.md` 和 `docs/database.md`；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库。

## 2026-06-08 确认证据块内多候选归属规则

- block/section/table row/cell 级定位可以进入 MVP，但候选值必须可靠归属。
- 同一证据块内存在多个金额、税率或比例候选时，必须能确定当前审核点所需候选值。
- 候选值归属不清时，不得形成 `PASS / ERROR / WARNING`。
- 候选值归属不清应输出 `NOT_CONCLUDED`，业务原因示例：“证据块内存在多个候选，无法可靠归属”。

本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`docs/frontend.md`、`docs/backend.md` 和 `docs/database.md`；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库。

## 2026-06-08 确认 Gemma 局部辅助候选归属边界

- 候选归属不清时允许 Gemma 辅助判断。
- Gemma 输入仅限相关证据块和必要上下文，不得提交整份合同。
- Gemma 只辅助 CandidateResolver 选择候选。
- Gemma 不得直接输出最终 `PASS / ERROR / WARNING`。
- Gemma 辅助后仍无法可靠归属时，必须输出 `NOT_CONCLUDED`，不得猜测候选继续裁判。
- Gemma 不可用或超时时，不阻断所有审核点；规则/候选本身可靠的点继续由后端确定性裁判。
- 只有依赖 Gemma 消歧且无法完成消歧的点输出 `NOT_CONCLUDED`。
- 后端仍负责最终确定性裁判。

本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`docs/ai-review.md` 和 `docs/backend.md`；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库。

## 2026-06-08 确认 Gemma 诊断可见性

- 普通结果页不展示内部 `SYS-MODEL-*` 技术码。
- 普通结果页只展示业务化 `NOT_CONCLUDED` 原因。
- 管理台任务详情展示内部 `SYS-MODEL-*`、模型调用状态、超时/不可用原因和诊断摘要。
- 评测报告也可展示上述内部诊断，用于质量分析。

本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`docs/frontend.md`、`docs/backend.md` 和 `docs/ai-review.md`；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库。

## 2026-06-08 确认模型原始输出可见性

- MVP 不保存完整模型原始输出，也不在管理台展示完整模型原始输出。
- 只保存模型调用状态、`SYS-MODEL-*` 诊断码、schema 校验结果、redacted artifact 摘要、token 用量、耗时、模型版本、调用时间和必要的候选归属结论摘要。
- 受控保存完整 redacted raw output、TTL 和审计展开延后到 Pilot / Production Readiness。
- 普通结果页仍只展示业务化 `NOT_CONCLUDED` 原因。

本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`docs/frontend.md`、`docs/backend.md` 和 `docs/ai-review.md`；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库。

## 2026-06-08 确认配置审核规则页面标签页范围

- `审核点` 进入 MVP，支持列表、详情展开和有限编辑。
- `模型配置` 在 MVP 只读展示当前模型版本和 endpoint 状态，不支持编辑。
- `提示词模板` 在 MVP 不支持编辑，仅可展示业务摘要或延后。
- `最终提示词预览` 在 MVP 不展示完整 prompt，延后到治理/技术视图。
- `任务日志` 不属于配置审核规则页面，应放在任务列表/任务详情层级。

本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`docs/frontend.md`、`docs/backend.md` 和 `docs/ai-review.md`；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库。

## 2026-06-08 确认任务日志 MVP 记录粒度

- MVP 任务日志只记录阶段级事件，不记录每个内部函数调用。
- 阶段级事件用于判断任务卡在解析、索引、计划、证据构建、规则审核、模型辅助或结果合成等阶段。
- 任务日志可记录 `taskId`、`executionId`、`stageName`、`attempt`、事件类型、发生时间、耗时、摘要状态、业务化原因和摘要级 `SYS-*` 诊断。
- 模型阶段只展示调用状态、token 用量、耗时、模型版本、schema 校验和诊断摘要。
- 任务日志不得展示完整 prompt、完整模型 raw output、endpoint secret、stack trace 或逐函数调试日志。

本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`docs/frontend.md`、`docs/backend.md` 和 `changelog/2026-06.md`。本次无当前 TASK 文件；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库。

## 2026-06-08 确认 Model Profile 切换与公网模型使用边界

- 管理台支持维护多个 `Model Profile`，包括本地 Gemma4、公网兼容 OpenAI API 模型和 mock fallback。
- 任务详情页顶部概览区展示本次 execution 实际使用的审核模型，包括模型展示名、provider 类型、模型名称和配置版本摘要。
- 管理员可通过启用状态、密钥配置状态、默认模型和 `usageScope` 控制新任务可选模型。
- 公网 DeepSeek 等模型 profile 可在业务允许的场景下用于链路验证、演示、评测或内部审核任务；能否用于真实合同由业务方、管理员和部署环境承担配置责任。
- 每个 execution 固定绑定当次模型 profile 和配置版本，历史快照不随后续模型切换变化。
- 模型不论来自本地还是公网，都不得直接决定最终业务 finding；最终点级状态仍由后端裁判合成。
- 新增 `decisions/ADR-006-model-profile-switching-and-public-provider-scope.md`，状态为 `Accepted`。

本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`docs/frontend.md`、`docs/backend.md`、`docs/ai-review.md`、`decisions/ADR-006-model-profile-switching-and-public-provider-scope.md` 和 `changelog/2026-06.md`。本次无当前 TASK 文件；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库。

## 2026-06-08 确认 AI 调优治理双链路架构

- 项目定位继续保持为 Contract Quality Control Platform / 合同质量控制中台，不改为 AI 合同审核平台。
- 确认生产审核链路和调优治理链路必须彻底隔离。
- 生产审核链路为 `Contract -> Parser -> CandidateResolver -> EvidenceSlot -> Review Engine -> Finding -> ReviewResultSnapshot`，原则是稳定、可解释、可审计。
- 调优治理链路为 `ReviewResultSnapshot -> TuningPacket -> CrossModelDiagnostic -> AITuningAdvice -> Candidate Change -> Regression Validation -> Human Approval -> Release`，原则是实验、诊断、优化、治理。
- 确认核心红线：`AI Advice != Production Change`。任何 AI 建议不得直接修改 prompt、rule、CandidateResolver、EvidenceSlot、ModelProfile 或后端确定性裁判，不得影响当前正式 ReviewResultSnapshot。
- MVP 只建设 TuningPacket、PointDiagnostic、ExecutionSummary 和 ExportConfig，支持复制诊断包给 AI 辅助分析；不自动调用公网 AI，不自动调优，不自动改规则。
- Pilot 才建设 CrossModelDiagnostic、CrossModelComparison 和结构化 AITuningAdvice。
- Production Readiness 才建设 Regression Center、Release Governance、Rollback、Audit 和 OptimizationEffect。
- DefinitionTermIndex 确认为索引层能力，不属于 Parser；Parser 产出结构化 blocks 后，DefinitionTermIndex 与 CandidateIndex 并行构建，并按需注入 compact definition context。
- 新增 `ADR-007-tuning-packet-architecture.md`、`ADR-008-definition-term-index.md` 和 `ADR-009-ai-tuning-governance.md`，状态均为 `Accepted`。

本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`docs/ARCHITECTURE.md`、`docs/ai-review.md`、`decisions/ADR-007-tuning-packet-architecture.md`、`decisions/ADR-008-definition-term-index.md`、`decisions/ADR-009-ai-tuning-governance.md` 和 `changelog/2026-06.md`。本次无当前 TASK 文件；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库。

## 2026-06-08 确认 AI 调优包 Tab MVP 边界

- 任务详情页 MVP 新增 `AI 调优包` tab。
- 该 tab 只用于生成、导出和复制 TuningPacket，帮助人和外部 AI 分析问题，不是自动调优系统。
- MVP 允许生成 TuningPacket、导出 TuningPacket、复制到外部 AI，并保存外部 AI 建议文本。
- MVP 不允许自动调用公网 AI、自动生成 AITuningAdvice、自动执行 CrossModelDiagnostic。
- MVP 不允许自动修改 prompt、rule、pattern、field lexicon、CandidateResolver、EvidenceSlot、ModelProfile 或 ReviewPointDefinition。
- MVP 不允许自动影响当前 ReviewResultSnapshot。

本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`docs/frontend.md`、`docs/ai-review.md` 和 `changelog/2026-06.md`。本次无当前 TASK 文件；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库。

## 2026-06-08 确认普通结果 URL 访问控制口径

- MVP 普通结果 URL 不做平台侧登录、公开令牌或独立访问控制。
- 外围系统负责对 URL 进行编码、加密、分发和访问控制。
- 平台暂不在结果 URL 权限方向投入额外能力。
- 平台仍必须保证普通结果 URL 不暴露 prompt、raw output、endpoint、stack trace、admin logs、secret 或管理台诊断详情。

本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`docs/frontend.md`、`docs/backend.md`、`docs/sap.md` 和 `changelog/2026-06.md`。本次无当前 TASK 文件；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库。

## 2026-06-08 确认任务创建模型选择边界

- 管理台创建任务时可以选择已启用且密钥已配置的模型 profile。
- 管理台不选择模型时，系统使用当前默认启用的 `Model Profile`。
- 外部 API MVP 不允许调用方直接指定 `modelProfileCode`。
- SAP/OA/采购系统创建任务时使用平台当前默认启用的 `Model Profile`，避免外部集成契约过早绑定模型配置细节。
- 外部系统可查看本次 execution 实际使用的审核模型摘要，但不能控制模型选择。
- 后续如需开放外部 caller 指定模型，必须通过 caller policy 白名单和独立 API 契约扩展。

本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`docs/frontend.md`、`docs/backend.md`、`docs/sap.md` 和 `changelog/2026-06.md`。本次无当前 TASK 文件；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库。

## 2026-06-08 确认首批审核点基线

- `ADR-005-first-review-points-selection.md` 状态从 `Proposed` 改为 `Accepted`。
- MVP 首批审核点固定为 9 个。
- 9 个审核点全部为 core review point。
- `TAX_AMOUNT_FORMULA_CONSISTENCY` 保留在 MVP，并作为 core review point。
- 后续如果样本验证发现税额公式误报高，应通过规则版本、容差策略和回归验证调整，不在 PRD 中继续悬置。

本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`ROADMAP.md`、`docs/ai-review.md`、`tasks/active/TASK-003-first-review-points-selection.md`、`decisions/ADR-005-first-review-points-selection.md` 和 `changelog/2026-06.md`。本次未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库。

## 2026-06-08 确认 MVP 首批合同类型

- MVP 首批合同类型只做“工程采购合同”。
- 管理台合同类型字段 MVP 仅展示“工程采购合同”，作为后续多合同分类预留字段。
- 首批 9 个审核点默认适用于工程采购合同。
- 材料供货合同、费用合同、杂项合同延后到 Pilot 或后续阶段。
- 系统可保留合同类型扩展结构，但 MVP 不做复杂合同类型路由。

本次更新 `PRD.md`、`CURRENT_CONTEXT.md`、`docs/ai-review.md` 和 `changelog/2026-06.md`。本次无当前 TASK 文件；未创建业务代码，未搭建脚手架，未安装依赖，未修改数据库。

## 2026-06-06 补齐项目记忆模块

基于已确认的 `AGENTS.md`、`CURRENT_CONTEXT.md`、`PROJECT_BRIEF.md` 和原始架构文档，补齐以下文件内容：

- `ROADMAP.md`：补充 V1 建议里程碑、验证方向和待确认事项。
- `tasks/TASK-000-template.md`：补充任务元信息、执行约束、必读/按需文档、文档更新要求和完成记录字段。
- `docs/frontend.md`：补充 NOT_CONCLUDED 体验、发布页、诊断展示和权限边界。
- `docs/backend.md`：补充解析质量门槛、合同类型画像、候选归属、缺证据归因、任务幂等和点级结果摘要。
- `docs/database.md`：补充不可变快照、版本包白名单、caller policy 审批、审计摘要、reviewCompleteness 和临时运行审批。
- `docs/ai-review.md`：补充模型输入、requestedRoles 资格、second pass、合成优先级、脱敏测试和模型运行记录。
- `docs/sap.md`：补充 diagnostic-only 边界、correction 错误码、类型建议可见性和外部契约测试。
- `docs/deployment.md`：补充负载演练、stage 幂等、紧急变更、恢复流程、90 天过期 API 行为和上线前样本验证。
- `decisions/ADR-000-template.md`：补充适用范围、回滚迁移和待确认区。

本次补齐未创建业务代码，未搭建前端或后端脚手架，未安装依赖。

## 2026-06-07 Superpowers 审查结论写回

将 Superpowers planning / design review / task decomposition 审查结论沉淀回项目记忆。

本次结论：

- 当前不建议直接开始编码。
- 当前可以开始“技术基线与 V1 MVP 收敛任务”。
- 不建议直接进入 project scaffold。
- scaffold 推迟到关键 ADR 完成之后。
- V1 愿景不等于 MVP，第一轮 MVP 应聚焦单份中文 Word 合同的最小审核闭环。
- Quality Copilot、完整治理、复杂发布审批、SAP/OA 深度 correction、生产保护能力不应进入第一轮 MVP。

更新文件：

- `CURRENT_CONTEXT.md`：写入当前不建议编码、不建议直接 scaffold、下一步优先创建并执行 MVP 收敛任务和关键 ADR。
- `ROADMAP.md`：新增 V1 分层，将 V1 拆为 MVP、Pilot、Production Readiness。

新增任务包：

- `tasks/active/TASK-001-v1-mvp-scope-gate.md`
- `tasks/active/TASK-002-entry-order-decision.md`
- `tasks/active/TASK-003-first-review-points-selection.md`
- `tasks/active/TASK-004-word-parser-baseline-plan.md`
- `tasks/active/TASK-005-model-gateway-and-budget-baseline.md`
- `tasks/active/TASK-006-scaffold-only-after-adr.md`

新增 ADR 草案：

- `decisions/ADR-001-technical-stack-and-repo-layout.md`
- `decisions/ADR-002-v1-result-and-diagnostic-contract.md`
- `decisions/ADR-003-task-execution-snapshot-model.md`

所有新增 ADR 状态均为 `Proposed`，等待人工确认；本次未创建业务代码，未搭建脚手架，未安装依赖。

## 2026-06-07 启动技术基线与 V1 MVP 收敛任务

开始执行技术基线与 V1 MVP 收敛任务。

本次完成：

- 完成 `TASK-001-v1-mvp-scope-gate`：形成 V1 MVP / Pilot / Production Readiness 分层规划基线。
- 启动 `TASK-002-entry-order-decision`：比较管理台上传优先、外部 API 优先、API-first + 简易管理台三种入口顺序。
- 对 `TASK-002` 写入初步推荐：API-first + 简易管理台，仍待人工确认。
- 更新 `CURRENT_CONTEXT.md` 的当前完成情况和下一步。
- 更新 `ROADMAP.md`，标记 MVP 分层来自 `TASK-001`，入口顺序推荐来自 `TASK-002` 且待确认。

本次未创建业务代码，未搭建脚手架，未安装依赖。`ADR-001`、`ADR-002`、`ADR-003` 仍为 `Proposed`。

## 2026-06-07 项目记忆写入规则治理

优化项目记忆写入规则的放置方式：

- `AGENTS.md`：保留项目记忆写入的短规则，只放强制原则和 `docs/context-management.md` 索引。
- `docs/context-management.md`：新增详细规则，说明必须写入的内容、写入哪个文件、不写入的内容、需要人工确认的内容、必须创建 ADR 的内容，以及每次任务结束的 Memory Writeback 输出格式。
- `CURRENT_CONTEXT.md`：记录本次文档治理已完成。

本次调整未改变项目目标、V1 范围、业务边界或核心红线。

本次未创建业务代码，未搭建前端或后端脚手架，未安装依赖。

## 2026-06-07 Next Task Handoff Prompt 治理

为项目补充 Next Task Handoff Prompt 输出规则，并明确区分“可执行任务”和“征求意见/建议”。

本次完成：

- 更新 `AGENTS.md`：要求每次任务结束后必须输出 `Next Task Handoff`，并限制只有明确下一任务时才能输出独立代码块形式的 `Next Task Handoff Prompt`。
- 更新 `docs/context-management.md`：新增 `## Next Task Handoff` 章节，细化可复制代码块的适用条件、禁止条件、必含内容、无明确下一任务时的处理方式，以及“执行任务”和“建议/征求意见”的区分标准。
- 更新 `tasks/TASK-000-template.md`：新增 `## Next Task Handoff` 模板小节，要求任务完成时显式判断是否存在明确下一任务，并禁止把无任务编号或仅为建议的内容放入代码块。
- 更新 `CURRENT_CONTEXT.md`：记录本次 Next Task Handoff Prompt 文档治理已完成。

本次调整仅涉及项目记忆文档治理，未修改业务代码，未创建脚手架，未安装依赖，未改变已确认的项目目标、业务边界、V1 范围或核心红线。

## 2026-06-07 任务命名规范治理

统一任务文档的命名与标题规范，保持工程路径稳定，同时提升中文可读性。

本次完成：

- 更新 `tasks/TASK-000-template.md`：新增任务命名规范，明确文件名使用英文 slug，标题使用中文，正文以中文为主，英文 slug 仅作稳定路径和工程标识。
- 更新 `docs/context-management.md`：补充任务文档命名与语言规范，明确文件名可保留英文 slug 以兼容工程，面向人阅读的标题、目标、验收标准和交付摘要统一使用中文。
- 更新 `tasks/active/TASK-001-v1-mvp-scope-gate.md`、`tasks/active/TASK-002-entry-order-decision.md`、`tasks/active/TASK-003-first-review-points-selection.md`、`tasks/active/TASK-004-word-parser-baseline-plan.md`、`tasks/active/TASK-005-model-gateway-and-budget-baseline.md`、`tasks/active/TASK-006-scaffold-only-after-adr.md`：为现有仅英文标题的任务补齐中文标题，不改文件名。
- 更新 `CURRENT_CONTEXT.md`：记录本次任务命名规范治理已完成。

本次调整仅涉及项目记忆文档治理，未修改业务代码，未创建脚手架，未安装依赖，未改变既有项目目标或 V1 范围。

## 2026-06-08 确认 MVP 管理台登录与敏感诊断展示边界

确认 MVP 管理台暂不做平台内登录、账号体系和权限矩阵。

本次确认：

- 管理台访问控制由部署环境、内网、VPN、反向代理或外围系统承担。
- 管理台常规页面和常规 API 默认只展示摘要级诊断信息。
- 管理台不得展示或返回完整 prompt、完整模型 raw output、endpoint secret、stack trace、完整合同敏感调试包或密钥。
- 该限制不影响 AI 调优治理链路；MVP 治理链路依赖结构化、最小必要的 TuningPacket，而不是暴露全部原始调试内容。
- 如后续需要完整敏感诊断导出，应作为 Pilot / Production Readiness 的受控能力单独设计，纳入权限、脱敏、审批和审计。
- MVP 配置变更仍记录配置版本、变更时间和操作者占位信息；操作者可暂记为 `SYSTEM`、`ADMIN_PLACEHOLDER` 或环境账号。

更新文件：

- `PRD.md`
- `CURRENT_CONTEXT.md`
- `docs/frontend.md`
- `docs/backend.md`
- `docs/database.md`
- `tasks/active/TASK-001-v1-mvp-scope-gate.md`

本次未创建业务代码，未搭建脚手架，未安装依赖。本次不新增 ADR；该变更是 MVP 范围边界确认，未改变核心审核链路或调优治理红线。
