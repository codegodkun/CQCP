# 2026-06 Changelog

## 2026-06-14 治理文件编码清理

本轮只处理长期记忆与治理文件，不涉及业务代码、数据库、Docker、PRD 或架构文档。

已完成：
- 重建 `tasks/MVP_TASK_MAP.md`，清理历史乱码并恢复为可读 UTF-8 版本。
- 重写 `tasks/active/TASK-022-persistent-result-query-adapter.md`，保留已确认事实、边界、验证结果与提交记录。
- 重整 `changelog/2026-06.md`，删除无法可靠恢复的历史乱码段落，仅保留可确认的月度记录。
- 更新 `CURRENT_CONTEXT.md`，同步当前阶段、已完成任务与治理收尾状态。

结论：
- 当前治理文件应以仓库文件版本为准，不再依赖受损历史段落。
- 如后续再发现旧文档乱码，应继续按“确认事实后重写，不猜测补全”的原则单独治理。

## 2026-06-13 CURRENT_CONTEXT 编码清理

本轮未改业务代码，只处理项目记忆文件中的历史乱码问题。

已完成：
- 发现 `CURRENT_CONTEXT.md` 旧内容存在历史编码损坏，新写入内容正常、旧段落大面积乱码。
- 未尝试逐段抢救受损文本，直接按 `AGENTS.md` 与 `docs/context-management.md` 规则重建 `CURRENT_CONTEXT.md`。
- 重建后的 `CURRENT_CONTEXT.md` 仅保留当前阶段、活跃任务、近期完成任务、已接受 ADR、阻塞项、待确认事项、禁止推进事项与下一步摘要。

结论：
- `CURRENT_CONTEXT.md` 已恢复为可读中文摘要版本。

## 2026-06-13 TASK-023 普通结果页最小实现

本轮在既有 `TASK-023` 父任务边界内完成普通结果页最小实现，只修改 `apps/admin-web/src` 下的前端文件与项目记忆文件；未修改后端、数据库迁移、`PRD.md`、`docs/ARCHITECTURE.md` 或 Docker 配置，未进入 `TASK-024`、`TASK-031`，未创建 `TASK_SPEC`，未分配 Claude Code / DeepSeek。

已完成：
- 新增 `apps/admin-web/src/publicResult/api.ts`，封装 `GET /api/v1/tasks/{taskId}/result` 调用。
- 新增 `apps/admin-web/src/publicResult/types.ts`，定义普通结果页最小消费类型。
- 新增 `apps/admin-web/src/publicResult/PublicResultPage.tsx`，实现基于 `taskId` 的普通结果查询页。
- 更新 `apps/admin-web/src/App.tsx`，将当前前端入口切到普通结果页。
- 更新 `apps/admin-web/src/App.test.tsx`，覆盖 `200 / 404 / 409 / 网络错误` 以及公开字段过滤。
- 更新 `apps/admin-web/src/styles.css`，补充普通结果页最小样式与移动端适配。
- 页面默认展示 `PASS / WARNING / ERROR / NOT_CONCLUDED / SKIPPED` 全部点级状态，并使用证据摘要与 `blockId` 提供最小原文定位反馈。
- 页面未展示 `SYS-*`、prompt、raw output、endpoint、stack trace、admin logs、secret；未混入管理台诊断详情。

验证结果：
- `docker compose -f deploy/compose/compose.yml --env-file deploy/env/.env.example ps`
- `docker compose -f deploy/compose/compose.yml --env-file deploy/env/.env.example exec postgres pg_isready -U cqcp -d cqcp`
- `npm.cmd run test`
- `npm.cmd run build`
- `npm.cmd run lint`

提交：
- 父任务建档 commit：`13d9783`
- 实现 commit：`b7ab8db`

## 2026-06-13 TASK-023 父任务建档

本轮只做 `TASK-023` 父任务建档与边界冻结，不写前端代码、不写后端代码、不修改数据库迁移、`PRD.md`、`docs/ARCHITECTURE.md` 或 Docker 配置，不创建 `TASK_SPEC`，不进入 `TASK-023` 实现。

已完成：
- 新增 `tasks/active/TASK-023-public-result-page-minimal-display.md`，冻结普通结果页最小展示父任务边界。
- 明确 `TASK-023` 只消费既有 `TASK-021` / `TASK-022` 结果查询与快照能力，不倒推修改审核链路。
- 明确普通结果页默认展示进入 `ReviewExecutionPlan` 的全部点级状态：`PASS / WARNING / ERROR / NOT_CONCLUDED / SKIPPED`。
- 明确普通结果页必须围绕“审核点 -> 证据 -> 原文定位”解释，并坚持业务结果与 `SYS-*` 诊断分流。
- 明确第一轮 MVP 不展示预算诊断，不提供“申请深度审核”入口；管理台诊断细节继续留在 `TASK-024`。

提交：
- commit：`13d9783`

## 2026-06-13 TASK-022 Persistent Result Query Adapter

本轮完成 `TASK-022` 的最小持久化查询适配层，实现 `GET /api/v1/tasks/{taskId}/result` 从数据库持久化快照读取结果，未修改 `PRD.md`、`docs/ARCHITECTURE.md`、Docker 配置，也未进入 `TASK-023`。

已完成：
- 新增 `PersistentTaskResultStore`，基于 `JdbcTemplate + ObjectMapper` 查询 `task` 与 `review_result_snapshot`。
- 结果选择策略为：`task_id = ?`、`superseded_by_execution_id IS NULL`、`created_at DESC LIMIT 1`。
- 保持 `TASK-021` 约定的语义不变：
  - `200`：有结果
  - `404`：任务不存在
  - `409`：任务存在但尚无结果
- 未修改 `ResultComposer`
- 未修改 `TaskExecutionStateMachine`
- 未新增 stage log
- 未修改 execution 状态

验证：
- `gradle test --tests "*PersistentTaskResultStoreTest" --tests "*TaskResultQueryServiceTest" --tests "*TaskResultQueryControllerTest"`
- `gradle test --tests "*MinimalReviewEngineTest" --tests "*ResultComposerTest" --tests "*TaskExecutionStateMachineTest" --tests "*TaskResultQueryServiceTest" --tests "*TaskResultQueryControllerTest" --tests "*PersistentTaskResultStoreTest"`
- `docker compose -f deploy/compose/compose.yml --env-file deploy/env/.env.example ps`
- `docker compose -f deploy/compose/compose.yml --env-file deploy/env/.env.example exec postgres pg_isready -U cqcp -d cqcp`

提交：
- 父任务建档 commit：`dde34dd`
- 实现 commit：`1a206d7`

## 2026-06-13 TASK-021 Result URL 查询 API

本轮完成 `TASK-021`，为结果快照提供最小公开查询接口，固定 `GET /api/v1/tasks/{taskId}/result` 的 200/404/409 语义，供后续公开结果页消费。

已完成：
- 新增 `TaskResultQueryController`
- 新增 `TaskResultQueryService`
- 新增 `TaskResultQueryExceptionHandler`
- 新增 `InMemoryTaskResultStore`
- 补充相关后端测试

提交：
- commit：`c5e4ddd`

## 2026-06-10 TASK-004 / TASK-005 收口

已确认并归档：
- `TASK-004-word-parser-baseline-plan`
- `TASK-005-model-gateway-and-budget-baseline`

同时补充并更新：
- `tasks/active/TASK-014-minimal-openapi-contract.md`
- `tasks/active/TASK-015-flyway-v1-bootstrap.md`
- `tasks/active/TASK-016-mvp-development-validation.md`
- `docs/backend.md`
- `docs/ai-review.md`
- `docs/deployment.md`
- `CURRENT_CONTEXT.md`

说明：
- 历史段落原文曾出现乱码，本处仅保留可确认事实，不补写无法可靠恢复的细节。

## 2026-06-10 ADR-002 Accepted

`decisions/ADR-002-v1-result-and-diagnostic-contract.md` 已从 `Proposed` 转为 `Accepted`。

确认要点：
- 点级状态统一为 `PASS / WARNING / ERROR / NOT_CONCLUDED / SKIPPED`
- `SYS-*` 与业务 Finding 分流
- 对外不暴露 prompt、raw output、endpoint、secret、stack trace 等内部信息
- `NOT_CONCLUDED` 作为业务不可下结论状态保留

## 2026-06-12 环境验证补记

已记录过的环境验证事实：
- Codex 在本仓库中完成过 `gradle test`、`gradle build`、`npm.cmd run test` 等基础验证。
- 历史原始文字中存在乱码，因此本节只保留“已完成环境验证”这一确认事实，不保留受损逐行日志。
