# 2026-06 Changelog

## 2026-06-15 TASK-DOC-001 文档治理补充修正

本轮只做文档治理补充修正，不启动新功能任务，不修改代码、数据库迁移、Docker 配置、`PRD.md` 或 `docs/ARCHITECTURE.md`。

已完成：
- 将 `README.md`、`docs/DEVELOPMENT.md`、`docs/VERIFY.md` 中的本机绝对路径链接修正为仓库相对路径。
- 对齐 `CURRENT_CONTEXT.md` 中 `TASK-024` 已提交并已 push 到 `origin/master` 的事实。
- 在 `AGENTS.md` 中补充文档更新规则，明确 `README.md`、`CURRENT_CONTEXT.md`、`changelog`、`docs/DEVELOPMENT.md`、`docs/VERIFY.md`、ADR 的职责与更新边界。

## 2026-06-15 TASK-DOC-001 项目入口与流程文档补齐

本轮只做文档治理，不做功能开发，不修改前端代码、后端代码、数据库迁移、Docker 配置、`PRD.md` 或 `docs/ARCHITECTURE.md`。

已完成：
- 新增 `README.md`，作为项目入口与导航文档。
- 新增 `docs/DEVELOPMENT.md`，固定开发流程、角色分工、Git 规则和停止条件。
- 新增 `docs/VERIFY.md`，固定通用验收原则、任务类型检查清单和提交前检查。
- 更新 `CURRENT_CONTEXT.md`，补充本轮文档治理结果。

确认事实：
- 当前仓库根目录不存在 `docker-compose.yml`。
- 当前仓库根目录不存在 `.env.example`。
- 当前标准 Compose 入口为 `deploy/compose/compose.yml`。
- 当前环境变量样例为 `deploy/env/.env.example`。

Memory Writeback：
- 已更新 `CURRENT_CONTEXT.md`。
- 已更新 `changelog/2026-06.md`。
- 当前不存在独立的 `TASK-DOC-001` 任务文件，本轮以用户直接下达的文档治理任务为准，因此未更新 `tasks/active/` 下的独立任务包。

## 2026-06-15 TASK-024 管理台诊断详情最小实现

本轮在 `TASK-024` 方案 A 边界内完成管理台诊断详情最小前端实现，只修改 `apps/admin-web/src` 下的前端文件与项目记忆文件；未修改后端、数据库迁移、`PRD.md`、`docs/ARCHITECTURE.md` 或 Docker 配置，未进入 `TASK-031`，未创建 `TASK_SPEC`，未分配 Claude Code / DeepSeek。

已完成：
- 新增 `apps/admin-web/src/adminDiagnostics/api.ts`，复用既有 `GET /api/v1/tasks/{taskId}/result` 查询管理台诊断数据。
- 新增 `apps/admin-web/src/adminDiagnostics/types.ts`，定义管理台最小消费的 diagnostics 类型。
- 新增 `apps/admin-web/src/adminDiagnostics/AdminDiagnosticPage.tsx`，实现 `/admin/diagnostics` 查询页。
- 更新 `apps/admin-web/src/App.tsx`，补充管理台诊断路由并保留公开结果页根路由。
- 更新 `apps/admin-web/src/App.test.tsx`，覆盖管理台诊断页路由、摘要、点级状态、`SYS-*` diagnostics 可见以及敏感字段不可见。
- 更新 `apps/admin-web/src/styles.css`，补充管理台摘要卡片与 diagnostics 列表样式。

页面最小展示结果：
- 支持输入 `taskId` 查询。
- 展示 `summary`。
- 展示 `reviewCompleteness`。
- 展示全部点级状态。
- 展示点级 diagnostics：`diagnosticCode`、`businessReason`、`evidenceSummary`、`evidenceBlockIds`。
- 管理台可见 `SYS-*`。
- 仍不展示 prompt、raw output、endpoint、stack trace、admin logs、secret。

验证结果：
- `npm.cmd run test -- src/App.test.tsx`
- `npm.cmd run build`

风险与遗留：
- 当前仍未提供 `contractName`、`currentStage`、`model summary`、`stageLogs`；若后续必须展示，应单独评估是否进入 `TASK-031`。
- 本轮仅完成前端最小实现，未引入新的管理台诊断接口。

Memory Writeback：
- 更新 `CURRENT_CONTEXT.md`，写入 `TASK-024` 完成状态、已落地页面路径、验证命令与遗留字段缺口。
- 更新 `tasks/active/TASK-024-admin-diagnostic-detail-minimal-display.md`，同步实现结果、测试结果和遗留问题。
- 更新 `tasks/MVP_TASK_MAP.md`，将 `TASK-024` 状态推进为已完成最小前端实现。

## 2026-06-15 TASK-024 方案确认

已确认 `TASK-024` 采用方案 A：
- 只基于现有 `ReviewResultSnapshot` 字段实现管理台最小诊断页
- 不补后端字段
- 不进入 `TASK-031`
- 不新增管理台诊断接口

结论：
- 下一窗可直接在 `TASK-024` 边界内进入前端最小实现。
- 若实现阶段再发现字段不足，应暂停并单独报告，不顺手扩大范围。

## 2026-06-15 TASK-024 实现前探查

本轮只做 `TASK-024` 的实现前探查与最小实现计划，不写前端代码、不写后端代码、不修改数据库迁移、`PRD.md`、`docs/ARCHITECTURE.md` 或 Docker 配置，不创建 `TASK_SPEC`，不进入 `TASK-031`。

已确认：
- `apps/admin-web` 当前正式页面只有公开结果页，现有入口与页面组织仍较轻量。
- 当前前端可复用 `react-router-dom`、`@tanstack/react-query`、现有自定义 CSS 与轻量页面结构。
- 现有 `GET /api/v1/tasks/{taskId}/result` 已能返回 `summary`、`reviewCompleteness`、`pointResults`、`diagnostics`、`sourceAnchors`、启停用审核点快照。
- 现有 `GET /api/v1/tasks/{taskId}/result` 不提供 `contractName`、`currentStage`、`model summary`、`stageLogs`。
- 后端已存在 `admindiagnostics` 视图模型与脱敏工厂，但当前未发现对外接口。

结论：
- 若 `TASK-024` 只消费现有 `ReviewResultSnapshot` 字段，可在前端边界内实现最小诊断页。
- 若 `TASK-024` 必须展示任务级头部、模型摘要与阶段日志，则已触及字段缺口，应先停下确认是否进入 `TASK-031` 或单独管理台接口任务。

Memory Writeback：
- 更新 `CURRENT_CONTEXT.md`，写入本轮探查结论、当前阻塞项与待确认事项。
- 更新 `tasks/active/TASK-024-admin-diagnostic-detail-minimal-display.md`，补充实现前探查结论与最小实现计划。
- 更新 `tasks/MVP_TASK_MAP.md`，同步 `TASK-024` 状态与字段缺口风险。

## 2026-06-14 TASK-024 父任务建档

本轮只做 `TASK-024` 父任务建档与边界冻结，不写前端代码、不写后端代码、不修改数据库迁移、`PRD.md`、`docs/ARCHITECTURE.md` 或 Docker 配置，不创建 `TASK_SPEC`，不进入 `TASK-024` 实现。

已完成：
- 新增 `tasks/active/TASK-024-admin-diagnostic-detail-minimal-display.md`，冻结管理台诊断详情最小展示父任务边界。
- 明确 `TASK-024` 只面向管理台内部诊断详情，不得混入 `TASK-023` 公开结果页。
- 明确 `TASK-024` 与 `TASK-031` 的边界：本轮不进入 mapper 补洞，不创建 `TASK_SPEC`。
- 同步更新 `CURRENT_CONTEXT.md` 与 `tasks/MVP_TASK_MAP.md`，将 `TASK-024` 状态推进为“已建档，待实现”。

结论：
- `TASK-024` 当前状态为“已建档，待实现”。
- 本轮未运行测试；仅完成父任务文档治理与项目记忆回写。

## 2026-06-14 治理文件编码清理

本轮只处理长期记忆与治理文件，不涉及业务代码、数据库、Docker、PRD 或架构文档。

已完成：
- 重建 `tasks/MVP_TASK_MAP.md`，清理历史乱码并恢复为可读 UTF-8 版本。
- 重写 `tasks/active/TASK-022-persistent-result-query-adapter.md`，保留已确认事实、边界、验证结果与提交记录。
- 重整 `changelog/2026-06.md`，删除无法可靠恢复的历史乱码段落，仅保留可确认的月度记录。
- 更新 `CURRENT_CONTEXT.md`，同步当前阶段、已完成任务与治理收口状态。

结论：
- 当前治理文件应以仓库文件版本为准，不再依赖受损历史段落。
- 如后续再发现旧文档乱码，应继续按“确认事实后重写，不猜测补全”的原则单独治理。

## 2026-06-13 CURRENT_CONTEXT 编码清理

本轮未改业务代码，只处理项目记忆文件中的历史乱码问题。

已完成：
- 发现 `CURRENT_CONTEXT.md` 旧内容存在历史编码损坏，新写入内容正常、旧段落大面积乱码。
- 未尝试逐段抢救受损文本，直接按 `AGENTS.md` 与 `docs/context-management.md` 规则重建 `CURRENT_CONTEXT.md`。
- 重建后的 `CURRENT_CONTEXT.md` 仅保留当前阶段、活跃任务、近期完成任务、已接受 ADR、阻塞项、待确认事项、禁止推进事项与下一步摘要。

结论：
- `CURRENT_CONTEXT.md` 已恢复为可读中文摘要版本。

## 2026-06-13 TASK-023 普通结果页最小实现

本轮在既有 `TASK-023` 父任务边界内完成普通结果页最小实现，只修改 `apps/admin-web/src` 下的前端文件与项目记忆文件；未修改后端、数据库迁移、`PRD.md`、`docs/ARCHITECTURE.md` 或 Docker 配置，未进入 `TASK-024`、`TASK-031`，未创建 `TASK_SPEC`，未分配 Claude Code / DeepSeek。

已完成：
- 新增 `apps/admin-web/src/publicResult/api.ts`，封装 `GET /api/v1/tasks/{taskId}/result` 调用。
- 新增 `apps/admin-web/src/publicResult/types.ts`，定义普通结果页最小消费类型。
- 新增 `apps/admin-web/src/publicResult/PublicResultPage.tsx`，实现基于 `taskId` 的普通结果查询页。
- 更新 `apps/admin-web/src/App.tsx`，将当前前端入口切到普通结果页。
- 更新 `apps/admin-web/src/App.test.tsx`，覆盖 `200 / 404 / 409 / 网络错误` 以及公开字段过滤。
- 更新 `apps/admin-web/src/styles.css`，补充普通结果页最小样式与移动端适配。
- 页面默认展示 `PASS / WARNING / ERROR / NOT_CONCLUDED / SKIPPED` 全部点级状态，并使用证据摘要与 `blockId` 提供最小原文定位反馈。
- 页面未展示 `SYS-*`、prompt、raw output、endpoint、stack trace、admin logs、secret；未混入管理台诊断详情。

验证结果：
- `docker compose -f deploy/compose/compose.yml --env-file deploy/env/.env.example ps`
- `docker compose -f deploy/compose/compose.yml --env-file deploy/env/.env.example exec postgres pg_isready -U cqcp -d cqcp`
- `npm.cmd run test`
- `npm.cmd run build`
- `npm.cmd run lint`

提交：
- 父任务建档 commit：`13d9783`
- 实现 commit：`b7ab8db`

## 2026-06-13 TASK-023 父任务建档

本轮只做 `TASK-023` 父任务建档与边界冻结，不写前端代码、不写后端代码、不修改数据库迁移、`PRD.md`、`docs/ARCHITECTURE.md` 或 Docker 配置，不创建 `TASK_SPEC`，不进入 `TASK-023` 实现。

已完成：
- 新增 `tasks/active/TASK-023-public-result-page-minimal-display.md`，冻结普通结果页最小展示父任务边界。
- 明确 `TASK-023` 只消费既有 `TASK-021` / `TASK-022` 结果查询与快照能力，不倒推修改审核链路。
- 明确普通结果页默认展示进入 `ReviewExecutionPlan` 的全部点级状态：`PASS / WARNING / ERROR / NOT_CONCLUDED / SKIPPED`。
- 明确普通结果页必须围绕“审核点 -> 证据 -> 原文定位”解释，并坚持业务结果与 `SYS-*` 诊断分流。
- 明确第一轮 MVP 不展示预算诊断，不提供“申请深度审核”入口；管理台诊断细节继续留在 `TASK-024`。

提交：
- commit：`13d9783`

## 2026-06-13 TASK-022 Persistent Result Query Adapter

本轮完成 `TASK-022` 的最小持久化查询适配层，实现 `GET /api/v1/tasks/{taskId}/result` 从数据库持久化快照读取结果，未修改 `PRD.md`、`docs/ARCHITECTURE.md`、Docker 配置，也未进入 `TASK-023`。

已完成：
- 新增 `PersistentTaskResultStore`，基于 `JdbcTemplate + ObjectMapper` 查询 `task` 与 `review_result_snapshot`。
- 结果选择策略为：`task_id = ?`、`superseded_by_execution_id IS NULL`、`created_at DESC LIMIT 1`。
- 保持 `TASK-021` 约定的语义不变：
  - `200`：有结果
  - `404`：任务不存在
  - `409`：任务存在但尚无结果
- 未修改 `ResultComposer`
- 未修改 `TaskExecutionStateMachine`
- 未新增 stage log
- 未修改 execution 状态

验证：
- `gradle test --tests "*PersistentTaskResultStoreTest" --tests "*TaskResultQueryServiceTest" --tests "*TaskResultQueryControllerTest"`
- `gradle test --tests "*MinimalReviewEngineTest" --tests "*ResultComposerTest" --tests "*TaskExecutionStateMachineTest" --tests "*TaskResultQueryServiceTest" --tests "*TaskResultQueryControllerTest" --tests "*PersistentTaskResultStoreTest"`
- `docker compose -f deploy/compose/compose.yml --env-file deploy/env/.env.example ps`
- `docker compose -f deploy/compose/compose.yml --env-file deploy/env/.env.example exec postgres pg_isready -U cqcp -d cqcp`

提交：
- 父任务建档 commit：`dde34dd`
- 实现 commit：`1a206d7`

## 2026-06-13 TASK-021 Result URL 查询 API

本轮完成 `TASK-021`，为结果快照提供最小公开查询接口，固定 `GET /api/v1/tasks/{taskId}/result` 的 `200 / 404 / 409` 语义，供后续公开结果页消费。

已完成：
- 新增 `TaskResultQueryController`
- 新增 `TaskResultQueryService`
- 新增 `TaskResultQueryExceptionHandler`
- 新增 `InMemoryTaskResultStore`
- 补充相关后端测试

提交：
- commit：`c5e4ddd`

## 2026-06-10 TASK-004 / TASK-005 收口

已确认并归档：
- `TASK-004-word-parser-baseline-plan`
- `TASK-005-model-gateway-and-budget-baseline`

同时补充并更新：
- `tasks/active/TASK-014-minimal-openapi-contract.md`
- `tasks/active/TASK-015-flyway-v1-bootstrap.md`
- `tasks/active/TASK-016-mvp-development-validation.md`
- `docs/backend.md`
- `docs/ai-review.md`
- `docs/deployment.md`
- `CURRENT_CONTEXT.md`

说明：
- 历史段落原文曾出现乱码，本处仅保留可确认事实，不补写无法可靠恢复的细节。

## 2026-06-10 ADR-002 Accepted

`decisions/ADR-002-v1-result-and-diagnostic-contract.md` 已从 `Proposed` 转为 `Accepted`。

确认要点：
- 点级状态统一为 `PASS / WARNING / ERROR / NOT_CONCLUDED / SKIPPED`
- `SYS-*` 与业务 Finding 分流
- 对外不暴露 prompt、raw output、endpoint、secret、stack trace 等内部信息
- `NOT_CONCLUDED` 作为业务不可下结论状态保留

## 2026-06-12 环境验证补记

已记录过的环境验证事实：
- Codex 在本仓库中完成过 `gradle test`、`gradle build`、`npm.cmd run test` 等基础验证。
- 历史原始文字中存在乱码，因此本节只保留“已完成环境验证”这一确认事实，不保留受损逐行日志。
