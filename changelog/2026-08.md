# 2026-08 变更记录

## 2026-08-01

### MILESTONE-MVP-002-CORE SourceAnchor 全路径审计整改

- 候选 HEAD `e044768fe7ac3aba6a0d4a0d2633261f4f141272` 的完整 verification
  全部通过并生成 freeze subjectIdentity `5bcfe410c6366313ff220552ba4f5845dceefb92ac20a9335aa4aa83523b293b`。
- 三方从零审计结果为 CC AUDIT `GO`、测试/安全 Codex auditor `GO`、代码/架构
  Codex auditor `NO_GO（P1=1 / blocking=1）`，因此三份结论整体失效；旧 freeze、
  verification、R7、browser 与审计报告均已保留为 invalidated 历史证据。
- finding 证明 legacy / `v20260715.1` whole-text fallback 会在拼接全文提取值后丢失
  matcher block/span，再按 label 与 candidate value containment 选择首块并构造错误
  SourceAnchor。主 Codex 先新增 4 条回归，得到 `33 tests / 3 failures` 的稳定红测，
  随后将 fallback 改为逐 block、offset-preserving matcher group 映射；TABLE_CELL
  仅在唯一 parser-issued span 命中时保留可靠 attribution，否则 fail closed。
- 绿测结果：`ParserBackedReviewInputPreparerEvidenceTest=33/33`，D1 联合门禁
  `11 suites / 451 tests / 0 failures / 0 errors / 0 skipped`，freeze count validator
  `4/4`。尚未执行新 HEAD 的完整 verification、freeze、三审、push、PR 或 merge；
  未启动 Provider 或模型公网调用。
- 首次完整 verification 在 Core boundary 阶段因当月 changelog 未列入精确
  allowlist 而 fail closed；尚未进入 R7、Compose 或 browser。partial 证据已封存，
  随后仅加入 `changelog/2026-08.md` 单文件并增加下一月份仍被拒绝的负向测试，
  未放宽目录级 Core 范围。
- HEAD `fc32b55…` 的完整 verification 与 freeze `c156b4a7…` 随后通过，但
  第一份全新代码/架构 Codex auditor 返回 `NO_GO（P1=1 / blocking=1）`。依硬停止
  条件未发送 CC、未启动第二 Codex auditor、未进入 Git 集成。finding 证明重叠
  TABLE_CELL fail-closed 回归使用 unverified scope，实际未覆盖 attribution/anchor
  门禁。
- 提交 `d00dcca…` 将相关测试 parser 改为 verified scope，并在原始候选上复现
  `2 tests / 1 failure`；生产修复仅对 `TABLE_CELL + blockAttribution=false + 无可靠
  cell identity` 返回 `SYS_EVIDENCE_BUNDLE_INVALID / INTERNAL_RULE_ERROR`，不改变
  普通 block attribution 的 `SYS_ROLE_CONFLICT`。绿测为定向 `2/2`、preparer
  `33/33`、collector `91/91`。新完整 verification、freeze 与三审待执行。

### MILESTONE-MVP-002-CORE PR #37 跨平台字节确定性修复

- HEAD `847bb2c7a76aef2236ff7c4ca8b7ea2d0b1ed7ec` 曾完成 Core verification，freeze
  subject 为 `3bc24a842a331ca9b86f491c26904ac1d84f108ecc69bbba8a33042ab510ec5b`，
  CC AUDIT 与两个全新 Codex auditor 均为 `GO / P0=P1=P2=blocking=0`；PR #37
  随后创建，但 GitHub CI 未通过，因此没有 merge。
- `Authorization evidence check` 的直接根因是 PR body 缺少必填区块；backend 的
  两个失败分别为 Track A 投影 checked-in bytes 与 TASK-036 v2 output hash 在
  Windows CRLF 和 Linux LF checkout 间不一致。该缺陷属于评测/证据字节确定性，
  不改变审核裁判、人工 ground truth 或业务 Finding。
- 候选 `76f5685…` 的 LF 方案虽通过 Windows/Linux 定向 `31/31`，但完整验证在
  `blind-unblind` fail closed：三份 LF human ground truth 与 immutable Track A
  freeze/DeepSeek seal 绑定的历史 CRLF SHA 不一致。此前 D1=`451/451`、D2=`20/20`、
  两轮 backend 各 `902/902`、admin-web=`70/70`、Core Node=`215/215`、bootJar、
  R7、blind freeze 与 DeepSeek seal 已通过；未生成 verification summary，未启动
  Compose/browser、freeze 或审计。
- 当前限定修复改为保留 seal 的跨平台精确字节策略：hash-sensitive 文件以
  `text eol=crlf` 保持 Git blob 不变并跨平台检出历史 CRLF bytes，Track A generator 显式写 CRLF，manifest 与 TASK-036 v2
  恢复历史 SHA；不迁移 seal、不改变人工 decisions。Windows 与完全断网 Linux
  定向均为 `31/31`，直接 `unblind` 恢复 27 项完成；新完整 verification 仍待从零执行。
- 由于 tracked diff 已变化，`3bc24a84…` 的 verification、freeze 和三份旧 GO
  全部失效。`76f5685…` 亦因上述 verification 失败不能冻结；本批次形成新的
  candidate，但尚未执行完整 Core verification、freeze、三审、
  PR CI 或 merge；Provider、Track B 新 holdout 和模型公网调用均未启动。

## 2026-08-02

### MILESTONE-MVP-002-CORE TABLE_ROW 与公共 SourceAnchor 原子整改

- HEAD `22e44fc45decb9cb2cec41848dfcf393f2c49378` 的从零 Core verification 已通过，
  freeze subject 为 `8d01dd9ad49a1c440bf0aa4ec2eee4a09600653c721dd570f2f66be86cf2b092`。
  第一份全新代码/架构 Codex auditor 返回 `NO_GO（P1=1 / P2=1 / blocking=2）`，
  因硬停止条件未启动第二名 Codex auditor、未发送 CC AUDIT，也未进入 Git 集成。
- P1 证明 ordinary pattern 与 v29 比例路径没有保留 capture value 的原始 source
  span，TABLE_ROW label/value 场景会降为不可靠 row anchor；P2 证明内部
  `TABLE_CELL` 曾进入公共 `SourceAnchor.locationLevel`。审计提出的公共 enum 扩展
  与 ADR-015 冲突，实际整改保持内部 cell identity，并将公共 anchor 归一为
  `BLOCK_LEVEL`。
- 主 Codex 以红绿回归完成限定修复：ordinary pattern 使用 group span；v29 使用
  可逆 NFKC/空白 projection；“无预付款”使用精确 token span；无法可靠映射继续
  fail closed。红测 `32 tests / 4 failures`，绿测 `32/32`；扩大定向为
  `6 suites / 200 tests / 0 failures`，OpenAPI verifier `8 paths / 48 schemas`、
  workbench Vitest `2/2`、admin-web build 和最后的 v29 class 复跑均通过。
- `8d01dd9a…` 的 freeze、verification 和 browser evidence 已原样封存为
  `invalidated-8d01dd9a`，未删除证据。当前未创建新完整 verification/freeze，
  未修改 Provider、Finding/verdict 或 Secret 边界，未执行模型公网调用。

### MILESTONE-MVP-002-CORE Formal R7 公共 anchor harness 整改

- 上述限定修复以 `97b55a0b13facbb30397ee79c3f0b1d7b0d31d67` 形成候选。首次
  verification 在 sandbox evidence 目录创建处停止；同一未变 HEAD 经外部权限重启
  后通过 Core scope `7/7`，但 Formal R7 为
  `39 MATCHED / 18 NOT_OBSERVABLE / 6 EXCLUDED` 并 fail closed。
- 18 个不可观察项全部为人工 `TABLE_CELL`。生产 SourceAnchor 已按 ADR-015 正确
  输出公共 `BLOCK_LEVEL`，并保留 parser-issued `table/.../cell/...` preview ref；
  test-only harness 却先按 location level 走 block 分支，未优先解析 preview ref。
- 主 Codex 新增真实公共 anchor 最小红测 `1/1 FAIL`，随后仅修改
  `Task034MvpE2EAcceptanceHarnessTest`：preview ref 优先解析，只有缺失 ref 时才以
  `blockId` 回退；synthetic cell anchors 同步使用 `BLOCK_LEVEL`。绿测 `1/1 PASS`，
  完整 harness `19/19 PASS`。
- 失败 verification 与 R7 已分别封存为
  `verification-failed-97b55a0-formal-r7/` 与
  `r7-failed-97b55a0-public-anchor-harness/`。为防 dirty worktree 错绑 commit，新的
  Formal R7 尚未执行；没有 freeze、审计、push、PR 更新或模型公网调用。

### MILESTONE-MVP-002-CORE D1 452 计数门禁同步

- test-only 修复提交 `91d5e161aa0eba03317a883e1ef93238931a3b44` 的 Formal R7
  已恢复 `57 MATCHED + 6 EXCLUDED` 并完成 seal/verify，seal SHA 为
  `bf199763f5f6ef2dbbc97b4869ce2b1f099fe5b69d35e8c6661b1ad2358e6100`。
- 完整 verification 随后在 D1 count gate 停止。11 个 suites 的 JUnit 实际为
  `452 tests / 0 failures / 0 errors / 0 skipped`；脚本与 freeze validator 固定 451。
  新增测试来自 TABLE_ROW capture span P1 回归，因此属于 evidence gate 滞后，不是
  产品失败。
- 限定整改只把 verification log 名称、精确计数、freeze validator 及其 test fixture
  同步为 452；freeze/core-scope Node 定向合计 `11/11 PASS`，当前脚本无残留 451
  门禁。历史运行的 451 数字保持不变。
- partial verification 和 R7 已封存为 `verification-failed-91d5e16-d1-count/`、
  `r7-superseded-91d5e16-d1-count/`。尚未形成新 clean candidate、完整 verification、
  freeze 或三审；未修改产品、Provider 或模型边界。

### MILESTONE-MVP-002-CORE `ad14800c` 三审与 GitHub CI EOL 修复

- HEAD `ad14800c161c096018f91fc8021feec9338a951b` 已完成从零 Core verification，
  freeze subject 为 `75d86031f7b65c0047270d2933c0397331ae53c9cf1957dc856f93d859a8fb66`；
  CC AUDIT 与两个全新 Codex auditor 均为
  `GO / P0=P1=P2=blocking=0`。PR #37 已更新授权证据并 push 精确受审 HEAD。
- GitHub run `30729941151` 中 Authorization evidence 与 admin-web 均通过，backend
  为 `904 tests / 1 failure / 3 skipped`。fresh Linux clone 逐文件 SHA 证明四个 JSON
  的历史 CRLF hash 正确；失败来自 `console-summary.md` 与
  `occurrence-comparison.csv` 的冻结 hash 为 LF，而 `.gitattributes` 错设 CRLF。
- 最小修复只把 TASK-036 v2 Markdown/CSV 改为 `text eol=lf`，四个 JSON 保持
  `text eol=crlf`，不改 hash 常量、不迁移 Track A/DeepSeek seal、不改变人工
  decisions、CQCP actual、Finding/verdict 或 Provider 边界。Windows 定向与断网
  Linux ephemeral-commit + fresh-clone 定向均为 `31/31 PASS`；Linux 六个 fixture
  SHA 全部等于现有冻结常量。
- 因 tracked 内容变化，`75d86031…` verification、freeze 与三份 GO 已整体失效；
  当前阶段一修复与双平台门禁已形成 clean HEAD，尚待该 HEAD 的完整 verification、
  单次 freeze、三方从零审计、CI 与 merge。未启动 Provider、Track B 新 holdout 或
  模型公网调用。

### MILESTONE-MVP-002-CORE D2 跨-shard role 原子预算整改

- EOL 修复候选 HEAD `c21a68d844a90dddc01b511e1b76403a5ed1c39b` 已完成完整
  Core verification 并冻结为 subject `72b934c271e15bbb4e5e76f24a7413015eb949f0bcb77b432177ea85ef63f84b`。
  第一名全新代码/架构 Codex auditor 返回 `NO_GO（P2=1 / blocking=1）`；CC
  `deepseek-v4-flash` 审计达到 `$8` 预算上限且未产出报告，第二名 Codex auditor
  未启动，因此该轮依硬停止条件终止。
- P2 证明 `FamilyModelCallPlanner` 逐个处理同一 role 的 shard request：第一块可先
  requested，第二块超预算后同一 role 又进入 uncovered，同时遗留部分 evidence。
  这违反 TASK_SPEC-036-D2 与 ARCHITECTURE §22.5 的完整 role evidence 和
  requested/uncovered 互斥边界，属于 Core 产品正确性问题。
- 用户批准最小修复后，主 Codex 用双 shard/同 role/第二 block 超预算回归复现
  `1/1 FAIL`；planner 现在先按 role 聚合并去重 required blocks，以 role 最高
  priority 排序后原子装入硬预算，并在 plan record 中拒绝 requested/uncovered
  overlap。修复没有改变 Provider、Finding/verdict、EvidenceSlot、CandidateResolver、
  公共 API 或数据库。
- 绿灯证据：D2 `21/21`；D2 + 历史 Track B runtime contract `22/22`；freeze/Core
  scope Node `11/11`。verification/freeze 对 D2 的精确计数由 20 同步为 21；历史
  20-test 运行记录不改。新的完整 Core verification、freeze、三方从零审计、PR CI
  与 merge 尚未开始。

### MILESTONE-MVP-002-CORE D2 required-block identity 整改

- HEAD `979ddbd3f9c903ee6cce40cf578676b084096f91` 完成完整 Core verification 并冻结为
  subject `3e4e8d00cb477ae79fdefac7449fb623ad123a16f8be096f9c07f559ff99587f`。
  重试后 CC AUDIT 与测试/安全 Codex auditor 全零 `GO`，代码/架构 Codex auditor
  返回 `NO_GO（P1=1 / blocking=1）`：`RuntimeEvidencePacket.ModelAssistAdmission`
  未保存 evaluator 已过滤的 `requiredBlockIds`，`toRoleRequest()` 又从全部 candidates
  重建集合，可把不满足局部比较条件的 block 纳入 family budget。
- 用户批准限定修复后，mixed-candidate JSON round-trip → role request → family plan
  红测精确得到额外 `block-c`。修复只在 admission 持久化精确 block 集合，并校验
  非空、去重和 allowed 一致性；role request 不再重建。旧 Track B corpus 字节与人工
  hash 保持不变，contract test 只在内存中用其独立 eligibility source signals 补齐字段。
- 绿灯证据：D2 `22/22`，D2 + 历史 Track B runtime contract `23/23`，freeze validator
  Node `4/4`。verification/freeze 门禁计数由 21 同步为 22；历史运行记录不改。
  `3e4e8d00…` 证据已迁入 rejected 封存目录，新的完整 verification、freeze、三审、
  PR CI 与 merge 尚未开始。

### MILESTONE-MVP-002-CORE 最终合并与 Track B holdout pre-seal

- D2 required-block identity 修复最终形成 HEAD
  `fe1d61bcf37060f84c8247493dd801774d6bc8b7`；从零 Core verification 冻结为
  subject `90c4ae9aca0bc06dcdb2ec5af98e93590970653be56fa3dbdcc86eab00451cba`。
  CC AUDIT 与两个全新 Codex auditor 全部
  `GO / P0=P1=P2=blocking=0`，GitHub run `30748527866` 全绿，PR #37 合并为
  `115be530480e2ff9b92a7076b2668c066a44ae5c`。
- 基于该主线建立独立 Track B holdout：12 个新 task/execution/packet identity，
  其中 5 MEDIUM + 4 CONFLICTED eligible，三个 control 分别验证 deterministic
  HIGH、invalid bundle 与 no reliable anchor zero-call。Codex proposed decisions 与
  runtime source signals 物理分离，确认前不构成 ground truth。
- pre-seal 精确定向为 Node `29/29`、JUnit 新旧 Track B + D2 `24/24`；人工
  challenge `TBH2-fac56106204c4b93a11befc577369360` 绑定 corpus SHA
  `15d1f845d65c5018363bba324d0527f36ff07ddf0703f188f1d557711a9e2b24`、
  challenge SHA
  `37a05c4b0d2ed4b21282f0fa09dd00938da60857a2d53e187490a28f6cf5d23a`。
  当前 `modelInputCreated=false / networkCallAllowed=false`，只等待项目负责人逐项
  人工确认；Provider A0/A1/A2 未开始，A3 不属于本 Milestone。

### MILESTONE-MVP-002-TRACK-B-HOLDOUT 封印前执行契约

- 依据项目负责人既有 standing egress grant，新增 `ADR-022` 与精确机器门禁；授权
  仅覆盖 MVP-002 的脱敏 EVALUATION、合成语料和冻结审计材料，固定 DeepSeek 官方
  endpoint/模型、Secret 进程内、strict JSON/non-streaming、zero-call 和
  `UNKNOWN_SIDE_EFFECT` 不重试边界。
- 新 holdout 不复用旧 run-v3 的 6-call family 分组，固定为 9 个 eligible packet 各
  一个 runtime-isomorphic request，3 个 controls 不进入调用集合。实际执行前 receipt
  必须绑定 actual input、dispatch、call set、9 个 outbound request hash、模型、调用数
  和时间。
- 离线 dispatch 回归证明：缺少人工 ground-truth seal 时不创建 model input/run
  目录；测试用 seal 存在时才生成 9×1 immutable dispatch，且
  `networkCallPerformed=false`。DeepSeek runner 复用既有 secure transport 与 one-time
  claim，覆盖 9-call 成功、不可重放和 UNKNOWN_SIDE_EFFECT 单次失败终态；Codex seal
  强制 `fork_turns="none"`。unblind 只在两份 blind execution evidence 验证后读取
  human seal；全维 100% 才 GO，结构合法但答案错误固定 NO_GO 且同一 holdout 不可
  重试。扩展 Node 定向 `51/51`、Java seam/新旧 Track B
  `24/24` 通过。
- 截至该 pre-seal checkpoint，真实输出只有 corpus、review draft/Markdown、preseal
  manifest 与 challenge 五个文件，尚无 `human-ground-truth.json`、`run-v1`、模型意见
  或网络调用；其后的正式执行事实见下一节。

### MILESTONE-MVP-002 Track B holdout 正式执行停止

- 项目负责人绑定 challenge `TBH2-fac56106204c4b93a11befc577369360`、challenge SHA
  `37a05c4b…`、corpus SHA `15d1f845…` 与 review SHA `f07b4b1c…`，确认 12 条
  `proposedExpected` 全部原样成为人工 decisions。decisions SHA 为 `f8557a88…`，
  human ground-truth seal SHA 为 `11fbcc06…`，create/verify 均通过。
- 离线 dispatch/preflight 绑定 model input `bd402b40…`、dispatch `8fe2a996…`、
  9×1 provider call set `8c63e377…` 与 standing-grant 派生 receipt `76d1b171…`；
  三个 controls 未进入调用集合，preflight 未读取 KEY、未联网。
- 一个全新 `fork_turns="none"` Codex blind evaluator 返回 9 条严格 schema opinion；
  opinion SHA `6f091ae1…`、receipt SHA `8a9ea6c8…` 已封存但未解盲。
- DeepSeek `deepseek-v4-pro` 正式执行完成第 1 个 call 后，第 2 个 call 因
  `SCHEMA_INVALID_OR_EMPTY / OPINION_SCHEMA_INVALID` fail closed；one-time claim
  `da3f1eb1…` 已消费，terminal blocked receipt `f79060fe…` 记录
  `networkAttempted=true / automaticRetryPerformed=false / completedCallCount=1`。
  未保存 raw response、reasoning 或 Secret；没有 DeepSeek accepted opinion，未执行
  unblind、freeze 或三方审计。
- 按有限轮次停止规则，同一 holdout 不得重试、改 prompt、换模型或补跑。Provider
  admission 保持 `NOT_ESTABLISHED`，A0/A1/A2 继续阻塞，`MILESTONE-MVP-002` 已
  标记为 `BLOCKED`；等待项目负责人决定是否另行定义后续评测治理任务。

### MILESTONE-MVP-002 Track B successor 授权与治理冻结

- 项目负责人明确授权另立 TASK-EVAL-003 后续独立评测：失败 holdout 与 claim 永久
  只读保留，不补跑、不解盲、不改写；successor 使用全新 identity、候选文本和值。
- 用户交互收敛为人工答案、重大范围变化和最终 merge；standing grant 范围内模型
  调用、CC、Codex auditor、验证、freeze、安全重试、commit、push、PR 和 CI 由主
  Codex 连续执行，不再逐轮确认。
- 新增 ADR-023，保持 12 packets / 9×1 calls / 3 zero-call controls、冻结 prompt/schema
  和全维 100% admission 门禁。只有明确证明 HTTP payload 未发送的 DNS/pre-send
  failure 可在冻结上限内安全重试；schema/content/finish_reason/HTTP-started/
  UNKNOWN_SIDE_EFFECT 均零重试。
- 正式两个 Codex auditors 必须全新、`fork_turns="none"`，显式使用
  `gpt-5.6-sol / reasoning_effort=xhigh`；CC AUDIT 继续使用 CC 当前连接的
  `deepseek-v4-flash`。Provider A0/A1/A2 仍须等待 successor admission 与三方全零 GO。
- successor pre-seal 随后完成：source SHA `84f04dbf…`、corpus SHA `a6090054…`、
  review SHA `f8c4c6a0…`、manifest SHA `1a22f599…`；相对两套 prior corpus 的
  identity、23 个 candidate values、19 个 evidence texts overlap 全部为 0。旧 holdout
  + successor contract/disjointness/preseal Node `6/6 PASS`。
- challenge `TBS1-5bfbe8b667944e7182df3ec537d45725`、challenge SHA
  `0bd06f76…` 已生成，等待项目负责人 12 条人工 decisions。当前无 successor human
  seal、model input、dispatch、claim 或网络调用。

### MILESTONE-MVP-002 Track B successor 正式执行停止

- 项目负责人绑定 challenge `TBS1-5bfbe8b667944e7182df3ec537d45725`、challenge SHA
  `0bd06f76…`、corpus SHA `a6090054…`、review SHA `f8c4c6a0…`，确认 12 条人工
  decisions 无修改；human confirmation SHA `38b7d534…`、seal SHA `6ad4a1e4…`。
- 正式身份为 model input `654168c6…`、9×1 call set `79a3ab0b…`、dispatch
  `f9d6cd05…`、standing-grant derived receipt `1788387d…`。Codex 全新盲评 9/9 strict
  schema accepted，opinion SHA `eb0face7…`、receipt SHA `594f503f…`，未解盲。
- DeepSeek `deepseek-v4-pro` 唯一正式 claim SHA `45f1d5ea…` 已消费；第 1 个 call
  返回稳定类别 `AUTHENTICATION_FAILED`。blocked receipt SHA `03f46cb8…` 记录
  `networkAttempted=true / completedCallCount=0 / automaticRetryPerformed=false`；未保存
  raw response、reasoning 或 Secret。
- 按 ADR-023 零重试边界，同一 successor 不换 KEY/模型补跑、不调 prompt、不解盲、
  不生成 freeze 或派发正式三审。Provider admission 保持 `NOT_ESTABLISHED`，A0/A1/A2
  继续阻塞；继续 admission 属于新的重大评测治理决策。

### MILESTONE-MVP-002 Track B final connectivity 与独立 admission 授权

- L0 connectivity/Provider 诊断发现旧脚本递归扫描后选中了旧项目归档中的历史/示例
  KEY；显式仓库外 Secret 对 `api.deepseek.com/models` 返回 200，实际可用模型为
  `deepseek-v4-flash`、`deepseek-v4-pro`。
- 两个全合成探针均成功：flash 返回精确 `PONG`；pro 在 thinking disabled、JSON mode、
  non-streaming 下返回 HTTP 200、`finish_reason=stop` 与严格 JSON sentinel。未保存 KEY、
  raw response 或 reasoning；该 L0 console 不冒充正式 admission evidence。
- 项目负责人批准 ADR-024 与 TASK-EVAL-004：先实现显式 Secret Reference 和任务专用
  hash-bound connectivity gate，再生成与前三套语料 identity/value/text 全部 disjoint
  的第四套 12-packet corpus。TASK-EVAL-002/003 的失败 claim 均保持只读、不得重试。
- connectivity GO 仍不建立 Provider admission；必须继续完成人工先封印、9×1 正式
  admission、immutable freeze 与三方全零审计。A0/A1/A2 继续阻塞。
- 正式 connectivity gate 随后完成：secure transport/gate 离线 `20/20 PASS`，evidence
  SHA `22d38b11…`；`/models` 与 exact `deepseek-v4-pro` synthetic strict JSON probe
  均通过，实际 KEY 泄漏文件数 0。evidence 明确 `formalAdmissionAffected=false`，当前
  转入第四套 corpus preseal，尚未创建 model input、dispatch 或 claim。

## 2026-08-03 TASK-EVAL-004 最终独立 admission 终态

- 项目负责人绑定 challenge `TBF1-0fc18b…`、challenge/corpus/review SHA 确认 12 条
  proposedExpected；confirmation SHA `b225f36a…`、human seal SHA `8716d66d…`。
- 离线冻结 model input `bd648dd1…`、call set `9881d520…`、dispatch `141d5344…`、
  derived receipt `f17018cf…`，精确为 9 calls / 9 eligible / 3 excluded controls；
  DeepSeek preflight 无网络。
- 全新 `fork_turns="none"` Codex blind evaluator 返回 9 条 strict opinion，封存
  opinion SHA `8abc4063…`、receipt SHA `70acc344…`；未解盲。
- DeepSeek `deepseek-v4-pro` 唯一 claim SHA `62a7451e…` 已消费。第 8 个 call 因
  `SCHEMA_INVALID_OR_EMPTY / OPINION_SCHEMA_INVALID` fail closed；前 7 个 calls
  完成，terminal blocked receipt SHA `8b113c74…`，无自动重试，未持久化 raw response、
  reasoning 或 Secret。
- 依 ADR-024 有限轮次与停止规则，同一 corpus/claim 不重试、不换模型/KEY、不调 prompt、
  不解盲、不生成 freeze/三审。Provider admission 为 `NOT_ESTABLISHED`，A0/A1/A2
  继续阻塞；不得声明 Production Ready 或解锁 TASK-028/031/032。
- 终态只读核验为 phase-appropriate Node `44/44 PASS`、实际 KEY 与 forbidden Provider
  field 泄漏 0、claim/model-input/Codex opinion contract PASS，且无 DeepSeek accepted
  opinion/unblind/audit。另记录 1 项纯证据测试债：model-dark preseal test 在真实目录已
  进入 post-seal 后仍要求 artifacts 不存在，纳入时为 `44/45`；依硬停止规则不在本轮
  自动修复。
- final preseal 已冻结：source `1a91aebb…`、corpus `ed216d43…`、review
  `65c775b2…`、manifest `b03fdca4…`；相对前三套 42 prior packets 的 identity、
  23 candidate values、19 evidence texts overlap 全部为 0。精确 Node 回归
  `22/22 PASS`。
- challenge `TBF1-0fc18b77754248c28d472b0245a57b81`、SHA `74f051c1…` 已生成，
  2026-08-05T02:46:07.009Z 到期。当前等待 12 条人工 decisions；human seal、model
  input、dispatch、claim 均不存在，正式 admission 仍未开始。

## 2026-08-03 TASK-EVAL-005 schema 稳定性诊断授权与治理冻结

- 项目负责人批准新的、有限的 TASK-EVAL-005 与 ADR-025。TASK-EVAL-004 claim 继续
  终态只读，不重试、不补跑、不解盲；新任务不得借用其输入作为 admission。
- 新任务先以全新合成诊断集执行旧版本 12-call baseline，最多创建两个版本化候选；
  单一候选须在两个独立 12-call pass 上累计 24/24 strict accepted，总诊断调用上限
  60。只保存 hash、计数、accepted 和稳定拒绝类别，不保存 raw request/response/
  content/reasoning 或 Secret。

## 2026-08-03 TASK-EVAL-005 第五套人工封印与盲评准备

- 项目负责人绑定 challenge `TB51-5deddd744f6f432f8f7760bd51899e16` 及实际
  challenge/corpus/review SHA，无修改接受 12 条人工 decisions；human seal SHA
  `85391e4573911f8e0147eb2ff2dda4908e79afe8029190d1c97fa2b73a96e4e3`。
- 9×1 dispatch 已冻结，3 个 controls 未进入调用集合；model input `e1516fb0…`、call set
  `365e1722…`、dispatch `31e9d718…`、derived receipt `6e9f6955…`。
- 全新 Codex blind evaluator 的 9 条 strict opinion 已封存为 `95ecd582…`；当前未解盲，
  DeepSeek claim 尚未创建，Provider admission 仍为 `NOT_ESTABLISHED`。

## 2026-08-03 TASK-EVAL-005 正式 admission 终态 NO-GO

- 唯一 `deepseek-v4-pro` 9×1 执行全部 strict schema accepted，无自动重试；claim
  `f9086db1…`、DeepSeek opinion `0e4f96fc…`，3 个 controls 保持 zero-call。
- 双意见先验验证后解盲：Codex 全维 100%；DeepSeek schema/reliable-anchor 100%，但
  4 个 CONFLICTED packet mismatch，role/candidate/anchor/abstention 各 55.56%。
- report `dbfe7d05…`、seal `520d7b38…` 已 verify；终态
  `SEALED_NO_GO_MODEL_MISMATCH / NOT_ESTABLISHED`。实际 KEY 泄漏与禁止 Provider raw
  字段匹配均为 0，phase-appropriate Node 回归 15/15 PASS；未 freeze、未三审。按硬边界
  不重试、不建第六套、不启动 A0/A1/A2。
- 前序门禁只在诊断稳定性 GO 后创建与前四套及诊断集完全 disjoint 的第五套
  12-packet corpus，并在任何 evaluator/model 访问前等待项目负责人绑定实际
  challenge/corpus/review hash 确认 12 条人工 ground truth；该确认随后已完成。
- 本次正式 admission 固定使用 `deepseek-v4-pro`、9×1 calls、3 个 zero-call controls
  和 schema/anchor/role/candidate/abstention 全维 100% 门槛。终态失败已经触发“不建
  第六套、A0/A1/A2 继续阻塞”的既定边界。
- D1 baseline 随后完成：12 个全新合成单包 input 相对前四套 identity/value/evidence
  overlap 全为 0；旧 prompt 12/12 strict accepted，result SHA `790b2a73…`。结合
  TASK-EVAL-004 第 8 call 失败，该结果只证明旧版本不是确定性必败，不证明稳定。
- Candidate 1 仅增加单包精确指令和完整合法 JSON 示例，prompt SHA `6a29b74f…`；
  qualification 12/12、confirmation 12/12，合计 24/24、0 rejection。总诊断 36/60，
  未创建 Candidate 2；stability freeze SHA `44468d84…`。
- 诊断 tests 5/5 PASS，实际 KEY 泄漏文件 0、raw Provider artifact 字段匹配 0。该 GO
  仅允许生成第五套 corpus，不等于 Provider admission，不放行 A0/A1/A2。
- 第五套 source/proposal/corpus/review/preseal 随后冻结：source `d8dd7df3…`、proposal
  `a5dd576e…`、corpus `17343dd3…`、review draft `0ab07f6b…`、review document
  `3c3ff928…`、manifest `b49a6e8a…`。
  相对前四套 corpus 及 diagnostic input 的 identity/value/evidence overlap 全为 0；
  5 MEDIUM + 4 CONFLICTED eligible、3 zero-call controls。
- challenge `TB51-5deddd744f6f432f8f7760bd51899e16` / SHA `f934a0e8…` 已生成并在
  到期 `2026-08-05T05:59:55.260Z` 前获得项目负责人确认；确认前 model
  input/evaluator/network 均保持禁止。诊断 + preseal 精确 Node suite 为 8/8 PASS。

## 2026-08-03 MILESTONE-MVP-002 终态证据收敛核验

- 项目负责人再次确认第五套 challenge/corpus/review 的精确 SHA 与 12 条人工
  decisions 无修改；该确认与现有 human seal `85391e45…` 完全一致，不触发重跑、
  补跑或新的模型调用。
- 两个独立 Codex 只读审计分别确认：Core/Track A 已完成，Track B Provider admission
  未建立；原计划的成功路径 M3/M4 无法合法完成，只允许形成终态 BLOCKED 证据保存
  subject。CC AUDIT 使用脱敏 allowlist 包复核后给出
  `TERMINAL_EVIDENCE_VALID / P0=0 / P1=0 / P2=3 / blocking=0`；包 manifest SHA 为
  `27097ca76528adb36929d879f73d5f2bc97b94c0bf7ae2aab694af923fdd20bc`。
- 初次 Codex 终态证据审计发现 TASK-EVAL-004/005 hash-bound 文件缺少 LF 属性；现已仅
  扩展 `.gitattributes`，62/62 个受影响文件均为 `text eol=lf` 且 CR 字节文件数为 0。
  report `dbfe7d05…` 与 seal `520d7b38…` 的 SHA 未变化，seal verify 仍通过。
- phase-appropriate Node suite 为 78/78 PASS；Java Track B runtime seam 为 1/1 PASS。
  全历史 wildcard sweep 为 133/154，21 项仅来自缺失旧 Track A/v2 archive fixture、
  已 superseded prompt hash 及 post-seal 阶段继续运行 model-dark preseal 断言，不作为
  当前终态 subject 的绿灯证据。
- standing grant 仍是尚未撤销的授权上限，但 ADR-025 终态已经使第六套、A0/A1/A2
  与新的 Provider evaluation call 无资格。最终是否以不可变记录方式撤销/终止该
  grant 仍待项目负责人决定；不得把授权存在解释为可继续调用。

## 2026-08-03 Provider 路径询问澄清与有限恢复批准

- 先前把项目负责人关于 BLOCKED 的询问误记为终止指令；项目负责人已明确澄清并批准
  ADR-026 / TASK-EVAL-006 的有限恢复。TASK-EVAL-005 的旧 claim、opinion、report、seal
  与 `SEALED_NO_GO_MODEL_MISMATCH` 保持不可变。
- 当前状态纠正为 `RECOVERY_ACTIVE / ADMISSION_NOT_ESTABLISHED`。新范围只建立
  model-facing EvidencePacket projection/prompt/schema v3，排除 runtime routing、
  diagnostic、identity/admission label；先执行 4-call non-admission diagnostic，4/4 后
  才允许第六套独立 12-packet corpus、人工先封印和唯一 9×1 admission，失败不建第七套。
- ADR-022 standing grant 继续有效；本恢复新增公网预算上限为 13 calls，并继续要求每次
  hash-bound receipt、strict JSON、零不安全重试、Secret/raw response/reasoning 不落盘。
  A0/A1/A2 在 admission 与三方全零 GO 前仍阻塞。
- 旧终态证据提交 `d08d1508f7349d6747e07e239001254396a20d9f` 的 fresh-checkout
  Node 78/78、Java 1/1、62/62 LF 与 seal verify 事实保留；因受审范围已变化，其审计结论
  不得用于新的 recovery subject 收口。

## 2026-08-03 TASK-EVAL-006 会话投影恢复诊断 GO

- v3 model-facing projection/prompt/schema/request builder 以 TDD 完成；runtime routing、
  diagnostic、identity/admission label 不进入 Provider payload，strict opinion schema 对
  JSON 字段顺序不敏感但继续拒绝字段漂移、duplicate key、非 stop 和伪 anchor。
- 4 个旧 CONFLICTED packet 经新投影执行精确 4 calls，全部 strict schema accepted、
  零自动重试；input `809c244e…`、dispatch `338d1168…`、claim `14715db0…`、opinion
  `eef82a45…`，Secret/raw response/reasoning 泄漏为 0。
- 本地解盲六维全部 4/4；report `a1472689…`、seal `fedc4e21…`，状态
  `SEALED_GO_RECOVERY_DIAGNOSTIC`。只放行第六套独立 corpus 创建，不建立 Provider
  admission，不启动 A0/A1/A2。

## 2026-08-03 TASK-EVAL-006 第六套人工确认门禁

- 第六套 source/proposal/corpus/review/preseal 已冻结；corpus `3a988a9a…`、review draft
  `8edd0ea4…`、manifest `937d6500…`。相对前五套及两份 diagnostic input 的
  identity/value/evidence overlap 全为 0。
- challenge `TB61-8e5e524790c9457a80d8e2fe2433b63d` / SHA `328f4349…`，包含
  5 MEDIUM + 4 CONFLICTED + 3 controls。项目负责人已绑定实际 hash 接受全部 12 条
  proposedExpected；confirmation `814e8146…`、human seal `0f23b9eb…`，封印早于模型访问。

## 2026-08-03 TASK-EVAL-006 第六套正式 admission GO

- v3 model input `bb8984ed…`、call set `1121a345…`、dispatch `a427e052…`、derived
  receipt `aa716878…`；精确 9×1 `deepseek-v4-pro` calls，3 controls zero-call。
- 全新 `gpt-5.6-sol/xhigh` Codex blind opinion `cccf0a4d…`；DeepSeek claim
  `600a821a…`、opinion `e863596a…`。9 calls 全部 strict schema accepted、零自动重试，
  Secret/raw response/reasoning 未落盘，模型未生成 Finding/verdict。
- 解盲后 Codex 与 DeepSeek 的 schema、可靠 anchor、role、candidate、anchor、abstention
  均为 9/9，controls 3/3；report `395c0d38…`、seal `2d879b13…`，状态
  `SEALED_GO_TRACK_B_RECOVERY_ADMISSION`。
- 当前进入 R5 verification/freeze/三审；A0/A1/A2 在三方全零 GO 前仍禁止。

## 2026-08-03 TASK-EVAL-006 R5 bounded verification PASS

- 最终限定验证为 phase-appropriate Node `70/70`、verification builder unit `1/1`、Java
  Track B runtime seam `1/1`；admission seal 原字节重建一致。
- 61 个 hash-bound 文本文件 CR=0；实际 DeepSeek KEY 泄漏文件=0、禁止 Provider payload
  文件=0；`git diff --check=0`。
- 全历史 Node glob 仍不是 fresh worktree 独立入口；其 ENOENT 与 preseal/postseal 阶段错配
  失败日志作为 non-gating harness 诊断保留。最终 verification result/console manifest 已
  重建并复验 PASS；下一步只创建 clean commit 与 immutable freeze，再执行三方全新审计；
  A0/A1/A2 仍禁止。
